# 8 寄存器分配

本章是系列文章的第八章，用着色算法描述了寄存器的分配过程。

上山采薇，薄暮苦饥。溪谷多风，霜露沾衣。野雉群雊，猿猴相追。还望故乡，郁何垒垒！高山有崖，林木有枝。忧来无方，人莫之知。人生如寄，多忧何为？今我不乐，岁月如驰。汤汤川流，中有行舟。随波转薄，有似客游。策我良马，被我轻裘。载驰载驱，聊以忘忧。

——曹丕的《善哉行·其一》

8.1寄存器分配

寄存器分配是为程序处理的值找到存储位置的问题

这些值可以存放到寄存器，也可以存放在内存中

寄存器更快，但数量有限

内存很多，但访问速度慢

好的寄存器分配算法尽量将使用更频繁的变量保存的寄存器中

### 8.1.1 寄存器分配的主要工作

寄存器指派

寄存器溢出处理

寄存器使用合并

### 8.1.2 寄存器的约束

硬盘硬件或者编译器的限制，某些值只能保存在特定的寄存器中

虚拟寄存器（程序中的变量）和物理寄存器（实际的寄存器）

calling convention（调用约定）

同一个程序点alive的多个变量必须指派不同的寄存器

### 8.1.3 寄存器分配与生命周期管理

最小寄存器数量 ≥ 最大生命周期变量集合

不过DCC888课程胶片里面给的这个例子，我不太认同：

![图](images/image_0082.png)

这样分配下来虽然MaxLive是2个，但MinReg需要3个。

为什么不能这样分配？因为最后输出是e和c，如果多个分支使用的e和c的寄存器不一样，那到汇聚点的时候，就没法直接用，还要做一次转移，这个转移也是需要额外的寄存器的，或者至少需要额外的计算。如果不做转移，就要插入一条store和一条load指令，这个成本更高。

![图](images/image_0083.png)

### 8.1.4 寄存器分配是个NP完成问题

它的复杂度和逻辑等同于图的着色问题。

同样的，对于这样的CFG，同样汇聚点上输出的变量往上的多个分支中，同一个变量需要使用同样的寄存器：

![图](images/image_0084.png)

转换成着色问题的逻辑变成这样，对下面的k种颜色，需要k+1个寄存器：

![图](images/image_0085.png)

## 8.2 线性扫描

线性扫描基于区间图的贪婪着色算法：

给定一个区间序列，重叠的区间必须给定不同的颜色，求最小颜色数。

贪婪着色有最优算法

但线性扫描不是贪婪着色的最优算法，而是最优算法的一个近似解。

### 8.2.1 基本块的线性化

通常用逆后根排序对CFG做排序生成线性化的BB块序列（前面worklist算法也用了逆后根排序，看来这个排序和程序执行之间的关系非常密切）。

![图](images/image_0086.png)

### 8.2.2 生成区间线

变量v的区间线Iv从v的生命期开始的程序点开始，到v的生命期结束的程序点结束。

为什么b和e的区间线要到第二个BB块最后，而不是在最后一次使用后就结束？因为后面还有分支，根据条件不同，第二个BB块还有可能从L6继续执行。

![图](images/image_0087.png)

### 8.2.3 区间线的线性扫描算法

算法描述如下

![图](images/image_0088.gif)

1 LINEARSCANREGISTERALLOCATION♧

2   active = {}

3   foreach interval i, in order of increasing start point

4     EXPIREOLDINTERVALS(i)

5     if length(active) = R then

6       SPILLATINTERVAL(i)

7     else

8       register[i] = a register removed from the pool of free registers.

9       Add i to active, sorted by increasing end point

10

11 EXPIREOLDINTERVALS(i)

12   foreach interval j in active, in order of increasing end point

13     if endpoint[j] ≥ startpoint[i] then

14       return

15     remove j from active

16     add register[j] to pool of free registers

17

18 SPILLATINTERVAL(i)

19   spill = last interval in active

20   register[i] = register[spill]

21   location[spill] = new stack location

22   remove spill from active

23   add i to active, sorted by increasing end point

![图](images/image_0089.gif)

上面的例程经过算法处理之后的寄存器分配结果如下：

![图](images/image_0090.png)

### 8.2.4 合并

上面的结果还不是最优解，需要经过合并

带合并过程的线性扫描算法如下：

![图](images/image_0091.gif)

1 LINEARSCANREGISTERALLOCATIONWITHCOALESCING

2   active = {}

3   foreach interval i, in order of increasing start point

4     EXPIREOLDINTERVALS(i)

5     if length(active) = R then

6       SPILLATINTERVAL(i)

7     else

8       if definition of i is "a = b" and register[b] ∈ free registers

9         register[i] = register[i(b)]

10         remove register[i(b)] from the list of free registers

11       else

12         register[i] = a register removed from the list of free registers

13       add i to active, sorted by increasing end point

![图](images/image_0092.gif)

合并之后的结果如下：

![图](images/image_0093.png)

### 8.2.5 生命周期黑洞

线性扫描不是最优解，在一些场景下，和最优解相差还非差大，例如多个分支之间存在生命周期黑洞的情况。

例如对下面的CFG：

![图](images/image_0094.png)

线性扫描处理的结果如下：

![图](images/image_0095.png)

按线性扫描的结果x和a是不能共寄存器的。但如果看生命周期分析结果：

![图](images/image_0096.png)

x出现后，a和b都不会再使用，也就是说x肯定是可以和a或者b共用一个寄存器的。

## 8.3 基于图着色的寄存器分配

### 8.3.1 干涉图（The Interference Graph）

最常见的寄存器分配算法家族是基于干涉图的理念推演出来的。

干涉图是基于控制流图中变量的生命周期范围的网络图：

每个变量是图的一个结点；

如果两个变量的生命周期存在重叠，则这2个节点在图上邻接，也就是存在一条边将2个节点连接起来，这样的边也称为干涉边（interference edges）。

除了干涉边，如果2个变量存在且仅存在一条move指令将2个变量关联起来，则在2个变量之间画一条虚线，称为合并边（coalescing edges）

### 8.3.2 肯普简化算法（Kempe's Simplification）

如果图中存在一个节点m，它的邻接节点小于k，设G' = G \ {m}，如果G'能被k种颜色着色，那么G也能被k种颜色着色。

通过肯普简化算法，可以将图简化到只有一个节点，或者简化到只剩下一些高阶节点，这样方便求出图的最小可着色的颜色数。

下面是简化过程：

![图](images/image_0097.png)

### 8.3.3 贪婪着色算法（Greedy Coloring）

贪婪着色的顺序是肯普简化算法删除节点的顺序的逆序，每次着色都要找一个邻接节点中不存在的颜色进行着色。

针对上面的简化过程，着色过程是这样的：

![图](images/image_0098.png)

![图](images/image_0099.png)

注意，上面的算法只是为了证明一个图是否能被K种颜色进行着色，但不关心是否可以用更少的颜色来着色。

## 8.4 循环进行寄存器合并

![图](images/image_0100.png)

### 8.4.1 build

就是基于生命周期生成干涉图的过程：

![图](images/image_0101.png)

### 8.4.2 simplify

使用肯普算法删除非move相关节点：

![图](images/image_0102.png)

### 8.4.3 Coalesce

合并过程是将move关联节点合并成一个：

![图](images/image_0103.png)

保守合并算法：

Briggs：节点a和b能合并当且仅当ab有更少的高阶（≥k）邻接节点

George：节点a和b能合并，当且仅当，所有a的邻接节点要么和b相互干涉，要么是一个低阶节点

### 8.4.4 freeze

如果前面的简化和合并都没有影响干涉图，尝试删除一条move干涉关系。

![图](images/image_0104.png)

### 8.4.5 潜在溢出

如果找不到低阶节点，就要选择一个节点作为潜在的溢出节点，并将它加入到简化节点栈中。

现在还没有确定会溢出，有可能着色时，发现很多标记了溢出的节点，能够有效着色。

### 8.4.6 溢出算法（Spilling Heuristics）

溢出算法的核心是找到一个溢出代价最小的节点，我们给循环内的节点一个更高的代价因子：

![图](images/image_0105.gif)

1 SPILLCOST(v)

2   cost = 0

3   foreach definition at block B, or use at block B

4     cost += 10^N/D, where

5       N is B's loop nesting factor

6       D is v's degree in the interference graph

![图](images/image_0106.gif)

![图](images/image_0107.png)

### 8.4.7 select

将栈中的节点出栈，并尝试用贪婪着色算法进行着色

![图](images/image_0108.png)

### 8.4.8 溢出

如果确定需要溢出，在变量前后增加load和store指令，并重新走一遍从build开始的整个迭代过程。

在只有3个寄存器的情况下，通过上面的计算，应该溢出c，将两次c的使用改成c0和c1，重新进行计算：

![图](images/image_0109.png)

![图](images/image_0110.png)

### 8.4.9 根据最终寄存器分配结果重写程序

![图](images/image_0111.png)

### 8.4.10 删除冗余的copy操作

![图](images/image_0112.png)

## 8.5 寄存器分配简史

Chaitin, G., Auslander, M., Chandra, A., Cocke, J., Hopkins, M., and Markstein, P. "Register allocation via coloring", Computer Languages, p 47-57 (1981)，首次将图着色引入寄存器分配。

George, L., and Appel, A., "Iterated Register Coalescing", North Holland, TOPLAS, p 300-324 (1996)，引入寄存器合并的迭代过程。

Poletto, M., and Sarkar, V., "Linear Scan Register Allocation", TOPLAS, p895-913 (1999)，将线性扫描引入寄存器分配。

## 8.6 LLVM的寄存器分配实现

### 8.6.1 LLVM中的基于生命期的寄存器分配

llvm\lib\CodeGen\RegAllocBase.cpp

| 1 | //===- RegAllocBase.cpp - Register Allocator Base Class -------------------===// |
| --- | --- |
| 2 | // |
| 3 | // Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions. |
| 4 | // See https://llvm.org/LICENSE.txt for license information. |
| 5 | // SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception |
| 6 | // |
| 7 | //===----------------------------------------------------------------------===// |
| 8 | // |
| 9 | // This file defines the RegAllocBase class which provides common functionality |
| 10 | // for LiveIntervalUnion-based register allocators. |
| 11 | // |
| 12 | //===----------------------------------------------------------------------===// |
| 13 |  |
| 14 | #include "RegAllocBase.h" |
| 15 | #include "llvm/ADT/SmallVector.h" |
| 16 | #include "llvm/ADT/Statistic.h" |
| 17 | #include "llvm/CodeGen/LiveInterval.h" |
| 18 | #include "llvm/CodeGen/LiveIntervals.h" |
| 19 | #include "llvm/CodeGen/LiveRegMatrix.h" |
| 20 | #include "llvm/CodeGen/MachineInstr.h" |
| 21 | #include "llvm/CodeGen/MachineModuleInfo.h" |
| 22 | #include "llvm/CodeGen/MachineRegisterInfo.h" |
| 23 | #include "llvm/CodeGen/Spiller.h" |
| 24 | #include "llvm/CodeGen/TargetRegisterInfo.h" |
| 25 | #include "llvm/CodeGen/VirtRegMap.h" |
| 26 | #include "llvm/Pass.h" |
| 27 | #include "llvm/Support/CommandLine.h" |
| 28 | #include "llvm/Support/Debug.h" |
| 29 | #include "llvm/Support/ErrorHandling.h" |
| 30 | #include "llvm/Support/Timer.h" |
| 31 | #include "llvm/Support/raw_ostream.h" |
| 32 | #include <cassert> |
| 33 |  |
| 34 | using namespace llvm; |
|   | // 定义一个宏，用于调试寄存器分配模块。 |
| 35 |  |
| 36 | #define DEBUG_TYPE "regalloc" |
|   | // 定义一个统计信息，用于记录新排队的活范围数量。 |
| 37 |  |
| 38 | STATISTIC(NumNewQueued    , "Number of new live ranges queued"); |
|   | // 临时的验证选项，直到我们可以将验证放入MachineVerifier中。 |
| 39 |  |
| 40 | // Temporary verification option until we can put verification inside |
| 41 | // MachineVerifier. |
|   | // 定义一个命令行选项，用于在寄存器分配期间启用验证。 |
| 42 | static cl::opt<bool, true> |
| 43 | VerifyRegAlloc("verify-regalloc", cl::location(RegAllocBase::VerifyEnabled), |
| 44 | cl::Hidden, cl::desc("Verify during register allocation")); |
| 45 |  |
| 46 | const char RegAllocBase::TimerGroupName[] = "regalloc"; |
|   | // 定义寄存器分配的定时器组名称和描述。 |
| 47 | const char RegAllocBase::TimerGroupDescription[] = "Register Allocation"; |
|   | // 初始化寄存器分配验证的启用状态为false。 |
| 48 | bool RegAllocBase::VerifyEnabled = false; |
| 49 |  |
| 50 | //===----------------------------------------------------------------------===// |
| 51 | //                         RegAllocBase Implementation |
| 52 | //===----------------------------------------------------------------------===// |
|   | // 固定虚表到这个文件，确保虚表只在此处定义。 |
| 53 |  |
| 54 | // Pin the vtable to this file. |
| 55 | void RegAllocBase::anchor() {} |
|   | // 初始化寄存器分配基础类。 |
| 56 |  |
| 57 | void RegAllocBase::init(VirtRegMap &vrm, |
| 58 | LiveIntervals &lis, |
| 59 | LiveRegMatrix &mat) { |
| 60 | TRI = &vrm.getTargetRegInfo(); |
| 61 | MRI = &vrm.getRegInfo(); |
| 62 | VRM = &vrm; |
| 63 | LIS = &lis; |
| 64 | Matrix = &mat; |
| 65 | MRI->freezeReservedRegs(vrm.getMachineFunction()); |
| 66 | RegClassInfo.runOnMachineFunction(vrm.getMachineFunction()); |
| 67 | } |
|   | // 访问所有活寄存器。如果它们已经被分配到物理寄存器，将它们与对应的LiveIntervalUnion统一， |
|   | // 否则将它们推入优先队列以便后续分配。 |
| 68 |  |
| 69 | // Visit all the live registers. If they are already assigned to a physical |
| 70 | // register, unify them with the corresponding LiveIntervalUnion, otherwise push |
| 71 | // them on the priority queue for later assignment. |
| 72 | void RegAllocBase::seedLiveRegs() { |
|   | // 创建一个命名区域定时器。 |
| 73 | NamedRegionTimer T("seed", "Seed Live Regs", TimerGroupName, |
| 74 | TimerGroupDescription, TimePassesIsEnabled); |
| 75 | for (unsigned i = 0, e = MRI->getNumVirtRegs(); i != e; ++i) { |
| 76 | unsigned Reg = Register::index2VirtReg(i); |
| 77 | if (MRI->reg_nodbg_empty(Reg)) |
| 78 | continue; |
| 79 | enqueue(&LIS->getInterval(Reg)); |
| 80 | } |
| 81 | } |
|   | // 顶层驱动函数，用于管理未分配VirtRegs的队列，并调用selectOrSplit实现。 |
| 82 |  |
| 83 | // Top-level driver to manage the queue of unassigned VirtRegs and call the |
| 84 | // selectOrSplit implementation. |
| 85 | void RegAllocBase::allocatePhysRegs() { |
|   | // 播种活寄存器。 |
| 86 | seedLiveRegs(); |
|   | // 继续为可用的物理寄存器分配虚拟寄存器。 |
| 87 |  |
| 88 | // Continue assigning vregs one at a time to available physical registers. |
| 89 | while (LiveInterval *VirtReg = dequeue()) { |
|   | // 确保寄存器尚未分配。 |
| 90 | assert(!VRM->hasPhys(VirtReg->reg) && "Register already assigned"); |
|   | // 当拼接代码片段时，未使用的寄存器可能会出现。 |
| 91 |  |
| 92 | // Unused registers can appear when the spiller coalesces snippets. |
| 93 | if (MRI->reg_nodbg_empty(VirtReg->reg)) { |
| 94 | LLVM_DEBUG(dbgs() << "Dropping unused " << *VirtReg << '\n'); |
| 95 | aboutToRemoveInterval(*VirtReg); |
| 96 | LIS->removeInterval(VirtReg->reg); |
| 97 | continue; |
| 98 | } |
|   | // 使所有干扰查询无效，活范围可能已经改变。 |
| 99 |  |
| 100 | // Invalidate all interference queries, live ranges could have changed. |
| 101 | Matrix->invalidateVirtRegs(); |
|   | // selectOrSplit请求分配器返回一个可用的物理寄存器（如果可能） |
|   | // 并填充由分割产生的新活区间列表。 |
| 102 |  |
| 103 | // selectOrSplit requests the allocator to return an available physical |
| 104 | // register if possible and populate a list of new live intervals that |
| 105 | // result from splitting. |
| 106 | LLVM_DEBUG(dbgs() << "\nselectOrSplit " |
| 107 | << TRI->getRegClassName(MRI->getRegClass(VirtReg->reg)) |
| 108 | << ':' << *VirtReg << " w=" << VirtReg->weight << '\n'); |
| 109 |  |
| 110 | using VirtRegVec = SmallVector<Register, 4>; |
| 111 |  |
| 112 | VirtRegVec SplitVRegs; |
| 113 | unsigned AvailablePhysReg = selectOrSplit(*VirtReg, SplitVRegs); |
| 114 |  |
| 115 | if (AvailablePhysReg == ~0u) { |
|   | // selectOrSplit未能找到寄存器！ |
|   | // 可能是由于内联汇编引起的。 |
|   | // ...（省略错误处理代码） |
|   | // 在报告错误后继续进行。 |
| 116 | // selectOrSplit failed to find a register! |
| 117 | // Probably caused by an inline asm. |
| 118 | MachineInstr *MI = nullptr; |
| 119 | for (MachineRegisterInfo::reg_instr_iterator |
| 120 | I = MRI->reg_instr_begin(VirtReg->reg), E = MRI->reg_instr_end(); |
| 121 | I != E; ) { |
| 122 | MI = &*(I++); |
| 123 | if (MI->isInlineAsm()) |
| 124 | break; |
| 125 | } |
| 126 | if (MI && MI->isInlineAsm()) { |
| 127 | MI->emitError("inline assembly requires more registers than available"); |
| 128 | } else if (MI) { |
| 129 | LLVMContext &Context = |
| 130 | MI->getParent()->getParent()->getMMI().getModule()->getContext(); |
| 131 | Context.emitError("ran out of registers during register allocation"); |
| 132 | } else { |
| 133 | report_fatal_error("ran out of registers during register allocation"); |
| 134 | } |
| 135 | // Keep going after reporting the error. |
| 136 | VRM->assignVirt2Phys(VirtReg->reg, |
| 137 | RegClassInfo.getOrder(MRI->getRegClass(VirtReg->reg)).front()); |
| 138 | continue; |
| 139 | } |
| 140 |  |
| 141 | if (AvailablePhysReg) |
| 142 | Matrix->assign(*VirtReg, AvailablePhysReg); |
| 143 |  |
| 144 | for (unsigned Reg : SplitVRegs) { |
| 145 | assert(LIS->hasInterval(Reg)); |
| 146 |  |
| 147 | LiveInterval *SplitVirtReg = &LIS->getInterval(Reg); |
| 148 | assert(!VRM->hasPhys(SplitVirtReg->reg) && "Register already assigned"); |
| 149 | if (MRI->reg_nodbg_empty(SplitVirtReg->reg)) { |
| 150 | assert(SplitVirtReg->empty() && "Non-empty but used interval"); |
| 151 | LLVM_DEBUG(dbgs() << "not queueing unused  " << *SplitVirtReg << '\n'); |
| 152 | aboutToRemoveInterval(*SplitVirtReg); |
| 153 | LIS->removeInterval(SplitVirtReg->reg); |
| 154 | continue; |
| 155 | } |
| 156 | LLVM_DEBUG(dbgs() << "queuing new interval: " << *SplitVirtReg << "\n"); |
| 157 | assert(Register::isVirtualRegister(SplitVirtReg->reg) && |
| 158 | "expect split value in virtual register"); |
|   | // 将新分割的虚拟寄存器加入队列。 |
| 159 | enqueue(SplitVirtReg); |
|   | // 增加新排队的活范围统计。 |
| 160 | ++NumNewQueued; |
| 161 | } |
| 162 | } |
| 163 | } |
| 164 |  |
| 165 | void RegAllocBase::postOptimization() { |
|   | // 调用 spiller 的 postOptimization 方法。 |
|   | // Spiller 是负责寄存器溢出处理的对象，这个方法可能包含一些优化后的清理工作。 |
| 166 | spiller().postOptimization(); |
|   | // 遍历所有已经标记为死亡的重排指令（DeadRemats）。 |
| 167 | for (auto DeadInst : DeadRemats) { |
|   | // 从LiveIntervals对象的映射中移除这些死亡指令。 |
|   | // 这是因为这些指令将要从指令流中移除，不再需要跟踪它们的寄存器使用情况。 |
| 168 | LIS->RemoveMachineInstrFromMaps(*DeadInst); |
|   | // 从它们的父指令组中移除这些死亡指令，实际上将它们从指令流中删除。 |
| 169 | DeadInst->eraseFromParent(); |
| 170 | } |
|   | // 清空死亡重排指令的列表，为下一次优化准备。 |
| 171 | DeadRemats.clear(); |
| 172 | } |

### 8.6.2 LLVM中的寄存器分配的贪婪算法

llvm\lib\CodeGen\RegAllocGreedy.cpp

llvm\lib\CodeGen\RegAllocGreedy.cpp

|   | // 定义一个宏DEBUG_TYPE，用于标识调试信息的类型，这里设置为"regalloc"，代表寄存器分配。 |
| --- | --- |
| 82 | #define DEBUG_TYPE "regalloc" |
|   | // 定义三个统计项，用于跟踪寄存器分配过程中的不同事件。 |
|   | // NumGlobalSplits：全局生命周期（live ranges）分割的次数。 |
| 83 |  |
| 84 | STATISTIC(NumGlobalSplits, "Number of split global live ranges"); |
| 85 | STATISTIC(NumLocalSplits,  "Number of split local live ranges"); |
| 86 | STATISTIC(NumEvicted,      "Number of interferences evicted"); |
|   | // 定义一个命令行选项SplitSpillMode，用于设置分割生命周期时的spill模式。 |
|   | // 这个选项是隐藏的，不会在命令行帮助中显示。 |
| 87 |  |
| 88 | static cl::opt<SplitEditor::ComplementSpillMode> SplitSpillMode( |
| 89 | "split-spill-mode", cl::Hidden, |
| 90 | cl::desc("Spill mode for splitting live ranges"), |
| 91 | cl::values(clEnumValN(SplitEditor::SM_Partition, "default", "Default"), |
| 92 | clEnumValN(SplitEditor::SM_Size, "size", "Optimize for size"), |
| 93 | clEnumValN(SplitEditor::SM_Speed, "speed", "Optimize for speed")), |
| 94 | cl::init(SplitEditor::SM_Speed)); |
|   | // 定义一个命令行选项LastChanceRecoloringMaxDepth，用于设置最后一次重新着色的最大深度。 |
| 95 |  |
| 96 | static cl::opt<unsigned> |
| 97 | LastChanceRecoloringMaxDepth("lcr-max-depth", cl::Hidden, |
| 98 | cl::desc("Last chance recoloring max depth"), |
| 99 | cl::init(5)); |
|   | // 定义一个命令行选项LastChanceRecoloringMaxInterference，用于设置最后一次重新着色时考虑的最大干扰数。 |
| 100 |  |
| 101 | static cl::opt<unsigned> LastChanceRecoloringMaxInterference( |
| 102 | "lcr-max-interf", cl::Hidden, |
| 103 | cl::desc("Last chance recoloring maximum number of considered" |
| 104 | " interference at a time"), |
| 105 | cl::init(8)); |
|   | // 定义一个命令行选项ExhaustiveSearch，用于设置是否进行穷举搜索以寻找寄存器， |
|   | // 绕过最后一次重新着色的深度和干扰限制。 |
| 106 |  |
| 107 | static cl::opt<bool> ExhaustiveSearch( |
| 108 | "exhaustive-register-search", cl::NotHidden, |
| 109 | cl::desc("Exhaustive Search for registers bypassing the depth " |
| 110 | "and interference cutoffs of last chance recoloring"), |
| 111 | cl::Hidden); |
|   | // 定义一个命令行选项EnableLocalReassignment，用于设置是否启用局部重新分配， |
|   | // 这可能会产生更好的分配决策，但可能会增加编译时间。 |
| 112 |  |
| 113 | static cl::opt<bool> EnableLocalReassignment( |
| 114 | "enable-local-reassign", cl::Hidden, |
| 115 | cl::desc("Local reassignment can yield better allocation decisions, but " |
| 116 | "may be compile time intensive"), |
| 117 | cl::init(false)); |
|   | // 定义一个命令行选项EnableDeferredSpilling，用于设置是否启用延迟spilling， |
|   | // 即不立即spill变量，而是推迟到分配结束时。 |
| 118 |  |
| 119 | static cl::opt<bool> EnableDeferredSpilling( |
| 120 | "enable-deferred-spilling", cl::Hidden, |
| 121 | cl::desc("Instead of spilling a variable right away, defer the actual " |
| 122 | "code insertion to the end of the allocation. That way the " |
| 123 | "allocator might still find a suitable coloring for this " |
| 124 | "variable because of other evicted variables."), |
| 125 | cl::init(false)); |
|   | // FIXME: 找到一个合适的默认值替换这个标志，并移除这个标志。 |
|   | // 定义一个命令行选项CSRFirstTimeCost，用于设置 |
|   | // 调用者保存寄存器（callee-saved register）第一次使用时的成本。 |
|   | // 这个选项是隐藏的，不会在命令行帮助中显示，并且默认初始化为0。 |
| 126 |  |
| 127 | // FIXME: Find a good default for this flag and remove the flag. |
| 128 | static cl::opt<unsigned> |
| 129 | CSRFirstTimeCost("regalloc-csr-first-time-cost", |
| 130 | cl::desc("Cost for first time use of callee-saved register."), |
| 131 | cl::init(0), cl::Hidden); |
|   | // 定义一个命令行选项ConsiderLocalIntervalCost，用于设置在选择最佳分割候选时是否考虑由分割候选创建的局部区间的成本。 |
| 132 |  |
| 133 | static cl::opt<bool> ConsiderLocalIntervalCost( |
| 134 | "consider-local-interval-cost", cl::Hidden, |
| 135 | cl::desc("Consider the cost of local intervals created by a split " |
| 136 | "candidate when choosing the best split candidate."), |
| 137 | cl::init(false)); |
|   | // 定义一个贪婪寄存器分配器（greedy register allocator）。 |
|   | // RegisterRegAlloc是一个用于注册寄存器分配器的类，这里注册了一个名为"greedy"的贪婪寄存器分配器。 |
|   | // createGreedyRegisterAllocator是一个工厂函数，用于创建贪婪寄存器分配器的实例。 |
| 138 |  |
| 139 | static RegisterRegAlloc greedyRegAlloc("greedy", "greedy register allocator", |
| 140 | createGreedyRegisterAllocator); |
| 141 |  |
| 142 | namespace { |
|   | // RAGreedy类继承自MachineFunctionPass、RegAllocBase， |
|   | // 并实现了LiveRangeEdit::Delegate接口。 |
| 143 |  |
| 144 | class RAGreedy : public MachineFunctionPass, |
| 145 | public RegAllocBase, |
| 146 | private LiveRangeEdit::Delegate { |
|   | // 定义了一些方便的别名。 |
| 147 | // Convenient shortcuts. |
| 148 | using PQueue = std::priority_queue<std::pair<unsigned, unsigned>>; |
| 149 | using SmallLISet = SmallPtrSet<LiveInterval *, 4>; |
| 150 | using SmallVirtRegSet = SmallSet<unsigned, 16>; |
|   | // 上下文环境 |
| 151 |  |
| 152 | // context |
| 153 | MachineFunction *MF; |
|   | // 一些有用的接口的快捷方式。 |
| 154 |  |
| 155 | // Shortcuts to some useful interface. |
| 156 | const TargetInstrInfo *TII; |
| 157 | const TargetRegisterInfo *TRI; |
| 158 | RegisterClassInfo RCI; |
|   | // 分析结果 |
| 159 |  |
| 160 | // analyses |
| 161 | SlotIndexes *Indexes; |
| 162 | MachineBlockFrequencyInfo *MBFI; |
| 163 | MachineDominatorTree *DomTree; |
| 164 | MachineLoopInfo *Loops; |
| 165 | MachineOptimizationRemarkEmitter *ORE; |
| 166 | EdgeBundles *Bundles; |
| 167 | SpillPlacement *SpillPlacer; |
| 168 | LiveDebugVariables *DebugVars; |
| 169 | AliasAnalysis *AA; |
|   | // 状态 |
| 170 |  |
| 171 | // state |
| 172 | std::unique_ptr<Spiller> SpillerInstance; |
| 173 | PQueue Queue; |
| 174 | unsigned NextCascade; |
|   | // Live ranges通过多个阶段进行分配。 |
| 175 |  |
| 176 | // Live ranges pass through a number of stages as we try to allocate them. |
| 177 | // Some of the stages may also create new live ranges: |
| 178 | // |
| 179 | // - Region splitting. |
| 180 | // - Per-block splitting. |
| 181 | // - Local splitting. |
| 182 | // - Spilling. |
| 183 | // |
| 184 | // Ranges produced by one of the stages skip the previous stages when they are |
| 185 | // dequeued. This improves performance because we can skip interference checks |
| 186 | // that are unlikely to give any results. It also guarantees that the live |
| 187 | // range splitting algorithm terminates, something that is otherwise hard to |
| 188 | // ensure. |
| 189 | enum LiveRangeStage { |
|   | // 新创建的live range，从未入队。 |
| 190 | /// Newly created live range that has never been queued. |
| 191 | RS_New, |
|   | // 仅尝试分配和驱逐，然后重新入队为RS_Split。 |
| 192 |  |
| 193 | /// Only attempt assignment and eviction. Then requeue as RS_Split. |
| 194 | RS_Assign, |
|   | // 如果分配不可能，则尝试live range分割。 |
| 195 |  |
| 196 | /// Attempt live range splitting if assignment is impossible. |
| 197 | RS_Split, |
|   | // 尝试更激进的live range分割，保证有进展。 |
| 198 |  |
| 199 | /// Attempt more aggressive live range splitting that is guaranteed to make |
| 200 | /// progress.  This is used for split products that may not be making |
| 201 | /// progress. |
| 202 | RS_Split2, |
|   | // live range将被spill，不再尝试分割。 |
| 203 |  |
| 204 | /// Live range will be spilled.  No more splitting will be attempted. |
| 205 | RS_Spill, |
|   | // live range在内存中，可能因为其他驱逐而最终进入寄存器。 |
| 206 |  |
| 207 |  |
| 208 | /// Live range is in memory. Because of other evictions, it might get moved |
| 209 | /// in a register in the end. |
| 210 | RS_Memory, |
|   | // 对这个live range无更多操作，如果不能分配则中止编译。 |
| 211 |  |
| 212 | /// There is nothing more we can do to this live range.  Abort compilation |
| 213 | /// if it can't be assigned. |
| 214 | RS_Done |
| 215 | }; |
|   | // CutOffStage枚举用于跟踪寄存器分配是否因为最后一次重新着色的截止而失败。 |
| 216 |  |
| 217 | // Enum CutOffStage to keep a track whether the register allocation failed |
| 218 | // because of the cutoffs encountered in last chance recoloring. |
| 219 | // Note: This is used as bitmask. New value should be next power of 2. |
| 220 | enum CutOffStage { |
|   | // 没有遇到截止。 |
| 221 | // No cutoffs encountered |
| 222 | CO_None = 0, |
|   | // 遇到了lcr-max-depth截止。 |
| 223 |  |
| 224 | // lcr-max-depth cutoff encountered |
| 225 | CO_Depth = 1, |
|   | // 遇到了lcr-max-interf截止。 |
| 226 |  |
| 227 | // lcr-max-interf cutoff encountered |
| 228 | CO_Interf = 2 |
| 229 | }; |
|   | // 截止信息。 |
| 230 |  |
| 231 | uint8_t CutOffInfo; |
| 232 |  |
| 233 | #ifndef NDEBUG |
|   | // 调试时使用的阶段名称数组。 |
| 234 | static const char *const StageName[]; |
| 235 | #endif |
|   | // RegInfo结构体用于存储每个live range的额外信息。 |
| 236 |  |
| 237 | // RegInfo - Keep additional information about each live range. |
| 238 | struct RegInfo { |
|   | // 当前阶段。 |
| 239 | LiveRangeStage Stage = RS_New; |
|   | // 防止驱逐循环的级联编号。 |
| 240 |  |
| 241 | // Cascade - Eviction loop prevention. See canEvictInterference(). |
| 242 | unsigned Cascade = 0; |
|   | // 默认构造函数。 |
| 243 |  |
| 244 | RegInfo() = default; |
| 245 | }; |
| 246 |  |
| 247 | IndexedMap<RegInfo, VirtReg2IndexFunctor> ExtraRegInfo; |
|   | // 获取虚拟寄存器的当前阶段。 |
| 248 |  |
| 249 | LiveRangeStage getStage(const LiveInterval &VirtReg) const { |
| 250 | return ExtraRegInfo[VirtReg.reg].Stage; |
| 251 | } |
|   | // 设置虚拟寄存器的阶段。 |
| 252 |  |
| 253 | void setStage(const LiveInterval &VirtReg, LiveRangeStage Stage) { |
| 254 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
| 255 | ExtraRegInfo[VirtReg.reg].Stage = Stage; |
| 256 | } |
|   | // 为一系列虚拟寄存器设置阶段。 |
| 257 |  |
| 258 | template<typename Iterator> |
| 259 | void setStage(Iterator Begin, Iterator End, LiveRangeStage NewStage) { |
| 260 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
| 261 | for (;Begin != End; ++Begin) { |
| 262 | unsigned Reg = *Begin; |
| 263 | if (ExtraRegInfo[Reg].Stage == RS_New) |
| 264 | ExtraRegInfo[Reg].Stage = NewStage; |
| 265 | } |
| 266 | } |
|   | // 驱逐干扰的成本结构体。 |
| 267 |  |
| 268 | /// Cost of evicting interference. |
| 269 | struct EvictionCost { |
| 270 | unsigned BrokenHints = 0; ///< Total number of broken hints. |
| 271 | float MaxWeight = 0;      ///< Maximum spill weight evicted. |
| 272 |  |
| 273 | EvictionCost() = default; |
| 274 |  |
| 275 | bool isMax() const { return BrokenHints == ~0u; } |
| 276 |  |
| 277 | void setMax() { BrokenHints = ~0u; } |
| 278 |  |
| 279 | void setBrokenHints(unsigned NHints) { BrokenHints = NHints; } |
| 280 |  |
| 281 | bool operator<(const EvictionCost &O) const { |
| 282 | return std::tie(BrokenHints, MaxWeight) < |
| 283 | std::tie(O.BrokenHints, O.MaxWeight); |
| 284 | } |
| 285 | }; |
|   | /// 这个类用于跟踪过去的驱逐事件，以便优化区域分割决策。 |
| 286 |  |
| 287 | /// EvictionTrack - Keeps track of past evictions in order to optimize region |
| 288 | /// split decision. |
| 289 | class EvictionTrack { |
| 290 |  |
| 291 | public: |
|   | /// 使用 std::pair 来存储驱逐者信息，包括驱逐者虚拟寄存器编号和物理寄存器编号。 |
| 292 | using EvictorInfo = |
| 293 | std::pair<unsigned /* evictor */, unsigned /* physreg */>; |
|   | /// 使用 llvm::DenseMap 来存储被驱逐者信息，键为被驱逐者虚拟寄存器编号，值为驱逐者信息。 |
| 294 | using EvicteeInfo = llvm::DenseMap<unsigned /* evictee */, EvictorInfo>; |
| 295 |  |
| 296 | private: |
|   | /// 存储每个在 selectOrSplit 最后阶段被驱逐的虚拟寄存器（Vreg）， |
|   | /// 映射到驱逐它的虚拟寄存器和它被驱逐的物理寄存器。 |
| 297 | /// Each Vreg that has been evicted in the last stage of selectOrSplit will |
| 298 | /// be mapped to the evictor Vreg and the PhysReg it was evicted from. |
| 299 | EvicteeInfo Evictees; |
| 300 |  |
| 301 | public: |
|   | /// 清除所有驱逐信息。 |
| 302 | /// Clear all eviction information. |
| 303 | void clear() { Evictees.clear(); } |
|   | /// 清除给定被驱逐者 Vreg 的驱逐信息。 |
|   | /// 当 Vreg 获得新的分配时，旧的驱逐信息不再相关。 |
|   | /// \param Evictee 我们想要清除收集的驱逐信息的被驱逐者 Vreg。 |
| 304 |  |
| 305 | ///  Clear eviction information for the given evictee Vreg. |
| 306 | /// E.g. when Vreg get's a new allocation, the old eviction info is no |
| 307 | /// longer relevant. |
| 308 | /// \param Evictee The evictee Vreg for whom we want to clear collected |
| 309 | /// eviction info. |
| 310 | void clearEvicteeInfo(unsigned Evictee) { Evictees.erase(Evictee); } |
|   | /// 跟踪新的驱逐事件。 |
|   | /// 驱逐者 vreg 已经从 Physreg 驱逐了被驱逐者 vreg。 |
|   | /// \param PhysReg 被驱逐者被驱逐的物理寄存器。 |
|   | /// \param Evictor 驱逐被驱逐者的虚拟寄存器。 |
|   | /// \param Evictee 被驱逐的虚拟寄存器。 |
| 311 |  |
| 312 | /// Track new eviction. |
| 313 | /// The Evictor vreg has evicted the Evictee vreg from Physreg. |
| 314 | /// \param PhysReg The physical register Evictee was evicted from. |
| 315 | /// \param Evictor The evictor Vreg that evicted Evictee. |
| 316 | /// \param Evictee The evictee Vreg. |
| 317 | void addEviction(unsigned PhysReg, unsigned Evictor, unsigned Evictee) { |
| 318 | Evictees[Evictee].first = Evictor; |
| 319 | Evictees[Evictee].second = PhysReg; |
| 320 | } |
|   | /// 返回驱逐 Evictee Vreg 从 PhysReg 的驱逐者 Vreg。 |
|   | /// \param Evictee 被驱逐的虚拟寄存器。 |
|   | /// \return 驱逐 Evictee Vreg 从 PhysReg 的驱逐者 Vreg。如果没有人驱逐 Evictee 从 PhysReg，则返回 0。 |
| 321 |  |
| 322 | /// Return the Evictor Vreg which evicted Evictee Vreg from PhysReg. |
| 323 | /// \param Evictee The evictee vreg. |
| 324 | /// \return The Evictor vreg which evicted Evictee vreg from PhysReg. 0 if |
| 325 | /// nobody has evicted Evictee from PhysReg. |
| 326 | EvictorInfo getEvictor(unsigned Evictee) { |
| 327 | if (Evictees.count(Evictee)) { |
| 328 | return Evictees[Evictee]; |
| 329 | } |
| 330 |  |
| 331 | return EvictorInfo(0, 0); |
| 332 | } |
| 333 | }; |
|   | // 用于跟踪过去的驱逐事件，以便优化区域分割决策。 |
| 334 |  |
| 335 | // Keeps track of past evictions in order to optimize region split decision. |
| 336 | EvictionTrack LastEvicted; |
|   | // 分割状态。 |
| 337 |  |
| 338 | // splitting state. |
| 339 | std::unique_ptr<SplitAnalysis> SA; |
| 340 | std::unique_ptr<SplitEditor> SE; |
|   | /// 缓存每个代码块的干扰图。 |
| 341 |  |
| 342 | /// Cached per-block interference maps |
| 343 | InterferenceCache IntfCache; |
|   | /// 存储当前寄存器使用的所有基本块。 |
| 344 |  |
| 345 | /// All basic blocks where the current register has uses. |
| 346 | SmallVector<SpillPlacement::BlockConstraint, 8> SplitConstraints; |
|   | /// 全局活范围分割候选信息。 |
| 347 |  |
| 348 | /// Global live range splitting candidate info. |
| 349 | struct GlobalSplitCandidate { |
|   | // 打算分配给该候选的寄存器，或者为0。 |
| 350 | // Register intended for assignment, or 0. |
| 351 | unsigned PhysReg; |
|   | // SplitKit区间索引，用于这个候选。 |
| 352 |  |
| 353 | // SplitKit interval index for this candidate. |
| 354 | unsigned IntvIdx; |
|   | // 对于PhysReg的干扰。 |
| 355 |  |
| 356 | // Interference for PhysReg. |
| 357 | InterferenceCache::Cursor Intf; |
|   | // 候选应该活跃的束。 |
| 358 |  |
| 359 | // Bundles where this candidate should be live. |
| 360 | BitVector LiveBundles; |
| 361 | SmallVector<unsigned, 8> ActiveBlocks; |
|   | // 重置候选信息。 |
| 362 |  |
| 363 | void reset(InterferenceCache &Cache, unsigned Reg) { |
| 364 | PhysReg = Reg; |
| 365 | IntvIdx = 0; |
| 366 | Intf.setPhysReg(Cache, Reg); |
| 367 | LiveBundles.clear(); |
| 368 | ActiveBlocks.clear(); |
| 369 | } |
|   | // 在所有LiveBundles中将B[i]设置为C，如果B[i]之前是NoCand。 |
| 370 |  |
| 371 | // Set B[i] = C for every live bundle where B[i] was NoCand. |
| 372 | unsigned getBundles(SmallVectorImpl<unsigned> &B, unsigned C) { |
| 373 | unsigned Count = 0; |
| 374 | for (unsigned i : LiveBundles.set_bits()) |
| 375 | if (B[i] == NoCand) { |
| 376 | B[i] = C; |
| 377 | Count++; |
| 378 | } |
| 379 | return Count; |
| 380 | } |
| 381 | }; |
|   | /// 分配顺序中每个物理寄存器的候选信息。 |
|   | /// 这个向量不会缩小，但会增长到最大寄存器类的大小。 |
| 382 |  |
| 383 | /// Candidate info for each PhysReg in AllocationOrder. |
| 384 | /// This vector never shrinks, but grows to the size of the largest register |
| 385 | /// class. |
| 386 | SmallVector<GlobalSplitCandidate, 32> GlobalCand; |
|   | // 枚举值，表示没有候选。 |
| 387 |  |
| 388 | enum : unsigned { NoCand = ~0u }; |
|   | /// 候选映射。每个边缘束被分配给一个GlobalCand条目，或者NoCand，表示栈区间。 |
| 389 |  |
| 390 | /// Candidate map. Each edge bundle is assigned to a GlobalCand entry, or to |
| 391 | /// NoCand which indicates the stack interval. |
| 392 | SmallVector<unsigned, 32> BundleCand; |
|   | /// 被调用者保存寄存器成本，每个机器函数计算一次。 |
| 393 |  |
| 394 | /// Callee-save register cost, calculated once per machine function. |
| 395 | BlockFrequency CSRCost; |
|   | /// 是否运行局部重新分配启发式。这些信息从TargetSubtargetInfo获得。 |
| 396 |  |
| 397 | /// Run or not the local reassignment heuristic. This information is |
| 398 | /// obtained from the TargetSubtargetInfo. |
| 399 | bool EnableLocalReassign; |
|   | /// 是否启用在选择最佳分割候选时考虑由分割候选创建的局部区间的成本。 |
| 400 |  |
| 401 | /// Enable or not the consideration of the cost of local intervals created |
| 402 | /// by a split candidate when choosing the best split candidate. |
| 403 | bool EnableAdvancedRASplitCost; |
|   | /// 可能稍后因驱逐而协调的断开的提示集合。 |
| 404 |  |
| 405 | /// Set of broken hints that may be reconciled later because of eviction. |
| 406 | SmallSetVector<LiveInterval *, 8> SetOfBrokenHints; |
| 407 |  |
| 408 | public: |
| 409 | RAGreedy(); |
|   | /// 返回pass名称。 |
| 410 |  |
| 411 | /// Return the pass name. |
| 412 | StringRef getPassName() const override { return "Greedy Register Allocator"; } |
|   | /// RAGreedy分析使用情况。 |
| 413 |  |
| 414 | /// RAGreedy analysis usage. |
| 415 | void getAnalysisUsage(AnalysisUsage &AU) const override; |
| 416 | void releaseMemory() override; |
| 417 | Spiller &spiller() override { return *SpillerInstance; } |
| 418 | void enqueue(LiveInterval *LI) override; |
| 419 | LiveInterval *dequeue() override; |
| 420 | Register selectOrSplit(LiveInterval&, SmallVectorImpl<Register>&) override; |
| 421 | void aboutToRemoveInterval(LiveInterval &) override; |
|   | /// 执行寄存器分配。 |
| 422 |  |
| 423 | /// Perform register allocation. |
| 424 | bool runOnMachineFunction(MachineFunction &mf) override; |
| 425 |  |
| 426 | MachineFunctionProperties getRequiredProperties() const override { |
| 427 | return MachineFunctionProperties().set( |
| 428 | MachineFunctionProperties::Property::NoPHIs); |
| 429 | } |
| 430 |  |
| 431 | static char ID; |
| 432 |  |
| 433 | private: |
| 434 | Register selectOrSplitImpl(LiveInterval &, SmallVectorImpl<Register> &, |
| 435 | SmallVirtRegSet &, unsigned = 0); |
| 436 |  |
| 437 | bool LRE_CanEraseVirtReg(unsigned) override; |
| 438 | void LRE_WillShrinkVirtReg(unsigned) override; |
| 439 | void LRE_DidCloneVirtReg(unsigned, unsigned) override; |
| 440 | void enqueue(PQueue &CurQueue, LiveInterval *LI); |
| 441 | LiveInterval *dequeue(PQueue &CurQueue); |
| 442 |  |
| 443 | BlockFrequency calcSpillCost(); |
| 444 | bool addSplitConstraints(InterferenceCache::Cursor, BlockFrequency&); |
| 445 | bool addThroughConstraints(InterferenceCache::Cursor, ArrayRef<unsigned>); |
| 446 | bool growRegion(GlobalSplitCandidate &Cand); |
| 447 | bool splitCanCauseEvictionChain(unsigned Evictee, GlobalSplitCandidate &Cand, |
| 448 | unsigned BBNumber, |
| 449 | const AllocationOrder &Order); |
| 450 | bool splitCanCauseLocalSpill(unsigned VirtRegToSplit, |
| 451 | GlobalSplitCandidate &Cand, unsigned BBNumber, |
| 452 | const AllocationOrder &Order); |
| 453 | BlockFrequency calcGlobalSplitCost(GlobalSplitCandidate &, |
| 454 | const AllocationOrder &Order, |
| 455 | bool *CanCauseEvictionChain); |
| 456 | bool calcCompactRegion(GlobalSplitCandidate&); |
| 457 | void splitAroundRegion(LiveRangeEdit&, ArrayRef<unsigned>); |
| 458 | void calcGapWeights(unsigned, SmallVectorImpl<float>&); |
| 459 | Register canReassign(LiveInterval &VirtReg, Register PrevReg); |
| 460 | bool shouldEvict(LiveInterval &A, bool, LiveInterval &B, bool); |
| 461 | bool canEvictInterference(LiveInterval&, Register, bool, EvictionCost&, |
| 462 | const SmallVirtRegSet&); |
| 463 | bool canEvictInterferenceInRange(LiveInterval &VirtReg, Register oPhysReg, |
| 464 | SlotIndex Start, SlotIndex End, |
| 465 | EvictionCost &MaxCost); |
| 466 | unsigned getCheapestEvicteeWeight(const AllocationOrder &Order, |
| 467 | LiveInterval &VirtReg, SlotIndex Start, |
| 468 | SlotIndex End, float *BestEvictWeight); |
| 469 | void evictInterference(LiveInterval&, Register, |
| 470 | SmallVectorImpl<Register>&); |
| 471 | bool mayRecolorAllInterferences(unsigned PhysReg, LiveInterval &VirtReg, |
| 472 | SmallLISet &RecoloringCandidates, |
| 473 | const SmallVirtRegSet &FixedRegisters); |
| 474 |  |
| 475 | Register tryAssign(LiveInterval&, AllocationOrder&, |
| 476 | SmallVectorImpl<Register>&, |
| 477 | const SmallVirtRegSet&); |
| 478 | unsigned tryEvict(LiveInterval&, AllocationOrder&, |
| 479 | SmallVectorImpl<Register>&, unsigned, |
| 480 | const SmallVirtRegSet&); |
| 481 | unsigned tryRegionSplit(LiveInterval&, AllocationOrder&, |
| 482 | SmallVectorImpl<Register>&); |
|   | /// 计算区域分割的成本。 |
| 483 | /// Calculate cost of region splitting. |
| 484 | unsigned calculateRegionSplitCost(LiveInterval &VirtReg, |
| 485 | AllocationOrder &Order, |
| 486 | BlockFrequency &BestCost, |
| 487 | unsigned &NumCands, bool IgnoreCSR, |
| 488 | bool *CanCauseEvictionChain = nullptr); |
|   | /// 执行区域分割。 |
| 489 | /// Perform region splitting. |
| 490 | unsigned doRegionSplit(LiveInterval &VirtReg, unsigned BestCand, |
| 491 | bool HasCompact, |
| 492 | SmallVectorImpl<Register> &NewVRegs); |
|   | /// 在首次使用被调用者保存寄存器之前检查其他选项。 |
| 493 | /// Check other options before using a callee-saved register for the first |
| 494 | /// time. |
| 495 | unsigned tryAssignCSRFirstTime(LiveInterval &VirtReg, AllocationOrder &Order, |
| 496 | Register PhysReg, unsigned &CostPerUseLimit, |
| 497 | SmallVectorImpl<Register> &NewVRegs); |
| 498 | void initializeCSRCost(); |
| 499 | unsigned tryBlockSplit(LiveInterval&, AllocationOrder&, |
| 500 | SmallVectorImpl<Register>&); |
| 501 | unsigned tryInstructionSplit(LiveInterval&, AllocationOrder&, |
| 502 | SmallVectorImpl<Register>&); |
| 503 | unsigned tryLocalSplit(LiveInterval&, AllocationOrder&, |
| 504 | SmallVectorImpl<Register>&); |
| 505 | unsigned trySplit(LiveInterval&, AllocationOrder&, |
| 506 | SmallVectorImpl<Register>&, |
| 507 | const SmallVirtRegSet&); |
| 508 | unsigned tryLastChanceRecoloring(LiveInterval &, AllocationOrder &, |
| 509 | SmallVectorImpl<Register> &, |
| 510 | SmallVirtRegSet &, unsigned); |
| 511 | bool tryRecoloringCandidates(PQueue &, SmallVectorImpl<Register> &, |
| 512 | SmallVirtRegSet &, unsigned); |
| 513 | void tryHintRecoloring(LiveInterval &); |
| 514 | void tryHintsRecoloring(); |
|   | /// 模拟一个复制端所携带的信息。 |
| 515 |  |
| 516 | /// Model the information carried by one end of a copy. |
| 517 | struct HintInfo { |
|   | /// 复制的频率。 |
| 518 | /// The frequency of the copy. |
| 519 | BlockFrequency Freq; |
|   | /// 虚拟寄存器或物理寄存器。 |
| 520 | /// The virtual register or physical register. |
| 521 | Register Reg; |
|   | /// 其当前分配的寄存器。 |
|   | /// 如果Reg == PhysReg，则为物理寄存器。 |
| 522 | /// Its currently assigned register. |
| 523 | /// In case of a physical register Reg == PhysReg. |
| 524 | MCRegister PhysReg; |
| 525 |  |
| 526 | HintInfo(BlockFrequency Freq, Register Reg, MCRegister PhysReg) |
| 527 | : Freq(Freq), Reg(Reg), PhysReg(PhysReg) {} |
| 528 | }; |
| 529 | using HintsInfo = SmallVector<HintInfo, 4>; |
|   | // 获取断开的提示信息的频率。 |
| 530 |  |
| 531 | BlockFrequency getBrokenHintFreq(const HintsInfo &, unsigned); |
|   | // 收集与给定虚拟寄存器相关的提示信息。 |
| 532 | void collectHintInfo(unsigned, HintsInfo &); |
|   | // 检查给定的物理寄存器是否是未使用的被调用者保存寄存器。 |
| 533 |  |
| 534 | bool isUnusedCalleeSavedReg(MCRegister PhysReg) const; |
|   | /// 计算并报告循环中的溢出和重载数量。 |
|   | // 该函数用于计算给定循环L中的溢出和重载数量。 |
|   | // Reloads: 重载的数量 |
|   | // FoldedReloads: 折叠的重载数量 |
|   | // Spills: 溢出的数量 |
|   | // FoldedSpills: 折叠的溢出数量 |
| 535 |  |
| 536 | /// Compute and report the number of spills and reloads for a loop. |
| 537 | void reportNumberOfSplillsReloads(MachineLoop *L, unsigned &Reloads, |
| 538 | unsigned &FoldedReloads, unsigned &Spills, |
| 539 | unsigned &FoldedSpills); |
|   | /// 报告每个循环中的溢出和重载数量。 |
|   | // 该函数遍历所有循环，并使用reportNumberOfSplillsReloads函数报告每个循环的 |
|   | // 溢出和重载数量。Loops是一个包含所有循环的集合。 |
| 540 |  |
| 541 | /// Report the number of spills and reloads for each loop. |
| 542 | void reportNumberOfSplillsReloads() { |
| 543 | for (MachineLoop *L : *Loops) { |
| 544 | unsigned Reloads, FoldedReloads, Spills, FoldedSpills; |
| 545 | reportNumberOfSplillsReloads(L, Reloads, FoldedReloads, Spills, |
| 546 | FoldedSpills); |
| 547 | } |
| 548 | } |
| 549 | }; |
| 550 |  |
| 551 | } // end anonymous namespace |
|   | // 为 RAGreedy 类的 ID 静态成员变量赋值。 |
| 552 |  |
| 553 | char RAGreedy::ID = 0; |
|   | // 将 llvm 命名空间中的 RAGreedyID 引用指向 RAGreedy 类的 ID。 |
| 554 | char &llvm::RAGreedyID = RAGreedy::ID; |
|   | // 初始化 RAGreedy pass的开始。 |
| 555 |  |
| 556 | INITIALIZE_PASS_BEGIN(RAGreedy, "greedy", |
| 557 | "Greedy Register Allocator", false, false) |
|   | // 添加 RAGreedy pass依赖的其他pass。 |
| 558 | INITIALIZE_PASS_DEPENDENCY(LiveDebugVariables) |
| 559 | INITIALIZE_PASS_DEPENDENCY(SlotIndexes) |
| 560 | INITIALIZE_PASS_DEPENDENCY(LiveIntervals) |
| 561 | INITIALIZE_PASS_DEPENDENCY(RegisterCoalescer) |
| 562 | INITIALIZE_PASS_DEPENDENCY(MachineScheduler) |
| 563 | INITIALIZE_PASS_DEPENDENCY(LiveStacks) |
| 564 | INITIALIZE_PASS_DEPENDENCY(MachineDominatorTree) |
| 565 | INITIALIZE_PASS_DEPENDENCY(MachineLoopInfo) |
| 566 | INITIALIZE_PASS_DEPENDENCY(VirtRegMap) |
| 567 | INITIALIZE_PASS_DEPENDENCY(LiveRegMatrix) |
| 568 | INITIALIZE_PASS_DEPENDENCY(EdgeBundles) |
| 569 | INITIALIZE_PASS_DEPENDENCY(SpillPlacement) |
| 570 | INITIALIZE_PASS_DEPENDENCY(MachineOptimizationRemarkEmitterPass) |
|   | // 初始化 RAGreedy pass的结束。 |
| 571 | INITIALIZE_PASS_END(RAGreedy, "greedy", |
| 572 | "Greedy Register Allocator", false, false) |
| 573 |  |
| 574 | #ifndef NDEBUG |
|   | // 在非生产模式下，定义 RAGreedy 阶段名称的数组。 |
| 575 | const char *const RAGreedy::StageName[] = { |
| 576 | "RS_New", |
| 577 | "RS_Assign", |
| 578 | "RS_Split", |
| 579 | "RS_Split2", |
| 580 | "RS_Spill", |
| 581 | "RS_Memory", |
| 582 | "RS_Done" |
| 583 | }; |
| 584 | #endif |
|   | // 比较浮点数时使用的滞后值。 |
|   | // 这有助于稳定基于浮点比较的决策。 |
| 585 |  |
| 586 | // Hysteresis to use when comparing floats. |
| 587 | // This helps stabilize decisions based on float comparisons. |
| 588 | const float Hysteresis = (2007 / 2048.0f); // 0.97998046875 |
|   | // 创建贪婪寄存器分配器实例的函数。 |
| 589 |  |
| 590 | FunctionPass* llvm::createGreedyRegisterAllocator() { |
| 591 | return new RAGreedy(); |
| 592 | } |
|   | // RAGreedy 构造函数。 |
| 593 |  |
| 594 | RAGreedy::RAGreedy(): MachineFunctionPass(ID) { |
| 595 | } |
|   | // 设置 RAGreedy pass的分析使用情况。 |
| 596 |  |
| 597 | void RAGreedy::getAnalysisUsage(AnalysisUsage &AU) const { |
| 598 | AU.setPreservesCFG(); |
| 599 | AU.addRequired<MachineBlockFrequencyInfo>(); |
| 600 | AU.addPreserved<MachineBlockFrequencyInfo>(); |
| 601 | AU.addRequired<AAResultsWrapperPass>(); |
| 602 | AU.addPreserved<AAResultsWrapperPass>(); |
| 603 | AU.addRequired<LiveIntervals>(); |
| 604 | AU.addPreserved<LiveIntervals>(); |
| 605 | AU.addRequired<SlotIndexes>(); |
| 606 | AU.addPreserved<SlotIndexes>(); |
| 607 | AU.addRequired<LiveDebugVariables>(); |
| 608 | AU.addPreserved<LiveDebugVariables>(); |
| 609 | AU.addRequired<LiveStacks>(); |
| 610 | AU.addPreserved<LiveStacks>(); |
| 611 | AU.addRequired<MachineDominatorTree>(); |
| 612 | AU.addPreserved<MachineDominatorTree>(); |
| 613 | AU.addRequired<MachineLoopInfo>(); |
| 614 | AU.addPreserved<MachineLoopInfo>(); |
| 615 | AU.addRequired<VirtRegMap>(); |
| 616 | AU.addPreserved<VirtRegMap>(); |
| 617 | AU.addRequired<LiveRegMatrix>(); |
| 618 | AU.addPreserved<LiveRegMatrix>(); |
| 619 | AU.addRequired<EdgeBundles>(); |
| 620 | AU.addRequired<SpillPlacement>(); |
| 621 | AU.addRequired<MachineOptimizationRemarkEmitterPass>(); |
| 622 | MachineFunctionPass::getAnalysisUsage(AU); |
| 623 | } |
| 624 |  |
| 625 | //===----------------------------------------------------------------------===// |
| 626 | //                     LiveRangeEdit delegate methods |
| 627 | //===----------------------------------------------------------------------===// |
|   | // LRE_CanEraseVirtReg - 确定是否可以擦除（即删除）一个虚拟寄存器。 |
| 628 |  |
| 629 | bool RAGreedy::LRE_CanEraseVirtReg(unsigned VirtReg) { |
| 630 | LiveInterval &LI = LIS->getInterval(VirtReg); |
|   | // 如果虚拟寄存器已经被分配了物理寄存器，则可以擦除。 |
| 631 | if (VRM->hasPhys(VirtReg)) { |
|   | // 从矩阵中取消分配。 |
| 632 | Matrix->unassign(LI); |
|   | // 通知即将移除区间。 |
| 633 | aboutToRemoveInterval(LI); |
| 634 | return true; |
| 635 | } |
|   | // 如果虚拟寄存器未分配物理寄存器，它可能在优先队列中。 |
|   | // RegAllocBase 将在出队后擦除它。 |
|   | // 尽管如此，清除活动范围，以便调试转储显示该 VirtReg 的正确状态。 |
| 636 | // Unassigned virtreg is probably in the priority queue. |
| 637 | // RegAllocBase will erase it after dequeueing. |
| 638 | // Nonetheless, clear the live-range so that the debug |
| 639 | // dump will show the right state for that VirtReg. |
| 640 | LI.clear(); |
| 641 | return false; |
| 642 | } |
|   | // LRE_WillShrinkVirtReg - 通知虚拟寄存器将要收缩（即减少其活动范围）。 |
| 643 |  |
| 644 | void RAGreedy::LRE_WillShrinkVirtReg(unsigned VirtReg) { |
| 645 | if (!VRM->hasPhys(VirtReg)) |
| 646 | return; |
|   | // 如果寄存器已经被分配，则将其重新放回队列以重新分配。 |
| 647 |  |
| 648 | // Register is assigned, put it back on the queue for reassignment. |
| 649 | LiveInterval &LI = LIS->getInterval(VirtReg); |
| 650 | Matrix->unassign(LI); |
| 651 | enqueue(&LI); |
| 652 | } |
|   | // LRE_DidCloneVirtReg - 通知虚拟寄存器已经被克隆。 |
| 653 |  |
| 654 | void RAGreedy::LRE_DidCloneVirtReg(unsigned New, unsigned Old) { |
|   | // 如果克隆的寄存器我们甚至还没有听说过，那么忽略它。 |
| 655 | // Cloning a register we haven't even heard about yet?  Just ignore it. |
| 656 | if (!ExtraRegInfo.inBounds(Old)) |
| 657 | return; |
|   | // LRE 可能会克隆一个虚拟寄存器，因为死代码消除导致它被分割成连接的组件。 |
|   | // 新的组件比原始的小得多，所以它们应该有机会被分配。 |
|   | // 与父级相同的阶段。 |
| 658 |  |
| 659 | // LRE may clone a virtual register because dead code elimination causes it to |
| 660 | // be split into connected components. The new components are much smaller |
| 661 | // than the original, so they should get a new chance at being assigned. |
| 662 | // same stage as the parent. |
| 663 | ExtraRegInfo[Old].Stage = RS_Assign; |
| 664 | ExtraRegInfo.grow(New); |
| 665 | ExtraRegInfo[New] = ExtraRegInfo[Old]; |
| 666 | } |
|   | // RAGreedy类的一个成员函数，用于释放内存资源 |
| 667 |  |
| 668 | void RAGreedy::releaseMemory() { |
|   | // 重置SpillerInstance，可能是用来释放与之相关的资源或内存 |
| 669 | SpillerInstance.reset(); |
|   | // 清空ExtraRegInfo，这可能是一个存储额外寄存器信息的容器 |
| 670 | ExtraRegInfo.clear(); |
|   | // 清空GlobalCand，这可能是一个存储全局候选信息的容器 |
| 671 | GlobalCand.clear(); |
| 672 | } |
|   | // RAGreedy类的一个成员函数，用于将LiveInterval对象加入队列 |
| 673 |  |
| 674 | void RAGreedy::enqueue(LiveInterval *LI) { enqueue(Queue, LI); } |
|   | // RAGreedy类的一个成员函数，用于将LiveInterval对象加入指定的优先队列 |
| 675 |  |
| 676 | void RAGreedy::enqueue(PQueue &CurQueue, LiveInterval *LI) { |
|   | // 根据LiveInterval的大小进行优先级排序，优先分配较大的区间 |
|   | // 队列中存储的是(size, reg)对 |
| 677 | // Prioritize live ranges by size, assigning larger ranges first. |
| 678 | // The queue holds (size, reg) pairs. |
| 679 | const unsigned Size = LI->getSize(); |
| 680 | const unsigned Reg = LI->reg; |
|   | // 断言确保只有虚拟寄存器才能被加入队列 |
| 681 | assert(Register::isVirtualRegister(Reg) && |
| 682 | "Can only enqueue virtual registers"); |
| 683 | unsigned Prio; |
|   | // 扩展ExtraRegInfo以包含当前寄存器的信息 |
| 684 |  |
| 685 | ExtraRegInfo.grow(Reg); |
|   | // 如果寄存器处于新状态，则更新其状态为分配状态 |
| 686 | if (ExtraRegInfo[Reg].Stage == RS_New) |
| 687 | ExtraRegInfo[Reg].Stage = RS_Assign; |
|   | // 如果寄存器处于分裂状态，则其优先级为其大小 |
| 688 |  |
| 689 | if (ExtraRegInfo[Reg].Stage == RS_Split) { |
| 690 | // Unsplit ranges that couldn't be allocated immediately are deferred until |
| 691 | // everything else has been allocated. |
| 692 | Prio = Size; |
| 693 | } else if (ExtraRegInfo[Reg].Stage == RS_Memory) { |
|   | // 内存操作数应该最后考虑，改变优先级使其按照相反的顺序分配 |
|   | // TODO: 将MemOp作为成员变量，并可能对提示进行处理 |
| 694 | // Memory operand should be considered last. |
| 695 | // Change the priority such that Memory operand are assigned in |
| 696 | // the reverse order that they came in. |
| 697 | // TODO: Make this a member variable and probably do something about hints. |
| 698 | static unsigned MemOp = 0; |
| 699 | Prio = MemOp++; |
| 700 | } else { |
|   | // 对于大型LiveInterval，使用全局分配启发式方法，以防止在极端情况下过度spill |
| 701 | // Giant live ranges fall back to the global assignment heuristic, which |
| 702 | // prevents excessive spilling in pathological cases. |
| 703 | bool ReverseLocal = TRI->reverseLocalAssignment(); |
| 704 | const TargetRegisterClass &RC = *MRI->getRegClass(Reg); |
| 705 | bool ForceGlobal = !ReverseLocal && |
| 706 | (Size / SlotIndex::InstrDist) > (2 * RC.getNumRegs()); |
|   | // 如果是分配状态且不是全局分配，且LiveInterval不为空，且在单个基本块中 |
| 707 |  |
| 708 | if (ExtraRegInfo[Reg].Stage == RS_Assign && !ForceGlobal && !LI->empty() && |
| 709 | LIS->intervalIsInOneMBB(*LI)) { |
|   | // 按照指令顺序分配原始局部范围，因为它们是单定义的， |
|   | // 这在没有全局干扰和其他约束的情况下产生最优着色 |
| 710 | // Allocate original local ranges in linear instruction order. Since they |
| 711 | // are singly defined, this produces optimal coloring in the absence of |
| 712 | // global interference and other constraints. |
| 713 | if (!ReverseLocal) |
| 714 | Prio = LI->beginIndex().getInstrDistance(Indexes->getLastIndex()); |
| 715 | else { |
|   | // 从底部向上分配可能允许许多短LRGs首先被分配到便宜的寄存器之一 |
|   | // 这对于具有许多物理寄存器的目标上的非常大的块可能要快得多 |
| 716 | // Allocating bottom up may allow many short LRGs to be assigned first |
| 717 | // to one of the cheap registers. This could be much faster for very |
| 718 | // large blocks on targets with many physical registers. |
| 719 | Prio = Indexes->getZeroIndex().getInstrDistance(LI->endIndex()); |
| 720 | } |
| 721 | Prio |= RC.AllocationPriority << 24; |
| 722 | } else { |
|   | // 全局和分裂范围按照长到短的顺序分配，不适合的长范围应该尽快spill或split， |
|   | // 以免造成干扰 |
| 723 | // Allocate global and split ranges in long->short order. Long ranges that |
| 724 | // don't fit should be spilled (or split) ASAP so they don't create |
| 725 | // interference.  Mark a bit to prioritize global above local ranges. |
| 726 | Prio = (1u << 29) + Size; |
| 727 | } |
|   | // 标记一个更高的位，以优先考虑全局和局部范围高于RS_Split |
| 728 | // Mark a higher bit to prioritize global and local above RS_Split. |
| 729 | Prio |= (1u << 31); |
|   | // 提升有物理寄存器提示的范围的优先级 |
| 730 |  |
| 731 | // Boost ranges that have a physical register hint. |
| 732 | if (VRM->hasKnownPreference(Reg)) |
| 733 | Prio |= (1u << 30); |
| 734 | } |
|   | // 虚拟寄存器编号是相同大小范围的决胜因素 |
|   | // 给较低的vreg编号更高的优先级，以便首先分配它们 |
| 735 | // The virtual register number is a tie breaker for same-sized ranges. |
| 736 | // Give lower vreg numbers higher priority to assign them first. |
| 737 | CurQueue.push(std::make_pair(Prio, ~Reg)); |
| 738 | } |
|   | // RAGreedy类的一个成员函数，用于从队列中出队一个LiveInterval对象 |
| 739 |  |
| 740 | LiveInterval *RAGreedy::dequeue() { return dequeue(Queue); } |
|   | // RAGreedy类的一个成员函数，用于从指定的优先队列中出队一个LiveInterval对象 |
| 741 |  |
| 742 | LiveInterval *RAGreedy::dequeue(PQueue &CurQueue) { |
|   | // 如果队列为空，则返回nullptr |
| 743 | if (CurQueue.empty()) |
| 744 | return nullptr; |
|   | // 从队列顶部取出LiveInterval对象 |
| 745 | LiveInterval *LI = &LIS->getInterval(~CurQueue.top().second); |
| 746 | CurQueue.pop(); |
| 747 | return LI; |
| 748 | } |
| 749 |  |
| 750 | //===----------------------------------------------------------------------===// |
| 751 | //                            Direct Assignment |
| 752 | //===----------------------------------------------------------------------===// |
|   | /// tryAssign - 尝试将虚拟寄存器VirtReg分配到一个可用的物理寄存器。 |
| 753 |  |
| 754 | /// tryAssign - Try to assign VirtReg to an available register. |
| 755 | Register RAGreedy::tryAssign(LiveInterval &VirtReg, |
| 756 | AllocationOrder &Order, |
| 757 | SmallVectorImpl<Register> &NewVRegs, |
| 758 | const SmallVirtRegSet &FixedRegisters) { |
|   | // 重置分配顺序 |
| 759 | Order.rewind(); |
| 760 | Register PhysReg; |
|   | // 遍历分配顺序，寻找一个没有干扰的物理寄存器 |
| 761 | while ((PhysReg = Order.next())) |
| 762 | if (!Matrix->checkInterference(VirtReg, PhysReg)) |
| 763 | break; |
|   | // 如果没有找到物理寄存器，或者当前的分配顺序是一个提示 |
| 764 | if (!PhysReg || Order.isHint()) |
| 765 | return PhysReg; |
|   | // 虽然找到了一个可用的物理寄存器，但可能还有更好的选择。 |
| 766 |  |
| 767 | // PhysReg is available, but there may be a better choice. |
|   | // 如果我们错过了一个简单的提示，尝试从首选寄存器中廉价地驱逐干扰 |
| 768 |  |
| 769 | // If we missed a simple hint, try to cheaply evict interference from the |
| 770 | // preferred register. |
| 771 | if (Register Hint = MRI->getSimpleHint(VirtReg.reg)) |
| 772 | if (Order.isHint(Hint)) { |
| 773 | LLVM_DEBUG(dbgs() << "missed hint " << printReg(Hint, TRI) << '\n'); |
| 774 | EvictionCost MaxCost; |
| 775 | MaxCost.setBrokenHints(1); |
|   | // 如果可以驱逐干扰 |
| 776 | if (canEvictInterference(VirtReg, Hint, true, MaxCost, FixedRegisters)) { |
| 777 | evictInterference(VirtReg, Hint, NewVRegs); |
| 778 | return Hint; |
| 779 | } |
|   | // 记录错过的提示，我们可能在分配结束时能够恢复 |
| 780 | // Record the missed hint, we may be able to recover |
| 781 | // at the end if the surrounding allocation changed. |
| 782 | SetOfBrokenHints.insert(&VirtReg); |
| 783 | } |
|   | // 尝试从一个更便宜的替代寄存器中驱逐干扰 |
| 784 |  |
| 785 | // Try to evict interference from a cheaper alternative. |
| 786 | unsigned Cost = TRI->getCostPerUse(PhysReg); |
|   | // 大多数寄存器的额外成本为0 |
| 787 |  |
| 788 | // Most registers have 0 additional cost. |
| 789 | if (!Cost) |
| 790 | return PhysReg; |
| 791 |  |
| 792 | LLVM_DEBUG(dbgs() << printReg(PhysReg, TRI) << " is available at cost " |
| 793 | << Cost << '\n'); |
| 794 | Register CheapReg = tryEvict(VirtReg, Order, NewVRegs, Cost, FixedRegisters); |
| 795 | return CheapReg ? CheapReg : PhysReg; |
| 796 | } |
| 797 |  |
| 798 | //===----------------------------------------------------------------------===// |
| 799 | //                         Interference eviction |
| 800 | //===----------------------------------------------------------------------===// |
|   | /// canReassign - 检查是否可以将虚拟寄存器VirtReg从其当前物理寄存器 |
|   | /// PrevReg重新分配到另一个物理寄存器。 |
| 801 |  |
| 802 | Register RAGreedy::canReassign(LiveInterval &VirtReg, Register PrevReg) { |
|   | // 创建一个分配顺序对象，用于遍历可能的物理寄存器 |
| 803 | AllocationOrder Order(VirtReg.reg, *VRM, RegClassInfo, Matrix); |
| 804 | Register PhysReg; |
|   | // 遍历所有可能的物理寄存器 |
| 805 | while ((PhysReg = Order.next())) { |
|   | // 如果找到的物理寄存器与当前寄存器相同，则跳过 |
| 806 | if (PhysReg == PrevReg) |
| 807 | continue; |
|   | // 获取物理寄存器对应的寄存器单元迭代器 |
| 808 |  |
| 809 | MCRegUnitIterator Units(PhysReg, TRI); |
| 810 | for (; Units.isValid(); ++Units) { |
|   | // 创建一个“子查询”，用于检查干扰 |
| 811 | // Instantiate a "subquery", not to be confused with the Queries array. |
| 812 | LiveIntervalUnion::Query subQ(VirtReg, Matrix->getLiveUnions()[*Units]); |
|   | // 如果发现干扰，则跳出循环 |
| 813 | if (subQ.checkInterference()) |
| 814 | break; |
| 815 | } |
|   | // 如果所有单元都没有干扰，则跳出循环，使用当前找到的物理寄存器 |
| 816 | // If no units have interference, break out with the current PhysReg. |
| 817 | if (!Units.isValid()) |
| 818 | break; |
| 819 | } |
|   | // 如果找到了一个没有干扰的物理寄存器，则打印调试信息 |
| 820 | if (PhysReg) |
| 821 | LLVM_DEBUG(dbgs() << "can reassign: " << VirtReg << " from " |
| 822 | << printReg(PrevReg, TRI) << " to " |
| 823 | << printReg(PhysReg, TRI) << '\n'); |
| 824 | return PhysReg; |
| 825 | } |
|   | /// shouldEvict - 确定是否应该将已分配的live range B驱逐，以便为A分配空间。 |
|   | /// 这个函数定义的驱逐策略与enqueue()定义的分配顺序一起决定了哪些寄存器最终会被分割和溢出。 |
| 826 |  |
| 827 | /// |
|   | /// 级联编号（Cascade numbers）被用来防止如果这个函数是一个循环关系时的无限循环。 |
| 828 | /// |
|   | /// @param A         需要被分配的live range。 |
|   | /// @param IsHint    当A即将被分配到其首选寄存器时为真。 |
|   | /// @param B         需要被驱逐的live range。 |
|   | /// @param BreaksHint 当B已经被分配到其首选寄存器时为真。 |
| 829 | /// shouldEvict - determine if A should evict the assigned live range B. The |
| 830 | /// eviction policy defined by this function together with the allocation order |
| 831 | /// defined by enqueue() decides which registers ultimately end up being split |
| 832 | /// and spilled. |
| 833 | /// |
| 834 | /// Cascade numbers are used to prevent infinite loops if this function is a |
| 835 | /// cyclic relation. |
| 836 | /// |
| 837 | /// @param A          The live range to be assigned. |
| 838 | /// @param IsHint     True when A is about to be assigned to its preferred |
| 839 | ///                   register. |
| 840 | /// @param B          The live range to be evicted. |
| 841 | /// @param BreaksHint True when B is already assigned to its preferred register. |
| 842 | bool RAGreedy::shouldEvict(LiveInterval &A, bool IsHint, |
| 843 | LiveInterval &B, bool BreaksHint) { |
| 844 | bool CanSplit = getStage(B) < RS_Spill; |
|   | // 如果B可以被分割，并且A是按照提示分配的，且B没有破坏任何提示，则积极驱逐B |
| 845 | // Be fairly aggressive about following hints as long as the evictee can be |
| 846 | // split. |
| 847 | if (CanSplit && IsHint && !BreaksHint) |
| 848 |  |
| 851 | return true; |
|   | // 如果A的权重大于B的权重，则驱逐B |
| 852 | if (A.weight > B.weight) { |
| 853 | LLVM_DEBUG(dbgs() << "should evict: " << B << " w= " << B.weight << '\n'); |
| 854 | return true; |
| 855 | } |
| 870 | return false; |
| 871 | } |
|   | /// canEvictInterference - 返回是否所有VirtReg和PhysReg之间的干扰都可以被驱逐。 |
| 872 | /// |
|   | /// @param VirtReg 即将被分配的live range。 |
|   | /// @param PhysReg 期望分配的寄存器。 |
|   | /// @param IsHint  当PhysReg是VirtReg的首选寄存器时为真。 |
|   | /// @param MaxCost 只寻找更便宜的候选寄存器，并在返回真时更新新成本。 |
|   | /// @returns 如果干扰可以被比MaxCost更便宜地驱逐，则返回真。 |
| 873 | /// canEvictInterference - Return true if all interferences between VirtReg and |
| 874 | /// PhysReg can be evicted. |
| 875 | /// |
| 876 | /// @param VirtReg Live range that is about to be assigned. |
| 877 | /// @param PhysReg Desired register for assignment. |
| 878 | /// @param IsHint  True when PhysReg is VirtReg's preferred register. |
| 879 | /// @param MaxCost Only look for cheaper candidates and update with new cost |
| 880 | ///                when returning true. |
| 881 | /// @returns True when interference can be evicted cheaper than MaxCost. |
| 882 | bool RAGreedy::canEvictInterference(LiveInterval &VirtReg, Register PhysReg, |
| 883 | bool IsHint, EvictionCost &MaxCost, |
| 884 | const SmallVirtRegSet &FixedRegisters) { |
|   | // 只能驱逐虚拟寄存器干扰 |
| 885 | // It is only possible to evict virtual register interference. |
| 886 | if (Matrix->checkInterference(VirtReg, PhysReg) > LiveRegMatrix::IK_VirtReg) |
| 890 | return false; |
| 891 | bool IsLocal = LIS->intervalIsInOneMBB(VirtReg); |
|   | // 查找VirtReg的级联编号。如果VirtReg从未参与过驱逐，则此编号未分配。 |
|   | // 如果分配了级联编号，则不允许驱逐具有相同或更新级联编号的任何东西。这防止了无限驱逐循环。 |
| 892 | // Find VirtReg's cascade number. This will be unassigned if VirtReg was never |
| 893 | // involved in an eviction before. If a cascade number was assigned, deny |
| 894 | // evicting anything with the same or a newer cascade number. This prevents |
| 895 | // infinite eviction loops. |
| 897 |  |
| 898 | // |
| 899 | // This works out so a register without a cascade number is allowed to evict |
| 900 | // anything, and it can be evicted by anything. |
| 901 | unsigned Cascade = ExtraRegInfo[VirtReg.reg].Cascade; |
| 902 | if (!Cascade) |
| 903 | Cascade = NextCascade; |
| 904 | EvictionCost Cost; |
| 905 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 906 | LiveIntervalUnion::Query &Q = Matrix->query(VirtReg, *Units); |
|   | // 如果有10个或更多的干扰，可能其中一个更重 |
| 907 | // If there is 10 or more interferences, chances are one is heavier. |
| 908 | if (Q.collectInterferingVRegs(10) >= 10) |
| 921 | return false; |
|   | // 检查任何干扰的live range是否比MaxWeight重 |
| 922 | // Check if any interfering live range is heavier than MaxWeight. |
| 923 | for (unsigned i = Q.interferingVRegs().size(); i; --i) { |
| 924 | LiveInterval *Intf = Q.interferingVRegs()[i - 1]; |
| 925 | assert(Register::isVirtualRegister(Intf->reg) && |
| 926 | "Only expecting virtual register interference from query"); |
|   | // 不允许驱逐我们正在为物理寄存器寻找的虚拟寄存器 |
| 927 | // Do not allow eviction of a virtual register if we are in the middle |
| 928 | // of last-chance recoloring and this virtual register is one that we |
| 929 | // have scavenged a physical register for. |
| 930 | if (FixedRegisters.count(Intf->reg)) |
| 933 | return false; |
|   | // 永远不驱逐溢出产品。它们不能分割或溢出 |
| 934 | // Never evict spill products. They cannot split or spill. |
| 935 | if (getStage(*Intf) == RS_Done) |
| 938 | return false; |
|   | // 一旦live range变得足够小，就迫切需要为其找到寄存器。 |
|   | // 这由无限溢出权重指示。这些紧急的live range几乎可以驱逐任何东西 |
| 939 | // Once a live range becomes small enough, it is urgent that we find a |
| 940 | // register for it. This is indicated by an infinite spill weight. These |
| 941 | // urgent live ranges get to evict almost anything. |
| 951 |  |
| 952 | // |
| 953 | // Also allow urgent evictions of unspillable ranges from a strictly |
| 954 | // larger allocation order. |
| 955 | bool Urgent = !VirtReg.isSpillable() && |
| 956 | (Intf->isSpillable() || |
| 957 | RegClassInfo.getNumAllocatableRegs(MRI->getRegClass(VirtReg.reg)) < |
| 958 | RegClassInfo.getNumAllocatableRegs(MRI->getRegClass(Intf->reg))); |
|   | // 只驱逐旧的级联或没有级联的live range |
| 959 | // Only evict older cascades or live ranges without a cascade. |
| 960 | unsigned IntfCascade = ExtraRegInfo[Intf->reg].Cascade; |
| 961 | if (Cascade <= IntfCascade) { |
| 962 | if (!Urgent) |
| 963 | return false; |
|   | // 我们允许为了紧急驱逐而破坏级联。这应该是最后的手段， |
|   | // 所以使其非常昂贵 |
| 964 | // We permit breaking cascades for urgent evictions. It should be the |
| 965 | // last resort, though, so make it really expensive. |
| 966 | Cost.BrokenHints += 10; |
| 967 | } |
|   | // 这会破坏一个满足的提示吗？ |
| 968 | // Would this break a satisfied hint? |
| 969 | bool BreaksHint = VRM->hasPreferredPhys(Intf->reg); |
|   | // 更新驱逐成本 |
| 970 | // Update eviction cost. |
| 971 | Cost.BrokenHints += BreaksHint; |
| 972 | Cost.MaxWeight = std::max(Cost.MaxWeight, Intf->weight); |
|   | // 如果这太贵了，就中止 |
| 973 | // Abort if this would be too expensive. |
| 974 | if (!(Cost < MaxCost)) |
| 978 |  |
| 981 | return false; |
| 982 | if (Urgent) |
| 983 | continue; |
|   | // 应用非紧急驱逐的驱逐策略 |
| 984 | // Apply the eviction policy for non-urgent evictions. |
| 985 | if (!shouldEvict(VirtReg, IsHint, *Intf, BreaksHint)) |
| 993 | return false; |
|   | // 如果!MaxCost.isMax()，那么我们只是在寻找一个便宜的寄存器。 |
|   | // 在这种情况下驱逐另一个局部live range可能导致次优着色 |
| 994 | // If !MaxCost.isMax(), then we're just looking for a cheap register. |
| 995 | // Evicting another local live range in this case could lead to suboptimal |
| 996 | // coloring. |
| 997 | if (!MaxCost.isMax() && IsLocal && LIS->intervalIsInOneMBB(*Intf) && |
| 998 | (!EnableLocalReassign || !canReassign(*Intf, PhysReg))) { |
| 999 | return false; |
| 1002 | } |
| 1003 | } |
| 1004 | } |
|   | // 更新最大成本 |
| 1005 | MaxCost = Cost; |
| 1006 | return true; |
| 1007 | } |
|   | /// canEvictInterferenceInRange - 检查在指定范围内，VirtReg和PhysReg之间的所有干扰是否都可以被驱逐。 |
| 1008 | /// |
|   | /// @param VirtReg 即将被分配的live range。 |
|   | /// @param PhysReg 期望分配的寄存器。 |
|   | /// @param Start   查找干扰的起始范围。 |
|   | /// @param End     查找干扰的结束范围。 |
|   | /// @param MaxCost 只寻找更便宜的候选寄存器，并在返回真时更新新成本。 |
|   | /// @return 如果干扰可以被比MaxCost更便宜地驱逐，则返回真。 |
| 1009 | /// Return true if all interferences between VirtReg and PhysReg between |
| 1010 | /// Start and End can be evicted. |
| 1011 | /// |
| 1012 | /// \param VirtReg Live range that is about to be assigned. |
| 1013 | /// \param PhysReg Desired register for assignment. |
| 1014 | /// \param Start   Start of range to look for interferences. |
| 1015 | /// \param End     End of range to look for interferences. |
| 1016 | /// \param MaxCost Only look for cheaper candidates and update with new cost |
| 1017 | ///                when returning true. |
| 1018 | /// \return True when interference can be evicted cheaper than MaxCost. |
| 1019 | bool RAGreedy::canEvictInterferenceInRange(LiveInterval &VirtReg, |
| 1020 | Register PhysReg, SlotIndex Start, |
| 1021 | SlotIndex End, |
| 1022 | EvictionCost &MaxCost) { |
| 1023 | EvictionCost Cost; |
|   | // 遍历PhysReg的所有寄存器单元 |
| 1024 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 1025 | LiveIntervalUnion::Query &Q = Matrix->query(VirtReg, *Units); |
|   | // 检查任何干扰的live range是否比MaxWeight重 |
| 1026 | // Check if any interfering live range is heavier than MaxWeight. |
| 1027 | for (unsigned i = Q.interferingVRegs().size(); i; --i) { |
| 1028 | LiveInterval *Intf = Q.interferingVRegs()[i - 1]; |
|   | // 如果干扰不覆盖感兴趣的段，则继续 |
| 1029 | // Check if interference overlast the segment in interest. |
| 1030 | if (!Intf->overlaps(Start, End)) |
| 1031 | continue; |
|   | // 不能驱逐非虚拟寄存器干扰 |
| 1032 | // Cannot evict non virtual reg interference. |
| 1033 | if (!Register::isVirtualRegister(Intf->reg)) |
| 1034 | return false; |
|   | // 永远不驱逐已经计算完成的寄存器。它们不能分割或溢出 |
| 1035 | // Never evict spill products. They cannot split or spill. |
| 1036 | if (getStage(*Intf) == RS_Done) |
| 1037 | return false; |
|   | // 这会破坏一个满足的提示吗？ |
| 1038 | // Would this break a satisfied hint? |
| 1039 | bool BreaksHint = VRM->hasPreferredPhys(Intf->reg); |
| 1040 | // Update eviction cost. |
| 1041 | Cost.BrokenHints += BreaksHint; |
| 1042 | Cost.MaxWeight = std::max(Cost.MaxWeight, Intf->weight); |
|   | // 如果这太贵了，就中止 |
| 1043 | // Abort if this would be too expensive. |
| 1044 | if (!(Cost < MaxCost)) |
| 1045 | return false; |
| 1046 | } |
| 1047 | } |
|   | // 如果最大权重为0，则返回假 |
| 1048 | if (Cost.MaxWeight == 0) |
| 1049 | return false; |
| 1050 | MaxCost = Cost; |
| 1051 | return true; |
| 1052 |  |
| 1064 | } |
|   | /// getCheapestEvicteeWeight - 返回在Start和End之间创建的局部分割间隔中，最佳的驱逐候选物理寄存器。 |
| 1065 | /// |
|   | /// @param Order            分配顺序 |
|   | /// @param VirtReg          即将被分配的live range。 |
|   | /// @param Start            查找干扰的起始范围 |
|   | /// @param End              查找干扰的结束范围 |
|   | /// @param BestEvictweight  该驱逐的驱逐成本 |
|   | /// @return 最佳驱逐候选的物理寄存器和在BestEvictweight中的驱逐成本 |
| 1066 | /// Return the physical register that will be best |
| 1067 | /// candidate for eviction by a local split interval that will be created |
| 1068 | /// between Start and End. |
| 1069 | /// |
| 1070 | /// \param Order            The allocation order |
| 1071 | /// \param VirtReg          Live range that is about to be assigned. |
| 1072 | /// \param Start            Start of range to look for interferences |
| 1073 | /// \param End              End of range to look for interferences |
| 1074 | /// \param BestEvictweight  The eviction cost of that eviction |
| 1075 | /// \return The PhysReg which is the best candidate for eviction and the |
| 1076 | /// eviction cost in BestEvictweight |
| 1077 | unsigned RAGreedy::getCheapestEvicteeWeight(const AllocationOrder &Order, |
| 1078 | LiveInterval &VirtReg, |
| 1079 | SlotIndex Start, SlotIndex End, |
| 1080 | float *BestEvictweight) { |
| 1081 | EvictionCost BestEvictCost; |
| 1082 | BestEvictCost.setMax(); |
| 1083 | BestEvictCost.MaxWeight = VirtReg.weight; |
| 1084 | unsigned BestEvicteePhys = 0; |
|   | // 遍历所有物理寄存器，找到最佳驱逐候选 |
| 1085 | // Go over all physical registers and find the best candidate for eviction |
| 1086 | for (auto PhysReg : Order.getOrder()) { |
|   | // 如果可以在指定范围内驱逐干扰，则继续 |
| 1087 | if (!canEvictInterferenceInRange(VirtReg, PhysReg, Start, End, |
| 1088 | BestEvictCost)) |
| 1089 | continue; |
|   | // 目前最佳 |
| 1090 | // Best so far. |
| 1091 | BestEvicteePhys = PhysReg; |
| 1093 | } |
| 1094 | *BestEvictweight = BestEvictCost.MaxWeight; |
| 1095 | return BestEvicteePhys; |
| 1096 | } |
|   | /// evictInterference - 驱逐任何阻止VirtReg被分配到PhysReg的干扰寄存器。 |
|   | /// 这个函数假定canEvictInterference返回了true。 |
| 1097 | /// evictInterference - Evict any interferring registers that prevent VirtReg |
| 1098 | /// from being assigned to Physreg. This assumes that canEvictInterference |
| 1099 | /// returned true. |
| 1100 | void RAGreedy::evictInterference(LiveInterval &VirtReg, Register PhysReg, |
| 1101 | SmallVectorImpl<Register> &NewVRegs) { |
|   | // 确保VirtReg有一个级联编号，并给每个被驱逐的寄存器分配该级联编号。 |
|   | // 这些live range只能被更新的级联驱逐，防止无限循环。 |
| 1102 | // Make sure that VirtReg has a cascade number, and assign that cascade |
| 1103 | // number to every evicted register. These live ranges than then only be |
| 1104 | // evicted by a newer cascade, preventing infinite loops. |
| 1105 | unsigned Cascade = ExtraRegInfo[VirtReg.reg].Cascade; |
| 1106 | if (!Cascade) |
| 1107 | Cascade = ExtraRegInfo[VirtReg.reg].Cascade = NextCascade++; |
| 1108 | LLVM_DEBUG(dbgs() << "evicting " << printReg(PhysReg, TRI) |
| 1109 | << " interference: Cascade " << Cascade << '\n'); |
|   | // 首先收集所有干扰的虚拟寄存器。 |
| 1110 | // Collect all interfering virtregs first. |
| 1111 | SmallVector<LiveInterval*, 8> Intfs; |
| 1112 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 1113 | LiveIntervalUnion::Query &Q = Matrix->query(VirtReg, *Units); |
|   | // 我们通常缓存干扰的VRegs，所以collectInterferingVRegs()应该很快， |
| 1114 | 如果不同的物理寄存器重叠了相同的寄存器单元，我们可能需要重新计算。 |
| 1115 | // We usually have the interfering VRegs cached so collectInterferingVRegs() |
| 1116 | // should be fast, we may need to recalculate if when different physregs |
| 1117 | // overlap the same register unit so we had different SubRanges queried |
| 1118 | // against it. |
| 1119 | Q.collectInterferingVRegs(); |
| 1120 | ArrayRef<LiveInterval*> IVR = Q.interferingVRegs(); |
| 1121 | Intfs.append(IVR.begin(), IVR.end()); |
| 1126 | } |
|   | // 其次驱逐它们。这将使查询失效。 |
| 1127 | // Evict them second. This will invalidate the queries. |
| 1128 | for (unsigned i = 0, e = Intfs.size(); i != e; ++i) { |
| 1129 | LiveInterval *Intf = Intfs[i]; |
|   | // 相同的VirtReg可能存在于多个RegUnits中。跳过重复项。 |
| 1130 | // The same VirtReg may be present in multiple RegUnits. Skip duplicates. |
| 1131 | if (!VRM->hasPhys(Intf->reg)) |
| 1136 |  |
| 1140 | continue; |
| 1141 | LastEvicted.addEviction(PhysReg, VirtReg.reg, Intf->reg); |
| 1142 | Matrix->unassign(*Intf); |
| 1143 | assert((ExtraRegInfo[Intf->reg].Cascade < Cascade || |
| 1144 | VirtReg.isSpillable() < Intf->isSpillable()) && |
| 1145 | "Cannot decrease cascade number, illegal eviction"); |
| 1146 | ExtraRegInfo[Intf->reg].Cascade = Cascade; |
| 1147 | ++NumEvicted; |
| 1148 | NewVRegs.push_back(Intf->reg); |
| 1149 | } |
| 1150 |  |
| 1154 |  |
| 1157 |  |
| 1161 | } |
| 1162 | /// isUnusedCalleeSavedReg – |
|   | /// 返回给定的\p PhysReg是否是一个未被用于分配的callee saved寄存器。 |
| 1163 | /// Returns true if the given \p PhysReg is a callee saved register and has not |
| 1164 | /// been used for allocation yet. |
| 1165 | bool RAGreedy::isUnusedCalleeSavedReg(MCRegister PhysReg) const { |
| 1166 | MCRegister CSR = RegClassInfo.getLastCalleeSavedAlias(PhysReg); |
| 1167 | if (!CSR) |
| 1168 | return false; |
| 1169 | return !Matrix->isPhysRegUsed(PhysReg); |
| 1170 | } |
|   | /// 尝试驱逐一个物理寄存器的所有干扰。 |
|   | /// 返回分配给VirtReg的物理寄存器，如果没有合适的寄存器则返回0。 |
| 1171 | /// tryEvict - Try to evict all interferences for a physreg. |
| 1172 | /// @param  VirtReg Currently unassigned virtual register. |
| 1173 | /// @param  Order   Physregs to try. |
| 1174 | /// @return         Physreg to assign VirtReg, or 0. |
| 1175 | unsigned RAGreedy::tryEvict(LiveInterval &VirtReg, |
| 1176 | AllocationOrder &Order, |
| 1177 | SmallVectorImpl<Register> &NewVRegs, |
| 1178 | unsigned CostPerUseLimit, |
| 1179 | const SmallVirtRegSet &FixedRegisters) { |
|   | // 计时器，用于性能分析。 |
| 1180 | NamedRegionTimer T("evict", "Evict", TimerGroupName, TimerGroupDescription, |
| 1181 | TimePassesIsEnabled); |
|   | // 跟踪迄今为止看到的最便宜的干扰。 |
| 1182 | // Keep track of the cheapest interference seen so far. |
| 1183 | EvictionCost BestCost; |
| 1184 | BestCost.setMax(); |
| 1185 | unsigned BestPhys = 0; |
| 1186 | unsigned OrderLimit = Order.getOrder().size(); |
|   | // 如果我们只是在寻找降低每个使用的花费，不要破坏任何提示， |
|   | // 并且只驱逐较小的溢出权重。 |
| 1187 | // When we are just looking for a reduced cost per use, don't break any |
| 1188 | // hints, and only evict smaller spill weights. |
| 1189 | if (CostPerUseLimit < ~0u) { |
| 1190 | BestCost.BrokenHints = 0; |
| 1191 | BestCost.MaxWeight = VirtReg.weight; |
|   | // 检查RC中的任何寄存器是否低于CostPerUseLimit。 |
| 1192 | // Check of any registers in RC are below CostPerUseLimit. |
| 1193 | const TargetRegisterClass *RC = MRI->getRegClass(VirtReg.reg); |
| 1194 | unsigned MinCost = RegClassInfo.getMinCost(RC); |
| 1195 | if (MinCost >= CostPerUseLimit) { |
|   | // 如果最小成本大于或等于CostPerUseLimit，则没有更便宜的寄存器可以找到。 |
| 1196 | LLVM_DEBUG(dbgs() << TRI->getRegClassName(RC) << " minimum cost = " |
| 1197 | << MinCost << ", no cheaper registers to be found.\n"); |
| 1198 | return 0; |
| 1201 |  |
| 1204 |  |
| 1210 | } |
|   | // 如果寄存器类有很多相同成本的寄存器，如果它们太贵，我们不需要看它们。 |
| 1211 | // It is normal for register classes to have a long tail of registers with |
| 1212 | // the same cost. We don't need to look at them if they're too expensive. |
| 1213 | if (TRI->getCostPerUse(Order.getOrder().back()) >= CostPerUseLimit) { |
| 1214 | OrderLimit = RegClassInfo.getLastCostChange(RC); |
| 1215 | LLVM_DEBUG(dbgs() << "Only trying the first " << OrderLimit |
| 1216 | << " regs.\n"); |
| 1223 | } |
| 1224 |  |
| 1230 | } |
|   | // 重置顺序。 |
| 1231 | Order.rewind(); |
| 1232 | while (MCRegister PhysReg = Order.next(OrderLimit)) { |
| 1233 | if (TRI->getCostPerUse(PhysReg) >= CostPerUseLimit) |
| 1234 | continue; |
|   | // 第一次使用callee-saved寄存器在函数中成本为1。 |
|   | // 当CostPerUseLimit很低时，不要开始使用CSR。 |
| 1235 | // The first use of a callee-saved register in a function has cost 1. |
| 1236 | // Don't start using a CSR when the CostPerUseLimit is low. |
| 1237 | if (CostPerUseLimit == 1 && isUnusedCalleeSavedReg(PhysReg)) { |
| 1238 | LLVM_DEBUG( |
| 1239 | dbgs() << printReg(PhysReg, TRI) << " would clobber CSR " |
| 1240 | << printReg(RegClassInfo.getLastCalleeSavedAlias(PhysReg), TRI) |
| 1241 | << '\n'); |
| 1242 | continue; |
| 1243 |  |
| 1248 | } |
| 1249 | if (!canEvictInterference(VirtReg, PhysReg, false, BestCost, |
| 1250 | FixedRegisters)) |
| 1251 | continue; |
|   | // 目前最佳。 |
| 1252 | // Best so far. |
| 1253 | BestPhys = PhysReg; |
|   | // 如果可以使用提示，则停止。 |
| 1254 | // Stop if the hint can be used. |
| 1255 | if (Order.isHint()) |
| 1256 | break; |
| 1258 |  |
| 1262 |  |
| 1269 | } |
| 1270 | if (!BestPhys) |
| 1271 | return 0; |
|   | // 驱逐干扰并分配BestPhys给VirtReg。 |
| 1272 | evictInterference(VirtReg, BestPhys, NewVRegs); |
| 1273 | return BestPhys; |
| 1274 | } |
| 1275 | //===----------------------------------------------------------------------===// |
| 1276 | //                              Region Splitting |
| 1277 | //===----------------------------------------------------------------------===// |
|   | /// 如果所有SplitConstraints中的偏好都得到满足， |
|   | /// 将SplitConstraints向量填充基于Physreg及其别名中的干扰模式， |
|   | /// 并添加约束到SpillPlacement，返回这次分裂的静态成本。 |
| 1278 | /// addSplitConstraints - Fill out the SplitConstraints vector based on the |
| 1279 | /// interference pattern in Physreg and its aliases. Add the constraints to |
| 1280 | /// SpillPlacement and return the static cost of this split in Cost, assuming |
| 1281 | /// that all preferences in SplitConstraints are met. |
|   | /// 如果没有带有正偏置的捆绑包，则返回false。 |
| 1282 | /// Return false if there are no bundles with positive bias. |
| 1283 | bool RAGreedy::addSplitConstraints(InterferenceCache::Cursor Intf, |
| 1284 | BlockFrequency &Cost) { |
| 1285 | ArrayRef<SplitAnalysis::BlockInfo> UseBlocks = SA->getUseBlocks(); |
|   | // 重置依赖于干扰的信息。 |
| 1286 | // Reset interference dependent info. |
| 1287 | SplitConstraints.resize(UseBlocks.size()); |
| 1288 | BlockFrequency StaticCost = 0; |
| 1289 | for (unsigned i = 0; i != UseBlocks.size(); ++i) { |
| 1290 | const SplitAnalysis::BlockInfo &BI = UseBlocks[i]; |
| 1291 | SpillPlacement::BlockConstraint &BC = SplitConstraints[i]; |
| 1292 | BC.Number = BI.MBB->getNumber(); |
| 1293 | Intf.moveToBlock(BC.Number); |
| 1294 | BC.Entry = BI.LiveIn ? SpillPlacement::PrefReg : SpillPlacement::DontCare; |
| 1295 | BC.Exit = (BI.LiveOut && |
| 1296 | !LIS->getInstructionFromIndex(BI.LastInstr)->isImplicitDef()) |
| 1297 | ? SpillPlacement::PrefReg |
| 1298 | : SpillPlacement::DontCare; |
| 1299 | BC.ChangesValue = BI.FirstDef.isValid(); |
| 1300 | if (!Intf.hasInterference()) |
| 1301 | continue; |
|   | // 插入溢出代码指令的数量。 |
| 1302 | // Number of spill code instructions to insert. |
| 1303 | unsigned Ins = 0; |
|   | // 活入值的干扰。 |
| 1304 | // Interference for the live-in value. |
| 1305 | if (BI.LiveIn) { |
| 1306 | if (Intf.first() <= Indexes->getMBBStartIdx(BC.Number)) { |
| 1307 | BC.Entry = SpillPlacement::MustSpill; |
| 1308 | ++Ins; |
| 1309 | } else if (Intf.first() < BI.FirstInstr) { |
| 1310 | BC.Entry = SpillPlacement::PrefSpill; |
| 1311 | ++Ins; |
| 1312 | } else if (Intf.first() < BI.LastInstr) { |
| 1313 | ++Ins; |
| 1314 | } |
|   | // 如果溢出不能在MBB开始处插入，则中止。 |
| 1315 | // Abort if the spill cannot be inserted at the MBB' start |
| 1316 | if (((BC.Entry == SpillPlacement::MustSpill) || |
| 1317 | (BC.Entry == SpillPlacement::PrefSpill)) && |
| 1318 | SlotIndex::isEarlierInstr(BI.FirstInstr, |
| 1319 | SA->getFirstSplitPoint(BC.Number))) |
| 1320 | return false; |
| 1332 | } |
|   | // 活出值的干扰。 |
| 1333 | // Interference for the live-out value. |
| 1334 | if (BI.LiveOut) { |
| 1335 | if (Intf.last() >= SA->getLastSplitPoint(BC.Number)) { |
| 1336 | BC.Exit = SpillPlacement::MustSpill; |
| 1337 | ++Ins; |
| 1338 | } else if (Intf.last() > BI.LastInstr) { |
| 1339 | BC.Exit = SpillPlacement::PrefSpill; |
| 1340 | ++Ins; |
| 1341 | } else if (Intf.last() > BI.FirstInstr) { |
| 1342 | ++Ins; |
| 1344 | } |
| 1349 |  |
| 1352 | } |
|   | // 累积插入溢出代码的总频率。 |
| 1353 | // Accumulate the total frequency of inserted spill code. |
| 1354 | while (Ins--) |
| 1355 | StaticCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1356 | } |
| 1357 | Cost = StaticCost; |
|   | // 为使用块添加约束。注意，这些是唯一可能增加正偏置的约束。 |
| 1358 | // Add constraints for use-blocks. Note that these are the only constraints |
| 1359 | // that may add a positive bias, it is downhill from here. |
| 1360 | SpillPlacer->addConstraints(SplitConstraints); |
| 1361 | return SpillPlacer->scanActiveBundles(); |
| 1362 | } |
|   | /// 从Blocks中的通过块向SpillPlacer添加约束和链接。 |
| 1363 | /// addThroughConstraints - Add constraints and links to SpillPlacer from the |
| 1364 | /// live-through blocks in Blocks. |
| 1365 | bool RAGreedy::addThroughConstraints(InterferenceCache::Cursor Intf, |
| 1366 | ArrayRef<unsigned> Blocks) { |
| 1367 | const unsigned GroupSize = 8; |
| 1368 | SpillPlacement::BlockConstraint BCS[GroupSize]; |
| 1369 | unsigned TBS[GroupSize]; |
| 1370 | unsigned B = 0, T = 0; |
| 1371 | for (unsigned i = 0; i != Blocks.size(); ++i) { |
| 1372 | unsigned Number = Blocks[i]; |
| 1373 | Intf.moveToBlock(Number); |
| 1374 | if (!Intf.hasInterference()) { |
| 1375 | assert(T < GroupSize && "Array overflow"); |
| 1376 | TBS[T] = Number; |
| 1377 | if (++T == GroupSize) { |
| 1378 | SpillPlacer->addLinks(makeArrayRef(TBS, T)); |
| 1379 | T = 0; |
| 1383 | } |
| 1384 | continue; |
| 1388 | } |
| 1389 | assert(B < GroupSize && "Array overflow"); |
| 1390 | BCS[B].Number = Number; |
|   | // 如果溢出不能在MBB开始处插入，则中止。 |
| 1391 | // Abort if the spill cannot be inserted at the MBB' start |
| 1392 | MachineBasicBlock *MBB = MF->getBlockNumbered(Number); |
| 1393 | if (!MBB->empty() && |
| 1394 | SlotIndex::isEarlierInstr(LIS->getInstructionIndex(MBB->instr_front()), |
| 1395 | SA->getFirstSplitPoint(Number))) |
| 1396 | return false; |
|   | // 活入值的干扰。 |
| 1397 | // Interference for the live-in value. |
| 1398 | if (Intf.first() <= Indexes->getMBBStartIdx(Number)) |
| 1399 | BCS[B].Entry = SpillPlacement::MustSpill; |
| 1400 | else |
| 1401 | BCS[B].Entry = SpillPlacement::PrefSpill; |
|   | // 活出值的干扰。 |
| 1402 | // Interference for the live-out value. |
| 1403 | if (Intf.last() >= SA->getLastSplitPoint(Number)) |
| 1404 | BCS[B].Exit = SpillPlacement::MustSpill; |
| 1405 | else |
| 1406 | BCS[B].Exit = SpillPlacement::PrefSpill; |
| 1407 | if (++B == GroupSize) { |
| 1408 | SpillPlacer->addConstraints(makeArrayRef(BCS, B)); |
| 1409 | B = 0; |
| 1415 |  |
| 1419 | } |
| 1421 | } |
| 1422 | SpillPlacer->addConstraints(makeArrayRef(BCS, B)); |
| 1423 | SpillPlacer->addLinks(makeArrayRef(TBS, T)); |
| 1424 | return true; |
| 1425 | } |
|   | /// 通过向SpillPlacer添加通过块来扩展区域。 |
| 1426 | bool RAGreedy::growRegion(GlobalSplitCandidate &Cand) { |
|   | // 跟踪尚未添加到SpillPlacer的通过块。 |
| 1427 | // Keep track of through blocks that have not been added to SpillPlacer. |
| 1428 | BitVector Todo = SA->getThroughBlocks(); |
| 1429 | SmallVectorImpl<unsigned> &ActiveBlocks = Cand.ActiveBlocks; |
| 1430 | unsigned AddedTo = 0; |
| 1431 | #ifndef NDEBUG |
| 1432 | unsigned Visited = 0; |
| 1433 | #endif |
| 1434 | while (true) { |
| 1435 | ArrayRef<unsigned> NewBundles = SpillPlacer->getRecentPositive(); |
|   | // 在PrefRegBundles的周边找到新的通过块。 |
| 1436 | // Find new through blocks in the periphery of PrefRegBundles. |
| 1437 | for (int i = 0, e = NewBundles.size(); i != e; ++i) { |
| 1438 | unsigned Bundle = NewBundles[i]; |
|   | // 查看全图中与Bundle连接的所有块。 |
| 1439 | // Look at all blocks connected to Bundle in the full graph. |
| 1440 | ArrayRef<unsigned> Blocks = Bundles->getBlocks(Bundle); |
| 1441 | for (ArrayRef<unsigned>::iterator I = Blocks.begin(), E = Blocks.end(); |
| 1442 | I != E; ++I) { |
| 1443 | unsigned Block = *I; |
| 1444 | if (!Todo.test(Block)) |
| 1445 | continue; |
| 1446 | Todo.reset(Block); |
|   | // 这是一个新的通过块。稍后将其添加到SpillPlacer。 |
| 1447 | // This is a new through block. Add it to SpillPlacer later. |
| 1448 | ActiveBlocks.push_back(Block); |
| 1449 | #ifndef NDEBUG |
| 1450 | ++Visited; |
| 1451 | #endif |
| 1452 | } |
| 1453 | } |
|   | // 有新的块要添加吗？ |
| 1454 | // Any new blocks to add? |
| 1455 | if (ActiveBlocks.size() == AddedTo) |
| 1456 | break; |
|   | // 根据干扰计算通过约束，或者在形成紧凑区域时假设所有通过块偏好溢出。 |
| 1457 | // Compute through constraints from the interference, or assume that all |
| 1458 | // through blocks prefer spilling when forming compact regions. |
| 1459 | auto NewBlocks = makeArrayRef(ActiveBlocks).slice(AddedTo); |
| 1460 | if (Cand.PhysReg) { |
| 1461 | if (!addThroughConstraints(Cand.Intf, NewBlocks)) |
| 1462 | return false; |
| 1463 | } else |
|   | // 在通过块上提供强烈的负偏置，以防止循环后边上不需要的活性。 |
| 1464 | // Provide a strong negative bias on through blocks to prevent unwanted |
| 1465 | // liveness on loop backedges. |
| 1466 | SpillPlacer->addPrefSpill(NewBlocks, /* Strong= */ true); |
| 1467 | AddedTo = ActiveBlocks.size(); |
|   | // 也许迭代可以启用更多的捆绑包？ |
| 1468 | // Perhaps iterating can enable more bundles? |
| 1469 | SpillPlacer->iterate(); |
| 1470 | } |
| 1471 | LLVM_DEBUG(dbgs() << ", v=" << Visited); |
| 1472 | return true; |
| 1473 | } |
|   | /// 如果当前活范围已经是紧凑的，或者紧凑区域会形成单个块区域，则返回false。 |
| 1474 | /// calcCompactRegion - Compute the set of edge bundles that should be live |
| 1475 | /// when splitting the current live range into compact regions.  Compact |
| 1476 | /// regions can be computed without looking at interference.  They are the |
| 1477 | /// regions formed by removing all the live-through blocks from the live range. |
| 1478 | /// |
| 1479 | /// Returns false if the current live range is already compact, or if the |
| 1480 | /// compact regions would form single block regions anyway. |
| 1481 | bool RAGreedy::calcCompactRegion(GlobalSplitCandidate &Cand) { |
|   | // 没有通过块，活范围已经是紧凑的。 |
| 1482 | // Without any through blocks, the live range is already compact. |
| 1483 | if (!SA->getNumThroughBlocks()) |
| 1486 | return false; |
|   | // 紧凑区域不对应任何物理寄存器。 |
| 1487 | // Compact regions don't correspond to any physreg. |
| 1488 | Cand.reset(IntfCache, 0); |
| 1489 | LLVM_DEBUG(dbgs() << "Compact region bundles"); |
|   | // 使用SpillPlacer确定活捆绑包。GrowRegion假装所有通过块都有干扰， |
|   | // 当PhysReg未设置时。 |
| 1490 | // Use the spill placer to determine the live bundles. GrowRegion pretends |
| 1491 | // that all the through blocks have interference when PhysReg is unset. |
| 1492 | SpillPlacer->prepare(Cand.LiveBundles); |
|   | // 静态分割成本将为零，因为Cand.Intf报告没有干扰。 |
| 1493 | // The static split cost will be zero since Cand.Intf reports no interference. |
| 1494 | BlockFrequency Cost; |
| 1495 | if (!addSplitConstraints(Cand.Intf, Cost)) { |
| 1496 | LLVM_DEBUG(dbgs() << ", none.\n"); |
| 1497 |  |
| 1499 |  |
| 1506 | return false; |
| 1507 | } |
| 1508 | if (!growRegion(Cand)) { |
| 1509 | LLVM_DEBUG(dbgs() << ", cannot spill all interferences.\n"); |
| 1510 |  |
| 1519 | return false; |
| 1520 |  |
| 1522 | } |
| 1523 | SpillPlacer->finish(); |
| 1524 | if (!Cand.LiveBundles.any()) { |
| 1525 | LLVM_DEBUG(dbgs() << ", none.\n"); |
| 1526 | return false; |
| 1527 | } |
| 1528 | LLVM_DEBUG({ |
| 1529 | for (int i : Cand.LiveBundles.set_bits()) |
| 1530 | dbgs() << " EB#" << i; |
| 1531 | dbgs() << ".\n"; |
| 1532 | }); |
| 1533 | return true; |
| 1541 |  |
| 1547 | } |
|   | /// 计算在所有使用块周围分割活范围的成本。 |
| 1548 | /// calcSpillCost - Compute how expensive it would be to split the live range in |
| 1549 | /// SA around all use blocks instead of forming bundle regions. |
| 1550 | BlockFrequency RAGreedy::calcSpillCost() { |
| 1551 | BlockFrequency Cost = 0; |
| 1552 | ArrayRef<SplitAnalysis::BlockInfo> UseBlocks = SA->getUseBlocks(); |
| 1553 | for (unsigned i = 0; i != UseBlocks.size(); ++i) { |
| 1554 | const SplitAnalysis::BlockInfo &BI = UseBlocks[i]; |
| 1555 | unsigned Number = BI.MBB->getNumber(); |
|   | // 我们通常只需要一个溢出指令 - 一个加载或存储。 |
| 1556 | // We normally only need one spill instruction - a load or a store. |
| 1557 | Cost += SpillPlacer->getBlockFrequency(Number); |
|   | // 除非值在块中被重新定义。 |
| 1558 | // Unless the value is redefined in the block. |
| 1559 | if (BI.LiveIn && BI.LiveOut && BI.FirstDef) |
| 1560 | Cost += SpillPlacer->getBlockFrequency(Number); |
| 1565 | } |
| 1566 | return Cost; |
| 1570 | } |
|   | /// 检查分割Evictee是否可能在编号为BBNumber的基本块中创建一个局部分割区间， |
|   | /// 该区间可能导致不良的驱逐链。 |
|   | /// 这是为了防止如下的不良驱逐序列： |
| 1571 | /// Check if splitting Evictee will create a local split interval in |
| 1572 | /// basic block number BBNumber that may cause a bad eviction chain. This is |
| 1573 | /// intended to prevent bad eviction sequences like: |
| 1574 | /// movl	%ebp, 8(%esp)           # 4-byte Spill |
| 1575 | /// movl	%ecx, %ebp |
| 1576 | /// movl	%ebx, %ecx |
| 1577 | /// movl	%edi, %ebx |
| 1578 | /// movl	%edx, %edi |
| 1579 | /// cltd |
| 1580 | /// idivl	%esi |
| 1581 | /// movl	%edi, %edx |
| 1582 | /// movl	%ebx, %edi |
| 1583 | /// movl	%ecx, %ebx |
| 1584 | /// movl	%ebp, %ecx |
| 1585 | /// movl	16(%esp), %ebp          # 4 - byte Reload |
| 1586 | /// |
|   | /// 这样的序列是在两种情况下创建的： |
| 1587 | /// Such sequences are created in 2 scenarios: |
| 1588 | /// |
|   | /// 第一种情况： |
| 1589 | /// Scenario #1: |
| 1590 | /// %0 is evicted from physreg0 by %1. |
| 1591 | /// Evictee %0 is intended for region splitting with split candidate |
| 1592 | /// physreg0 (the reg %0 was evicted from). |
| 1593 | /// Region splitting creates a local interval because of interference with the |
| 1594 | /// evictor %1 (normally region splitting creates 2 interval, the "by reg" |
| 1595 | /// and "by stack" intervals and local interval created when interference |
| 1596 | /// occurs). |
| 1597 | /// One of the split intervals ends up evicting %2 from physreg1. |
| 1598 | /// Evictee %2 is intended for region splitting with split candidate |
| 1599 | /// physreg1. |
| 1600 | /// One of the split intervals ends up evicting %3 from physreg2, etc. |
| 1601 | /// |
|   | /// 第二种情况： |
| 1602 | /// Scenario #2 |
| 1603 | /// %0 is evicted from physreg0 by %1. |
| 1604 | /// %2 is evicted from physreg2 by %3 etc. |
| 1605 | /// Evictee %0 is intended for region splitting with split candidate |
| 1606 | /// physreg1. |
| 1607 | /// Region splitting creates a local interval because of interference with the |
| 1608 | /// evictor %1. |
| 1609 | /// One of the split intervals ends up evicting back original evictor %1 |
| 1610 | /// from physreg0 (the reg %0 was evicted from). |
| 1611 | /// Another evictee %2 is intended for region splitting with split candidate |
| 1612 | /// physreg1. |
| 1613 | /// One of the split intervals ends up evicting %3 from physreg2, etc. |
| 1614 | /// |
| 1615 | /// \param Evictee  The register considered to be split. |
| 1616 | /// \param Cand     The split candidate that determines the physical register |
| 1617 | ///                 we are splitting for and the interferences. |
| 1618 | /// \param BBNumber The number of a BB for which the region split process will |
| 1619 | ///                 create a local split interval. |
| 1620 | /// \param Order    The physical registers that may get evicted by a split |
| 1621 | ///                 artifact of Evictee. |
| 1622 | /// \return True if splitting Evictee may cause a bad eviction chain, false |
| 1623 | /// otherwise. |
| 1624 | bool RAGreedy::splitCanCauseEvictionChain(unsigned Evictee, |
| 1625 | GlobalSplitCandidate &Cand, |
| 1626 | unsigned BBNumber, |
| 1627 | const AllocationOrder &Order) { |
|   | // 获取最后一个被Evictee驱逐的寄存器信息。 |
| 1628 | EvictionTrack::EvictorInfo VregEvictorInfo = LastEvicted.getEvictor(Evictee); |
| 1629 | unsigned Evictor = VregEvictorInfo.first; |
| 1630 | unsigned PhysReg = VregEvictorInfo.second; |
|   | // 如果没有实际的驱逐者。 |
| 1631 | // No actual evictor. |
| 1632 | if (!Evictor || !PhysReg) |
| 1633 | return false; |
|   | // 获取可能被Evictee驱逐的寄存器中权重最大的那个。 |
| 1634 | float MaxWeight = 0; |
| 1635 | unsigned FutureEvictedPhysReg = |
| 1636 | getCheapestEvicteeWeight(Order, LIS->getInterval(Evictee), |
| 1637 | Cand.Intf.first(), Cand.Intf.last(), &MaxWeight); |
|   | // 如果分割候选寄存器是驱逐寄存器，或者分割产生的寄存器将驱逐驱逐寄存器。 |
| 1638 | // The bad eviction chain occurs when either the split candidate is the |
| 1639 | // evicting reg or one of the split artifact will evict the evicting reg. |
| 1640 | if ((PhysReg != Cand.PhysReg) && (PhysReg != FutureEvictedPhysReg)) |
| 1641 | return false; |
|   | // 将干扰游标移动到指定的基本块。 |
| 1642 | Cand.Intf.moveToBlock(BBNumber); |
|   | // 检查驱逐者是否在给定的基本块中包含干扰（与Evictee）。如果是这样， |
|   | // 这个干扰导致了Evictee从PhysReg中被驱逐。这表明我们在区域分割期间将创建 |
|   | // 一个局部区间以避免这种干扰。这个局部区间可能导致不良的驱逐链。 |
| 1643 | // Check to see if the Evictor contains interference (with Evictee) in the |
| 1644 | // given BB. If so, this interference caused the eviction of Evictee from |
| 1645 | // PhysReg. This suggest that we will create a local interval during the |
| 1646 | // region split to avoid this interference This local interval may cause a bad |
| 1647 | // eviction chain. |
| 1648 | if (!LIS->hasInterval(Evictor)) |
| 1649 | return false; |
| 1650 | LiveInterval &EvictorLI = LIS->getInterval(Evictor); |
| 1651 | if (EvictorLI.FindSegmentContaining(Cand.Intf.first()) == EvictorLI.end()) |
| 1652 | return false; |
|   | // 现在，检查我们即将创建的局部区间是否足够昂贵以至于会驱逐某人。如果是这样， |
|   | // 这可能导致不良的驱逐链。 |
| 1653 | // Now, check to see if the local interval we will create is going to be |
| 1654 | // expensive enough to evict somebody If so, this may cause a bad eviction |
| 1655 | // chain. |
| 1656 | VirtRegAuxInfo VRAI(*MF, *LIS, VRM, getAnalysis<MachineLoopInfo>(), *MBFI); |
| 1657 | float splitArtifactWeight = |
| 1658 | VRAI.futureWeight(LIS->getInterval(Evictee), |
| 1659 | Cand.Intf.first().getPrevIndex(), Cand.Intf.last()); |
| 1660 | if (splitArtifactWeight >= 0 && splitArtifactWeight < MaxWeight) |
| 1661 | return false; |
|   | // 如果上述条件都不满足，那么分割Evictee可能会导致不良的驱逐链。 |
| 1662 | return true; |
| 1663 | } |
|   | /// 检查分割VirtRegToSplit是否可能在编号为BBNumber的基本块中 |
|   | /// 创建一个局部分割区间，该区间可能导致溢出。 |
| 1664 | /// Check if splitting VirtRegToSplit will create a local split interval |
| 1665 | /// in basic block number BBNumber that may cause a spill. |
| 1666 | /// |
| 1667 | /// \param VirtRegToSplit The register considered to be split. |
| 1668 | /// \param Cand           The split candidate that determines the physical |
| 1669 | ///                       register we are splitting for and the interferences. |
| 1670 | /// \param BBNumber       The number of a BB for which the region split process |
| 1671 | ///                       will create a local split interval. |
| 1672 | /// \param Order          The physical registers that may get evicted by a |
| 1673 | ///                       split artifact of VirtRegToSplit. |
| 1674 | /// \return True if splitting VirtRegToSplit may cause a spill, false |
| 1675 | /// otherwise. |
| 1676 | bool RAGreedy::splitCanCauseLocalSpill(unsigned VirtRegToSplit, |
| 1677 | GlobalSplitCandidate &Cand, |
| 1678 | unsigned BBNumber, |
| 1679 | const AllocationOrder &Order) { |
| 1680 | Cand.Intf.moveToBlock(BBNumber); |
|   | // 检查局部区间是否能找到不干扰的分配。 |
| 1681 | // Check if the local interval will find a non interfereing assignment. |
| 1682 | for (auto PhysReg : Order.getOrder()) { |
| 1683 | if (!Matrix->checkInterference(Cand.Intf.first().getPrevIndex(), |
| 1684 | Cand.Intf.last(), PhysReg)) |
| 1685 | return false; |
| 1699 | } |
|   | // 检查局部区间是否会驱逐一个更便宜的区间。 |
| 1700 | // Check if the local interval will evict a cheaper interval. |
| 1701 | float CheapestEvictWeight = 0; |
| 1702 | unsigned FutureEvictedPhysReg = getCheapestEvicteeWeight( |
| 1703 | Order, LIS->getInterval(VirtRegToSplit), Cand.Intf.first(), |
| 1704 | Cand.Intf.last(), &CheapestEvictWeight); |
|   | // 如果找到了可以被驱逐的区间。 |
| 1705 | // Have we found an interval that can be evicted? |
| 1706 | if (FutureEvictedPhysReg) { |
| 1707 | VirtRegAuxInfo VRAI(*MF, *LIS, VRM, getAnalysis<MachineLoopInfo>(), *MBFI); |
| 1708 | float splitArtifactWeight = |
| 1709 | VRAI.futureWeight(LIS->getInterval(VirtRegToSplit), |
| 1710 | Cand.Intf.first().getPrevIndex(), Cand.Intf.last()); |
|   | // 如果局部区间的权重高于最便宜的被驱逐区间权重，它将驱逐它，不会溢出。 |
| 1711 | // Will the weight of the local interval be higher than the cheapest evictee |
| 1712 | // weight? If so it will evict it and will not cause a spill. |
| 1713 | if (splitArtifactWeight >= 0 && splitArtifactWeight > CheapestEvictWeight) |
| 1714 | return false; |
| 1717 | } |
|   | // 如果局部区间找不到不干扰的分配，也不能驱逐一个不值得的区间，那么它可能导致溢出。 |
| 1718 | // The local interval is not able to find non interferencing assignment and |
| 1719 | // not able to evict a less worthy interval, therfore, it can cause a spill. |
| 1720 | return true; |
| 1725 | } |
|   | /// 计算遵循LiveBundles中的分割模式的全局分割成本。 |
|   | /// 这个成本应该加到SplitConstraints中干扰模式的局部成本上。 |
| 1726 | /// calcGlobalSplitCost - Return the global split cost of following the split |
| 1727 | /// pattern in LiveBundles. This cost should be added to the local cost of the |
| 1728 | /// interference pattern in SplitConstraints. |
| 1729 | /// |
| 1730 | BlockFrequency RAGreedy::calcGlobalSplitCost(GlobalSplitCandidate &Cand, |
| 1731 | const AllocationOrder &Order, |
| 1732 | bool *CanCauseEvictionChain) { |
| 1733 | BlockFrequency GlobalCost = 0; |
| 1734 | const BitVector &LiveBundles = Cand.LiveBundles; |
| 1735 | unsigned VirtRegToSplit = SA->getParent().reg; |
| 1736 | ArrayRef<SplitAnalysis::BlockInfo> UseBlocks = SA->getUseBlocks(); |
| 1737 | for (unsigned i = 0; i != UseBlocks.size(); ++i) { |
| 1738 | const SplitAnalysis::BlockInfo &BI = UseBlocks[i]; |
| 1739 | SpillPlacement::BlockConstraint &BC = SplitConstraints[i]; |
| 1740 | bool RegIn  = LiveBundles[Bundles->getBundle(BC.Number, false)]; |
| 1741 | bool RegOut = LiveBundles[Bundles->getBundle(BC.Number, true)]; |
| 1742 | unsigned Ins = 0; |
| 1743 | Cand.Intf.moveToBlock(BC.Number); |
|   | // 检查在区域分割期间是否会创建局部区间。 |
|   | // 如果启用了选项，计算高级分割成本（局部区间的成本）。 |
| 1744 | // Check wheather a local interval is going to be created during the region |
| 1745 | // split. Calculate adavanced spilt cost (cost of local intervals) if option |
| 1746 | // is enabled. |
| 1747 | if (EnableAdvancedRASplitCost && Cand.Intf.hasInterference() && BI.LiveIn && |
| 1748 | BI.LiveOut && RegIn && RegOut) { |
| 1749 | if (CanCauseEvictionChain && |
| 1750 | splitCanCauseEvictionChain(VirtRegToSplit, Cand, BC.Number, Order)) { |
|   | // 这个干扰导致我们从这个分配中被驱逐，我们可能会驱逐其他人， |
|   | // 最终有人会溢出，加上那个成本。 |
| 1751 | // This interference causes our eviction from this assignment, we might |
| 1752 | // evict somebody else and eventually someone will spill, add that cost. |
| 1753 | // See splitCanCauseEvictionChain for detailed description of scenarios. |
| 1754 | GlobalCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1755 | GlobalCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1756 | *CanCauseEvictionChain = true; |
| 1757 | } else if (splitCanCauseLocalSpill(VirtRegToSplit, Cand, BC.Number, |
| 1758 | Order)) { |
|   | // 这个干扰导致局部区间溢出，加上那个成本。 |
| 1759 | // This interference causes local interval to spill, add that cost. |
| 1760 | GlobalCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1761 | GlobalCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1762 | } |
| 1763 | } |
| 1764 | if (BI.LiveIn) |
| 1765 | Ins += RegIn != (BC.Entry == SpillPlacement::PrefReg); |
| 1766 | if (BI.LiveOut) |
| 1767 | Ins += RegOut != (BC.Exit == SpillPlacement::PrefReg); |
| 1768 | while (Ins--) |
| 1769 | GlobalCost += SpillPlacer->getBlockFrequency(BC.Number); |
| 1770 | } |
| 1771 | for (unsigned i = 0, e = Cand.ActiveBlocks.size(); i != e; ++i) { |
| 1772 | unsigned Number = Cand.ActiveBlocks[i]; |
| 1773 | bool RegIn  = LiveBundles[Bundles->getBundle(Number, false)]; |
| 1774 | bool RegOut = LiveBundles[Bundles->getBundle(Number, true)]; |
| 1775 | if (!RegIn && !RegOut) |
| 1779 |  |
| 1782 | continue; |
| 1783 | if (RegIn && RegOut) { |
|   | // 如果这个块有干扰，我们需要双倍的溢出代码。 |
| 1784 | // We need double spill code if this block has interference. |
| 1785 | Cand.Intf.moveToBlock(Number); |
| 1786 | if (Cand.Intf.hasInterference()) { |
| 1787 | GlobalCost += SpillPlacer->getBlockFrequency(Number); |
| 1788 | GlobalCost += SpillPlacer->getBlockFrequency(Number); |
|   | // 检查在区域分割期间是否会创建局部区间。 |
| 1789 | // Check wheather a local interval is going to be created during the |
| 1790 | // region split. |
| 1791 | if (EnableAdvancedRASplitCost && CanCauseEvictionChain && |
| 1792 | splitCanCauseEvictionChain(VirtRegToSplit, Cand, Number, Order)) { |
| 1793 | // This interference cause our eviction from this assignment, we might |
| 1794 | // evict somebody else, add that cost. |
| 1795 | // See splitCanCauseEvictionChain for detailed description of |
| 1796 | // scenarios. |
| 1797 | GlobalCost += SpillPlacer->getBlockFrequency(Number); |
| 1798 | GlobalCost += SpillPlacer->getBlockFrequency(Number); |
| 1799 | *CanCauseEvictionChain = true; |
| 1801 | } |
| 1802 |  |
| 1805 | } |
| 1806 | continue; |
| 1809 | } |
| 1810 | // live-in / stack-out or stack-in live-out. |
| 1811 | GlobalCost += SpillPlacer->getBlockFrequency(Number); |
| 1818 |  |
| 1825 | } |
| 1826 | return GlobalCost; |
| 1831 | } |
|   | /// 围绕由BundleCand和GlobalCand确定的区域分割当前的活范围。 |
| 1832 | /// splitAroundRegion - Split the current live range around the regions |
| 1833 | /// determined by BundleCand and GlobalCand. |
| 1834 | /// |
| 1835 | /// Before calling this function, GlobalCand and BundleCand must be initialized |
| 1836 | /// so each bundle is assigned to a valid candidate, or NoCand for the |
| 1837 | /// stack-bound bundles.  The shared SA/SE SplitAnalysis and SplitEditor |
| 1838 | /// objects must be initialized for the current live range, and intervals |
| 1839 | /// created for the used candidates. |
| 1840 | /// |
| 1841 | /// @param LREdit    The LiveRangeEdit object handling the current split. |
| 1842 | /// @param UsedCands List of used GlobalCand entries. Every BundleCand value |
| 1843 | ///                  must appear in this list. |
| 1844 | void RAGreedy::splitAroundRegion(LiveRangeEdit &LREdit, |
| 1845 | ArrayRef<unsigned> UsedCands) { |
|   | // 这些是为新全局范围创建的区间。我们可能会为局部范围创建更多的区间。 |
| 1846 | // These are the intervals created for new global ranges. We may create more |
| 1847 | // intervals for local ranges. |
| 1848 | const unsigned NumGlobalIntvs = LREdit.size(); |
| 1849 | LLVM_DEBUG(dbgs() << "splitAroundRegion with " << NumGlobalIntvs |
| 1850 | << " globals.\n"); |
| 1851 | assert(NumGlobalIntvs && "No global intervals configured"); |
|   | // 当处理适当的子类时，即使只有一个指令也要隔离。 |
|   | // 这保证了栈区间的寄存器类膨胀，因为它全部是复制的。 |
| 1852 | // Isolate even single instructions when dealing with a proper sub-class. |
| 1853 | // That guarantees register class inflation for the stack interval because it |
| 1854 | // is all copies. |
| 1855 | unsigned Reg = SA->getParent().reg; |
| 1856 | bool SingleInstrs = RegClassInfo.isProperSubClass(MRI->getRegClass(Reg)); |
|   | // 首先处理所有使用块。 |
| 1857 | // First handle all the blocks with uses. |
| 1858 | ArrayRef<SplitAnalysis::BlockInfo> UseBlocks = SA->getUseBlocks(); |
| 1859 | for (unsigned i = 0; i != UseBlocks.size(); ++i) { |
| 1860 | const SplitAnalysis::BlockInfo &BI = UseBlocks[i]; |
| 1861 | unsigned Number = BI.MBB->getNumber(); |
| 1862 | unsigned IntvIn = 0, IntvOut = 0; |
| 1863 | SlotIndex IntfIn, IntfOut; |
| 1864 | if (BI.LiveIn) { |
| 1865 | unsigned CandIn = BundleCand[Bundles->getBundle(Number, false)]; |
| 1866 | if (CandIn != NoCand) { |
| 1867 | GlobalSplitCandidate &Cand = GlobalCand[CandIn]; |
| 1868 | IntvIn = Cand.IntvIdx; |
| 1869 | Cand.Intf.moveToBlock(Number); |
| 1870 | IntfIn = Cand.Intf.first(); |
| 1877 | } |
| 1878 | } |
| 1879 | if (BI.LiveOut) { |
| 1880 | unsigned CandOut = BundleCand[Bundles->getBundle(Number, true)]; |
| 1881 | if (CandOut != NoCand) { |
| 1882 | GlobalSplitCandidate &Cand = GlobalCand[CandOut]; |
| 1883 | IntvOut = Cand.IntvIdx; |
| 1884 | Cand.Intf.moveToBlock(Number); |
| 1885 | IntfOut = Cand.Intf.last(); |
| 1889 |  |
| 1895 | } |
| 1905 | } |
|   | // 为具有多个使用的隔离块创建单独的区间。 |
| 1906 | // Create separate intervals for isolated blocks with multiple uses. |
| 1907 | if (!IntvIn && !IntvOut) { |
| 1908 | LLVM_DEBUG(dbgs() << printMBBReference(*BI.MBB) << " isolated.\n"); |
| 1909 | if (SA->shouldSplitSingleBlock(BI, SingleInstrs)) |
| 1910 | SE->splitSingleBlock(BI); |
| 1912 |  |
| 1914 |  |
| 1918 | continue; |
| 1919 | } |
| 1920 | if (IntvIn && IntvOut) |
| 1921 | SE->splitLiveThroughBlock(Number, IntvIn, IntfIn, IntvOut, IntfOut); |
| 1922 | else if (IntvIn) |
| 1923 | SE->splitRegInBlock(BI, IntvIn, IntfIn); |
| 1924 | else |
| 1925 | SE->splitRegOutBlock(BI, IntvOut, IntfOut); |
| 1929 | } |
|   | // 处理通过块。相关通过块存储在每个候选者的ActiveBlocks列表中。 |
|   | // 我们需要过滤掉重复项。 |
| 1930 | // Handle live-through blocks. The relevant live-through blocks are stored in |
| 1931 | // the ActiveBlocks list with each candidate. We need to filter out |
| 1932 | // duplicates. |
| 1933 | BitVector Todo = SA->getThroughBlocks(); |
| 1934 | for (unsigned c = 0; c != UsedCands.size(); ++c) { |
| 1935 | ArrayRef<unsigned> Blocks = GlobalCand[UsedCands[c]].ActiveBlocks; |
| 1936 | for (unsigned i = 0, e = Blocks.size(); i != e; ++i) { |
| 1937 | unsigned Number = Blocks[i]; |
| 1938 | if (!Todo.test(Number)) |
| 1939 | continue; |
| 1940 | Todo.reset(Number); |
| 1941 | unsigned IntvIn = 0, IntvOut = 0; |
| 1942 | SlotIndex IntfIn, IntfOut; |
| 1943 | unsigned CandIn = BundleCand[Bundles->getBundle(Number, false)]; |
| 1944 | if (CandIn != NoCand) { |
| 1945 | GlobalSplitCandidate &Cand = GlobalCand[CandIn]; |
| 1946 | IntvIn = Cand.IntvIdx; |
| 1947 | Cand.Intf.moveToBlock(Number); |
| 1948 | IntfIn = Cand.Intf.first(); |
| 1949 | } |
| 1950 | unsigned CandOut = BundleCand[Bundles->getBundle(Number, true)]; |
| 1951 | if (CandOut != NoCand) { |
| 1952 | GlobalSplitCandidate &Cand = GlobalCand[CandOut]; |
| 1953 | IntvOut = Cand.IntvIdx; |
| 1954 | Cand.Intf.moveToBlock(Number); |
| 1955 | IntfOut = Cand.Intf.last(); |
| 1956 | } |
| 1957 | if (!IntvIn && !IntvOut) |
| 1958 | continue; |
| 1959 | SE->splitLiveThroughBlock(Number, IntvIn, IntfIn, IntvOut, IntfOut); |
| 1961 |  |
| 1964 |  |
| 1974 | } |
| 1975 | } |
| 1976 | ++NumGlobalSplits; |
| 1977 | SmallVector<unsigned, 8> IntvMap; |
| 1978 | SE->finish(&IntvMap); |
| 1979 | DebugVars->splitRegister(Reg, LREdit.regs(), *LIS); |
| 1980 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
| 1981 | unsigned OrigBlocks = SA->getNumLiveBlocks(); |
|   | // 整理由分割创建的新区间。我们得到四种类型： |
|   | // - 剩余区间不应再分割。 |
|   | // - 候选区间可以分配给Cand.PhysReg。 |
|   | // - 块局部分割是局部分割的候选。 |
|   | // - DCE残留物应重新排队。 |
| 1982 | // Sort out the new intervals created by splitting. We get four kinds: |
| 1983 | // - Remainder intervals should not be split again. |
| 1984 | // - Candidate intervals can be assigned to Cand.PhysReg. |
| 1985 | // - Block-local splits are candidates for local splitting. |
| 1986 | // - DCE leftovers should go back on the queue. |
| 1987 | for (unsigned i = 0, e = LREdit.size(); i != e; ++i) { |
| 1988 | LiveInterval &Reg = LIS->getInterval(LREdit.get(i)); |
|   | // 忽略DCE中的旧区间。 |
| 1989 | // Ignore old intervals from DCE. |
| 1990 | if (getStage(Reg) != RS_New) |
| 1991 | continue; |
|   | // 剩余区间。不要再尝试分割，如果不分配则溢出。 |
| 1992 | // Remainder interval. Don't try splitting again, spill if it doesn't |
| 1993 | // allocate. |
| 1994 | if (IntvMap[i] == 0) { |
| 1995 | setStage(Reg, RS_Spill); |
| 1996 | continue; |
| 1997 | } |
|   | // 全局区间。只要活动块的数量严格减少，就允许重复分割。 |
| 1998 | // Global intervals. Allow repeated splitting as long as the number of live |
| 1999 | // blocks is strictly decreasing. |
| 2000 | if (IntvMap[i] < NumGlobalIntvs) { |
| 2001 | if (SA->countLiveBlocks(&Reg) >= OrigBlocks) { |
| 2002 | LLVM_DEBUG(dbgs() << "Main interval covers the same " << OrigBlocks |
| 2003 | << " blocks as original.\n"); |
|   | // 作为安全防护，不允许重复分割。 |
| 2004 | // Don't allow repeated splitting as a safe guard against looping. |
| 2005 | setStage(Reg, RS_Split2); |
| 2013 | } |
| 2014 | continue; |
| 2015 | } |
|   | // 其他区间被视为新的。这包括为具有多个使用的块创建的局部区间， |
|   | // 以及DCE创建的任何内容。 |
| 2016 | // Other intervals are treated as new. This includes local intervals created |
| 2017 | // for blocks with multiple uses, and anything created by DCE. |
| 2018 | } |
| 2019 | if (VerifyEnabled) |
| 2020 | MF->verify(this, "After splitting live range around region"); |
| 2021 |  |
| 2024 |  |
| 2026 |  |
| 2033 | } |
|   | /// 尝试围绕AllocationOrder确定的区域分割VirtReg的活范围。 |
| 2034 | unsigned RAGreedy::tryRegionSplit(LiveInterval &VirtReg, AllocationOrder &Order, |
| 2035 | SmallVectorImpl<Register> &NewVRegs) { |
|   | // 如果目标寄存器接口建议不要对VirtReg进行区域分割，则返回0。 |
| 2036 | if (!TRI->shouldRegionSplitForVirtReg(*MF, VirtReg)) |
| 2037 | return 0; |
| 2038 | unsigned NumCands = 0; |
| 2039 | BlockFrequency SpillCost = calcSpillCost(); |
| 2040 | BlockFrequency BestCost; |
|   | // 检查我们是否可以围绕一个紧凑区域分割这个活范围。 |
| 2041 | // Check if we can split this live range around a compact region. |
| 2042 | bool HasCompact = calcCompactRegion(GlobalCand.front()); |
| 2043 | if (HasCompact) { |
|   | // 是的，保持GlobalCand[0]作为紧凑区域候选。 |
| 2044 | // Yes, keep GlobalCand[0] as the compact region candidate. |
| 2045 | NumCands = 1; |
| 2046 | BestCost = BlockFrequency::getMaxFrequency(); |
| 2047 | } else { |
|   | // 没有紧凑区域的好处，我们的退路将是逐块分割。 |
|   | // 确保我们找到一个比溢出更便宜的解决方案。 |
| 2048 | // No benefit from the compact region, our fallback will be per-block |
| 2049 | // splitting. Make sure we find a solution that is cheaper than spilling. |
| 2050 | BestCost = SpillCost; |
| 2051 | LLVM_DEBUG(dbgs() << "Cost of isolating all blocks = "; |
| 2052 | MBFI->printBlockFreq(dbgs(), BestCost) << '\n'); |
| 2058 | } |
| 2059 | bool CanCauseEvictionChain = false; |
| 2060 | unsigned BestCand = |
| 2061 | calculateRegionSplitCost(VirtReg, Order, BestCost, NumCands, |
| 2062 | false /*IgnoreCSR*/, &CanCauseEvictionChain); |
|   | // 分割候选区域可能会导致不良的驱逐序列。 |
|   | // 为了避免它，我们需要将成本与溢出成本进行比较，而不是当前的最大频率。 |
| 2063 | // Split candidates with compact regions can cause a bad eviction sequence. |
| 2064 | // See splitCanCauseEvictionChain for detailed description of scenarios. |
| 2065 | // To avoid it, we need to comapre the cost with the spill cost and not the |
| 2066 | // current max frequency. |
| 2067 | if (HasCompact && (BestCost > SpillCost) && (BestCand != NoCand) && |
| 2068 | CanCauseEvictionChain) { |
| 2073 | return 0; |
| 2074 | } |
|   | // 没有找到解决方案，退回到单块分割。 |
| 2075 | // No solutions found, fall back to single block splitting. |
| 2076 | if (!HasCompact && BestCand == NoCand) |
| 2079 |  |
| 2082 | return 0; |
| 2083 | return doRegionSplit(VirtReg, BestCand, HasCompact, NewVRegs); |
| 2084 | } |
|   | /// 计算围绕AllocationOrder确定的区域分割VirtReg的成本。 |
| 2085 | unsigned RAGreedy::calculateRegionSplitCost(LiveInterval &VirtReg, |
| 2086 | AllocationOrder &Order, |
| 2087 | BlockFrequency &BestCost, |
| 2088 | unsigned &NumCands, bool IgnoreCSR, |
| 2089 | bool *CanCauseEvictionChain) { |
| 2090 | unsigned BestCand = NoCand; |
| 2091 | Order.rewind(); |
| 2092 | while (unsigned PhysReg = Order.next()) { |
| 2093 | if (IgnoreCSR && isUnusedCalleeSavedReg(PhysReg)) |
| 2101 | continue; |
|   | // 在我们用完干扰缓存游标之前丢弃坏候选。 |
| 2102 | // Discard bad candidates before we run out of interference cache cursors. |
| 2103 | // This will only affect register classes with a lot of registers (>32). |
| 2104 | if (NumCands == IntfCache.getMaxCursors()) { |
| 2105 | unsigned WorstCount = ~0u; |
| 2106 | unsigned Worst = 0; |
| 2107 | for (unsigned i = 0; i != NumCands; ++i) { |
| 2108 | if (i == BestCand || !GlobalCand[i].PhysReg) |
| 2109 | continue; |
| 2110 | unsigned Count = GlobalCand[i].LiveBundles.count(); |
| 2111 | if (Count < WorstCount) { |
| 2112 | Worst = i; |
| 2113 | WorstCount = Count; |
| 2118 |  |
| 2122 | } |
| 2123 | } |
| 2124 | --NumCands; |
| 2125 | GlobalCand[Worst] = GlobalCand[NumCands]; |
| 2126 | if (BestCand == NumCands) |
| 2127 | BestCand = Worst; |
| 2128 | } |
| 2129 | if (GlobalCand.size() <= NumCands) |
| 2130 | GlobalCand.resize(NumCands+1); |
| 2131 | GlobalSplitCandidate &Cand = GlobalCand[NumCands]; |
| 2132 | Cand.reset(IntfCache, PhysReg); |
| 2133 | SpillPlacer->prepare(Cand.LiveBundles); |
| 2134 | BlockFrequency Cost; |
| 2135 | if (!addSplitConstraints(Cand.Intf, Cost)) { |
| 2136 | LLVM_DEBUG(dbgs() << printReg(PhysReg, TRI) << "\tno positive bundles\n"); |
| 2137 | continue; |
| 2138 | } |
| 2139 | LLVM_DEBUG(dbgs() << printReg(PhysReg, TRI) << "\tstatic = "; |
| 2140 | MBFI->printBlockFreq(dbgs(), Cost)); |
| 2141 | if (Cost >= BestCost) { |
| 2142 | LLVM_DEBUG({ |
| 2143 | if (BestCand == NoCand) |
| 2144 | dbgs() << " worse than no bundles\n"; |
| 2145 | else |
| 2146 | dbgs() << " worse than " |
| 2147 | << printReg(GlobalCand[BestCand].PhysReg, TRI) << '\n'; |
| 2148 | }); |
| 2152 | continue; |
| 2153 | } |
| 2154 | if (!growRegion(Cand)) { |
| 2155 | LLVM_DEBUG(dbgs() << ", cannot spill all interferences.\n"); |
| 2156 | continue; |
| 2157 | } |
| 2158 | SpillPlacer->finish(); |
|   | // 没有活动捆绑包，推迟到splitSingleBlocks()。 |
| 2159 | // No live bundles, defer to splitSingleBlocks(). |
| 2160 | if (!Cand.LiveBundles.any()) { |
| 2161 | LLVM_DEBUG(dbgs() << " no bundles.\n"); |
| 2162 | continue; |
| 2170 |  |
| 2177 | } |
| 2178 | bool HasEvictionChain = false; |
| 2179 | Cost += calcGlobalSplitCost(Cand, Order, &HasEvictionChain); |
| 2180 | LLVM_DEBUG({ |
| 2181 | dbgs() << ", total = "; |
| 2182 | MBFI->printBlockFreq(dbgs(), Cost) << " with bundles"; |
| 2183 | for (int i : Cand.LiveBundles.set_bits()) |
| 2184 | dbgs() << " EB#" << i; |
| 2185 | dbgs() << ".\n"; |
| 2186 | }); |
| 2187 | if (Cost < BestCost) { |
| 2188 | BestCand = NumCands; |
| 2189 | BestCost = Cost; |
| 2190 | // See splitCanCauseEvictionChain for detailed description of bad |
| 2191 | // eviction chain scenarios. |
| 2192 | if (CanCauseEvictionChain) |
| 2193 | *CanCauseEvictionChain = HasEvictionChain; |
| 2196 |  |
| 2201 | } |
| 2202 | ++NumCands; |
| 2204 | } |
| 2205 | if (CanCauseEvictionChain && BestCand != NoCand) { |
| 2206 | // See splitCanCauseEvictionChain for detailed description of bad |
| 2207 | // eviction chain scenarios. |
| 2208 | LLVM_DEBUG(dbgs() << "Best split candidate of vreg " |
| 2209 | << printReg(VirtReg.reg, TRI) << "  may "); |
| 2210 | if (!(*CanCauseEvictionChain)) |
| 2211 | LLVM_DEBUG(dbgs() << "not "); |
| 2212 | LLVM_DEBUG(dbgs() << "cause bad eviction chain\n"); |
| 2213 | } |
| 2214 | return BestCand; |
| 2215 | } |
|   | /// 对VirtReg执行区域分割，最佳候选区域由之前的分析确定。 |
| 2216 | unsigned RAGreedy::doRegionSplit(LiveInterval &VirtReg, unsigned BestCand, |
| 2217 | bool HasCompact, |
| 2218 | SmallVectorImpl<Register> &NewVRegs) { |
| 2219 | SmallVector<unsigned, 8> UsedCands; |
|   | // 准备分割编辑器。 |
| 2220 | // Prepare split editor. |
| 2221 | LiveRangeEdit LREdit(&VirtReg, NewVRegs, *MF, *LIS, VRM, this, &DeadRemats); |
| 2222 | SE->reset(LREdit, SplitSpillMode); |
|   | // 为所有边缘捆绑包分配首选候选或NoCand。 |
| 2223 | // Assign all edge bundles to the preferred candidate, or NoCand. |
| 2224 | BundleCand.assign(Bundles->getNumBundles(), NoCand); |
|   | // 为最佳候选区域分配捆绑包。 |
| 2225 | // Assign bundles for the best candidate region. |
| 2226 | if (BestCand != NoCand) { |
| 2227 | GlobalSplitCandidate &Cand = GlobalCand[BestCand]; |
| 2228 | if (unsigned B = Cand.getBundles(BundleCand, BestCand)) { |
| 2229 | UsedCands.push_back(BestCand); |
| 2230 | Cand.IntvIdx = SE->openIntv(); |
| 2231 | LLVM_DEBUG(dbgs() << "Split for " << printReg(Cand.PhysReg, TRI) << " in " |
| 2232 | << B << " bundles, intv " << Cand.IntvIdx << ".\n"); |
| 2233 | (void)B; |
| 2237 | } |
| 2238 | } |
|   | // 为紧凑区域分配捆绑包。 |
| 2239 | // Assign bundles for the compact region. |
| 2240 | if (HasCompact) { |
| 2241 | GlobalSplitCandidate &Cand = GlobalCand.front(); |
| 2242 | assert(!Cand.PhysReg && "Compact region has no physreg"); |
| 2243 | if (unsigned B = Cand.getBundles(BundleCand, 0)) { |
| 2244 | UsedCands.push_back(0); |
| 2245 | Cand.IntvIdx = SE->openIntv(); |
| 2246 | LLVM_DEBUG(dbgs() << "Split for compact region in " << B |
| 2247 | << " bundles, intv " << Cand.IntvIdx << ".\n"); |
| 2248 | (void)B; |
| 2249 | } |
| 2250 | } |
| 2251 | splitAroundRegion(LREdit, UsedCands); |
| 2252 | return 0; |
| 2266 | } |
| 2267 | //===----------------------------------------------------------------------===// |
| 2268 | //                            Per-Block Splitting |
| 2269 | //===----------------------------------------------------------------------===// |
|   | /// 对每个使用全局活范围的块进行分割。 |
|   | /// 这会创建许多局部活范围，如果它们无法分配寄存器， |
|   | /// 将由tryLocalSplit进一步分割。 |
| 2270 | /// tryBlockSplit - Split a global live range around every block with uses. This |
| 2271 | /// creates a lot of local live ranges, that will be split by tryLocalSplit if |
| 2272 | /// they don't allocate. |
| 2273 | unsigned RAGreedy::tryBlockSplit(LiveInterval &VirtReg, AllocationOrder &Order, |
| 2274 | SmallVectorImpl<Register> &NewVRegs) { |
| 2275 | assert(&SA->getParent() == &VirtReg && "Live range wasn't analyzed"); |
| 2276 | Register Reg = VirtReg.reg; |
| 2277 | bool SingleInstrs = RegClassInfo.isProperSubClass(MRI->getRegClass(Reg)); |
|   | // 准备分割编辑器。 |
| 2278 | LiveRangeEdit LREdit(&VirtReg, NewVRegs, *MF, *LIS, VRM, this, &DeadRemats); |
| 2279 | SE->reset(LREdit, SplitSpillMode); |
| 2280 | ArrayRef<SplitAnalysis::BlockInfo> UseBlocks = SA->getUseBlocks(); |
| 2281 | for (unsigned i = 0; i != UseBlocks.size(); ++i) { |
| 2282 | const SplitAnalysis::BlockInfo &BI = UseBlocks[i]; |
| 2283 | if (SA->shouldSplitSingleBlock(BI, SingleInstrs)) |
| 2284 | SE->splitSingleBlock(BI); |
| 2285 | } |
|   | // 如果没有块被分割。 |
| 2286 | // No blocks were split. |
| 2287 | if (LREdit.empty()) |
| 2288 | return 0; |
|   | // 我们对一些块进行了分割。 |
| 2289 | // We did split for some blocks. |
| 2290 | SmallVector<unsigned, 8> IntvMap; |
| 2291 | SE->finish(&IntvMap); |
|   | // 通知LiveDebugVariables关于新范围的信息。 |
| 2292 | // Tell LiveDebugVariables about the new ranges. |
| 2293 | DebugVars->splitRegister(Reg, LREdit.regs(), *LIS); |
| 2294 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
|   | // 整理由分割创建的新区间。剩余区间直接溢出，新局部区间保持RS_New状态。 |
| 2295 | // Sort out the new intervals created by splitting. The remainder interval |
| 2296 | // goes straight to spilling, the new local ranges get to stay RS_New. |
| 2297 | for (unsigned i = 0, e = LREdit.size(); i != e; ++i) { |
| 2298 | LiveInterval &LI = LIS->getInterval(LREdit.get(i)); |
| 2299 | if (getStage(LI) == RS_New && IntvMap[i] == 0) |
| 2300 | setStage(LI, RS_Spill); |
| 2301 | } |
| 2302 | if (VerifyEnabled) |
| 2303 | MF->verify(this, "After splitting live range around basic blocks"); |
| 2304 | return 0; |
| 2305 | } |
| 2306 | //===----------------------------------------------------------------------===// |
| 2307 | //                         Per-Instruction Splitting |
| 2308 | //===----------------------------------------------------------------------===// |
|   | /// 获取符合\p MI对\p Reg的约束，并且也在\p SuperRC中的可分配寄存器的数量。 |
| 2309 | /// Get the number of allocatable registers that match the constraints of \p Reg |
| 2310 | /// on \p MI and that are also in \p SuperRC. |
| 2311 | static unsigned getNumAllocatableRegsForConstraints( |
| 2312 | const MachineInstr *MI, unsigned Reg, const TargetRegisterClass *SuperRC, |
| 2313 | const TargetInstrInfo *TII, const TargetRegisterInfo *TRI, |
| 2314 | const RegisterClassInfo &RCI) { |
| 2315 | assert(SuperRC && "Invalid register class"); |
|   | // 获取由于MI对Reg的约束而影响的寄存器类。 |
| 2316 | const TargetRegisterClass *ConstrainedRC = |
| 2317 | MI->getRegClassConstraintEffectForVReg(Reg, SuperRC, TII, TRI, |
| 2318 | /* ExploreBundle */ true); |
| 2319 | if (!ConstrainedRC) |
| 2320 | return 0; |
| 2321 | return RCI.getNumAllocatableRegs(ConstrainedRC); |
| 2325 |  |
| 2328 |  |
| 2333 | } |
|   | /// 尝试对活范围围绕单个指令进行分割。这通常并不值得， |
|   | /// 因为spiller本质上也在做相同的事情。 |
|   | /// 但是，当活范围处于受限寄存器类中时， |
|   | /// 插入复制可能有助于将活范围的一部分移动到更大的寄存器类中。 |
|   | /// 这类似于向更大的寄存器类进行spill。 |
| 2334 | /// tryInstructionSplit - Split a live range around individual instructions. |
| 2335 | /// This is normally not worthwhile since the spiller is doing essentially the |
| 2336 | /// same thing. However, when the live range is in a constrained register |
| 2337 | /// class, it may help to insert copies such that parts of the live range can |
| 2338 | /// be moved to a larger register class. |
| 2339 | /// |
| 2340 | /// This is similar to spilling to a larger register class. |
| 2341 | unsigned |
| 2342 | RAGreedy::tryInstructionSplit(LiveInterval &VirtReg, AllocationOrder &Order, |
| 2343 | SmallVectorImpl<Register> &NewVRegs) { |
| 2344 | const TargetRegisterClass *CurRC = MRI->getRegClass(VirtReg.reg); |
|   | // 如果没有更大的子类，那么这样做没有意义。 |
| 2345 | // There is no point to this if there are no larger sub-classes. |
| 2346 | if (!RegClassInfo.isProperSubClass(CurRC)) |
| 2347 | return 0; |
|   | // 总是启用分割spill模式，因为我们实际上是在向寄存器中spill。 |
| 2348 | // Always enable split spill mode, since we're effectively spilling to a |
| 2349 | // register. |
| 2350 | LiveRangeEdit LREdit(&VirtReg, NewVRegs, *MF, *LIS, VRM, this, &DeadRemats); |
| 2351 | SE->reset(LREdit, SplitEditor::SM_Size); |
| 2352 | ArrayRef<SlotIndex> Uses = SA->getUseSlots(); |
| 2353 | if (Uses.size() <= 1) |
| 2354 | return 0; |
| 2355 | LLVM_DEBUG(dbgs() << "Split around " << Uses.size() |
| 2356 | << " individual instrs.\n"); |
| 2357 | const TargetRegisterClass *SuperRC = |
| 2358 | TRI->getLargestLegalSuperClass(CurRC, *MF); |
| 2359 | unsigned SuperRCNumAllocatableRegs = RCI.getNumAllocatableRegs(SuperRC); |
|   | // 如果这次分割会放宽虚拟寄存器的约束，则围绕每个非复制指令进行分割。 |
|   | // 否则，分割只是插入了无法合并的复制，这对分配没有帮助。 |
| 2360 | // Split around every non-copy instruction if this split will relax |
| 2361 | // the constraints on the virtual register. |
| 2362 | // Otherwise, splitting just inserts uncoalescable copies that do not help |
| 2363 | // the allocation. |
| 2364 | for (unsigned i = 0; i != Uses.size(); ++i) { |
| 2365 | if (const MachineInstr *MI = Indexes->getInstructionFromIndex(Uses[i])) |
| 2366 | if (MI->isFullCopy() || |
| 2367 | SuperRCNumAllocatableRegs == |
| 2368 | getNumAllocatableRegsForConstraints(MI, VirtReg.reg, SuperRC, TII, |
| 2369 | TRI, RCI)) { |
| 2370 | LLVM_DEBUG(dbgs() << "    skip:\t" << Uses[i] << '\t' << *MI); |
| 2379 | continue; |
| 2380 | } |
| 2381 | SE->openIntv(); |
| 2382 | SlotIndex SegStart = SE->enterIntvBefore(Uses[i]); |
| 2383 | SlotIndex SegStop  = SE->leaveIntvAfter(Uses[i]); |
| 2384 | SE->useIntv(SegStart, SegStop); |
| 2388 | } |
| 2389 | if (LREdit.empty()) { |
| 2390 | LLVM_DEBUG(dbgs() << "All uses were copies.\n"); |
| 2394 |  |
| 2397 | return 0; |
| 2398 | } |
| 2402 |  |
| 2405 |  |
| 2410 | SmallVector<unsigned, 8> IntvMap; |
| 2411 | SE->finish(&IntvMap); |
| 2412 | DebugVars->splitRegister(VirtReg.reg, LREdit.regs(), *LIS); |
| 2413 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
|   | // 将所有新寄存器分配给RS_Spill。这是最后的机会。 |
| 2414 | // Assign all new registers to RS_Spill. This was the last chance. |
| 2415 | setStage(LREdit.begin(), LREdit.end(), RS_Spill); |
| 2416 | return 0; |
| 2427 | } |
| 2431 |  |
| 2434 |  |
| 2435 | //===----------------------------------------------------------------------===// |
| 2436 | //                             Local Splitting |
| 2437 | //===----------------------------------------------------------------------===// |
|   | /// 计算在SA->UseSlots中的两个条目之间使用物理寄存器PhysReg所需的最大溢出权重。 |
|   | /// GapWeight[i]表示UseSlots[i]和UseSlots[i+1]之间的间隔。 |
| 2438 | /// calcGapWeights - Compute the maximum spill weight that needs to be evicted |
| 2439 | /// in order to use PhysReg between two entries in SA->UseSlots. |
| 2440 | /// |
| 2441 | /// GapWeight[i] represents the gap between UseSlots[i] and UseSlots[i+1]. |
| 2442 | /// |
| 2443 | void RAGreedy::calcGapWeights(unsigned PhysReg, |
| 2444 | SmallVectorImpl<float> &GapWeight) { |
| 2445 | assert(SA->getUseBlocks().size() == 1 && "Not a local interval"); |
| 2446 | const SplitAnalysis::BlockInfo &BI = SA->getUseBlocks().front(); |
| 2447 | ArrayRef<SlotIndex> Uses = SA->getUseSlots(); |
| 2448 | const unsigned NumGaps = Uses.size()-1; |
|   | // 干扰检查的起始和结束点。 |
| 2449 | // Start and end points for the interference check. |
| 2450 | SlotIndex StartIdx = |
| 2451 | BI.LiveIn ? BI.FirstInstr.getBaseIndex() : BI.FirstInstr; |
| 2452 | SlotIndex StopIdx = |
| 2453 | BI.LiveOut ? BI.LastInstr.getBoundaryIndex() : BI.LastInstr; |
| 2454 | GapWeight.assign(NumGaps, 0.0f); |
|   | // 添加每个重叠寄存器的干扰。 |
| 2455 | // Add interference from each overlapping register. |
| 2456 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 2457 | if (!Matrix->query(const_cast<LiveInterval&>(SA->getParent()), *Units) |
| 2458 | .checkInterference()) |
| 2459 | continue; |
|   | // 我们知道VirtReg是从FirstInstr到LastInstr的连续区间， |
|   | // 所以我们不需要InterferenceQuery。 |
|   | // 覆盖指令的干扰会在围绕指令的两个间隔中计算。 |
|   | // StartIdx之前和StopIdx之后的干扰是例外。 |
| 2460 | // We know that VirtReg is a continuous interval from FirstInstr to |
| 2461 | // LastInstr, so we don't need InterferenceQuery. |
| 2462 |  |
| 2464 |  |
| 2465 | // |
| 2466 | // Interference that overlaps an instruction is counted in both gaps |
| 2467 | // surrounding the instruction. The exception is interference before |
| 2468 | // StartIdx and after StopIdx. |
| 2470 | // |
| 2471 | LiveIntervalUnion::SegmentIter IntI = |
| 2472 | Matrix->getLiveUnions()[*Units] .find(StartIdx); |
| 2473 | for (unsigned Gap = 0; IntI.valid() && IntI.start() < StopIdx; ++IntI) { |
|   | // 跳过IntI之前的间隔。 |
| 2474 | // Skip the gaps before IntI. |
| 2475 | while (Uses[Gap+1].getBoundaryIndex() < IntI.start()) |
| 2476 | if (++Gap == NumGaps) |
| 2477 | break; |
| 2478 | if (Gap == NumGaps) |
| 2479 | break; |
|   | // 更新IntI覆盖的间隔。 |
| 2480 | // Update the gaps covered by IntI. |
| 2481 | const float weight = IntI.value()->weight; |
| 2482 | for (; Gap != NumGaps; ++Gap) { |
| 2483 | GapWeight[Gap] = std::max(GapWeight[Gap], weight); |
| 2484 | if (Uses[Gap+1].getBaseIndex() >= IntI.stop()) |
| 2485 | break; |
| 2487 | } |
| 2488 | if (Gap == NumGaps) |
| 2489 | break; |
| 2492 |  |
| 2498 |  |
| 2500 | } |
| 2501 | } |
|   | // 添加固定干扰。 |
| 2502 | // Add fixed interference. |
| 2515 |  |
| 2516 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 2517 | const LiveRange &LR = LIS->getRegUnit(*Units); |
| 2518 | LiveRange::const_iterator I = LR.find(StartIdx); |
| 2519 | LiveRange::const_iterator E = LR.end(); |
|   | // 同上循环。将任何重叠的间隔标记为HUGE_VALF。 |
| 2520 | // Same loop as above. Mark any overlapped gaps as HUGE_VALF. |
| 2521 | for (unsigned Gap = 0; I != E && I->start < StopIdx; ++I) { |
| 2522 | while (Uses[Gap+1].getBoundaryIndex() < I->start) |
| 2523 | if (++Gap == NumGaps) |
| 2524 | break; |
| 2525 | if (Gap == NumGaps) |
| 2526 | break; |
| 2527 | for (; Gap != NumGaps; ++Gap) { |
| 2528 | GapWeight[Gap] = huge_valf; |
| 2529 | if (Uses[Gap+1].getBaseIndex() >= I->end) |
| 2530 | break; |
| 2539 | } |
| 2540 | if (Gap == NumGaps) |
| 2541 | break; |
| 2542 | } |
| 2544 | } |
| 2545 |  |
| 2555 | } |
|   | /// 尝试将虚拟寄存器 VirtReg 分割成更小的区间，这些区间位于其唯一的基本块内。 |
| 2556 | /// tryLocalSplit - Try to split VirtReg into smaller intervals inside its only |
| 2557 | /// basic block. |
| 2558 | /// |
| 2559 | unsigned RAGreedy::tryLocalSplit(LiveInterval &VirtReg, AllocationOrder &Order, |
| 2560 | SmallVectorImpl<Register> &NewVRegs) { |
|   | // 待办：当前函数仅处理单个 UseBlock，应该可以推广。 |
| 2561 | // TODO: the function currently only handles a single UseBlock; it should be |
| 2562 | // possible to generalize. |
| 2563 | if (SA->getUseBlocks().size() != 1) |
| 2564 | return 0; |
|   | // 获取第一个 UseBlock 的信息。 |
| 2565 | const SplitAnalysis::BlockInfo &BI = SA->getUseBlocks().front(); |
|   | // 如果区间是 live-in 或 live-out 但仅覆盖单个块，这是可能的。 |
|   | // 我们不对此做特殊处理，假设区间从 FirstInstr 连续到 LastInstr。 |
|   | // 我们需要确保不对这样的区间做非法操作。 |
| 2566 | // Note that it is possible to have an interval that is live-in or live-out |
| 2567 | // while only covering a single block - A phi-def can use undef values from |
| 2568 | // predecessors, and the block could be a single-block loop. |
| 2569 | // We don't bother doing anything clever about such a case, we simply assume |
| 2570 | // that the interval is continuous from FirstInstr to LastInstr. We should |
| 2571 | // make sure that we don't do anything illegal to such an interval, though. |
| 2572 | ArrayRef<SlotIndex> Uses = SA->getUseSlots(); |
| 2573 | if (Uses.size() <= 2) |
| 2574 | return 0; |
| 2575 | const unsigned NumGaps = Uses.size()-1; |
| 2576 | LLVM_DEBUG({ |
|   | // 调试输出，打印出所有的 UseSlots。 |
| 2577 | dbgs() << "tryLocalSplit: "; |
| 2578 | for (unsigned i = 0, e = Uses.size(); i != e; ++i) |
| 2579 | dbgs() << ' ' << Uses[i]; |
| 2580 | dbgs() << '\n'; |
| 2581 | }); |
|   | // 如果 VirtReg 跨越任何寄存器掩码操作数，计算寄存器掩码间隙列表。 |
| 2582 | // If VirtReg is live across any register mask operands, compute a list of |
| 2583 | // gaps with register masks. |
| 2584 | SmallVector<unsigned, 8> RegMaskGaps; |
| 2585 | if (Matrix->checkRegMaskInterference(VirtReg)) { |
|   | // 获取整个块的寄存器掩码槽。 |
| 2586 | // Get regmask slots for the whole block. |
| 2587 | ArrayRef<SlotIndex> RMS = LIS->getRegMaskSlotsInBlock(BI.MBB->getNumber()); |
| 2588 | LLVM_DEBUG(dbgs() << RMS.size() << " regmasks in block:"); |
|   | // 限制在 VirtReg 的活动范围内。 |
| 2589 | // Constrain to VirtReg's live range. |
| 2590 | unsigned ri = |
| 2591 | llvm::lower_bound(RMS, Uses.front().getRegSlot()) - RMS.begin(); |
| 2592 | unsigned re = RMS.size(); |
| 2593 | for (unsigned i = 0; i != NumGaps && ri != re; ++i) { |
|   | // 查找 Uses[i] <= RMS <= Uses[i+1]。 |
| 2594 | // Look for Uses[i] <= RMS <= Uses[i+1]. |
| 2595 | assert(!SlotIndex::isEarlierInstr(RMS[ri], Uses[i])); |
| 2596 | if (SlotIndex::isEarlierInstr(Uses[i+1], RMS[ri])) |
| 2597 | continue; |
|   | // 跳过与最后一个使用相同的指令上的寄存器掩码。它不与活动范围重叠。 |
| 2598 | // Skip a regmask on the same instruction as the last use. It doesn't |
| 2599 | // overlap the live range. |
| 2600 | if (SlotIndex::isSameInstr(Uses[i+1], RMS[ri]) && i+1 == NumGaps) |
| 2601 | break; |
| 2602 | LLVM_DEBUG(dbgs() << ' ' << RMS[ri] << ':' << Uses[i] << '-' |
| 2603 | << Uses[i + 1]); |
| 2604 | RegMaskGaps.push_back(i); |
|   | // 将 ri 推进到下一个间隙。使用上的寄存器掩码在两个间隙中都计算。 |
| 2605 | // Advance ri to the next gap. A regmask on one of the uses counts in |
| 2606 | // both gaps. |
| 2607 | while (ri != re && SlotIndex::isEarlierInstr(RMS[ri], Uses[i+1])) |
| 2608 | ++ri; |
| 2609 | } |
| 2610 | LLVM_DEBUG(dbgs() << '\n'); |
| 2611 | } |
|   | // 由于我们允许局部分割结果再次分割，存在创建无限循环的风险。 |
|   | // 我们使用以下规则来防止这种情况： |
|   | // 1. 对于 getStage() < RS_Split2 的范围，允许任何分割。 |
|   | // 2. 对于 getStage() == RS_Split2 的范围，需要取得进展。所有新范围必须比分割前有更少的指令。 |
|   | // 3. 新范围如果指令数相同，则标记为 RS_Split2，更小的范围标记为 RS_New。 |
|   | // 这些规则允许一次 3 -> 2+3 分割，这也防止了过度分割和无限循环。 |
| 2612 | // Since we allow local split results to be split again, there is a risk of |
| 2613 | // creating infinite loops. It is tempting to require that the new live |
| 2614 | // ranges have less instructions than the original. That would guarantee |
| 2615 | // convergence, but it is too strict. A live range with 3 instructions can be |
| 2616 | // split 2+3 (including the COPY), and we want to allow that. |
| 2622 |  |
| 2623 | // |
| 2624 | // Instead we use these rules: |
| 2628 |  |
| 2631 |  |
| 2632 | // |
| 2633 | // 1. Allow any split for ranges with getStage() < RS_Split2. (Except for the |
| 2634 | //    noop split, of course). |
| 2635 | // 2. Require progress be made for ranges with getStage() == RS_Split2. All |
| 2636 | //    the new ranges must have fewer instructions than before the split. |
| 2637 | // 3. New ranges with the same number of instructions are marked RS_Split2, |
| 2638 | //    smaller ranges are marked RS_New. |
| 2639 |  |
| 2640 | // |
| 2641 | // These rules allow a 3 -> 2+3 split once, which we need. They also prevent |
| 2642 | // excessive splitting and infinite loops. |
| 2651 |  |
| 2652 | // |
| 2653 | bool ProgressRequired = getStage(VirtReg) >= RS_Split2; |
|   | // 最佳分割候选。 |
| 2654 | // Best split candidate. |
| 2655 | unsigned BestBefore = NumGaps; |
| 2656 | unsigned BestAfter = 0; |
| 2657 | float BestDiff = 0; |
|   | // 计算块频率。 |
| 2658 | const float blockFreq = |
| 2659 | SpillPlacer->getBlockFrequency(BI.MBB->getNumber()).getFrequency() * |
| 2660 | (1.0f / MBFI->getEntryFreq()); |
| 2661 | SmallVector<float, 8> GapWeight; |
|   | // 重置分配顺序。 |
| 2662 | Order.rewind(); |
| 2663 | while (unsigned PhysReg = Order.next()) { |
| 2664 | // Keep track of the largest spill weight that would need to be evicted in |
| 2665 | // order to make use of PhysReg between UseSlots[i] and UseSlots[i+1]. |
| 2666 | calcGapWeights(PhysReg, GapWeight); |
|   | // 移除任何有寄存器掩码破坏的间隙。 |
| 2667 | // Remove any gaps with regmask clobbers. |
| 2668 | if (Matrix->checkRegMaskInterference(VirtReg, PhysReg)) |
| 2669 | for (unsigned i = 0, e = RegMaskGaps.size(); i != e; ++i) |
| 2670 | GapWeight[RegMaskGaps[i]] = huge_valf; |
|   | // 尝试找到最佳的间隙序列来关闭。 |
|   | // 新的溢出权重必须大于任何间隙干扰。 |
|   | // 我们将在 Uses[SplitBefore] 之前和 Uses[SplitAfter] 之后进行分割。 |
| 2671 | // Try to find the best sequence of gaps to close. |
| 2672 | // The new spill weight must be larger than any gap interference. |
| 2673 | // We will split before Uses[SplitBefore] and after Uses[SplitAfter]. |
| 2674 | unsigned SplitBefore = 0, SplitAfter = 1; |
|   | // MaxGap 应该是 max(GapWeight[SplitBefore..SplitAfter-1])。 |
|   | // 它是需要被驱逐的溢出权重。 |
| 2675 | // MaxGap should always be max(GapWeight[SplitBefore..SplitAfter-1]). |
| 2676 | // It is the spill weight that needs to be evicted. |
| 2677 | float MaxGap = GapWeight[0]; |
| 2678 | while (true) { |
|   | // 分割前后是否活动？ |
| 2679 | // Live before/after split? |
| 2680 | const bool LiveBefore = SplitBefore != 0 || BI.LiveIn; |
| 2681 | const bool LiveAfter = SplitAfter != NumGaps || BI.LiveOut; |
| 2682 | LLVM_DEBUG(dbgs() << printReg(PhysReg, TRI) << ' ' << Uses[SplitBefore] |
| 2683 | << '-' << Uses[SplitAfter] << " i=" << MaxGap); |
|   | // 在区间变得如此之大以至于我们不会取得进展之前停止。 |
| 2684 | // Stop before the interval gets so big we wouldn't be making progress. |
| 2685 | if (!LiveBefore && !LiveAfter) { |
| 2686 | LLVM_DEBUG(dbgs() << " all\n"); |
| 2687 | break; |
| 2695 | } |
|   | // 应该扩展还是缩小区间？ |
| 2696 | // Should the interval be extended or shrunk? |
| 2697 | bool Shrink = true; |
|   | // 新范围将有多少个间隙？ |
| 2698 | // How many gaps would the new range have? |
| 2699 | unsigned NewGaps = LiveBefore + SplitAfter - SplitBefore + LiveAfter; |
|   | // 合法的，不会导致循环？ |
| 2700 | // Legally, without causing looping? |
| 2701 | bool Legal = !ProgressRequired || NewGaps < NumGaps; |
| 2702 | if (Legal && MaxGap < huge_valf) { |
|   | // 估计新的溢出权重。每个指令读取或写入寄存器。 |
|   | // 保守地假设没有读-修改-写指令。 |
| 2707 |  |
| 2708 | // |
|   | // 尝试猜测新区间的大小。 |
| 2709 | // Estimate the new spill weight. Each instruction reads or writes the |
| 2710 | // register. Conservatively assume there are no read-modify-write |
| 2711 | // instructions. |
| 2712 | // |
| 2713 | // Try to guess the size of the new interval. |
| 2714 | const float EstWeight = normalizeSpillWeight( |
| 2715 | blockFreq * (NewGaps + 1), |
| 2716 | Uses[SplitBefore].distance(Uses[SplitAfter]) + |
| 2717 | (LiveBefore + LiveAfter) * SlotIndex::InstrDist, |
| 2728 | 1); |
|   | // 这次分割可以分配吗？ |
|   | // 从不分配所有间隙，我们不会取得进展。 |
| 2729 | // Would this split be possible to allocate? |
| 2730 | // Never allocate all gaps, we wouldn't be making progress. |
| 2731 | LLVM_DEBUG(dbgs() << " w=" << EstWeight); |
| 2732 | if (EstWeight * Hysteresis >= MaxGap) { |
| 2733 | Shrink = false; |
| 2734 | float Diff = EstWeight - MaxGap; |
| 2735 | if (Diff > BestDiff) { |
| 2736 | LLVM_DEBUG(dbgs() << " (best)"); |
| 2737 | BestDiff = Hysteresis * Diff; |
| 2738 | BestBefore = SplitBefore; |
| 2739 | BestAfter = SplitAfter; |
| 2741 | } |
| 2744 |  |
| 2747 | } |
| 2749 | } |
|   | // 尝试缩小。 |
| 2750 | // Try to shrink. |
| 2751 | if (Shrink) { |
| 2752 | if (++SplitBefore < SplitAfter) { |
| 2753 | LLVM_DEBUG(dbgs() << " shrink\n"); |
|   | // 在必要时重新计算最大值。 |
| 2754 | // Recompute the max when necessary. |
| 2755 | if (GapWeight[SplitBefore - 1] >= MaxGap) { |
| 2756 | MaxGap = GapWeight[SplitBefore]; |
| 2757 | for (unsigned i = SplitBefore + 1; i != SplitAfter; ++i) |
| 2758 | MaxGap = std::max(MaxGap, GapWeight[i]); |
| 2759 | } |
| 2760 | continue; |
| 2775 | } |
| 2776 | MaxGap = 0; |
| 2777 | } |
|   | // 尝试扩展区间。 |
| 2778 | // Try to extend the interval. |
| 2779 | if (SplitAfter >= NumGaps) { |
| 2780 | LLVM_DEBUG(dbgs() << " end\n"); |
| 2781 | break; |
| 2782 | } |
| 2783 | LLVM_DEBUG(dbgs() << " extend\n"); |
| 2784 | MaxGap = std::max(MaxGap, GapWeight[SplitAfter++]); |
| 2785 | } |
| 2786 | } |
|   | // 没有找到任何候选者？ |
| 2787 | // Didn't find any candidates? |
| 2788 | if (BestBefore == NumGaps) |
| 2796 |  |
| 2800 | return 0; |
| 2801 | LLVM_DEBUG(dbgs() << "Best local split range: " << Uses[BestBefore] << '-' |
| 2802 | << Uses[BestAfter] << ", " << BestDiff << ", " |
| 2803 | << (BestAfter - BestBefore + 1) << " instrs\n"); |
|   | // 开始编辑 LiveRange。 |
| 2804 | LiveRangeEdit LREdit(&VirtReg, NewVRegs, *MF, *LIS, VRM, this, &DeadRemats); |
| 2805 | SE->reset(LREdit); |
|   | // 开始编辑区间，准备进行分割。 |
| 2806 | SE->openIntv(); |
| 2807 | SlotIndex SegStart = SE->enterIntvBefore(Uses[BestBefore]); |
| 2808 | SlotIndex SegStop  = SE->leaveIntvAfter(Uses[BestAfter]); |
|   | // 标记新区间的使用。 |
| 2809 | SE->useIntv(SegStart, SegStop); |
|   | // 创建一个映射，用于记录分割后的新区间。 |
| 2810 | SmallVector<unsigned, 8> IntvMap; |
| 2811 | SE->finish(&IntvMap); |
|   | // 将分割操作记录到调试变量中。 |
| 2812 | DebugVars->splitRegister(VirtReg.reg, LREdit.regs(), *LIS); |
|   | // 如果新区间的指令数量与之前相同，将其标记为 RS_Split2， |
|   | // 这样下一次分割将被迫取得进展。 |
|   | // 否则，将新区间保留为 RS_New，以便它们可以参与竞争。 |
| 2813 | // If the new range has the same number of instructions as before, mark it as |
| 2814 | // RS_Split2 so the next split will be forced to make progress. Otherwise, |
| 2815 | // leave the new intervals as RS_New so they can compete. |
| 2816 | bool LiveBefore = BestBefore != 0 || BI.LiveIn; |
| 2817 | bool LiveAfter = BestAfter != NumGaps || BI.LiveOut; |
| 2818 | unsigned NewGaps = LiveBefore + BestAfter - BestBefore + LiveAfter; |
| 2819 | if (NewGaps >= NumGaps) { |
| 2820 | LLVM_DEBUG(dbgs() << "Tagging non-progress ranges: "); |
| 2821 | assert(!ProgressRequired && "Didn't make progress when it was required."); |
| 2822 | for (unsigned i = 0, e = IntvMap.size(); i != e; ++i) |
| 2823 | if (IntvMap[i] == 1) { |
| 2824 | setStage(LIS->getInterval(LREdit.get(i)), RS_Split2); |
| 2825 | LLVM_DEBUG(dbgs() << printReg(LREdit.get(i))); |
| 2833 |  |
| 2839 | } |
| 2840 | LLVM_DEBUG(dbgs() << '\n'); |
| 2849 | } |
|   | // 增加局部分割的计数。 |
| 2850 | ++NumLocalSplits; |
|   | // 返回0表示分割成功，如果有其他返回值则表示特定的错误或状态。 |
| 2851 | return 0; |
| 2864 | } |
| 2865 | //===----------------------------------------------------------------------===// |
| 2866 | //                          Live Range Splitting |
| 2867 | //===----------------------------------------------------------------------===// |
|   | /// trySplit - 尝试分割 VirtReg 或其干扰之一，使其可以被分配。 |
|   | /// @return 当 VirtReg 可以被分配和/或有新的 NewVRegs 时返回物理寄存器。 |
| 2868 | /// trySplit - Try to split VirtReg or one of its interferences, making it |
| 2869 | /// assignable. |
| 2870 | /// @return Physreg when VirtReg may be assigned and/or new NewVRegs. |
| 2871 | unsigned RAGreedy::trySplit(LiveInterval &VirtReg, AllocationOrder &Order, |
| 2872 | SmallVectorImpl<Register> &NewVRegs, |
| 2873 | const SmallVirtRegSet &FixedRegisters) { |
|   | // 区间必须是 Split2 或更少。 |
| 2874 | // Ranges must be Split2 or less. |
| 2875 | if (getStage(VirtReg) >= RS_Spill) |
| 2876 | return 0; |
|   | // 局部区间单独处理。 |
| 2877 | // Local intervals are handled separately. |
| 2878 | if (LIS->intervalIsInOneMBB(VirtReg)) { |
|   | // 命名区域计时器，用于性能分析。 |
| 2879 | NamedRegionTimer T("local_split", "Local Splitting", TimerGroupName, |
| 2880 | TimerGroupDescription, TimePassesIsEnabled); |
| 2881 | SA->analyze(&VirtReg); |
|   | // 尝试局部分割。 |
| 2882 | Register PhysReg = tryLocalSplit(VirtReg, Order, NewVRegs); |
| 2883 | if (PhysReg || !NewVRegs.empty()) |
| 2884 | return PhysReg; |
|   | // 如果局部分割失败，尝试指令分割。 |
| 2885 | return tryInstructionSplit(VirtReg, Order, NewVRegs); |
| 2886 | } |
|   | // 命名区域计时器，用于性能分析。 |
| 2887 | NamedRegionTimer T("global_split", "Global Splitting", TimerGroupName, |
| 2888 | TimerGroupDescription, TimePassesIsEnabled); |
| 2889 | SA->analyze(&VirtReg); |
|   | // FIXME: SplitAnalysis 可能修复来自合并器的破坏的 live 范围。 |
|   | // 这可能导致范围变得可分配，这意味着 tryRegionSplit 不会取得进展。 |
|   | // 当合并器被修复后，这个检查应该被断言替换。 |
| 2890 | // FIXME: SplitAnalysis may repair broken live ranges coming from the |
| 2891 | // coalescer. That may cause the range to become allocatable which means that |
| 2892 | // tryRegionSplit won't be making progress. This check should be replaced with |
| 2893 | // an assertion when the coalescer is fixed. |
| 2894 | if (SA->didRepairRange()) { |
|   | // VirtReg 发生了变化，所以所有缓存的查询都无效了。 |
| 2895 | // VirtReg has changed, so all cached queries are invalid. |
| 2896 | Matrix->invalidateVirtRegs(); |
|   | // 如果可以分配，尝试分配 VirtReg。 |
| 2897 | if (Register PhysReg = tryAssign(VirtReg, Order, NewVRegs, FixedRegisters)) |
| 2898 | return PhysReg; |
| 2899 | } |
|   | // 首先尝试在多个块上进行分割。RS_Split2 区间已经通过区域分割取得了 |
|   | // 可疑的进展，因此它们直接进行单块分割。 |
| 2900 | // First try to split around a region spanning multiple blocks. RS_Split2 |
| 2901 | // ranges already made dubious progress with region splitting, so they go |
| 2902 | // straight to single block splitting. |
| 2903 | if (getStage(VirtReg) < RS_Split2) { |
| 2904 | unsigned PhysReg = tryRegionSplit(VirtReg, Order, NewVRegs); |
| 2905 | if (PhysReg || !NewVRegs.empty()) |
| 2906 | return PhysReg; |
| 2907 | } |
|   | // 然后隔离块。 |
| 2908 | // Then isolate blocks. |
| 2909 | return tryBlockSplit(VirtReg, Order, NewVRegs); |
| 2910 | } |
| 2911 | //===----------------------------------------------------------------------===// |
| 2912 | //                          Last Chance Recoloring |
| 2913 | //===----------------------------------------------------------------------===// |
|   | /// hasTiedDef - 检查寄存器 reg 是否有任何被绑定的定义操作数。 |
|   | /// @param MRI 指向 MachineRegisterInfo 实例的指针，包含有关机器操作数和寄存器的信息。 |
|   | /// @param reg 要检查的寄存器编号。 |
|   | /// @return 如果 reg 有被绑定的定义操作数，则返回 true；否则返回 false。 |
| 2914 | /// Return true if \p reg has any tied def operand. |
| 2915 | static bool hasTiedDef(MachineRegisterInfo *MRI, unsigned reg) { |
|   | // 遍历所有定义了 reg 的操作数。 |
| 2916 | for (const MachineOperand &MO : MRI->def_operands(reg)) |
|   | // 如果发现任何一个操作数是被绑定的，立即返回 true。 |
| 2917 | if (MO.isTied()) |
| 2918 | return true; |
|   | // 如果遍历完所有定义操作数后没有发现被绑定的操作数，返回 false。 |
| 2919 | return false; |
| 2920 | } |
|   | /// mayRecolorAllInterferences - 检查是否可以重新着色与 VirtReg 在 |
|   | /// PhysReg 上发生干扰的虚拟寄存器，以释放 PhysReg。 |
|   | /// @param PhysReg 要检查的物理寄存器。 |
|   | /// @param VirtReg 要分配的虚拟寄存器。 |
|   | /// @param RecoloringCandidates 需要重新着色的所有 live 区间的集合。 |
|   | /// @param FixedRegisters 不能重新着色的虚拟寄存器集合。 |
|   | /// @return 如果可以重新着色所有干扰，则返回 true；否则返回 false。 |
| 2921 | /// mayRecolorAllInterferences - Check if the virtual registers that |
| 2922 | /// interfere with \p VirtReg on \p PhysReg (or one of its aliases) may be |
| 2923 | /// recolored to free \p PhysReg. |
| 2924 | /// When true is returned, \p RecoloringCandidates has been augmented with all |
| 2925 | /// the live intervals that need to be recolored in order to free \p PhysReg |
| 2926 | /// for \p VirtReg. |
| 2927 | /// \p FixedRegisters contains all the virtual registers that cannot be |
| 2928 | /// recolored. |
| 2929 | bool |
| 2930 | RAGreedy::mayRecolorAllInterferences(unsigned PhysReg, LiveInterval &VirtReg, |
| 2931 | SmallLISet &RecoloringCandidates, |
| 2932 | const SmallVirtRegSet &FixedRegisters) { |
| 2933 | const TargetRegisterClass *CurRC = MRI->getRegClass(VirtReg.reg); |
|   | // 遍历 PhysReg 及其别名对应的所有寄存器单元。 |
| 2934 | for (MCRegUnitIterator Units(PhysReg, TRI); Units.isValid(); ++Units) { |
| 2935 | LiveIntervalUnion::Query &Q = Matrix->query(VirtReg, *Units); |
| 2936 | // If there is LastChanceRecoloringMaxInterference or more interferences, |
| 2937 | // chances are one would not be recolorable. |
| 2938 | if (Q.collectInterferingVRegs(LastChanceRecoloringMaxInterference) >= |
| 2939 | LastChanceRecoloringMaxInterference && !ExhaustiveSearch) { |
| 2940 | LLVM_DEBUG(dbgs() << "Early abort: too many interferences.\n"); |
| 2941 | CutOffInfo |= CO_Interf; |
| 2942 | return false; |
| 2950 | } |
|   | // 遍历所有干扰的虚拟寄存器。 |
| 2951 | for (unsigned i = Q.interferingVRegs().size(); i; --i) { |
| 2952 | LiveInterval *Intf = Q.interferingVRegs()[i - 1]; |
|   | // 如果干扰寄存器已完成并且位于与 VirtReg 相同的寄存器类中， |
|   | // 则它不能被重新着色，因为它与 VirtReg 处于相同的状态。 |
|   | // 但是，如果 VirtReg 有绑定的定义而干扰寄存器没有，则仍需检查是否可以重新着色。 |
| 2953 | // If Intf is done and sit on the same register class as VirtReg, |
| 2954 | // it would not be recolorable as it is in the same state as VirtReg. |
| 2955 | // However, if VirtReg has tied defs and Intf doesn't, then |
| 2956 | // there is still a point in examining if it can be recolorable. |
| 2957 | if (((getStage(*Intf) == RS_Done && |
| 2958 | MRI->getRegClass(Intf->reg) == CurRC) && |
| 2959 | !(hasTiedDef(MRI, VirtReg.reg) && !hasTiedDef(MRI, Intf->reg))) || |
| 2960 | FixedRegisters.count(Intf->reg)) { |
| 2961 | LLVM_DEBUG( |
| 2962 | dbgs() << "Early abort: the interference is not recolorable.\n"); |
| 2963 | return false; |
| 2964 | } |
|   | // 将需要重新着色的干扰寄存器添加到候选集合中。 |
| 2965 | RecoloringCandidates.insert(Intf); |
| 2966 | } |
| 2967 | } |
|   | // 如果所有干扰都可以被重新着色，则返回 true。 |
| 2968 | return true; |
| 2969 | } |
|   | /// tryLastChanceRecoloring - 尝试通过重新着色其干扰来为 VirtReg 分配一个颜色。 |
|   | /// @param VirtReg 要分配颜色的虚拟寄存器。 |
|   | /// @param Order 定义了 VirtReg 的优先分配顺序。 |
|   | /// @param NewVRegs 将包含在此过程中创建的任何新虚拟寄存器（分裂、溢出）。 |
|   | /// @param FixedRegisters 包含所有不能重新着色的虚拟寄存器。 |
|   | /// @param Depth 给出最后机会着色的当前深度。 |
|   | /// @return 一个可以用于 VirtReg 的物理寄存器，或者如果没有可用的则返回 ~0u。 |
| 2970 | /// tryLastChanceRecoloring - Try to assign a color to \p VirtReg by recoloring |
| 2971 | /// its interferences. |
| 2972 | /// Last chance recoloring chooses a color for \p VirtReg and recolors every |
| 2973 | /// virtual register that was using it. The recoloring process may recursively |
| 2974 | /// use the last chance recoloring. Therefore, when a virtual register has been |
| 2975 | /// assigned a color by this mechanism, it is marked as Fixed, i.e., it cannot |
| 2976 | /// be last-chance-recolored again during this recoloring "session". |
| 2977 | /// E.g., |
| 2979 | /// Let |
| 2980 | /// vA can use {R1, R2    } |
| 2981 | /// vB can use {    R2, R3} |
| 2982 | /// vC can use {R1        } |
| 2983 | /// Where vA, vB, and vC cannot be split anymore (they are reloads for |
| 2984 | /// instance) and they all interfere. |
| 2985 | /// |
| 2986 | /// vA is assigned R1 |
| 2987 | /// vB is assigned R2 |
| 2988 | /// vC tries to evict vA but vA is already done. |
| 2989 | /// Regular register allocation fails. |
| 2990 | /// |
| 2991 | /// Last chance recoloring kicks in: |
| 2992 | /// vC does as if vA was evicted => vC uses R1. |
| 2993 | /// vC is marked as fixed. |
| 2994 | /// vA needs to find a color. |
| 2995 | /// None are available. |
| 2996 | /// vA cannot evict vC: vC is a fixed virtual register now. |
| 2997 | /// vA does as if vB was evicted => vA uses R2. |
| 2998 | /// vB needs to find a color. |
| 2999 | /// R3 is available. |
| 3000 | /// Recoloring => vC = R1, vA = R2, vB = R3 |
| 3001 | /// |
| 3002 | /// \p Order defines the preferred allocation order for \p VirtReg. |
| 3003 | /// \p NewRegs will contain any new virtual register that have been created |
| 3004 | /// (split, spill) during the process and that must be assigned. |
| 3005 | /// \p FixedRegisters contains all the virtual registers that cannot be |
| 3006 | /// recolored. |
| 3007 | /// \p Depth gives the current depth of the last chance recoloring. |
| 3008 | /// \return a physical register that can be used for VirtReg or ~0u if none |
| 3009 | /// exists. |
| 3010 | unsigned RAGreedy::tryLastChanceRecoloring(LiveInterval &VirtReg, |
| 3011 | AllocationOrder &Order, |
| 3015 |  |
| 3017 | SmallVectorImpl<Register> &NewVRegs, |
| 3018 | SmallVirtRegSet &FixedRegisters, |
| 3019 | unsigned Depth) { |
| 3020 | LLVM_DEBUG(dbgs() << "Try last chance recoloring for " << VirtReg << '\n'); |
|   | // 必须是完成状态的区间。 |
| 3021 | // Ranges must be Done. |
| 3022 | assert((getStage(VirtReg) >= RS_Done || !VirtReg.isSpillable()) && |
| 3023 | "Last chance recoloring should really be last chance"); |
|   | // 设置最大深度为 LastChanceRecoloringMaxDepth。 |
| 3024 | // Set the max depth to LastChanceRecoloringMaxDepth. |
| 3025 | // We may want to reconsider that if we end up with a too large search space |
| 3026 | // for target with hundreds of registers. |
| 3027 | // Indeed, in that case we may want to cut the search space earlier. |
| 3028 | if (Depth >= LastChanceRecoloringMaxDepth && !ExhaustiveSearch) { |
| 3029 | LLVM_DEBUG(dbgs() << "Abort because max depth has been reached.\n"); |
| 3030 | CutOffInfo |= CO_Depth; |
| 3031 | return ~0u; |
| 3037 | } |
|   | // 将需要重新着色的所有 live 区间的集合。 |
| 3038 | // Set of Live intervals that will need to be recolored. |
| 3039 | SmallLISet RecoloringCandidates; |
|   | // 记录原始的虚拟寄存器到物理寄存器的映射，以防重新着色失败。 |
| 3040 | // Record the original mapping virtual register to physical register in case |
| 3041 | // the recoloring fails. |
| 3042 | DenseMap<Register, Register> VirtRegToPhysReg; |
|   | // 标记 VirtReg 为固定，即在此重新着色“会话”中不会再被重新着色。 |
| 3043 | // Mark VirtReg as fixed, i.e., it will not be recolored pass this point in |
| 3044 | // this recoloring "session". |
| 3045 | assert(!FixedRegisters.count(VirtReg.reg)); |
| 3046 | FixedRegisters.insert(VirtReg.reg); |
| 3047 | SmallVector<Register, 4> CurrentNewVRegs; |
| 3048 | Order.rewind(); |
| 3049 | while (Register PhysReg = Order.next()) { |
| 3050 | LLVM_DEBUG(dbgs() << "Try to assign: " << VirtReg << " to " |
| 3051 | << printReg(PhysReg, TRI) << '\n'); |
| 3052 | RecoloringCandidates.clear(); |
| 3053 | VirtRegToPhysReg.clear(); |
| 3054 | CurrentNewVRegs.clear(); |
|   | // 只有虚拟寄存器的干扰才能重新着色。 |
| 3055 | // It is only possible to recolor virtual register interference. |
| 3056 | if (Matrix->checkInterference(VirtReg, PhysReg) > |
| 3057 | LiveRegMatrix::IK_VirtReg) { |
| 3058 | LLVM_DEBUG( |
| 3059 | dbgs() << "Some interferences are not with virtual registers.\n"); |
| 3060 | continue; |
| 3064 | } |
|   | // 如果明显我们不能重新着色所有干扰，早点放弃这个物理寄存器。 |
| 3065 | // Early give up on this PhysReg if it is obvious we cannot recolor all |
| 3066 | // the interferences. |
| 3067 | if (!mayRecolorAllInterferences(PhysReg, VirtReg, RecoloringCandidates, |
| 3068 | FixedRegisters)) { |
| 3069 | LLVM_DEBUG(dbgs() << "Some interferences cannot be recolored.\n"); |
| 3070 | continue; |
| 3076 | } |
|   | // RecoloringCandidates 包含所有在 PhysReg 上与 VirtReg 干扰的虚拟寄存器。 |
|   | // 将它们排队等待重新着色并执行实际的重新着色。 |
| 3077 | // RecoloringCandidates contains all the virtual registers that interfer |
| 3078 | // with VirtReg on PhysReg (or one of its aliases). |
| 3079 | // Enqueue them for recoloring and perform the actual recoloring. |
| 3080 | PQueue RecoloringQueue; |
| 3081 | for (SmallLISet::iterator It = RecoloringCandidates.begin(), |
| 3082 | EndIt = RecoloringCandidates.end(); |
| 3083 | It != EndIt; ++It) { |
| 3084 | Register ItVirtReg = (*It)->reg; |
| 3085 | enqueue(RecoloringQueue, *It); |
| 3086 | assert(VRM->hasPhys(ItVirtReg) && |
| 3087 | "Interferences are supposed to be with allocated variables"); |
|   | // 记录当前分配。 |
| 3088 | // Record the current allocation. |
| 3089 | VirtRegToPhysReg[ItVirtReg] = VRM->getPhys(ItVirtReg); |
|   | // 取消相关结构的设置。 |
| 3090 | // unset the related struct. |
| 3091 | Matrix->unassign(**It); |
| 3094 |  |
| 3104 | } |
|   | // 假设 VirtReg 被分配到 PhysReg，以便底层的重新着色有正确的干扰和可用颜色信息。 |
| 3105 | // Do as if VirtReg was assigned to PhysReg so that the underlying |
| 3106 | // recoloring has the right information about the interferes and |
| 3107 | // available colors. |
| 3108 | Matrix->assign(VirtReg, PhysReg); |
|   | // 保存当前重新着色状态。 |
|   | // 如果我们不能重新着色所有干扰， |
|   | // 我们将不得不从这一点开始下一次物理寄存器的重新着色。 |
| 3109 | // Save the current recoloring state. |
| 3110 | // If we cannot recolor all the interferences, we will have to start again |
| 3111 | // at this point for the next physical register. |
| 3112 | SmallVirtRegSet SaveFixedRegisters(FixedRegisters); |
| 3113 | if (tryRecoloringCandidates(RecoloringQueue, CurrentNewVRegs, |
| 3114 | FixedRegisters, Depth)) { |
|   | // 将排队的 vregs 推入主队列。 |
| 3115 | // Push the queued vregs into the main queue. |
| 3116 | for (Register NewVReg : CurrentNewVRegs) |
| 3117 | NewVRegs.push_back(NewVReg); |
|   | // 不要干扰全局分配过程。 |
|   | // 例如，VirtReg 必须取消分配。 |
| 3118 | // Do not mess up with the global assignment process. |
| 3119 | // I.e., VirtReg must be unassigned. |
| 3120 | Matrix->unassign(VirtReg); |
| 3121 | return PhysReg; |
| 3123 | } |
| 3124 | LLVM_DEBUG(dbgs() << "Fail to assign: " << VirtReg << " to " |
| 3125 | << printReg(PhysReg, TRI) << '\n'); |
|   | // 重新着色尝试失败，撤销更改。 |
| 3126 | // The recoloring attempt failed, undo the changes. |
| 3127 | FixedRegisters = SaveFixedRegisters; |
| 3128 | Matrix->unassign(VirtReg); |
|   | // 对于新创建的 vreg，如果它也在 RecoloringCandidates 中， |
|   | // 不要将其添加到 NewVRegs 中，因为它的物理寄存器将被恢复。 |
|   | // 其他在 CurrentNewVRegs 中的 vregs 是由调用 selectOrSplit 创建的， |
|   | // 应该被添加到 NewVRegs 中。 |
| 3129 | // For a newly created vreg which is also in RecoloringCandidates, |
| 3130 | // don't add it to NewVRegs because its physical register will be restored |
| 3131 | // below. Other vregs in CurrentNewVRegs are created by calling |
| 3132 | // selectOrSplit and should be added into NewVRegs. |
| 3133 | for (SmallVectorImpl<Register>::iterator Next = CurrentNewVRegs.begin(), |
| 3134 | End = CurrentNewVRegs.end(); |
| 3135 | Next != End; ++Next) { |
| 3136 | if (RecoloringCandidates.count(&LIS->getInterval(*Next))) |
| 3137 | continue; |
| 3138 | NewVRegs.push_back(*Next); |
| 3140 |  |
| 3147 | } |
| 3148 | for (SmallLISet::iterator It = RecoloringCandidates.begin(), |
| 3149 | EndIt = RecoloringCandidates.end(); |
| 3150 | It != EndIt; ++It) { |
| 3151 | Register ItVirtReg = (*It)->reg; |
| 3152 | if (VRM->hasPhys(ItVirtReg)) |
| 3153 | Matrix->unassign(**It); |
| 3154 | Register ItPhysReg = VirtRegToPhysReg[ItVirtReg]; |
| 3155 | Matrix->assign(**It, ItPhysReg); |
| 3161 | } |
| 3162 |  |
| 3174 | } |
|   | // 最后机会着色也没有奏效，放弃。 |
| 3175 | // Last chance recoloring did not worked either, give up. |
| 3176 | return ~0u; |
| 3177 | } |
|   | /// tryRecoloringCandidates - 尝试为 RecoloringQueue 中的每个寄存器分配新的颜色。 |
|   | /// @param RecoloringQueue 待重新着色的寄存器队列。 |
|   | /// @param NewVRegs 存储重新着色过程中创建的新虚拟寄存器。 |
|   | /// @param FixedRegisters 包含所有已经被重新着色的寄存器的集合（输入/输出参数）。 |
|   | /// @param Depth 最后机会着色的当前深度。 |
|   | /// @return 如果 RecoloringQueue 中的所有虚拟寄存器都成功重新着色，则返回 true； |
|   | /// 否则返回 false。 |
| 3178 | /// tryRecoloringCandidates - Try to assign a new color to every register |
| 3179 | /// in \RecoloringQueue. |
| 3180 | /// \p NewRegs will contain any new virtual register created during the |
| 3181 | /// recoloring process. |
| 3182 | /// \p FixedRegisters[in/out] contains all the registers that have been |
| 3183 | /// recolored. |
| 3184 | /// \return true if all virtual registers in RecoloringQueue were successfully |
| 3185 | /// recolored, false otherwise. |
| 3186 | bool RAGreedy::tryRecoloringCandidates(PQueue &RecoloringQueue, |
| 3187 | SmallVectorImpl<Register> &NewVRegs, |
| 3188 | SmallVirtRegSet &FixedRegisters, |
| 3189 | unsigned Depth) { |
|   | // 只要队列不为空，就继续处理。 |
| 3190 | while (!RecoloringQueue.empty()) { |
| 3191 | LiveInterval *LI = dequeue(RecoloringQueue); |
| 3192 | LLVM_DEBUG(dbgs() << "Try to recolor: " << *LI << '\n'); |
|   | // 尝试为 LI 分配一个新的物理寄存器或进行分裂。 |
| 3193 | Register PhysReg = selectOrSplitImpl(*LI, NewVRegs, FixedRegisters, |
| 3194 | Depth + 1); |
|   | // 如果分裂发生，live-range 可能实际上是空的。 |
|   | // 在这种情况下，即使我们没有为它找到替代颜色，也可以继续重新着色。 |
|   | // 毕竟，最终不会有任何需要为 LI 着色的内容。 |
| 3195 | // When splitting happens, the live-range may actually be empty. |
| 3196 | // In that case, this is okay to continue the recoloring even |
| 3197 | // if we did not find an alternative color for it. Indeed, |
| 3198 | // there will not be anything to color for LI in the end. |
| 3199 | if (PhysReg == ~0u || (!PhysReg && !LI->empty())) |
| 3200 | return false; |
|   | // 如果没有找到物理寄存器，检查 live-range 是否为空。 |
| 3201 | if (!PhysReg) { |
| 3202 | assert(LI->empty() && "Only empty live-range do not require a register"); |
| 3203 | LLVM_DEBUG(dbgs() << "Recoloring of " << *LI |
| 3204 | << " succeeded. Empty LI.\n"); |
| 3205 | continue; |
| 3206 | } |
| 3207 | LLVM_DEBUG(dbgs() << "Recoloring of " << *LI |
| 3208 | << " succeeded with: " << printReg(PhysReg, TRI) << '\n'); |
|   | // 为 LI 分配物理寄存器，并标记为已固定。 |
| 3209 | Matrix->assign(*LI, PhysReg); |
| 3210 | FixedRegisters.insert(LI->reg); |
| 3211 | } |
|   | // 如果所有寄存器都成功重新着色，则返回 true。 |
| 3212 | return true; |
| 3213 | } |
| 3214 | //===----------------------------------------------------------------------===// |
| 3215 | //                            Main Entry Point |
| 3216 | //===----------------------------------------------------------------------===// |
|   | /// selectOrSplit - 尝试为虚拟寄存器 VirtReg 分配一个物理寄存器或者分裂它。 |
|   | /// @param VirtReg 要分配或分裂的虚拟寄存器。 |
|   | /// @param NewVRegs 存储在此过程中创建的新虚拟寄存器。 |
|   | /// @return 分配的物理寄存器，或者 ~0U 如果分配失败且遇到了截止条件。 |
| 3217 | Register RAGreedy::selectOrSplit(LiveInterval &VirtReg, |
| 3218 | SmallVectorImpl<Register> &NewVRegs) { |
| 3219 | CutOffInfo = CO_None; |
| 3220 | LLVMContext &Ctx = MF->getFunction().getContext(); |
| 3221 | SmallVirtRegSet FixedRegisters; |
| 3222 | Register Reg = selectOrSplitImpl(VirtReg, NewVRegs, FixedRegisters); |
| 3223 | if (Reg == ~0U && (CutOffInfo != CO_None)) { |
| 3224 | uint8_t CutOffEncountered = CutOffInfo & (CO_Depth | CO_Interf); |
| 3225 | if (CutOffEncountered == CO_Depth) |
| 3226 | Ctx.emitError("register allocation failed: maximum depth for recoloring " |
| 3227 | "reached. Use -fexhaustive-register-search to skip " |
| 3228 | "cutoffs"); |
| 3229 | else if (CutOffEncountered == CO_Interf) |
| 3230 | Ctx.emitError("register allocation failed: maximum interference for " |
| 3231 | "recoloring reached. Use -fexhaustive-register-search " |
| 3232 | "to skip cutoffs"); |
| 3233 | else if (CutOffEncountered == (CO_Depth | CO_Interf)) |
| 3234 | Ctx.emitError("register allocation failed: maximum interference and " |
| 3235 | "depth for recoloring reached. Use " |
| 3236 | "-fexhaustive-register-search to skip cutoffs"); |
| 3237 | } |
| 3238 | return Reg; |
| 3239 | } |
|   | /// tryAssignCSRFirstTime - 首次使用 callee-saved register (CSR) 有成本， |
|   | /// 因为它会导致在 prologue/epilogue 中添加 push/pop。 |
|   | /// 如果 live-range 在冷路径上，分裂或溢出它可能比首次使用 CSR 的成本更低。 |
|   | /// @param VirtReg 要分配的虚拟寄存器。 |
|   | /// @param Order 分配顺序。 |
|   | /// @param PhysReg 要考虑的物理寄存器。 |
|   | /// @param CostPerUseLimit 每次使用的成本限制。 |
|   | /// @param NewVRegs 存储新创建的虚拟寄存器。 |
|   | /// @return 如果决定使用 CSR，则返回物理寄存器；否则返回 0。 |
| 3240 | /// Using a CSR for the first time has a cost because it causes push|pop |
| 3241 | /// to be added to prologue|epilogue. Splitting a cold section of the live |
| 3242 | /// range can have lower cost than using the CSR for the first time; |
| 3243 | /// Spilling a live range in the cold path can have lower cost than using |
| 3244 | /// the CSR for the first time. Returns the physical register if we decide |
| 3245 | /// to use the CSR; otherwise return 0. |
| 3246 | unsigned RAGreedy::tryAssignCSRFirstTime(LiveInterval &VirtReg, |
| 3247 | AllocationOrder &Order, |
| 3248 | Register PhysReg, |
| 3249 | unsigned &CostPerUseLimit, |
| 3250 | SmallVectorImpl<Register> &NewVRegs) { |
|   | // 如果虚拟寄存器已决定溢出且可溢出，且溢出成本小于 CSR 成本，则选择溢出。 |
| 3251 | if (getStage(VirtReg) == RS_Spill && VirtReg.isSpillable()) { |
| 3252 | // We choose spill over using the CSR for the first time if the spill cost |
| 3253 | // is lower than CSRCost. |
| 3254 | SA->analyze(&VirtReg); |
| 3255 | if (calcSpillCost() >= CSRCost) |
| 3256 | return PhysReg; |
| 3257 | // We are going to spill, set CostPerUseLimit to 1 to make sure that |
| 3258 | // we will not use a callee-saved register in tryEvict. |
| 3259 | CostPerUseLimit = 1; |
| 3260 | return 0; |
| 3261 | } |
|   | // 如果虚拟寄存器阶段小于 RS_Split，且分裂成本小于 CSR 成本，则选择预分裂。 |
| 3262 | if (getStage(VirtReg) < RS_Split) { |
| 3263 | // We choose pre-splitting over using the CSR for the first time if |
| 3264 | // the cost of splitting is lower than CSRCost. |
| 3265 | SA->analyze(&VirtReg); |
| 3266 | unsigned NumCands = 0; |
| 3267 | BlockFrequency BestCost = CSRCost; // Don't modify CSRCost. |
| 3268 | unsigned BestCand = calculateRegionSplitCost(VirtReg, Order, BestCost, |
| 3269 | NumCands, true /*IgnoreCSR*/); |
| 3270 | if (BestCand == NoCand) |
| 3271 | // Use the CSR if we can't find a region split below CSRCost. |
| 3272 | return PhysReg; |
| 3273 | // Perform the actual pre-splitting. |
| 3274 | doRegionSplit(VirtReg, BestCand, false/*HasCompact*/, NewVRegs); |
| 3275 | return 0; |
| 3276 | } |
| 3277 | return PhysReg; |
| 3278 | } |
|   | /// aboutToRemoveInterval - 当一个 live-interval 即将被移除时，清理与之相关的无效信息。 |
|   | /// @param LI 即将被移除的 live-interval。 |
| 3279 | void RAGreedy::aboutToRemoveInterval(LiveInterval &LI) { |
| 3280 | // Do not keep invalid information around. |
| 3281 | SetOfBrokenHints.remove(&LI); |
| 3282 | } |
|   | /// initializeCSRCost - 初始化 Callee Saved Register (CSR) 的首次使用成本。 |
| 3283 | void RAGreedy::initializeCSRCost() { |
|   | // 使用命令行选项和 TRI 报告的值中较大的一个作为 CSRCost。 |
| 3284 | // We use the larger one out of the command-line option and the value report |
| 3285 | // by TRI. |
| 3286 | CSRCost = BlockFrequency( |
| 3287 | std::max((unsigned)CSRFirstTimeCost, TRI->getCSRFirstUseCost())); |
| 3288 | if (!CSRCost.getFrequency()) |
| 3289 | return; |
|   | // 相对于 Entry == 2^14 的原始成本；根据实际情况进行缩放。 |
| 3290 | // Raw cost is relative to Entry == 2^14; scale it appropriately. |
| 3291 | uint64_t ActualEntry = MBFI->getEntryFreq(); |
| 3292 | if (!ActualEntry) { |
| 3293 | CSRCost = 0; |
| 3294 | return; |
| 3295 | } |
| 3296 | uint64_t FixedEntry = 1 << 14; |
| 3297 | if (ActualEntry < FixedEntry) |
| 3298 | CSRCost *= BranchProbability(ActualEntry, FixedEntry); |
| 3299 | else if (ActualEntry <= UINT32_MAX) |
|   | // 反转分数并进行除法。 |
| 3300 | // Invert the fraction and divide. |
| 3301 | CSRCost /= BranchProbability(FixedEntry, ActualEntry); |
| 3302 | else |
|   | // 一般不能使用 BranchProbability，因为它接受的是 32 位数字。 |
| 3303 | // Can't use BranchProbability in general, since it takes 32-bit numbers. |
| 3304 | CSRCost = CSRCost.getFrequency() * (ActualEntry / FixedEntry); |
| 3305 | } |
|   | /// collectHintInfo - 为寄存器 Reg 收集暗示信息。 |
|   | /// 结果存储在 Out 中。 |
|   | /// Out 在填充之前不会被清空。 |
| 3306 | /// Collect the hint info for \p Reg. |
| 3307 | /// The results are stored into \p Out. |
| 3308 | /// \p Out is not cleared before being populated. |
| 3309 | void RAGreedy::collectHintInfo(unsigned Reg, HintsInfo &Out) { |
| 3310 | for (const MachineInstr &Instr : MRI->reg_nodbg_instructions(Reg)) { |
| 3311 | if (!Instr.isFullCopy()) |
| 3312 | continue; |
|   | // 查找复制的另一端。 |
| 3313 | // Look for the other end of the copy. |
| 3314 | Register OtherReg = Instr.getOperand(0).getReg(); |
| 3315 | if (OtherReg == Reg) { |
| 3316 | OtherReg = Instr.getOperand(1).getReg(); |
| 3317 | if (OtherReg == Reg) |
| 3318 | continue; |
| 3319 | } |
|   | // 获取当前分配。 |
| 3320 | // Get the current assignment. |
| 3321 | Register OtherPhysReg = Register::isPhysicalRegister(OtherReg) |
| 3322 | ? OtherReg |
| 3323 | : VRM->getPhys(OtherReg); |
|   | // 推送收集到的信息。 |
| 3324 | // Push the collected information. |
| 3325 | Out.push_back(HintInfo(MBFI->getBlockFreq(Instr.getParent()), OtherReg, |
| 3326 | OtherPhysReg)); |
| 3327 | } |
| 3328 | } |
|   | /// getBrokenHintFreq - 使用给定的 List 计算如果使用 PhysReg，破坏的暗示的成本。 |
|   | /// @return List 对于 PhysReg 的成本。 |
| 3329 | /// Using the given \p List, compute the cost of the broken hints if |
| 3330 | /// \p PhysReg was used. |
| 3331 | /// \return The cost of \p List for \p PhysReg. |
| 3332 | BlockFrequency RAGreedy::getBrokenHintFreq(const HintsInfo &List, |
| 3333 | unsigned PhysReg) { |
| 3334 | BlockFrequency Cost = 0; |
| 3335 | for (const HintInfo &Info : List) { |
| 3336 | if (Info.PhysReg != PhysReg) |
| 3337 | Cost += Info.Freq; |
| 3338 | } |
| 3339 | return Cost; |
| 3340 | } |
|   | /// tryHintRecoloring - 使用分配给 VirtReg 的寄存器， |
|   | /// 尝试重新着色所有与 VirtReg 相关的复制 live 范围。 |
|   | /// 然后，重新着色会传播到所有已经被重新着色的 live 范围，依此类推， |
|   | /// 直到没有更多的复制可以合并或者这样做不再有利可图。 |
|   | /// 对于给定的 live 范围，是否“有利可图”是根据 |
|   | /// 它将引入的非恒等复制的频率之和来确定的。 |
| 3341 | /// Using the register assigned to \p VirtReg, try to recolor |
| 3342 | /// all the live ranges that are copy-related with \p VirtReg. |
| 3343 | /// The recoloring is then propagated to all the live-ranges that have |
| 3344 | /// been recolored and so on, until no more copies can be coalesced or |
| 3345 | /// it is not profitable. |
| 3346 | /// For a given live range, profitability is determined by the sum of the |
| 3347 | /// frequencies of the non-identity copies it would introduce with the old |
| 3348 | /// and new register. |
| 3349 | void RAGreedy::tryHintRecoloring(LiveInterval &VirtReg) { |
|   | // 我们有一个破坏的暗示，检查是否可以通过重用 PhysReg |
|   | // 来修复复制相关的 live 范围。 |
|   | // 的确，我们驱逐了一些寄存器，PhysReg 可能对其他 live 范围可用。 |
| 3350 | // We have a broken hint, check if it is possible to fix it by |
| 3351 | // reusing PhysReg for the copy-related live-ranges. Indeed, we evicted |
| 3352 | // some register and PhysReg may be available for the other live-ranges. |
| 3353 | SmallSet<unsigned, 4> Visited; |
| 3354 | SmallVector<unsigned, 2> RecoloringCandidates; |
| 3355 | HintsInfo Info; |
| 3356 | unsigned Reg = VirtReg.reg; |
| 3357 | Register PhysReg = VRM->getPhys(Reg); |
|   | // 从输入的 live 区间开始重新着色算法，然后它将传播到与之相关的复制 live 区间。 |
| 3358 | // Start the recoloring algorithm from the input live-interval, then |
| 3359 | // it will propagate to the ones that are copy-related with it. |
| 3360 | Visited.insert(Reg); |
| 3361 | RecoloringCandidates.push_back(Reg); |
| 3362 | LLVM_DEBUG(dbgs() << "Trying to reconcile hints for: " << printReg(Reg, TRI) |
| 3363 | << '(' << printReg(PhysReg, TRI) << ")\n"); |
| 3364 | do { |
| 3365 | Reg = RecoloringCandidates.pop_back_val(); |
|   | // 我们不能重新着色物理寄存器。 |
| 3366 | // We cannot recolor physical register. |
| 3367 | if (Register::isPhysicalRegister(Reg)) |
| 3368 | continue; |
| 3369 | assert(VRM->hasPhys(Reg) && "We have unallocated variable!!"); |
|   | // 获取与此虚拟寄存器映射的 live 区间，以便检查与新颜色的干扰。 |
| 3370 | // Get the live interval mapped with this virtual register to be able |
| 3371 | // to check for the interference with the new color. |
| 3372 | LiveInterval &LI = LIS->getInterval(Reg); |
| 3373 | Register CurrPhys = VRM->getPhys(Reg); |
|   | // 检查新颜色是否符合寄存器类约束，并且在此 live 范围内是空闲的。 |
| 3374 | // Check that the new color matches the register class constraints and |
| 3375 | // that it is free for this live range. |
| 3376 | if (CurrPhys != PhysReg && (!MRI->getRegClass(Reg)->contains(PhysReg) || |
| 3377 | Matrix->checkInterference(LI, PhysReg))) |
| 3378 | continue; |
| 3379 | LLVM_DEBUG(dbgs() << printReg(Reg, TRI) << '(' << printReg(CurrPhys, TRI) |
| 3380 | << ") is recolorable.\n"); |
|   | // 收集暗示信息。 |
| 3381 | // Gather the hint info. |
| 3382 | Info.clear(); |
| 3383 | collectHintInfo(Reg, Info); |
|   | // 检查重新着色 live 范围是否会增加非恒等复制的成本。 |
| 3384 | // Check if recoloring the live-range will increase the cost of the |
| 3385 | // non-identity copies. |
| 3386 | if (CurrPhys != PhysReg) { |
| 3387 | LLVM_DEBUG(dbgs() << "Checking profitability:\n"); |
| 3388 | BlockFrequency OldCopiesCost = getBrokenHintFreq(Info, CurrPhys); |
| 3389 | BlockFrequency NewCopiesCost = getBrokenHintFreq(Info, PhysReg); |
| 3390 | LLVM_DEBUG(dbgs() << "Old Cost: " << OldCopiesCost.getFrequency() |
| 3391 | << "\nNew Cost: " << NewCopiesCost.getFrequency() |
| 3392 | << '\n'); |
| 3393 | if (OldCopiesCost < NewCopiesCost) { |
| 3394 | LLVM_DEBUG(dbgs() << "=> Not profitable.\n"); |
| 3395 | continue; |
| 3396 | } |
|   | // 在这一点上，成本要么更便宜，要么相等。 |
|   | // 如果相等，我们认为这是划算的，因为它可能暴露出更多的重新着色机会。 |
| 3397 | // At this point, the cost is either cheaper or equal. If it is |
| 3398 | // equal, we consider this is profitable because it may expose |
| 3399 | // more recoloring opportunities. |
| 3400 | LLVM_DEBUG(dbgs() << "=> Profitable.\n"); |
| 3401 | // Recolor the live-range. |
| 3402 | Matrix->unassign(LI); |
| 3403 | Matrix->assign(LI, PhysReg); |
| 3404 | } |
|   | // 推送所有复制相关的 live 范围，以继续协调破坏的暗示。 |
| 3405 | // Push all copy-related live-ranges to keep reconciling the broken |
| 3406 | // hints. |
| 3407 | for (const HintInfo &HI : Info) { |
| 3408 | if (Visited.insert(HI.Reg).second) |
| 3409 | RecoloringCandidates.push_back(HI.Reg); |
| 3410 | } |
| 3411 | } while (!RecoloringCandidates.empty()); |
| 3412 | } |
|   | /// 尝试重新着色破坏的提示。 |
|   | /// 当一个被驱逐的变量释放了一个寄存器给一个更大的生命周期时， |
|   | /// 可以通过重新着色来修复破坏的提示。 |
|   | /// 考虑以下示例： |
| 3413 | /// BB1: |
| 3414 | ///   a = |
| 3415 | ///   b = |
| 3416 | /// BB2: |
| 3417 | ///   ... |
| 3418 | ///   = b |
| 3419 | ///   = a |
|   | /// 假设b被拆分： |
| 3420 | /// BB1: |
| 3421 | ///   a = |
| 3422 | ///   b = |
| 3423 | /// BB2: |
| 3424 | ///   c = b |
| 3425 | ///   ... |
| 3426 | ///   d = c |
| 3427 | ///   = d |
| 3428 | ///   = a |
|   | /// 由于分配工作的方式，b、c和d可能被分配不同的颜色。现在，如果a稍后被驱逐： |
| 3429 | /// BB1: |
| 3430 | ///   a = |
| 3431 | ///   st a, SpillSlot |
| 3432 | ///   b = |
| 3433 | /// BB2: |
| 3434 | ///   c = b |
| 3435 | ///   ... |
| 3436 | ///   d = c |
| 3437 | ///   = d |
| 3438 | ///   e = ld SpillSlot |
| 3439 | ///   = e |
|   | /// 这很可能会为b、c和d分配相同的寄存器，从而消除2个复制操作。 |
| 3440 | /// Try to recolor broken hints. |
| 3441 | /// Broken hints may be repaired by recoloring when an evicted variable |
| 3442 | /// freed up a register for a larger live-range. |
| 3443 | /// Consider the following example: |
| 3444 | /// BB1: |
| 3445 | ///   a = |
| 3446 | ///   b = |
| 3447 | /// BB2: |
| 3448 | ///   ... |
| 3449 | ///   = b |
| 3450 | ///   = a |
| 3451 | /// Let us assume b gets split: |
| 3452 | /// BB1: |
| 3453 | ///   a = |
| 3454 | ///   b = |
| 3455 | /// BB2: |
| 3456 | ///   c = b |
| 3457 | ///   ... |
| 3458 | ///   d = c |
| 3459 | ///   = d |
| 3460 | ///   = a |
| 3461 | /// Because of how the allocation work, b, c, and d may be assigned different |
| 3462 | /// colors. Now, if a gets evicted later: |
| 3463 | /// BB1: |
| 3464 | ///   a = |
| 3465 | ///   st a, SpillSlot |
| 3466 | ///   b = |
| 3467 | /// BB2: |
| 3468 | ///   c = b |
| 3469 | ///   ... |
| 3470 | ///   d = c |
| 3471 | ///   = d |
| 3472 | ///   e = ld SpillSlot |
| 3473 | ///   = e |
| 3474 | /// This is likely that we can assign the same register for b, c, and d, |
| 3475 | /// getting rid of 2 copies. |
| 3476 | void RAGreedy::tryHintsRecoloring() { |
| 3477 | for (LiveInterval *LI : SetOfBrokenHints) { |
| 3478 | assert(Register::isVirtualRegister(LI->reg) && |
| 3479 | "Recoloring is possible only for virtual registers"); |
|   | // 周围可能有一些死亡定义（例如，因为调试使用）。 |
|   | // 忽略它们。 |
| 3480 | // Some dead defs may be around (e.g., because of debug uses). |
| 3481 | // Ignore those. |
| 3482 | if (!VRM->hasPhys(LI->reg)) |
| 3483 | continue; |
| 3484 | tryHintRecoloring(*LI); |
| 3485 | } |
| 3486 | } |
|   | /// 实现选择或拆分虚拟寄存器。 |
| 3487 | Register RAGreedy::selectOrSplitImpl(LiveInterval &VirtReg, |
| 3488 | SmallVectorImpl<Register> &NewVRegs, |
| 3489 | SmallVirtRegSet &FixedRegisters, |
| 3490 | unsigned Depth) { |
| 3491 | unsigned CostPerUseLimit = ~0u; |
|   | // 首先尝试分配一个空闲寄存器。 |
| 3492 | // First try assigning a free register. |
| 3493 | AllocationOrder Order(VirtReg.reg, *VRM, RegClassInfo, Matrix); |
| 3494 | if (unsigned PhysReg = tryAssign(VirtReg, Order, NewVRegs, FixedRegisters)) { |
|   | // 如果VirtReg得到了分配，驱逐信息就不再相关了。 |
| 3495 | // If VirtReg got an assignment, the eviction info is no longre relevant. |
| 3496 | LastEvicted.clearEvicteeInfo(VirtReg.reg); |
|   | // 当NewVRegs不为空时，我们可能已经做出了一些决策， |
|   | // 比如驱逐一个虚拟寄存器， |
|   | // 根据之前的决策使用物理寄存器。 |
| 3497 | // When NewVRegs is not empty, we may have made decisions such as evicting |
| 3498 | // a virtual register, go with the earlier decisions and use the physical |
| 3499 | // register. |
| 3500 | if (CSRCost.getFrequency() && isUnusedCalleeSavedReg(PhysReg) && |
| 3501 | NewVRegs.empty()) { |
| 3502 | Register CSRReg = tryAssignCSRFirstTime(VirtReg, Order, PhysReg, |
| 3503 | CostPerUseLimit, NewVRegs); |
| 3504 | if (CSRReg || !NewVRegs.empty()) |
|   | // 如果我们决定使用CSR或由于预拆分创建新的虚拟寄存器，现在就返回。 |
| 3505 | // Return now if we decide to use a CSR or create new vregs due to |
| 3506 | // pre-splitting. |
| 3507 | return CSRReg; |
| 3508 | } else |
| 3509 | return PhysReg; |
| 3510 | } |
| 3511 | LiveRangeStage Stage = getStage(VirtReg); |
| 3512 | LLVM_DEBUG(dbgs() << StageName[Stage] << " Cascade " |
| 3513 | << ExtraRegInfo[VirtReg.reg].Cascade << '\n'); |
|   | // 尝试驱逐一个不太值得的生命周期，但仅限于主队列中的范围。 |
|   | // RS_Split范围已经尝试过这样做了，它们不应该有第二次机会， |
|   | // 直到它们被拆分。 |
| 3514 | // Try to evict a less worthy live range, but only for ranges from the primary |
| 3515 | // queue. The RS_Split ranges already failed to do this, and they should not |
| 3516 | // get a second chance until they have been split. |
| 3517 | if (Stage != RS_Split) |
| 3518 | if (Register PhysReg = |
| 3519 | tryEvict(VirtReg, Order, NewVRegs, CostPerUseLimit, |
| 3520 | FixedRegisters)) { |
| 3521 | Register Hint = MRI->getSimpleHint(VirtReg.reg); |
|   | // 如果VirtReg有提示，并且该提示被破坏了，记录这个虚拟寄存器 |
|   | // 作为破坏提示的重新着色候选。 |
|   | // 的确，因为我们在它的邻域驱逐了一个变量，我们至少可以部分重新着色 |
|   | // 一些与复制相关的生命周期。 |
| 3522 | // If VirtReg has a hint and that hint is broken record this |
| 3523 | // virtual register as a recoloring candidate for broken hint. |
| 3524 | // Indeed, since we evicted a variable in its neighborhood it is |
| 3525 | // likely we can at least partially recolor some of the |
| 3526 | // copy-related live-ranges. |
| 3527 | if (Hint && Hint != PhysReg) |
| 3528 | SetOfBrokenHints.insert(&VirtReg); |
|   | // 如果VirtReg驱逐了某人，它作为被驱逐者的信息就不再相关了。 |
| 3529 | // If VirtReg eviction someone, the eviction info for it as an evictee is |
| 3530 | // no longre relevant. |
| 3531 | LastEvicted.clearEvicteeInfo(VirtReg.reg); |
| 3532 | return PhysReg; |
| 3533 | } |
| 3534 | assert((NewVRegs.empty() || Depth) && "Cannot append to existing NewVRegs"); |
|   | // 我们第一次看到一个生命周期时，不要尝试拆分或溢出。 |
|   | // 等到第二次，当所有较小的范围都被分配后。 |
|   | // 这可以更好地了解拆分周围的干扰。 |
| 3535 | // The first time we see a live range, don't try to split or spill. |
| 3536 | // Wait until the second time, when all smaller ranges have been allocated. |
| 3537 | // This gives a better picture of the interference to split around. |
| 3538 | if (Stage < RS_Split) { |
| 3539 | setStage(VirtReg, RS_Split); |
| 3540 | LLVM_DEBUG(dbgs() << "wait for second round\n"); |
| 3541 | NewVRegs.push_back(VirtReg.reg); |
| 3542 | return 0; |
| 3543 | } |
| 3544 | if (Stage < RS_Spill) { |
|   | // 尝试拆分VirtReg或干扰。 |
| 3545 | // Try splitting VirtReg or interferences. |
| 3546 | unsigned NewVRegSizeBefore = NewVRegs.size(); |
| 3547 | Register PhysReg = trySplit(VirtReg, Order, NewVRegs, FixedRegisters); |
| 3548 | if (PhysReg || (NewVRegs.size() - NewVRegSizeBefore)) { |
|   | // 如果VirtReg被拆分，驱逐信息就不再相关了。 |
| 3549 | // If VirtReg got split, the eviction info is no longer relevant. |
| 3550 | LastEvicted.clearEvicteeInfo(VirtReg.reg); |
| 3551 | return PhysReg; |
| 3552 | } |
| 3553 | } |
|   | // 如果我们不能从溢出中分配寄存器，可能是有一些无效的内联汇编。 |
|   | // 基类将会报告它。 |
| 3554 | // If we couldn't allocate a register from spilling, there is probably some |
| 3555 | // invalid inline assembly. The base class will report it. |
| 3556 | if (Stage >= RS_Done || !VirtReg.isSpillable()) |
| 3557 | return tryLastChanceRecoloring(VirtReg, Order, NewVRegs, FixedRegisters, |
| 3558 | Depth); |
|   | // 最后溢出VirtReg本身。 |
| 3559 | // Finally spill VirtReg itself. |
| 3560 | if (EnableDeferredSpilling && getStage(VirtReg) < RS_Memory) { |
|   | // TODO: 这是实验性的，特别是我们没有正确地模拟由溢出完成的生命周期拆分。 |
|   | // 我们需要与溢出器深度集成才能在这里做正确的事情。 |
|   | // 无论如何，这对于早期测试仍然是好的。 |
| 3561 | // TODO: This is experimental and in particular, we do not model |
| 3562 | // the live range splitting done by spilling correctly. |
| 3563 | // We would need a deep integration with the spiller to do the |
| 3564 | // right thing here. Anyway, that is still good for early testing. |
| 3565 | setStage(VirtReg, RS_Memory); |
| 3566 | LLVM_DEBUG(dbgs() << "Do as if this register is in memory\n"); |
| 3567 | NewVRegs.push_back(VirtReg.reg); |
| 3568 | } else { |
| 3569 | NamedRegionTimer T("spill", "Spiller", TimerGroupName, |
| 3570 | TimerGroupDescription, TimePassesIsEnabled); |
| 3571 | LiveRangeEdit LRE(&VirtReg, NewVRegs, *MF, *LIS, VRM, this, &DeadRemats); |
| 3572 | spiller().spill(LRE); |
| 3573 | setStage(NewVRegs.begin(), NewVRegs.end(), RS_Done); |
|   | // 告诉LiveDebugVariables关于新范围的信息。不被新寄存器覆盖的范围 |
|   | // 保留在LDV中（仍然映射到旧寄存器）， |
|   | // 直到我们在稍后阶段重写LDV中的溢出位置。 |
| 3574 | // Tell LiveDebugVariables about the new ranges. Ranges not being covered by |
| 3575 | // the new regs are kept in LDV (still mapping to the old register), until |
| 3576 | // we rewrite spilled locations in LDV at a later stage. |
| 3577 | DebugVars->splitRegister(VirtReg.reg, LRE.regs(), *LIS); |
| 3578 | if (VerifyEnabled) |
| 3579 | MF->verify(this, "After spilling"); |
| 3580 | } |
|   | // 请求分配的虚拟寄存器被溢出了，所以告诉调用者在这一轮不要分配任何东西。 |
| 3581 | // The live virtual register requesting allocation was spilled, so tell |
| 3582 | // the caller not to allocate anything during this round. |
| 3583 | return 0; |
| 3584 | } |
|   | /// 报告循环中的溢出和重载数量。 |
|   | /// 这个函数递归地计算给定循环及其子循环中的溢出和重载数量。 |
|   | /// @param L 指向当前循环的指针。 |
|   | /// @param Reloads 用于存储重载数量的引用。 |
|   | /// @param FoldedReloads 用于存储折叠重载数量的引用。 |
|   | /// @param Spills 用于存储溢出数量的引用。 |
|   | /// @param FoldedSpills 用于存储折叠溢出数量的引用。 |
| 3585 | void RAGreedy::reportNumberOfSplillsReloads(MachineLoop *L, unsigned &Reloads, |
| 3586 | unsigned &FoldedReloads, |
| 3587 | unsigned &Spills, |
| 3588 | unsigned &FoldedSpills) { |
|   | // 初始化计数器。 |
| 3589 | Reloads = 0; |
| 3590 | FoldedReloads = 0; |
| 3591 | Spills = 0; |
| 3592 | FoldedSpills = 0; |
|   | // 递归地计算子循环中的溢出和重载数量，并累加到总数中。 |
| 3593 | // Sum up the spill and reloads in subloops. |
| 3594 | for (MachineLoop *SubLoop : *L) { |
| 3595 | unsigned SubReloads; |
| 3596 | unsigned SubFoldedReloads; |
| 3597 | unsigned SubSpills; |
| 3598 | unsigned SubFoldedSpills; |
| 3599 | reportNumberOfSplillsReloads(SubLoop, SubReloads, SubFoldedReloads, |
| 3600 | SubSpills, SubFoldedSpills); |
| 3601 | Reloads += SubReloads; |
| 3602 | FoldedReloads += SubFoldedReloads; |
| 3603 | Spills += SubSpills; |
| 3604 | FoldedSpills += SubFoldedSpills; |
| 3605 | } |
|   | // 获取机器框架信息。 |
| 3606 | const MachineFrameInfo &MFI = MF->getFrameInfo(); |
| 3607 | int FI; |
|   | // 遍历循环中的所有基本块。 |
| 3608 | for (MachineBasicBlock *MBB : L->getBlocks()) |
|   | // 处理未包含在子循环中的块。 |
| 3609 | // Handle blocks that were not included in subloops. |
| 3610 | if (Loops->getLoopFor(MBB) == L) |
| 3611 | for (MachineInstr &MI : *MBB) { |
|   | // 用于存储访问的内存操作数。 |
| 3612 | SmallVector<const MachineMemOperand *, 2> Accesses; |
|   | // 判断是否是栈槽访问的函数。 |
| 3613 | auto isSpillSlotAccess = [&MFI](const MachineMemOperand *A) { |
| 3614 | return MFI.isSpillSlotObjectIndex( |
| 3615 | cast<FixedStackPseudoSourceValue>(A->getPseudoValue()) |
| 3616 | ->getFrameIndex()); |
| 3617 | }; |
|   | // 如果是从一个栈槽加载，增加重载计数。 |
| 3618 | if (TII->isLoadFromStackSlot(MI, FI) && MFI.isSpillSlotObjectIndex(FI)) |
| 3619 | ++Reloads; |
|   | // 如果是折叠的从栈槽加载，增加折叠重载计数。 |
| 3620 | else if (TII->hasLoadFromStackSlot(MI, Accesses) && |
| 3621 | llvm::any_of(Accesses, isSpillSlotAccess)) |
| 3622 | ++FoldedReloads; |
|   | // 如果是向一个栈槽存储，增加溢出计数。 |
| 3623 | else if (TII->isStoreToStackSlot(MI, FI) && |
| 3624 | MFI.isSpillSlotObjectIndex(FI)) |
| 3625 | ++Spills; |
|   | // 如果是折叠的向栈槽存储，增加折叠溢出计数。 |
| 3626 | else if (TII->hasStoreToStackSlot(MI, Accesses) && |
| 3627 | llvm::any_of(Accesses, isSpillSlotAccess)) |
| 3628 | ++FoldedSpills; |
| 3629 | } |
|   | // 如果有溢出或重载，使用ORE（优化remark emitter）报告。 |
| 3630 | if (Reloads || FoldedReloads || Spills || FoldedSpills) { |
| 3631 | using namespace ore; |
| 3632 | ORE->emit([&]() { |
| 3633 | MachineOptimizationRemarkMissed R(DEBUG_TYPE, "LoopSpillReload", |
| 3634 | L->getStartLoc(), L->getHeader()); |
|   | // 如果有溢出，添加到remark。 |
| 3635 | if (Spills) |
| 3636 | R << NV("NumSpills", Spills) << " spills "; |
|   | // 如果有折叠溢出，添加到remark。 |
| 3637 | if (FoldedSpills) |
| 3638 | R << NV("NumFoldedSpills", FoldedSpills) << " folded spills "; |
|   | // 如果有重载，添加到remark。 |
| 3639 | if (Reloads) |
| 3640 | R << NV("NumReloads", Reloads) << " reloads "; |
|   | // 如果有折叠重载，添加到remark。 |
| 3641 | if (FoldedReloads) |
| 3642 | R << NV("NumFoldedReloads", FoldedReloads) << " folded reloads "; |
|   | // 添加remark的结尾。 |
| 3643 | R << "generated in loop"; |
| 3644 | return R; |
| 3645 | }); |
| 3646 | } |
| 3647 | } |
|   | /// 在机器函数上运行贪婪寄存器分配。 |
|   | /// 这个函数初始化所需的分析，执行寄存器分配，然后清理。 |
|   | /// @param mf 机器函数的引用。 |
| 3648 | bool RAGreedy::runOnMachineFunction(MachineFunction &mf) { |
|   | // 输出调试信息，显示正在执行贪婪寄存器分配的函数名。 |
| 3649 | LLVM_DEBUG(dbgs() << "********** GREEDY REGISTER ALLOCATION **********\n" |
| 3650 | << "********** Function: " << mf.getName() << '\n'); |
|   | // 初始化成员变量。 |
| 3651 | MF = &mf; |
| 3652 | TRI = MF->getSubtarget().getRegisterInfo(); |
| 3653 | TII = MF->getSubtarget().getInstrInfo(); |
| 3654 | RCI.runOnMachineFunction(mf); |
|   | // 根据命令行参数或子目标设置确定是否启用局部重新分配。 |
| 3655 | EnableLocalReassign = EnableLocalReassignment || |
| 3656 | MF->getSubtarget().enableRALocalReassignment( |
| 3657 | MF->getTarget().getOptLevel()); |
|   | // 根据命令行参数或子目标设置确定是否启用高级拆分成本计算。 |
| 3658 | EnableAdvancedRASplitCost = |
| 3659 | ConsiderLocalIntervalCost.getNumOccurrences() |
| 3660 | ? ConsiderLocalIntervalCost |
| 3661 | : MF->getSubtarget().enableAdvancedRASplitCost(); |
|   | // 如果启用了验证，验证机器函数。 |
| 3662 | if (VerifyEnabled) |
| 3663 | MF->verify(this, "Before greedy register allocator"); |
|   | // 初始化寄存器分配基础结构。 |
| 3664 | RegAllocBase::init(getAnalysis<VirtRegMap>(), |
| 3665 | getAnalysis<LiveIntervals>(), |
| 3666 | getAnalysis<LiveRegMatrix>()); |
| 3667 | Indexes = &getAnalysis<SlotIndexes>(); |
| 3668 | MBFI = &getAnalysis<MachineBlockFrequencyInfo>(); |
| 3669 | DomTree = &getAnalysis<MachineDominatorTree>(); |
| 3670 | ORE = &getAnalysis<MachineOptimizationRemarkEmitterPass>().getORE(); |
|   | // 创建内联溢出处理器。 |
| 3671 | SpillerInstance.reset(createInlineSpiller(*this, *MF, *VRM)); |
| 3672 | Loops = &getAnalysis<MachineLoopInfo>(); |
| 3673 | Bundles = &getAnalysis<EdgeBundles>(); |
| 3674 | SpillPlacer = &getAnalysis<SpillPlacement>(); |
| 3675 | DebugVars = &getAnalysis<LiveDebugVariables>(); |
| 3676 | AA = &getAnalysis<AAResultsWrapperPass>().getAAResults(); |
|   | // 初始化调用保存寄存器成本。 |
| 3677 | initializeCSRCost(); |
|   | // 计算溢出权重和提示。 |
| 3678 | calculateSpillWeightsAndHints(*LIS, mf, VRM, *Loops, *MBFI); |
|   | // 调试：转储生命周期间隔。 |
| 3679 | LLVM_DEBUG(LIS->dump()); |
|   | // 创建拆分分析和编辑器。 |
| 3680 | SA.reset(new SplitAnalysis(*VRM, *LIS, *Loops)); |
| 3681 | SE.reset(new SplitEditor(*SA, *AA, *LIS, *VRM, *DomTree, *MBFI)); |
| 3682 | ExtraRegInfo.clear(); |
| 3683 | ExtraRegInfo.resize(MRI->getNumVirtRegs()); |
| 3684 | NextCascade = 1; |
|   | // 初始化接口缓存。 |
| 3685 | IntfCache.init(MF, Matrix->getLiveUnions(), Indexes, LIS, TRI); |
| 3686 | GlobalCand.resize(32);  // This will grow as needed. |
| 3687 | SetOfBrokenHints.clear(); |
| 3688 | LastEvicted.clear(); |
|   | // 分配物理寄存器。 |
| 3689 | allocatePhysRegs(); |
|   | // 尝试重新着色破坏的提示。 |
| 3690 | tryHintsRecoloring(); |
|   | // 执行后优化。 |
| 3691 | postOptimization(); |
|   | // 报告溢出和重载的数量。 |
| 3692 | reportNumberOfSplillsReloads(); |
|   | // 释放内存。 |
| 3693 | releaseMemory(); |
| 3694 | return true; |
| 3695 | } |

第 3 篇

并行优化分析

