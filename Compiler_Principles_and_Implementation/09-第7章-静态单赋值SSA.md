# 7 静态单赋值（SSA）

本章是系列文章的第七章，终于来到了鼎鼎大名的SSA，SSA是编译器领域最伟大的发明之一，也是影响最广的发明。

## 7.1 控制流图回顾

对下面的c代码保存成7.1.cc：

1 int max(int a, int b) {

2   int ans = a;

3   if (b > a) {

4     ans = b;

5   }

6   return ans;

7 }

直接用clang生成bc → dot → svg，最终svg的结果如下：

![图](images/image_0071.png)

如果经过一轮opt的优化“opt -mem2reg 7.1.ll -o 7.1.1.bc”之后的结果，就变成了这样（注意，需要删除ll里面的optnone属性，否则opt不会生效）：

![图](images/image_0072.png)

除了我们本来准备跑的mem2reg的pass外，优化前后最后一个BB里是不是还多了一个phi函数？

### 7.1.1 静态单赋值范式（SSA Form）

静态单赋值，字面意思是对静态的变量只有一次赋值点。这是现在所有编译器都广泛使用的属性，也是编译器历史上最具有突破性意义的属性，简化了各种分析和优化的过程。

1991年SSA的奠基论文被引用达到2800+次，这还是截止2019年的数据，这个引用次数每年还在增加。

几乎每本讲编译器的书都会说到SSA。google学术上用SSA能搜到5000+个结果。

每年来自全世界的编译器专家，都会在SSA研讨会上庆祝一次SSA的诞生。

和静态单赋值对应的是动态单赋值，也就是程序执行过程中，每个变量只能赋值一次。和动态单赋值不同，静态单赋值，只要求每个变量的赋值程序点只能有一个，这个程序点可以出现在循环内部（这意味着动态执行过程中这个程序点会多次执行）。

## 7.2 从SSA来到SSA去

### 7.2.1 将线性代码转换成SSA Form

如果一个程序没有任何分叉，则称这个程序是线性代码。

例如下面的代码：

1 double baskhara(double a, double b, double c) {

2   double delta = b * b - 4 * a * c;

3   double sqrDelta = sqrt(delta);

4   double root = (b + sqrDelta) / 2 * a;

5   return root;

6 }

其实它本身就是符合SSA定义的（每个变量只定义一次），但一般经过opt转换之后的代码是这样：

1 define double @baskhara(double %a, double %b, double %c) {

2   %1 = fmul double %b, %b

3   %2 = fmul double 4.000000e+00, %a

4   %3 = fmul double %2, %c

5   %4 = fsub double %1, %3

6   %5 = call double @sqrt(double %4)

7   %6 = fadd double %b, %5

8   %7 = fdiv double %6, 2.000000e+00

9   %8 = fmul double %7, %a

10   ret double %8

11 }

线性代码转换成SSA范式的算法比较直接：

1 for each variable a:

2     Count[a] = 0

3     Stack[a] = [0]

4 rename_basic_block(B) =

5     for each instruction S in block B:

6         for each use of a variable x in S:

7             i = top(Stack[x])

8             replace the use of x with xi

9         for each variable a that S defines

10             count[a] = Count[a] + 1

11             i = Count[a]

12             push i onto Stack[a]

13             replace definition of a with ai

例如，下面的c代码：

1 a = x + y;

2 b = a - 1;

3 a = y + b;

4 b = 4 * x;

5 a = a + b;

经过SSA转换之后会变成这样：

1 a1 = x0 + y0;

2 b1 = a1 - 1;

3 a2 = y0 + b1;

4 b2 = 4 * x0;

5 a3 = a2 + b2;

### 7.2.2 Phi函数

前面说了线性代码的SSA转换过程，那非线性代码应该怎么处理呢？

例如下面的控制流图，SSA转换之后L5处使用的b是哪一个b？：

![图](images/image_0073.png)

答案是要看情况，如果控制流图上从L4执行到L5，则L5处的b应该是b1；如果是从L2执行到L5，则L5处的b应该是b0。

为了处理这种情况，需要引入phi函数（φ），φ函数会根据路径做选择，根据进入φ函数的路径选择不同的定义。

插入φ函数之后的SSA转换结果如下：

![图](images/image_0074.png)

φ函数会插入到每个基本块的最开始地方，对N个变量生成N个φ函数，φ函数的参数个数取决于执行到该基本块的直接前驱有几个。

![图](images/image_0075.png)

### 7.2.3 临界边

如果一条边的起始点BB有多个直接后继BB，终止点的BB有多个前驱BB，则称为该边为临界边。

### 7.2.4 临界边分裂

在临界边上插入一个空的BB（这个BB只有一个简单的goto语句），来解决临界边的上的φ函数自动注入问题。

### 7.2.5 φ函数的插入策略

存在一个基本块x包含b的定义

存在一个非x的基本块y包含b的定义

存在至少一条路径Pxz从x到z

存在至少一条路径Pyz从y到z

Pyz和Pxz除了z节点外，没有其他公共节点

z不会同时出现在Pxz和Pyz路径中间，但可以出现在其中一条路径的中间

### 7.2.6 SSA范式的支配属性

在一个有根的有向图中，d支配n的意思是所有从根节点到n的路径都通过d。

在严格SSA范式（严格的意思是所有变量都是在使用前初始化）程序中，每个变量的定义都支配它的使用：

在基本块n中，如果x是φ函数的第i个参数，则x的定义支配n的第3个前驱。

在一个使用x的不存在φ函数的基本块n中，x的定义支配基本块n。

### 7.2.7 支配前沿（The Dominance Frontier）

一个节点x严格支配节点w，当且仅当x支配w，并且x≠w。

节点x的支配前沿是所有具有下面属性的节点w的集合：x支配w的前驱，但不严格支配w。

支配前沿策略：如果节点x函数变量a的定义，那么x的支配前沿中的任意节点z都需要一个a的φ函数。

支配前沿迭代：因为φ函数本身会产生一个定义，所以需要循环执行支配前沿策略，直到没有节点需要额外增加φ函数。

定理：迭代支配前沿策略和迭代路径覆盖策略生成同样的φ函数集合。

### 7.2.8 支配前沿的计算

DF[n] = DFlocal[n] ∪ { DFup[c] | c ∈ children[n] }
Where:
DFlocal[n]: 不被n严格支配（SSA的1989年版本要求的是严格支配，但1991年版本优化成直接支配，前一篇在ACM会议上，后一篇在ACM期刊上，Cytron果然是混职级的高手）的n的后继节点
DFup[c]: c的支配前沿集合中不被n严格支配的节点
children[n]: 支配树中n的子结点集合

转换成算法之后的伪代码如下：

1 computeDF[n]:

2 S = {}

3 for each node y in succ[n]

4     if idom(y) ≠ n

5         S = S ∪ {y}

6 for each child c of n in the dom-tree

7     computeDF[c]

8     for each w ∈ DF[c]

9         if n does not dom w, or n = w

10             S = S ∪ {w}

11 DF[n] = S

### 7.2.9 插入φ函数

插入的算法描述如下：

1 place-phi-functions:

2   for each node n:

3     for each variable a ∈ Aorig[n]:

4       defsites[a] = defsites[a] ∪ [n]

5   for each variable a:

6     W = defsites[a]

7     while W ≠ empty list

8       remove some node n from W

9       for each y in DF[n]:

10       if a ∉ Aphi[y]

11         insert-phi(y, a)

12         Aphi[y] = Aphi[y] ∪ {a}

13         if a ∉ Aorig[y]

14         W = W ∪ {y}

15

16 insert-phi(y, a):

17   insert the statement a = ϕ(a, a, …, a)

18   at the top of block y, where the

19   phi-function has as many arguments

20   as y has predecessors

21 Where:

22 Aorig[n]:  the  set  of  variables  defined  at  node  "n"

23 Aphi[y]:  the  set  of  variables  that  have  phi-functions  at  node  "y"

### 7.2.10 变量重命名

1 rename(n):

2   rename-basic-block(n)

3   for each successor Y of n, where n is the j-th predecessor of Y:

4     for each phi-function f in Y, where the operand of f is ‘a’

5       i = top(Stack[a])

6       replace j-th operand with ai

7   for each child X of n:

8     rename(X)

9   for each instruction S ∈ n:

10     for each variable v that S defines:

11       pop Stack[v]

rename-basic-block的定义参照之前的，这里只是增加了一些场景。

## 7.3 跑一下整个流程

### 7.3.1 伪代码

1 i = 1

2 j = 1

3 k = 0

4 while k < 100

5   if j < 20

6     j = i

7     k = k + 1

8   else

9     j = k

10     k = k + 2

11 return j

### 7.3.2 生成控制流图

![图](images/image_0076.png)

### 7.3.3 根据控制流图生成支配树

![图](images/image_0077.png)

### 7.3.4 计算支配前沿

一般从支配树的叶子节点开始计算，第一轮计算所有叶子节点：

DF(7) = {9}, DF(9) = {3}, DF(5) = {9}, DF(10) = {}

第二轮去掉支配树的所有叶子节点，计算第二轮叶子节点的支配前沿：

DF(4) = {3}

第三轮删掉叶子节点，并计算当前叶子节点的支配前沿：

DF(3) = {3}

第四轮删掉叶子节点，并计算当前叶子节点的支配前沿：

DF(0) = {}

### 7.3.5 插入φ函数

上一节求出来的DF集合其实只有2个元素，所以只需要在L3和L9的基本块开始处插入φ函数，存在多种定义的变量只有j和k，所以下面在L3和L9插入j和k的φ函数：

![图](images/image_0078.png)

### 7.3.6 φ函数的参数个数

是否存在只有一个前驱的φ函数？如果只有一个前驱，那说明变量只有一个定义，自然就不需要φ函数。

是否存在参数多余2个的φ函数？如果前驱个数大于2，自然就会出现参数多余2的φ函数。

### 7.3.7 变量重命名

![图](images/image_0079.png)

### 7.3.8 优化SSA范式

上面生成的SSA范式，从SSA的定义上看虽然已经是最简的了，但可能存在一些用不上的变量定义，砍掉这些冗余的定义是生命周期检查的工作，经过生命周期检查，仅在变量i还处在生命周期范围内的程序点才需要插入i的φ函数。

下面L1处的i的定义后面没机会使用了，所以L1处的φ函数插入是不必要的：

![图](images/image_0080.png)

## 7.4 使用SSA简化分析

SSA范式可以用来简化各种基于数据流的分析。SSA范式之前，数据流分析的某个变量的定义是一个集合，SSA范式转换之后这些变量都变成了唯一定义；而且由于每个变量只有一次定义，相当于说每个变量都可以转换成常量（循环内定义的变量除外，每次循环迭代，变量都会被重新定义）。

### 7.4.1 简化冗余代码删除

如果一个变量定义了，没有使用，并且该定义的语句也没有其他副作用，可以将该变量定义的语句删除。（SSA之前变量是否被使用的含义就要复杂多了，因为会有多个版本的变量定义）

给每个SSA转换之后的每个变量保存一个计数器，初始化为0。遍历一遍代码，每次使用就将计数器加一，遍历完如果某个变量的使用计数器为0，则可以删除变量的定义语句。

### 7.4.2 简化常量传播

因为每个变量的定义都只有一个定义，所以在变量定义时就能判断变量是常量，还是真的变量。如果变量的定义依赖某个外部输入，则它不是常量。如果变量的定义依赖的是一个常量，或者依赖的变量是一个常量，则常量可以一直传播下去，所有类似的变量都能转换成常量。直到明确所有变量都是依赖某个外部输入。

如果碰到φ函数怎么办？因为φ函数会给变量的赋值增加多种可能性，所以变量的定义变成了一个集合，只有当集合中所有定义都是常量的情况下，才能将该变量转换成常量。

下面是llvm的常量传播的实现：

![图](images/image_0081.png)

### 7.4.3 SSA范式转换之后的生命周期分析

新的生命周期分析算法如下：

1 For each statement S in the program:

2   IN[S] = OUT[S] = {}

3 For each variable v in the program:

4   For each statement S that uses v:

5     live(S, v)

6 live(S, v):

7   IN[S] = IN[S] ∪ {v}

8   For each P in pred(S):

9     OUT[P] = OUT[P] ∪ {v}

10     if P does not define v

11       live(P, v)

## 7.5 SSA简史

“An Efficient Method of Computing Static Single Assignment Form,” appeared in the conference Record of the 16th ACM Symposium on principles of Programming Languages (Jan. 1989). https://c9x.me/compile/bib/ssa.pdf Cython因为SSA发了2篇ACM，在国内也绝对是升职高手😊

Efficiently Computing Static Single Assignment Form and the Control Dependence Graph, ACM Transactions on Programming Languages and Systems, VO1 13, NO 4, October, le91, Pages 451.490. Efficiently computing static single assignment form and the control dependence graph (utexas.edu).

Lengauer, T. and Tarjan, R. "A Fast Algorithm for Finding Dominators in a Flowgraph", TOPLAS, 1:1 (1979) pp 121-141.

Briggs, P. and Cooper, K. and Harvey, J. and Simpson, L. "Practical Improvements to the Construction and Destruction of Static Single Assignment Form", SP&E (28:8), (1998) pp 859-881.

## 7.6 LLVM的SSA实现

### 7.6.1 LLVM中的MemorySSA

LLVM中的MemorySSA提供了一个接口，用于构建/使用内存SSA（静态单赋值形式），以便通过使用/定义图来遍历内存指令。

MemorySSA类构建了一种SSA形式，将诸如加载（loads）、存储（stores）、原子操作（atomics）和调用（calls）等内存访问指令相互关联。此外，它进行了一种简单的“堆版本控制”。每当程序中的内存状态发生变化时，我们都会生成一个新的堆版本。它生成的MemoryDef/Uses/Phis会叠加在现有指令之上。

假定原来的IR是这样的：

LLVM IR

| 1 | define i32 @main() #0 { |
| --- | --- |
| 2 | entry: |
| 3 | %call = call noalias i8* @_Znwm(i64 4) #2 |
| 4 | %0 = bitcast i8* %call to i32* |
| 5 | %call1 = call noalias i8* @_Znwm(i64 4) #2 |
| 6 | %1 = bitcast i8* %call1 to i32* |
| 7 | store i32 5, i32* %0, align 4 |
| 8 | store i32 7, i32* %1, align 4 |
| 9 | %2 = load i32* %0, align 4 |
| 10 | %3 = load i32* %1, align 4 |
| 11 | %add = add nsw i32 %2, %3 |
| 12 | ret i32 %add |
| 13 | } |

经过MemorySSA处理过之后，会加上一堆MemoryDef和MemoryUse。

LLVM IR

| 1 | define i32 @main() #0 { |
| --- | --- |
| 2 | entry: |
| 3 | ; 1 = MemoryDef(0) |
| 4 | %call = call noalias i8* @_Znwm(i64 4) #3 |
| 5 | %2 = bitcast i8* %call to i32* |
| 6 | ; 2 = MemoryDef(1) |
| 7 | %call1 = call noalias i8* @_Znwm(i64 4) #3 |
| 8 | %4 = bitcast i8* %call1 to i32* |
| 9 | ; 3 = MemoryDef(2) |
| 10 | store i32 5, i32* %2, align 4 |
| 11 | ; 4 = MemoryDef(3) |
| 12 | store i32 7, i32* %4, align 4 |
| 13 | ; MemoryUse(3) |
| 14 | %7 = load i32* %2, align 4 |
| 15 | ; MemoryUse(4) |
| 16 | %8 = load i32* %4, align 4 |
| 17 | %add = add nsw i32 %7, %8 |
| 18 | ret i32 %add |
| 19 | } |

通过这个例子，我们可以总结出内存申请（new或者malloc）和修改是MemoryDef，内存的加载并不会产生新的对象，所以会当做MemoryUse处理。

内存加载到寄存器之后，代码对寄存器的读写操作是不是MemorySSA该处理的代码？答案是普通寄存器的读写在LLVM IR里面天然就是SSA化的，所以不用额外处理了。

llvm\lib\Analysis\MemorySSA.cpp

|   | /// 调试类型定义 |
| --- | --- |
| 60 | #define DEBUG_TYPE "memoryssa" |
|   | /// PASS注册 |
| 61 |  |
| 62 | INITIALIZE_PASS_BEGIN(MemorySSAWrapperPass, "memoryssa", "Memory SSA", false, |
| 63 | true) |
| 64 | INITIALIZE_PASS_DEPENDENCY(DominatorTreeWrapperPass) |
| 65 | INITIALIZE_PASS_DEPENDENCY(AAResultsWrapperPass) |
| 66 | INITIALIZE_PASS_END(MemorySSAWrapperPass, "memoryssa", "Memory SSA", false, |
| 67 | true) |
| 68 |  |
| 69 | INITIALIZE_PASS_BEGIN(MemorySSAPrinterLegacyPass, "print-memoryssa", |
| 70 | "Memory SSA Printer", false, false) |
| 71 | INITIALIZE_PASS_DEPENDENCY(MemorySSAWrapperPass) |
| 72 | INITIALIZE_PASS_END(MemorySSAPrinterLegacyPass, "print-memoryssa", |
| 73 | "Memory SSA Printer", false, false) |
|   | /// 定义一个命令行选项，用于设置MemorySSA在进行分析时尝试走过的存储/Phi |
|   | /// 节点的最大数量。这个选项是隐藏的，并且默认值为100。 |
| 74 |  |
| 75 | static cl::opt<unsigned> MaxCheckLimit( |
| 76 | "memssa-check-limit", cl::Hidden, cl::init(100), |
| 77 | cl::desc("The maximum number of stores/phis MemorySSA" |
| 78 | "will consider trying to walk past (default = 100)")); |
|   | // 如果启用了昂贵的检查，则始终验证MemorySSA。 |
| 79 |  |
| 80 | // Always verify MemorySSA if expensive checking is enabled. |
| 81 | #ifdef EXPENSIVE_CHECKS |
|   | // 如果定义了EXPENSIVE_CHECKS，则启用MemorySSA的验证 |
| 82 | bool llvm::VerifyMemorySSA = true; |
| 83 | #else |
|   | // 否则，不启用MemorySSA的验证 |
| 84 | bool llvm::VerifyMemorySSA = false; |
| 85 | #endif |
|   | /// 定义一个命令行选项，用于在旧版pass管理器中为循环pass启用MemorySSA依赖。 |
| 86 | /// Enables memory ssa as a dependency for loop passes in legacy pass manager. |
| 87 | cl::opt<bool> llvm::EnableMSSALoopDependency( |
| 88 | "enable-mssa-loop-dependency", cl::Hidden, cl::init(true), |
| 89 | cl::desc("Enable MemorySSA dependency for loop pass manager")); |
|   | /// 定义一个命令行选项，用于启用MemorySSA的验证。 |
|   | /// 这个选项使用了静态变量VerifyMemorySSA的位置。 |
| 90 |  |
| 91 | static cl::opt<bool, true> |
| 92 | VerifyMemorySSAX("verify-memoryssa", cl::location(VerifyMemorySSA), |
| 93 | cl::Hidden, cl::desc("Enable verification of MemorySSA.")); |
| 94 |  |
| 95 | namespace llvm { |
|   | /// 定义一个用于在注释中打印Memory SSA信息的汇编注释器类。 |
| 96 |  |
| 97 | /// An assembly annotator class to print Memory SSA information in |
| 98 | /// comments. |
| 99 | class MemorySSAAnnotatedWriter : public AssemblyAnnotationWriter { |
|   | // MemorySSA类是这个注释器类的朋友，可以访问私有成员 |
| 100 | friend class MemorySSA; |
|   | // 指向MemorySSA对象的指针 |
| 101 |  |
| 102 | const MemorySSA *MSSA; |
| 103 |  |
| 104 | public: |
|   | // 构造函数，初始化MemorySSA指针 |
| 105 | MemorySSAAnnotatedWriter(const MemorySSA *M) : MSSA(M) {} |
|   | // 重写基类方法，用于在基本块开始时添加注释 |
| 106 |  |
| 107 | void emitBasicBlockStartAnnot(const BasicBlock *BB, |
| 108 | formatted_raw_ostream &OS) override { |
|   | // 如果基本块有关联的内存访问，则打印它 |
| 109 | if (MemoryAccess *MA = MSSA->getMemoryAccess(BB)) |
| 110 | OS << "; " << *MA << "\n"; |
| 111 | } |
|   | // 重写基类方法，用于在指令上添加注释 |
| 112 |  |
| 113 | void emitInstructionAnnot(const Instruction *I, |
| 114 | formatted_raw_ostream &OS) override { |
|   | // 如果指令有关联的内存访问，则打印它 |
| 115 | if (MemoryAccess *MA = MSSA->getMemoryAccess(I)) |
| 116 | OS << "; " << *MA << "\n"; |
| 117 | } |
| 118 | }; |
| 119 |  |
| 120 | } // end namespace llvm |
|   | // 匿名命名空间，用于定义一些只在当前文件中使用的类和函数。 |
| 121 |  |
| 122 | namespace { |
|   | /// MemoryLocOrCall 类用于区分内存位置（MemoryLocation）和函数调用（CallBase）。 |
|   | /// 这个类简化了那些需要“影响内存的指令和相关数据”作为键的代码。 |
|   | /// 例如，这个类被用作 use optimizer 中的 densemap 键。 |
| 123 |  |
| 124 | /// Our current alias analysis API differentiates heavily between calls and |
| 125 | /// non-calls, and functions called on one usually assert on the other. |
| 126 | /// This class encapsulates the distinction to simplify other code that wants |
| 127 | /// "Memory affecting instructions and related data" to use as a key. |
| 128 | /// For example, this class is used as a densemap key in the use optimizer. |
| 129 | class MemoryLocOrCall { |
| 130 | public: |
| 131 | bool IsCall = false; |
|   | // 构造函数，从 MemoryUseOrDef 对象创建 MemoryLocOrCall |
| 132 |  |
| 133 | MemoryLocOrCall(MemoryUseOrDef *MUD) |
| 134 | : MemoryLocOrCall(MUD->getMemoryInst()) {} |
| 135 | MemoryLocOrCall(const MemoryUseOrDef *MUD) |
| 136 | : MemoryLocOrCall(MUD->getMemoryInst()) {} |
|   | // 构造函数，从指令创建 MemoryLocOrCall |
| 137 |  |
| 138 | MemoryLocOrCall(Instruction *Inst) { |
| 139 | if (auto *C = dyn_cast<CallBase>(Inst)) { |
| 140 | IsCall = true; |
| 141 | Call = C; |
| 142 | } else { |
| 143 | IsCall = false; |
| 144 | // There is no such thing as a memorylocation for a fence inst, and it is |
| 145 | // unique in that regard. |
| 146 | if (!isa<FenceInst>(Inst)) |
| 147 | Loc = MemoryLocation::get(Inst); |
| 148 | } |
| 149 | } |
|   | // 从 MemoryLocation 创建 MemoryLocOrCall。这里加explicit， |
|   | // 主要是为了避免入参是别的类型的时候，被编译器自动插入了 |
|   | // MemoryLocation的构造函数。 |
| 150 |  |
| 151 | explicit MemoryLocOrCall(const MemoryLocation &Loc) : Loc(Loc) {} |
|   | // 获取调用信息 |
| 152 |  |
| 153 | const CallBase *getCall() const { |
| 154 | assert(IsCall); |
| 155 | return Call; |
| 156 | } |
|   | // 获取内存位置 |
| 157 |  |
| 158 | MemoryLocation getLoc() const { |
| 159 | assert(!IsCall); |
| 160 | return Loc; |
| 161 | } |
|   | // 比较两个 MemoryLocOrCall 对象是否相等 |
| 162 |  |
| 163 | bool operator==(const MemoryLocOrCall &Other) const { |
| 164 | if (IsCall != Other.IsCall) |
| 165 | return false; |
| 166 |  |
| 167 | if (!IsCall) |
| 168 | return Loc == Other.Loc; |
| 169 |  |
| 170 | if (Call->getCalledOperand() != Other.Call->getCalledOperand()) |
| 171 | return false; |
| 172 |  |
| 173 | return Call->arg_size() == Other.Call->arg_size() && |
| 174 | std::equal(Call->arg_begin(), Call->arg_end(), |
| 175 | Other.Call->arg_begin()); |
| 176 | } |
| 177 |  |
| 178 | private: |
| 179 | union { |
| 180 | const CallBase *Call; // 函数调用 |
| 181 | MemoryLocation Loc; // 内存位置 |
| 182 | }; |
| 183 | }; |
| 184 |  |
| 185 | } // end anonymous namespace |
|   | // 定义 DenseMapInfo 特化，用于 MemoryLocOrCall 类型的键 |
| 186 |  |
| 187 | namespace llvm { |
| 188 |  |
| 189 | template <> struct DenseMapInfo<MemoryLocOrCall> { |
| 190 | static inline MemoryLocOrCall getEmptyKey() { |
| 191 | return MemoryLocOrCall(DenseMapInfo<MemoryLocation>::getEmptyKey()); |
| 192 | } |
| 193 |  |
| 194 | static inline MemoryLocOrCall getTombstoneKey() { |
| 195 | return MemoryLocOrCall(DenseMapInfo<MemoryLocation>::getTombstoneKey()); |
| 196 | } |
| 197 |  |
| 198 | static unsigned getHashValue(const MemoryLocOrCall &MLOC) { |
| 199 | if (!MLOC.IsCall) |
| 200 | return hash_combine( |
| 201 | MLOC.IsCall, |
| 202 | DenseMapInfo<MemoryLocation>::getHashValue(MLOC.getLoc())); |
| 203 |  |
| 204 | hash_code hash = |
| 205 | hash_combine(MLOC.IsCall, DenseMapInfo<const Value *>::getHashValue( |
| 206 | MLOC.getCall()->getCalledOperand())); |
| 207 |  |
| 208 | for (const Value *Arg : MLOC.getCall()->args()) |
| 209 | hash = hash_combine(hash, DenseMapInfo<const Value *>::getHashValue(Arg)); |
| 210 | return hash; |
| 211 | } |
| 212 |  |
| 213 | static bool isEqual(const MemoryLocOrCall &LHS, const MemoryLocOrCall &RHS) { |
| 214 | return LHS == RHS; |
| 215 | } |
| 216 | }; |
| 217 |  |
| 218 | } // end namespace llvm |
|   | /// 检查是否可以将 Use 提升到 MayClobber 之上。 |
|   | /// 这个函数只进行单向检查，不会反过来检查。 |
|   | /// 这个函数假设，对于 MemorySSA 的目的，Use 直接跟在 MayClobber 后面， |
|   | /// 它们之间没有可能篡改内存的操作。 |
|   | /// （可能篡改内存的操作包括内存屏障、别名存储等。） |
| 219 |  |
| 220 | /// This does one-way checks to see if Use could theoretically be hoisted above |
| 221 | /// MayClobber. This will not check the other way around. |
| 222 | /// |
| 223 | /// This assumes that, for the purposes of MemorySSA, Use comes directly after |
| 224 | /// MayClobber, with no potentially clobbering operations in between them. |
| 225 | /// (Where potentially clobbering ops are memory barriers, aliased stores, etc.) |
| 226 | static bool areLoadsReorderable(const LoadInst *Use, |
| 227 | const LoadInst *MayClobber) { |
| 228 | bool VolatileUse = Use->isVolatile(); // 检查 Use 是否是 volatile 操作 |
| 229 | bool VolatileClobber = MayClobber->isVolatile(); |
|   | // 如果两个操作都是 volatile，则它们不能被重新排序。 |
| 230 | // Volatile operations may never be reordered with other volatile operations. |
| 231 | if (VolatileUse && VolatileClobber) |
| 232 | return false; |
|   | // 否则，volatile 属性在此并不重要。根据语言规范： |
| 234 | // 'optimizers may change the order of volatile operations relative to |
| 235 | // non-volatile operations.' |
|   | // 即优化器可以改变 volatile 操作与非 volatile 操作的顺序。 |
| 236 | // Otherwise, volatile doesn't matter here. From the language reference: |
| 237 | // 'optimizers may change the order of volatile operations relative to |
| 238 | // non-volatile operations.'" |
|   | // 如果一个加载操作是 seq_cst（顺序一致）的，它不能被移动到其他加载操作之上。 |
|   | // 如果它的顺序更弱，它可以被移动到其他加载操作之上。 |
|   | // 我们需要确保 MayClobber 不是一个 acquire（获取）加载，因为加载不能被移动到 |
|   | // acquire 加载之上。 |
|   | // 注意，这明确 *允许* 相同地址的单调（或更弱）加载操作的自由重新排序。 |
| 239 | // If a load is seq_cst, it cannot be moved above other loads. If its ordering |
| 240 | // is weaker, it can be moved above other loads. We just need to be sure that |
| 241 | // MayClobber isn't an acquire load, because loads can't be moved above |
| 242 | // acquire loads. |
| 243 | // |
| 244 | // Note that this explicitly *does* allow the free reordering of monotonic (or |
| 245 | // weaker) loads of the same address. |
|   | // 检查 Use 是否是顺序一致的加载 |
| 246 | bool SeqCstUse = Use->getOrdering() == AtomicOrdering::SequentiallyConsistent; |
|   | // 检查 MayClobber 是否至少是 acquire 顺序 |
| 247 | bool MayClobberIsAcquire = isAtLeastOrStrongerThan(MayClobber->getOrdering(), |
| 248 | AtomicOrdering::Acquire); |
|   | // 如果 Use 是顺序一致的或者 MayClobber 是 acquire 的，则不允许重新排序 |
| 249 | return !(SeqCstUse || MayClobberIsAcquire); |
| 251 |  |
| 255 | } |
| 256 | namespace { |
|   | // ClobberAlias 结构体用于表示一个指令是否覆盖另一个指令的别名信息。 |
| 257 | struct ClobberAlias { |
| 258 | bool IsClobber; // 布尔值，表示是否发生了覆盖 |
| 268 | Optional<AliasResult> AR; // 可选的 AliasResult，存储别名关系的详细信息 |
| 269 |  |
| 281 | }; |
| 282 | } // end anonymous namespace |
|   | // 模板函数，用于查询一个内存定义是否覆盖了另一个指令的内存位置。 |
|   | // AliasAnalysisType 参数化类型，表示别名分析的类型。 |
| 283 | // Return a pair of {IsClobber (bool), AR (AliasResult)}. It relies on AR being |
| 284 | // ignored if IsClobber = false. |
| 285 | template <typename AliasAnalysisType> |
| 286 | static ClobberAlias |
| 287 | instructionClobbersQuery(const MemoryDef *MD, const MemoryLocation &UseLoc, |
| 288 | const Instruction *UseInst, AliasAnalysisType &AA) { |
|   | // 获取定义内存的指令 |
| 289 | Instruction *DefInst = MD->getMemoryInst(); |
|   | // 断言获取的指令非空 |
| 290 | assert(DefInst && "Defining instruction not actually an instruction"); |
|   | // 尝试将使用指令转换为函数调用 |
| 291 | const auto *UseCall = dyn_cast<CallBase>(UseInst); |
| 292 | Optional<AliasResult> AR; // 可选的别名结果 |
| 293 | if (const IntrinsicInst *II = dyn_cast<IntrinsicInst>(DefInst)) { |
|   | // 这些内联汇编指令虽然会影响内存，但它们主要是作为标记使用的 |
|   | // FIXME: 我们可能根本不希望 MemorySSA 模型包含这些（包括为它们创建 MemoryAccesses）： |
|   | // 我们最终只是在发明不存在的覆盖。有关详细信息，请参见 D43269。 |
| 294 | // These intrinsics will show up as affecting memory, but they are just |
| 295 | // markers, mostly. |
| 296 | // |
| 297 | // FIXME: We probably don't actually want MemorySSA to model these at all |
| 298 | // (including creating MemoryAccesses for them): we just end up inventing |
| 299 | // clobbers where they don't really exist at all. Please see D43269 for |
| 300 | // context. |
| 301 | switch (II->getIntrinsicID()) { |
| 302 | case Intrinsic::lifetime_start: // 内存生命周期开始 |
| 303 | if (UseCall) |
| 304 | return {false, NoAlias}; // 如果使用指令是函数调用，则不覆盖 |
| 305 | AR = AA.alias(MemoryLocation(II->getArgOperand(1)), UseLoc); // 别名分析 |
| 306 | return {AR != NoAlias, AR}; // 返回是否覆盖及别名结果 |
| 307 | case Intrinsic::lifetime_end: // 内存生命周期结束 |
| 308 | case Intrinsic::invariant_start: // 内存不变开始 |
| 309 | case Intrinsic::invariant_end: // 内存不变结束 |
| 310 | case Intrinsic::assume: // 假设 |
| 311 | return {false, NoAlias}; // 这些内联汇编指令不覆盖 |
| 312 | case Intrinsic::dbg_addr: // 调试地址 |
| 313 | case Intrinsic::dbg_declare: // 调试声明 |
| 314 | case Intrinsic::dbg_label: // 调试标签 |
| 315 | case Intrinsic::dbg_value: // 调试值 |
|   | // 调试信息不应该有关联的定义 |
| 316 | llvm_unreachable("debuginfo shouldn't have associated defs!"); |
| 317 | default: |
| 318 | break; // 默认情况，不处理 |
| 326 | } |
| 327 |  |
| 332 | } |
|   | // 如果使用指令是函数调用 |
| 333 | if (UseCall) { |
| 334 | ModRefInfo I = AA.getModRefInfo(DefInst, UseCall); // 获取修改和引用信息 |
| 335 | AR = isMustSet(I) ? MustAlias : MayAlias; // 根据修改和引用信息设置别名结果 |
| 336 | return {isModOrRefSet(I), AR}; // 返回是否覆盖及别名结果 |
| 337 | } |
|   | // 如果定义指令和使用指令都是加载指令 |
| 338 | if (auto *DefLoad = dyn_cast<LoadInst>(DefInst)) |
| 339 | if (auto *UseLoad = dyn_cast<LoadInst>(UseInst)) |
|   | // 如果加载指令不能重新排序，则它们相互覆盖 |
| 340 | return {!areLoadsReorderable(UseLoad, DefLoad), MayAlias}; |
|   | // 获取定义指令对使用位置的修改和引用信息 |
| 341 | ModRefInfo I = AA.getModRefInfo(DefInst, UseLoc); |
| 342 | AR = isMustSet(I) ? MustAlias : MayAlias; // 根据修改和引用信息设置别名结果 |
| 343 | return {isModSet(I), AR}; // 返回是否覆盖及别名结果 |
| 348 |  |
| 350 |  |
| 355 | } |
|   | // 模板函数，用于查询一个内存定义是否覆盖了另一个内存使用或定义的内存位置。 |
|   | // AliasAnalysisType 参数化类型，表示别名分析的类型。 |
| 356 | template <typename AliasAnalysisType> |
| 357 | static ClobberAlias instructionClobbersQuery(MemoryDef *MD,  // 指向内存定义的指针 |
|   | // 指向内存使用或定义的指针 |
| 358 | const MemoryUseOrDef *MU, |
|   | // 包含内存位置或函数调用信息的对象 |
| 359 | const MemoryLocOrCall &UseMLOC, |
| 360 | AliasAnalysisType &AA) { // 别名分析对象的引用 |
|   | // FIXME: 这是一个临时解决方案，允许在推动 MemoryLocOrCall 到不同地方时存在单一的 |
|   | // instructionClobbersQuery 函数。 |
| 361 | // FIXME: This is a temporary hack to allow a single instructionClobbersQuery |
| 362 | // to exist while MemoryLocOrCall is pushed through places. |
| 363 | if (UseMLOC.IsCall) |
|   | // 如果 UseMLOC 表示一个函数调用，则调用不带 MemoryLocation 参数的 |
|   | // instructionClobbersQuery 函数。 |
| 364 | return instructionClobbersQuery(MD, MemoryLocation(), MU->getMemoryInst(), |
| 365 | AA); |
|   | // 否则，调用带 MemoryLocation 参数的 instructionClobbersQuery 函数。 |
| 366 | return instructionClobbersQuery(MD, UseMLOC.getLoc(), MU->getMemoryInst(), |
| 367 | AA); |
| 369 | } |
|   | // 当 MD 可能与 MU 别名时返回 true，否则返回 false。 |
|   | // 这个函数用于检查一个内存定义（MD）是否可能覆盖 |
|   | //（即与）一个内存使用或定义（MU）相同的内存位置。 |
| 370 | // Return true when MD may alias MU, return false otherwise. |
| 371 | bool MemorySSAUtil::defClobbersUseOrDef(MemoryDef *MD, // 指向内存定义的指针 |
| 372 | const MemoryUseOrDef *MU, // 指向内存使用或定义的指针 |
| 373 | AliasAnalysis &AA // 别名分析对象的引用 |
| 376 | ) { |
|   | // 使用 instructionClobbersQuery 函数来查询 MD 是否覆盖 MU。 |
|   | // 这里将 MU 包装为 MemoryLocOrCall 对象，然后传递给 instructionClobbersQuery 函数。 |
|   | // instructionClobbersQuery 函数返回一个 ClobberAlias 结构体，我们只关心 IsClobber 字段， |
|   | // 它表示是否发生了覆盖。 |
| 377 | return instructionClobbersQuery(MD, MU, MemoryLocOrCall(MU), AA).IsClobber; |
| 382 | } |
| 383 | namespace { |
|   | // UpwardsMemoryQuery 结构体用于在内存SSA分析中向上查询内存访问信息。 |
| 384 | struct UpwardsMemoryQuery { |
|   | // 如果原始查询是针对函数调用开始的，则为 true。 |
| 385 | // True if our original query started off as a call |
| 386 | bool IsCall = false; |
|   | // 我们开始查询时使用的指针位置。如果 IsCall 为 true，则该值为空。 |
| 387 | // The pointer location we started the query with. This will be empty if |
| 388 | // IsCall is true. |
| 389 | MemoryLocation StartingLoc; |
|   | // 这是我们进行查询的指令。 |
| 390 | // This is the instruction we were querying about. |
| 391 | const Instruction *Inst = nullptr; |
|   | // 这是实际调用时使用的 MemoryAccess，用于测试局部支配关系。 |
| 392 | // The MemoryAccess we actually got called with, used to test local domination |
| 393 | const MemoryAccess *OriginalAccess = nullptr; |
|   | // 可选的别名结果，默认为 MayAlias，表示可能别名。 |
| 394 | Optional<AliasResult> AR = MayAlias; |
|   | // 是否跳过自身访问的标记。 |
| 395 | bool SkipSelfAccess = false; |
|   | // 默认构造函数。 |
| 396 | UpwardsMemoryQuery() = default; |
|   | // 构造函数，接受一个指令和一个内存访问对象。 |
| 397 | UpwardsMemoryQuery(const Instruction *Inst, const MemoryAccess *Access) |
| 398 | : IsCall(isa<CallBase>(Inst)), Inst(Inst), OriginalAccess(Access) { |
| 399 | if (!IsCall) |
| 400 | StartingLoc = MemoryLocation::get(Inst); |
| 405 |  |
| 410 | } |
| 411 | }; |
| 412 | } // end anonymous namespace |
|   | // 静态函数，用于检查给定的内存定义（MD）是否表示特定内存位置（Loc）的生命周期结束。 |
| 413 | static bool lifetimeEndsAt(MemoryDef *MD, const MemoryLocation &Loc, |
| 414 | BatchAAResults &AA) { |
|   | // 获取内存定义关联的指令。 |
| 415 | Instruction *Inst = MD->getMemoryInst(); |
|   | // 尝试将指令转换为内联汇编指令（IntrinsicInst）。 |
| 416 | if (IntrinsicInst *II = dyn_cast<IntrinsicInst>(Inst)) { |
|   | // 根据内联汇编指令的ID进行判断。 |
| 417 | switch (II->getIntrinsicID()) { |
| 418 | case Intrinsic::lifetime_end: |
|   | // 使用别名分析（AA）来检查 lifetime.end 指令的参数（通常是指向内存的指针） |
|   | // 和给定的内存位置（Loc）是否别名。 |
|   | // 如果是 MustAlias，表示它们指向相同的内存位置，即生命周期在此结束。 |
| 419 | return AA.alias(MemoryLocation(II->getArgOperand(1)), Loc) == MustAlias; |
| 420 | default: // 对于其他类型的内联汇编指令，返回 false。 |
| 421 | return false; |
| 422 | } |
| 423 | } |
|   | // 如果指令不是内联汇编指令，也返回 false。 |
| 424 | return false; |
| 425 | } |
|   | // 模板函数，用于检查给定的指令是否可以简单地优化为“live on entry”。 |
|   | // “live on entry”直译是在该函数内存活的意思，也就是生命周期从函数开始到函数结束。 |
|   | // 在 LLVM 编译器框架中，"LiveOnEntry"（LOE）是一个内存SSA（Static Single |
|   | // Assignment）的概念，用于描述某些内存定义在函数的开始就已经存在，而不是在函数内 |
|   | // 部的某个点被定义。这意味着这些内存位置在函数开始执行时就已经被初始化，且在函 |
|   | // 数的整个执行过程中不会被修改。 |
|   | // ### LiveOnEntry 的特点： |
| 428 | // |
|   | // 1. **全局和静态变量**：通常，全局变量和静态变量在程序的生命周期内保持存在，它们 |
|   | // 的值在函数开始执行时就已经确定，因此它们通常被认为是 LiveOnEntry。 |
| 429 | // |
|   | // 2. **常量内存**：如果一个内存位置始终指向一个常量值，那么这个内存位置也可以被认 |
|   | // 为是 LiveOnEntry。这是因为常量值在程序执行期间不会改变。 |
| 430 | // |
|   | // 3. **函数参数**：函数的参数也可以被视为 LiveOnEntry，因为它们在函数调用时就已经 |
|   | // 被传递进来，且在函数的整个执行过程中，它们的值不会被修改（除非函数内部有显式的 |
|   | // 修改操作）。 |
| 431 | // |
|   | // 4. **不变性**：在 LLVM 中，如果一个加载指令被标记为 `invariant.load`，这意味着该 |
|   | // 加载操作加载的内存位置在当前函数的执行过程中不会改变。这样的加载指令可以被认为 |
|   | // 是 LiveOnEntry。 |
| 432 | // |
|   | // ### LiveOnEntry 的优化： |
| 439 | // |
|   | // - **指令重排**：由于 LiveOnEntry 的内存定义在函数开始时就已经存在，编译器可以更自 |
|   | // 由地重新安排这些内存定义相关的指令，以优化代码的执行顺序，提高性能。 |
|   | // - **消除冗余操作**：如果一个指令的结果在函数的整个执行过程中都不会改变，编译器可 |
|   | // 以消除对该内存位置的重复读写操作，减少不必要的计算和内存访问。 |
|   | // - **内存优化**：LiveOnEntry 的信息可以帮助编译器更好地理解程序的内存使用模式，从而 |
|   | // 进行更有效的内存优化。 |
| 445 |  |
| 446 | // |
|   | // 在 LLVM 的内存SSA中，LiveOnEntry 的概念是内存优化和代码生成过程中的一个重 |
|   | // 要部分，它允许编译器做出更准确的优化决策。 |
| 447 | template <typename AliasAnalysisType> |
| 448 | static bool isUseTriviallyOptimizableToLiveOnEntry(AliasAnalysisType &AA, |
| 449 | const Instruction *I) { |
|   | // 检查指令 I 是否是一个加载指令（LoadInst）。 |
|   | // 如果是加载指令，进一步检查以下条件： |
|   | // 1. 指令 I 是否有元数据 LLVMContext::MD_invariant_load，这表示加载的内存位置在函数执行期间不会改变。 |
|   | // 2. 使用别名分析对象 AA 检查加载的内存位置是否指向常量内存。 |
|   | // 如果满足上述任一条件，说明加载的内存不会被覆盖，因此可以安全地优化为“live on entry”。 |
| 450 | // If the memory can't be changed, then loads of the memory can't be |
| 451 | // clobbered. |
| 452 | return isa<LoadInst>(I) && (I->hasMetadata(LLVMContext::MD_invariant_load) || |
| 453 | AA.pointsToConstantMemory(MemoryLocation( |
| 454 | cast<LoadInst>(I)->getPointerOperand()))); |
| 458 | } |
|   | /// 验证 `Start` 被 `ClobberAt` 覆盖，并且 `Start` 和 `ClobberAt` 之间的任何操作 |
|   | /// 都不会覆盖 `Start`。 |
| 459 | /// |
|   | /// 这个函数设计得尽可能简单和自包含。因为它不使用缓存等，所以可能相对较昂贵。 |
| 460 | /// |
|   | /// \param Start    我们想要从其开始遍历的 MemoryAccess。 |
|   | /// \param ClobberAt 覆盖 Start 的操作。 |
|   | /// \param StartLoc  Start 的 MemoryLocation。 |
|   | /// \param MSSA     Start 和 ClobberAt 所属的 MemorySSA 实例。 |
|   | /// \param Query    我们用于搜索的 UpwardsMemoryQuery。 |
|   | /// \param AA       我们用于搜索的 AliasAnalysis。 |
|   | /// \param AllowImpreciseClobber 除非我们进行放宽验证，否则始终为 false。 |
| 461 | /// Verifies that `Start` is clobbered by `ClobberAt`, and that nothing |
| 462 | /// inbetween `Start` and `ClobberAt` can clobbers `Start`. |
| 463 | /// |
| 464 | /// This is meant to be as simple and self-contained as possible. Because it |
| 465 | /// uses no cache, etc., it can be relatively expensive. |
| 466 | /// |
| 467 | /// \param Start     The MemoryAccess that we want to walk from. |
| 468 | /// \param ClobberAt A clobber for Start. |
| 469 | /// \param StartLoc  The MemoryLocation for Start. |
| 470 | /// \param MSSA      The MemorySSA instance that Start and ClobberAt belong to. |
| 471 | /// \param Query     The UpwardsMemoryQuery we used for our search. |
| 472 | /// \param AA        The AliasAnalysis we used for our search. |
| 473 | /// \param AllowImpreciseClobber Always false, unless we do relaxed verify. |
| 474 | template <typename AliasAnalysisType> |
| 475 | LLVM_ATTRIBUTE_UNUSED static void |
| 476 | checkClobberSanity(const MemoryAccess *Start, MemoryAccess *ClobberAt, |
| 477 | const MemoryLocation &StartLoc, const MemorySSA &MSSA, |
| 478 | const UpwardsMemoryQuery &Query, AliasAnalysisType &AA, |
| 479 | bool AllowImpreciseClobber = false) { |
|   | // 断言 ClobberAt 支配 Start，否则抛出错误。 |
| 480 | assert(MSSA.dominates(ClobberAt, Start) && "Clobber doesn't dominate start?"); |
|   | // 如果 Start 是一个 LiveOnEntry 定义，那么它必须自己覆盖自己。 |
| 481 | if (MSSA.isLiveOnEntryDef(Start)) { |
| 482 | assert(MSSA.isLiveOnEntryDef(ClobberAt) && |
| 483 | "liveOnEntry must clobber itself"); |
| 484 | return; |
| 487 | } |
| 488 | bool FoundClobber = false; |
| 489 | DenseSet<ConstMemoryAccessPair> VisitedPhis; |
| 490 | SmallVector<ConstMemoryAccessPair, 8> Worklist; |
| 491 | Worklist.emplace_back(Start, StartLoc); |
|   | // 遍历从 Start 到 ClobberAt 的所有路径，同时寻找覆盖。如果找到，就报错。 |
| 492 | // Walk all paths from Start to ClobberAt, while looking for clobbers. If one |
| 493 | // is found, complain. |
| 494 | while (!Worklist.empty()) { |
| 495 | auto MAP = Worklist.pop_back_val(); |
|   | // 我们只关心从 Start 到 ClobberAt 之间没有操作覆盖 Start。 |
|   | // 重新访问节点不会给我们带来新信息。 |
| 496 | // All we care about is that nothing from Start to ClobberAt clobbers Start. |
| 497 | // We learn nothing from revisiting nodes. |
| 498 | if (!VisitedPhis.insert(MAP).second) |
| 499 | continue; |
| 500 | for (const auto *MA : def_chain(MAP.first)) { |
| 501 | if (MA == ClobberAt) { |
| 502 | if (const auto *MD = dyn_cast<MemoryDef>(MA)) { |
|   | // 检查是否覆盖，但不使用短路逻辑，因为 MD 可能只对 N 个 MemoryLocations |
|   | // 中的一个覆盖。 |
| 503 | // instructionClobbersQuery isn't essentially free, so don't use `|=`, |
| 504 | // since it won't let us short-circuit. |
| 505 | // |
| 506 | // Also, note that this can't be hoisted out of the `Worklist` loop, |
| 507 | // since MD may only act as a clobber for 1 of N MemoryLocations. |
| 508 | FoundClobber = FoundClobber || MSSA.isLiveOnEntryDef(MD); |
| 509 | if (!FoundClobber) { |
| 510 | ClobberAlias CA = |
| 511 | instructionClobbersQuery(MD, MAP.second, Query.Inst, AA); |
| 512 | if (CA.IsClobber) { |
| 513 | FoundClobber = true; |
| 514 | // Not used: CA.AR; |
| 515 | } |
| 516 | } |
| 517 | } |
| 518 | break; |
| 519 | } |
|   | // 我们不应该在到达 ClobberAt 之前遇到 liveOnEntry。 |
| 520 | // We should never hit liveOnEntry, unless it's the clobber. |
| 521 | assert(!MSSA.isLiveOnEntryDef(MA) && "Hit liveOnEntry before clobber?"); |
| 522 | if (const auto *MD = dyn_cast<MemoryDef>(MA)) { |
|   | // 如果 Start 是一个定义，跳过自己。 |
| 523 | // If Start is a Def, skip self. |
| 524 | if (MD == Start) |
| 525 | continue; |
| 526 | assert(!instructionClobbersQuery(MD, MAP.second, Query.Inst, AA) |
| 527 | .IsClobber && |
| 528 | "Found clobber before reaching ClobberAt!"); |
| 529 | continue; |
| 537 | } |
| 538 | if (const auto *MU = dyn_cast<MemoryUse>(MA)) { |
| 539 | (void)MU; |
| 540 | assert (MU == Start && |
| 541 | "Can only find use in def chain if Start is a use"); |
| 542 | continue; |
| 548 | } |
| 549 | assert(isa<MemoryPhi>(MA)); |
| 550 | Worklist.append( |
| 551 | upward_defs_begin({const_cast<MemoryAccess *>(MA), MAP.second}, |
| 552 | MSSA.getDomTree()), |
| 553 | upward_defs_end()); |
| 568 | } |
| 569 |  |
| 573 | } |
|   | // 如果在优化后进行验证，ClobberAt 可能是保守的覆盖，我们现在可以推断它不是真正的覆盖。 |
|   | // 如果是这种情况，不要因为那个而使验证失败。 |
| 574 | // If the verify is done following an optimization, it's possible that |
| 575 | // ClobberAt was a conservative clobbering, that we can now infer is not a |
| 576 | // true clobbering access. Don't fail the verify if that's the case. |
| 577 | // We do have accesses that claim they're optimized, but could be optimized |
| 578 | // further. Updating all these can be expensive, so allow it for now (FIXME). |
| 579 | if (AllowImpreciseClobber) |
| 580 | return; |
|   | // 如果 ClobberAt 是一个 MemoryPhi，我们可以假设它上面的某个操作是覆盖。 |
|   | // 否则，ClobberAt 应该在某个时候起到了覆盖的作用。 |
| 581 | // If ClobberAt is a MemoryPhi, we can assume something above it acted as a |
| 582 | // clobber. Otherwise, `ClobberAt` should've acted as a clobber at some point. |
| 583 | assert((isa<MemoryPhi>(ClobberAt) || FoundClobber) && |
| 584 | "ClobberAt never acted as a clobber"); |
| 585 | } |
| 586 | namespace { |
|   | /// ClobberWalker 类用于遍历和尝试优化内存覆盖。 |
| 587 | /// Our algorithm for walking (and trying to optimize) clobbers, all wrapped up |
| 588 | /// in one class. |
| 589 | template <class AliasAnalysisType> class ClobberWalker { |
| 590 | /// Save a few bytes by using unsigned instead of size_t. |
| 591 | using ListIndex = unsigned; |
|   | /// DefPath 结构体表示一系列连续的 MemoryDefs，可能以 MemoryPhi 结尾。 |
| 592 | /// Represents a span of contiguous MemoryDefs, potentially ending in a |
| 593 | /// MemoryPhi. |
| 594 | struct DefPath { |
| 595 | MemoryLocation Loc; |
| 596 | // Note that, because we always walk in reverse, Last will always dominate |
| 597 | // First. Also note that First and Last are inclusive. |
| 598 | MemoryAccess *First; |
| 599 | MemoryAccess *Last; |
| 600 | Optional<ListIndex> Previous; |
|   | /// 构造函数，初始化 DefPath。 |
| 601 | DefPath(const MemoryLocation &Loc, MemoryAccess *First, MemoryAccess *Last, |
| 602 | Optional<ListIndex> Previous) |
| 603 | : Loc(Loc), First(First), Last(Last), Previous(Previous) {} |
|   | /// 另一个构造函数，用于初始化只有单个内存访问的 DefPath。 |
| 604 | DefPath(const MemoryLocation &Loc, MemoryAccess *Init, |
| 605 | Optional<ListIndex> Previous) |
| 606 | : DefPath(Loc, Init, Init, Previous) {} |
| 613 | }; |
|   | /// 类成员变量。 |
| 614 | const MemorySSA &MSSA; |
| 615 | AliasAnalysisType &AA; |
| 616 | DominatorTree &DT; |
| 617 | UpwardsMemoryQuery *Query; |
| 618 | unsigned *UpwardWalkLimit; |
| 619 | // Phi optimization bookkeeping |
| 620 | SmallVector<DefPath, 32> Paths; |
| 621 | DenseSet<ConstMemoryAccessPair> VisitedPhis; |
|   | /// 查找从 MemoryPhi 可以合法优化到的最近的内存定义或内存Phi。 |
| 622 | /// Find the nearest def or phi that `From` can legally be optimized to. |
| 623 | const MemoryAccess *getWalkTarget(const MemoryPhi *From) const { |
| 624 | assert(From->getNumOperands() && "Phi with no operands?"); |
| 625 | BasicBlock *BB = From->getBlock(); |
| 626 | MemoryAccess *Result = MSSA.getLiveOnEntryDef(); |
| 627 | DomTreeNode *Node = DT.getNode(BB); |
| 628 | while ((Node = Node->getIDom())) { |
| 629 | auto *Defs = MSSA.getBlockDefs(Node->getBlock()); |
| 630 | if (Defs) |
| 631 | return &*Defs->rbegin(); |
| 632 | } |
| 633 | return Result; |
| 634 | } |
|   | /// 结构体，用于存储向上遍历至Phi节点或覆盖（Clobber）的结果。 |
| 635 | /// Result of calling walkToPhiOrClobber. |
| 636 | struct UpwardsWalkResult { |
|   | /// 遍历的结果。可能是覆盖、遍历的最后一个元素，或两者都有。当找到覆盖时， |
|   | /// 包含别名信息。 |
| 637 | /// The "Result" of the walk. Either a clobber, the last thing we walked, or |
| 638 | /// both. Include alias info when clobber found. |
| 639 | MemoryAccess *Result; |
|   | /// 指示是否已知为覆盖。 |
| 640 | bool IsKnownClobber; |
|   | /// 可选的别名结果。 |
| 641 | Optional<AliasResult> AR; |
| 642 | }; |
|   | /// 从Desc.Last开始，向上遍历至下一个Phi节点或覆盖。 |
|   | /// 这将更新Desc.Last。（可选）也可以在StopAt处停止。 |
| 643 | /// |
|   | /// 这不检查StopAt是否为覆盖。 |
| 644 | /// Walk to the next Phi or Clobber in the def chain starting at Desc.Last. |
| 645 | /// This will update Desc.Last as it walks. It will (optionally) also stop at |
| 646 | /// StopAt. |
| 647 | /// |
| 648 | /// This does not test for whether StopAt is a clobber |
| 656 |  |
| 662 |  |
| 663 | UpwardsWalkResult |
| 664 | walkToPhiOrClobber(DefPath &Desc, const MemoryAccess *StopAt = nullptr, |
| 665 | const MemoryAccess *SkipStopAt = nullptr) const { |
|   | /// 断言Desc.Last不是MemoryUse类型。 |
| 666 | assert(!isa<MemoryUse>(Desc.Last) && "Uses don't exist in my world"); |
|   | /// 断言UpwardWalkLimit有效。 |
| 667 | assert(UpwardWalkLimit && "Need a valid walk limit"); |
| 668 | bool LimitAlreadyReached = false; |
|   | // 如果(*UpwardWalkLimit)为0，由于tryOptimizePhi中的循环，将其设置为1。 |
|   | // 这将不会进行任何alias()调用。它要么在下面的循环的第一个迭代中返回， |
|   | // 要么如果所有def链都没有MemoryDefs，则将其重新设置为0。 |
| 669 | // (*UpwardWalkLimit) may be 0 here, due to the loop in tryOptimizePhi. Set |
| 670 | // it to 1. This will not do any alias() calls. It either returns in the |
| 671 | // first iteration in the loop below, or is set back to 0 if all def chains |
| 672 | // are free of MemoryDefs. |
| 673 | if (!*UpwardWalkLimit) { |
| 674 | *UpwardWalkLimit = 1; |
| 675 | LimitAlreadyReached = true; |
| 678 | } |
| 679 | for (MemoryAccess *Current : def_chain(Desc.Last)) { |
| 680 | Desc.Last = Current; |
| 681 | if (Current == StopAt || Current == SkipStopAt) |
| 682 | return {Current, false, MayAlias}; |
| 683 | if (auto *MD = dyn_cast<MemoryDef>(Current)) { |
| 684 | if (MSSA.isLiveOnEntryDef(MD)) |
| 685 | return {MD, true, MustAlias}; |
| 686 | if (!--*UpwardWalkLimit) |
| 687 | return {Current, true, MayAlias}; |
| 688 | ClobberAlias CA = |
| 689 | instructionClobbersQuery(MD, Desc.Loc, Query->Inst, AA); |
| 690 | if (CA.IsClobber) |
| 691 | return {MD, true, CA.AR}; |
| 692 | } |
| 693 |  |
| 695 | } |
| 696 | if (LimitAlreadyReached) |
| 697 | *UpwardWalkLimit = 0; |
|   | /// 断言Desc.Last是MemoryPhi类型。 |
| 698 | assert(isa<MemoryPhi>(Desc.Last) && |
| 699 | "Ended at a non-clobber that's not a phi?"); |
| 700 | return {Desc.Last, false, MayAlias}; |
| 701 | } |
|   | /// 为Phi节点添加搜索。 |
| 702 | void addSearches(MemoryPhi *Phi, SmallVectorImpl<ListIndex> &PausedSearches, |
| 703 | ListIndex PriorNode) { |
| 704 | auto UpwardDefs = make_range( |
| 705 | upward_defs_begin({Phi, Paths[PriorNode].Loc}, DT), upward_defs_end()); |
| 706 | for (const MemoryAccessPair &P : UpwardDefs) { |
| 707 | PausedSearches.push_back(Paths.size()); |
| 708 | Paths.emplace_back(P.second, P.first, PriorNode); |
| 709 | } |
| 710 |  |
| 715 | } |
|   | /// 表示在找到覆盖后终止的搜索。这个覆盖可能在LastNode..SearchStart的路径中， |
|   | /// 也可能不在，因为它可能是从缓存中检索到的。 |
| 716 | /// Represents a search that terminated after finding a clobber. This clobber |
| 717 | /// may or may not be present in the path of defs from LastNode..SearchStart, |
| 718 | /// since it may have been retrieved from cache. |
| 719 | struct TerminatedPath { |
|   | /// 覆盖的内存访问。 |
| 720 | MemoryAccess *Clobber; |
|   | /// 最后一个节点的索引。 |
| 721 | ListIndex LastNode; |
| 722 | }; |
|   | /// 获取一个阻止我们优化到给定Phi节点的访问。 |
| 723 | /// |
|   | /// PausedSearches是一个数组，包含Paths数组中的索引。它的传入值是停在最后一个 |
|   | /// Phi优化目标上的搜索的索引。 |
|   | /// 它的状态在函数返回后是未指定的。 |
| 727 |  |
| 731 |  |
| 736 |  |
| 738 | /// |
|   | /// 如果这个函数返回None，NewPaused是一个在StopWhere终止的搜索的向量。否则， |
|   | /// NewPaused的状态是未指定的。 |
| 739 | /// Get an access that keeps us from optimizing to the given phi. |
| 740 | /// |
| 741 | /// PausedSearches is an array of indices into the Paths array. Its incoming |
| 742 | /// value is the indices of searches that stopped at the last phi optimization |
| 743 | /// target. It's left in an unspecified state. |
| 744 |  |
| 752 |  |
| 753 | /// |
| 754 | /// If this returns None, NewPaused is a vector of searches that terminated |
| 755 | /// at StopWhere. Otherwise, NewPaused is left in an unspecified state. |
| 756 | Optional<TerminatedPath> |
| 757 | getBlockingAccess(const MemoryAccess *StopWhere, |
| 758 | SmallVectorImpl<ListIndex> &PausedSearches, |
| 759 | SmallVectorImpl<ListIndex> &NewPaused, |
| 760 | SmallVectorImpl<TerminatedPath> &Terminated) { |
|   | /// 断言PausedSearches不为空。 |
| 761 | assert(!PausedSearches.empty() && "No searches to continue?"); |
|   | // 在这里，广度优先搜索（BFS）和深度优先搜索（DFS）没有太大区别， |
|   | // 所以我们使用PausedSearches作为栈来进行DFS。 |
| 762 | // BFS vs DFS really doesn't make a difference here, so just do a DFS with |
| 763 | // PausedSearches as our stack. |
| 764 | while (!PausedSearches.empty()) { |
| 765 | ListIndex PathIndex = PausedSearches.pop_back_val(); |
| 766 | DefPath &Node = Paths[PathIndex]; |
|   | // 如果我们已经用这个MemoryLocation访问过这条路径，我们就不需要再次访问。 |
| 770 |  |
| 772 | // |
|   | // 注意：我们只是丢弃这些路径，使得缓存行为变得不一致。例如，给定一个菱形： |
| 773 | //  A |
| 774 | // B C |
| 775 | //  D |
| 779 |  |
| 781 |  |
| 782 | // |
|   | // ...如果我们走D, B, A, C，我们只会为A, B和D缓存Phi优化的结果；C会被跳过，因为它在这里终止了。 |
|   | // 这可能不是最糟糕的事情，因为： |
|   | //   - 我们通常以自顶向下的顺序查询事物，所以如果我们在不需要{C, MemLoc}的缓存条目的情况下走到了D， |
|   | //     那么这些缓存条目最终可能不会被使用。 |
|   | //   - 我们仍然为A缓存了东西，所以C只需要向上走一点。 |
|   | // 如果这种行为变得有问题，我们可以在不增加太多额外工作的情况下修复。 |
| 783 | // If we've already visited this path with this MemoryLocation, we don't |
| 784 | // need to do so again. |
| 785 | // |
| 786 | // NOTE: That we just drop these paths on the ground makes caching |
| 787 | // behavior sporadic. e.g. given a diamond: |
| 788 | //  A |
| 789 | // B C |
| 790 | //  D |
| 794 |  |
| 799 |  |
| 801 | // |
| 802 | // ...If we walk D, B, A, C, we'll only cache the result of phi |
| 803 | // optimization for A, B, and D; C will be skipped because it dies here. |
| 804 | // This arguably isn't the worst thing ever, since: |
| 805 | //   - We generally query things in a top-down order, so if we got below D |
| 806 | //     without needing cache entries for {C, MemLoc}, then chances are |
| 807 | //     that those cache entries would end up ultimately unused. |
| 808 | //   - We still cache things for A, so C only needs to walk up a bit. |
| 809 | // If this behavior becomes problematic, we can fix without a ton of extra |
| 810 | // work. |
| 811 | if (!VisitedPhis.insert({Node.Last, Node.Loc}).second) |
| 812 | continue; |
| 813 | const MemoryAccess *SkipStopWhere = nullptr; |
| 814 | if (Query->SkipSelfAccess && Node.Loc == Query->StartingLoc) { |
| 815 | assert(isa<MemoryDef>(Query->OriginalAccess)); |
| 816 | SkipStopWhere = Query->OriginalAccess; |
| 817 | } |
| 818 | UpwardsWalkResult Res = walkToPhiOrClobber(Node, |
| 819 | /*StopAt=*/StopWhere, |
| 820 | /*SkipStopAt=*/SkipStopWhere); |
| 821 | if (Res.IsKnownClobber) { |
| 822 | assert(Res.Result != StopWhere && Res.Result != SkipStopWhere); |
|   | // 如果这不是一个缓存命中，我们在遍历时遇到了覆盖。这是一个失败。 |
| 823 | // If this wasn't a cache hit, we hit a clobber when walking. That's a |
| 824 | // failure. |
| 825 | TerminatedPath Term{Res.Result, PathIndex}; |
| 826 | if (!MSSA.dominates(Res.Result, StopWhere)) |
| 827 | return Term; |
|   | // 否则，这是一个有效的潜在优化目标。 |
| 828 | // Otherwise, it's a valid thing to potentially optimize to. |
| 829 | Terminated.push_back(Term); |
| 830 | continue; |
| 831 | } |
| 832 | if (Res.Result == StopWhere || Res.Result == SkipStopWhere) { |
|   | // 我们达到了目标。保存这条路径，以防我们想要继续遍历。 |
|   | // 如果我们处于跳过OriginalAccess的模式，并且我们回到了OriginalAccess，不要保存路径， |
|   | // 我们只是回到了自身。 |
| 833 | // We've hit our target. Save this path off for if we want to continue |
| 834 | // walking. If we are in the mode of skipping the OriginalAccess, and |
| 835 | // we've reached back to the OriginalAccess, do not save path, we've |
| 836 | // just looped back to self. |
| 837 | if (Res.Result != SkipStopWhere) |
| 838 | NewPaused.push_back(PathIndex); |
| 839 | continue; |
| 848 | } |
| 849 | assert(!MSSA.isLiveOnEntryDef(Res.Result) && "liveOnEntry is a clobber"); |
| 850 | addSearches(cast<MemoryPhi>(Res.Result), PausedSearches, PathIndex); |
| 851 |  |
| 858 | } |
| 859 | return None; |
| 860 |  |
| 866 | } |
|   | /// 定义一个泛型定义路径迭代器，用于遍历特定类型的路径。 |
| 867 | template <typename T, typename Walker> |
| 868 | struct generic_def_path_iterator |
| 869 | : public iterator_facade_base<generic_def_path_iterator<T, Walker>, |
| 870 | std::forward_iterator_tag, T *> { |
|   | /// 默认构造函数，创建一个未初始化的迭代器。 |
| 871 | generic_def_path_iterator() {} |
|   | /// 构造函数，初始化迭代器并设置起始节点。 |
|   | /// @param W 指向Walker对象的指针，Walker对象负责管理路径。 |
|   | /// @param N 路径中当前节点的索引。 |
| 872 | generic_def_path_iterator(Walker *W, ListIndex N) : W(W), N(N) {} |
|   | /// 重载解引用运算符，返回当前节点的引用。 |
| 873 | T &operator*() const { return curNode(); } |
|   | /// 重载前置自增运算符，移动迭代器到下一个节点。 |
| 874 | generic_def_path_iterator &operator++() { |
| 875 | N = curNode().Previous; |
| 876 | return *this; |
| 879 |  |
| 888 | } |
|   | /// 重载等于运算符，用于比较两个迭代器是否相等。 |
|   | /// @param O 要比较的另一个迭代器。 |
| 889 | bool operator==(const generic_def_path_iterator &O) const { |
| 890 | if (N.hasValue() != O.N.hasValue()) |
| 891 | return false; |
| 892 | return !N.hasValue() || *N == *O.N; |
| 896 | } |
| 897 | private: |
|   | /// 获取当前节点的引用。 |
|   | /// @return 当前节点的引用。 |
| 898 | T &curNode() const { return W->Paths[*N]; } |
|   | /// 指向Walker对象的指针，Walker对象负责管理路径。 |
| 899 | Walker *W = nullptr; |
|   | /// 当前节点在路径列表中的索引，是一个可选值。 |
| 900 | Optional<ListIndex> N = None; |
| 901 | }; |
|   | /// 使用DefPath和ClobberWalker定义一个迭代器类型。 |
| 902 | using def_path_iterator = generic_def_path_iterator<DefPath, ClobberWalker>; |
|   | /// 使用const DefPath和const ClobberWalker定义一个const迭代器类型。 |
| 903 | using const_def_path_iterator = |
| 904 | generic_def_path_iterator<const DefPath, const ClobberWalker>; |
|   | /// 创建一个定义路径的迭代器范围。 |
|   | /// @param From 起始索引。 |
|   | /// @return 返回一个迭代器范围，从From开始到路径的末尾。 |
| 905 | iterator_range<def_path_iterator> def_path(ListIndex From) { |
| 906 | return make_range(def_path_iterator(this, From), def_path_iterator()); |
| 910 |  |
| 912 | } |
|   | /// 创建一个const定义路径的迭代器范围。 |
|   | /// @param From 起始索引。 |
|   | /// @return 返回一个const迭代器范围，从From开始到路径的末尾。 |
|   | /// 这个函数是const成员函数，因此它不会修改任何成员变量。 |
| 913 | iterator_range<const_def_path_iterator> const_def_path(ListIndex From) const { |
| 914 | return make_range(const_def_path_iterator(this, From), |
| 915 | const_def_path_iterator()); |
| 918 | } |
|   | /// 定义一个结构体，用于存储Phi优化的结果。 |
| 919 | struct OptznResult { |
|   | /// 包含我们结果的主要路径。 |
| 920 | /// The path that contains our result. |
| 921 | TerminatedPath PrimaryClobber; |
|   | /// 我们可以合法地从这些路径缓存回退，但它们不一定是Phi优化的结果。 |
| 922 | /// The paths that we can legally cache back from, but that aren't |
| 923 | /// necessarily the result of the Phi optimization. |
| 924 | SmallVector<TerminatedPath, 4> OtherClobbers; |
| 925 | }; |
|   | /// 获取DefPath对象在Paths容器中的索引。 |
| 926 | ListIndex defPathIndex(const DefPath &N) const { |
|   | // 为了使断言看起来更清晰，我们不需要对N取地址 |
| 927 | // The assert looks nicer if we don't need to do &N |
| 928 | const DefPath *NP = &N; |
| 929 | assert(!Paths.empty() && NP >= &Paths.front() && NP <= &Paths.back() && |
| 930 | "Out of bounds DefPath!"); |
| 931 | return NP - &Paths.front(); // 计算并返回索引 |
| 932 | } |
|   | /// 尝试尽可能地优化Phi节点。返回一个Paths的SmallVector，这些Paths作为合法的覆盖。 |
|   | /// 注意，这不会返回*所有*覆盖。 |
| 933 | /// |
|   | /// Phi优化算法简述： |
|   | ///   - 找到我们可以优化到的最早的def/phi，记为A |
|   | ///   - 检查从起始内存访问的所有路径是否最终都到达A |
|   | ///     - 如果不是，优化不可能。 |
|   | ///     - 否则，从A走到另一个覆盖或phi，记为A'。 |
|   | ///       - 如果A'是一个def，我们就完成了。 |
|   | ///       - 如果A'是一个phi，尝试优化它。 |
| 934 | /// |
|   | /// 一条路径是一系列{MemoryAccess, MemoryLocation}对。当找到一个覆盖MemoryLocation的MemoryAccess时，路径终止。 |
| 935 | /// Try to optimize a phi as best as we can. Returns a SmallVector of Paths |
| 936 | /// that act as legal clobbers. Note that this won't return *all* clobbers. |
| 937 | /// |
| 938 | /// Phi optimization algorithm tl;dr: |
| 939 | ///   - Find the earliest def/phi, A, we can optimize to |
| 940 | ///   - Find if all paths from the starting memory access ultimately reach A |
| 941 | ///     - If not, optimization isn't possible. |
| 942 | ///     - Otherwise, walk from A to another clobber or phi, A'. |
| 943 | ///       - If A' is a def, we're done. |
| 944 | ///       - If A' is a phi, try to optimize it. |
| 945 | /// |
| 946 | /// A path is a series of {MemoryAccess, MemoryLocation} pairs. A path |
| 947 | /// terminates when a MemoryAccess that clobbers said MemoryLocation is found. |
| 948 | OptznResult tryOptimizePhi(MemoryPhi *Phi, MemoryAccess *Start, |
| 949 | const MemoryLocation &Loc) { |
| 950 | assert(Paths.empty() && VisitedPhis.empty() && |
| 951 | "Reset the optimization state."); |
|   | // 将起始路径添加到Paths中 |
| 952 | Paths.emplace_back(Loc, Start, Phi, None); |
|   | // 存储在调用addSearches/getBlockingAccess之前我们有多少个“有效”的优化节点。 |
|   | // 如果我们有一个阻止器，这对于缓存是必要的。 |
| 953 | // Stores how many "valid" optimization nodes we had prior to calling |
| 954 | // addSearches/getBlockingAccess. Necessary for caching if we had a blocker. |
| 955 | auto PriorPathsSize = Paths.size(); |
| 956 | SmallVector<ListIndex, 16> PausedSearches; |
| 957 | SmallVector<ListIndex, 8> NewPaused; |
| 958 | SmallVector<TerminatedPath, 4> TerminatedPaths; |
| 959 | addSearches(Phi, PausedSearches, 0); |
|   | // 将具有“最被支配”覆盖的TerminatedPath移动到Paths的末尾。 |
| 960 | // Moves the TerminatedPath with the "most dominated" Clobber to the end of |
| 961 | // Paths. |
| 962 | auto MoveDominatedPathToEnd = [&](SmallVectorImpl<TerminatedPath> &Paths) { |
| 963 | assert(!Paths.empty() && "Need a path to move"); |
| 964 | auto Dom = Paths.begin(); |
| 965 | for (auto I = std::next(Dom), E = Paths.end(); I != E; ++I) |
| 966 | if (!MSSA.dominates(I->Clobber, Dom->Clobber)) |
| 967 | Dom = I; |
| 968 | auto Last = Paths.end() - 1; |
| 969 | if (Last != Dom) |
| 970 | std::iter_swap(Last, Dom); |
| 975 |  |
| 979 |  |
| 985 | }; |
|   | /// 当前Phi节点。 |
| 986 | MemoryPhi *Current = Phi; |
|   | /// 无限循环，直到找到优化结果。 |
| 987 | while (true) { |
|   | /// 断言当前Phi节点不是liveOnEntry定义，因为那应该被视为一个覆盖。 |
| 988 | assert(!MSSA.isLiveOnEntryDef(Current) && |
| 989 | "liveOnEntry wasn't treated as a clobber?"); |
|   | /// 获取遍历目标。 |
| 990 | const auto *Target = getWalkTarget(Current); |
|   | // 断言所有TerminatedPath都必须支配Target。 |
| 991 | // If a TerminatedPath doesn't dominate Target, then it wasn't a legal |
| 992 | // optimization for the prior phi. |
| 993 | assert(all_of(TerminatedPaths, [&](const TerminatedPath &P) { |
| 994 | return MSSA.dominates(P.Clobber, Target); |
| 995 | })); |
|   | /// 如果存在阻止Phi优化的TerminatedPath。 |
| 996 | // FIXME: This is broken, because the Blocker may be reported to be |
| 997 | // liveOnEntry, and we'll happily wait for that to disappear (read: never) |
| 998 | // For the moment, this is fine, since we do nothing with blocker info. |
| 999 | if (Optional<TerminatedPath> Blocker = getBlockingAccess( |
| 1000 | Target, PausedSearches, NewPaused, TerminatedPaths)) { |
|   | // 找到我们开始的节点。 |
| 1001 | // Find the node we started at. We can't search based on N->Last, since |
| 1002 | // we may have gone around a loop with a different MemoryLocation. |
| 1003 | auto Iter = find_if(def_path(Blocker->LastNode), [&](const DefPath &N) { |
| 1004 | return defPathIndex(N) < PriorPathsSize; |
| 1005 | }); |
| 1006 | assert(Iter != def_path_iterator()); |
| 1007 | DefPath &CurNode = *Iter; |
| 1008 | assert(CurNode.Last == Current); |
|   | // 我们不能可靠地缓存所有NewPaused回来。考虑一个情况，NewPaused中有两个路径； |
|   | // 其中一个不能在这个Phi之上优化，而另一个可以。如果我们缓存了第二个路径， |
|   | // 我们将得到次优的缓存条目。 |
| 1009 | // Two things: |
| 1010 | // A. We can't reliably cache all of NewPaused back. Consider a case |
| 1011 | //    where we have two paths in NewPaused; one of which can't optimize |
| 1012 | //    above this phi, whereas the other can. If we cache the second path |
| 1013 | //    back, we'll end up with suboptimal cache entries. We can handle |
| 1014 | //    cases like this a bit better when we either try to find all |
| 1015 | //    clobbers that block phi optimization, or when our cache starts |
| 1016 | //    supporting unfinished searches. |
| 1017 | // B. We can't reliably cache TerminatedPaths back here without doing |
| 1018 | //    extra checks; consider a case like: |
| 1019 | //       T |
| 1020 | //      / \ |
| 1021 | //     D   C |
| 1022 | //      \ / |
| 1023 | //       S |
| 1024 | //    Where T is our target, C is a node with a clobber on it, D is a |
| 1025 | //    diamond (with a clobber *only* on the left or right node, N), and |
| 1026 | //    S is our start. Say we walk to D, through the node opposite N |
| 1027 | //    (read: ignoring the clobber), and see a cache entry in the top |
| 1028 | //    node of D. That cache entry gets put into TerminatedPaths. We then |
| 1029 | //    walk up to C (N is later in our worklist), find the clobber, and |
| 1030 | //    quit. If we append TerminatedPaths to OtherClobbers, we'll cache |
| 1031 | //    the bottom part of D to the cached clobber, ignoring the clobber |
| 1032 | //    in N. Again, this problem goes away if we start tracking all |
| 1033 | //    blockers for a given phi optimization. |
| 1034 | TerminatedPath Result{CurNode.Last, defPathIndex(CurNode)}; |
| 1035 | return {Result, {}}; |
| 1037 | } |
|   | // 如果没有剩余的搜索，那么所有路径都导致有效的覆盖，我们从缓存中获取； |
|   | // 选择离起始点最近的，并允许其余的被缓存回来。 |
| 1038 | // If there's nothing left to search, then all paths led to valid clobbers |
| 1039 | // that we got from our cache; pick the nearest to the start, and allow |
| 1040 | // the rest to be cached back. |
| 1041 | if (NewPaused.empty()) { |
| 1042 | MoveDominatedPathToEnd(TerminatedPaths); |
| 1043 | TerminatedPath Result = TerminatedPaths.pop_back_val(); |
| 1044 | return {Result, std::move(TerminatedPaths)}; |
| 1047 | } |
| 1048 | MemoryAccess *DefChainEnd = nullptr; |
| 1049 | SmallVector<TerminatedPath, 4> Clobbers; |
| 1050 | for (ListIndex Paused : NewPaused) { |
| 1051 | UpwardsWalkResult WR = walkToPhiOrClobber(Paths[Paused]); |
| 1052 | if (WR.IsKnownClobber) |
| 1053 | Clobbers.push_back({WR.Result, Paused}); |
| 1054 | else |
| 1055 | // Micro-opt: If we hit the end of the chain, save it. |
| 1056 | DefChainEnd = WR.Result; |
| 1058 |  |
| 1060 |  |
| 1063 | } |
| 1064 | if (!TerminatedPaths.empty()) { |
| 1065 | // If we couldn't find the dominating phi/liveOnEntry in the above loop, |
| 1066 | // do it now. |
| 1067 | if (!DefChainEnd) |
| 1068 | for (auto *MA : def_chain(const_cast<MemoryAccess *>(Target))) |
| 1069 | DefChainEnd = MA; |
| 1070 | assert(DefChainEnd && "Failed to find dominating phi/liveOnEntry"); |
| 1071 | // If any of the terminated paths don't dominate the phi we'll try to |
| 1072 | // optimize, we need to figure out what they are and quit. |
| 1073 | const BasicBlock *ChainBB = DefChainEnd->getBlock(); |
| 1074 | for (const TerminatedPath &TP : TerminatedPaths) { |
| 1075 | // Because we know that DefChainEnd is as "high" as we can go, we |
| 1076 | // don't need local dominance checks; BB dominance is sufficient. |
| 1077 | if (DT.dominates(ChainBB, TP.Clobber->getBlock())) |
| 1078 | Clobbers.push_back(TP); |
| 1079 |  |
| 1083 | } |
| 1084 | } |
|   | // 如果我们在def链中拥有覆盖，找到离Current最近的并退出。 |
| 1085 | // If we have clobbers in the def chain, find the one closest to Current |
| 1086 | // and quit. |
| 1087 | if (!Clobbers.empty()) { |
| 1088 | MoveDominatedPathToEnd(Clobbers); |
| 1089 | TerminatedPath Result = Clobbers.pop_back_val(); |
| 1090 | return {Result, std::move(Clobbers)}; |
| 1104 | } |
| 1105 | assert(all_of(NewPaused, |
| 1106 | [&](ListIndex I) { return Paths[I].Last == DefChainEnd; })); |
| 1107 | // Because liveOnEntry is a clobber, this must be a phi. |
| 1108 | auto *DefChainPhi = cast<MemoryPhi>(DefChainEnd); |
| 1109 | PriorPathsSize = Paths.size(); |
| 1110 | PausedSearches.clear(); |
| 1111 | for (ListIndex I : NewPaused) |
| 1112 | addSearches(DefChainPhi, PausedSearches, I); |
| 1113 | NewPaused.clear(); |
| 1114 | Current = DefChainPhi; |
| 1127 | } |
| 1129 | } |
|   | /// 验证优化结果。 |
| 1130 | void verifyOptResult(const OptznResult &R) const { |
| 1131 | assert(all_of(R.OtherClobbers, [&](const TerminatedPath &P) { |
| 1132 | return MSSA.dominates(P.Clobber, R.PrimaryClobber.Clobber); |
| 1133 | })); |
| 1134 | } |
|   | /// 重置Phi优化状态。 |
| 1135 | void resetPhiOptznState() { |
| 1136 | Paths.clear(); // 清除路径列表。 |
| 1137 | VisitedPhis.clear(); // 清除已访问Phi节点列表。 |
| 1138 | } |
| 1139 | public: |
|   | /// ClobberWalker类的构造函数。 |
|   | /// @param MSSA MemorySSA分析结果。 |
|   | /// @param AA 别名分析对象。 |
|   | /// @param DT DominatorTree对象。 |
| 1140 | ClobberWalker(const MemorySSA &MSSA, AliasAnalysisType &AA, DominatorTree &DT) |
| 1141 | : MSSA(MSSA), AA(AA), DT(DT) {} |
|   | /// 获取别名分析对象。 |
| 1142 | AliasAnalysisType *getAA() { return &AA; } |
|   | /// 查找给定查询的最近覆盖，如果可能的话优化phis。 |
|   | /// @param Start 起始内存访问。 |
|   | /// @param Q 查询对象。 |
|   | /// @param UpWalkLimit 向上遍历的限制。 |
| 1143 | /// Finds the nearest clobber for the given query, optimizing phis if |
| 1144 | /// possible. |
| 1145 | MemoryAccess *findClobber(MemoryAccess *Start, UpwardsMemoryQuery &Q, |
| 1146 | unsigned &UpWalkLimit) { |
| 1147 | Query = &Q; |
| 1148 | UpwardWalkLimit = &UpWalkLimit; |
| 1149 | // Starting limit must be > 0. |
| 1150 | if (!UpWalkLimit) |
| 1151 | UpWalkLimit++; |
| 1152 | MemoryAccess *Current = Start; |
|   | // 如果我们得到一个MemoryUse，默默地获取它的定义访问。 |
|   | // （这有一个好副作用，确保我们永远不会缓存uses） |
| 1153 | // This walker pretends uses don't exist. If we're handed one, silently grab |
| 1154 | // its def. (This has the nice side-effect of ensuring we never cache uses) |
| 1155 | if (auto *MU = dyn_cast<MemoryUse>(Start)) |
| 1156 | Current = MU->getDefiningAccess(); |
| 1157 | DefPath FirstDesc(Q.StartingLoc, Current, Current, None); |
|   | // 对于没有复杂phi优化需求的常见情况的快速路径 |
| 1158 | // Fast path for the overly-common case (no crazy phi optimization |
| 1159 | // necessary) |
| 1160 | UpwardsWalkResult WalkResult = walkToPhiOrClobber(FirstDesc); |
| 1161 | MemoryAccess *Result; |
| 1162 | if (WalkResult.IsKnownClobber) { |
| 1163 | Result = WalkResult.Result; |
| 1164 | Q.AR = WalkResult.AR; |
| 1165 | } else { |
|   | // 尝试优化Phi。 |
| 1166 | OptznResult OptRes = tryOptimizePhi(cast<MemoryPhi>(FirstDesc.Last), |
| 1167 | Current, Q.StartingLoc); |
| 1168 | verifyOptResult(OptRes); |
| 1169 | resetPhiOptznState(); |
| 1170 | Result = OptRes.PrimaryClobber.Clobber; |
| 1178 | } |
| 1179 | #ifdef EXPENSIVE_CHECKS |
|   | // 如果不跳过自身访问并且向上遍历限制大于0，则进行昂贵的检查。 |
| 1180 | if (!Q.SkipSelfAccess && *UpwardWalkLimit > 0) |
| 1181 | checkClobberSanity(Current, Result, Q.StartingLoc, MSSA, Q, AA); |
| 1182 | #endif |
|   | // 返回找到的覆盖。 |
| 1183 | return Result; |
| 1184 | } |
| 1185 | }; |
|   | /// RenamePassData结构体用于存储重命名过程中的数据。 |
| 1186 | struct RenamePassData { |
|   | /// 指向支配树节点的指针。 |
| 1187 | DomTreeNode *DTN; |
|   | /// 指向当前节点的子节点迭代器。 |
| 1188 | DomTreeNode::const_iterator ChildIt; |
|   | /// 指向进入当前节点的内存访问。 |
| 1189 | MemoryAccess *IncomingVal; |
|   | /// 构造函数，初始化结构体成员。 |
| 1190 | RenamePassData(DomTreeNode *D, DomTreeNode::const_iterator It, |
| 1191 | MemoryAccess *M) |
| 1192 | : DTN(D), ChildIt(It), IncomingVal(M) {} |
|   | /// 交换当前对象与另一个RenamePassData对象的值。 |
| 1193 | void swap(RenamePassData &RHS) { |
| 1194 | std::swap(DTN, RHS.DTN); |
| 1195 | std::swap(ChildIt, RHS.ChildIt); |
| 1196 | std::swap(IncomingVal, RHS.IncomingVal); |
| 1207 | } |
| 1208 | }; |
| 1209 | } // end anonymous namespace |
| 1210 | namespace llvm { |
|   | /// ClobberWalkerBase类是一个模板类，用于执行别名分析并寻找内存访问的覆盖。 |
| 1211 | template <class AliasAnalysisType> class MemorySSA::ClobberWalkerBase { |
|   | /// ClobberWalker实例，用于执行别名分析。 |
| 1212 | ClobberWalker<AliasAnalysisType> Walker; |
|   | /// 指向MemorySSA实例的指针。 |
| 1213 | MemorySSA *MSSA; |
| 1214 | public: |
|   | /// 构造函数，初始化Walker和MSSA。 |
| 1215 | ClobberWalkerBase(MemorySSA *M, AliasAnalysisType *A, DominatorTree *D) |
| 1216 | : Walker(*M, *A, *D), MSSA(M) {} |
|   | /// 获取覆盖指定内存位置的内存访问。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param Loc 内存位置。 |
|   | /// @param UpwardWalkLimit 向上遍历的限制。 |
| 1217 | MemoryAccess *getClobberingMemoryAccessBase(MemoryAccess *, |
| 1218 | const MemoryLocation &, |
| 1219 | unsigned &); |
|   | // 获取覆盖的内存访问，可以选择跳过原始查询访问。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param UpwardWalkLimit 向上遍历的限制。 |
|   | /// @param SkipSelf 是否跳过自身。 |
| 1220 | // Third argument (bool), defines whether the clobber search should skip the |
| 1221 | // original queried access. If true, there will be a follow-up query searching |
| 1222 | // for a clobber access past "self". Note that the Optimized access is not |
| 1223 | // updated if a new clobber is found by this SkipSelf search. If this |
| 1224 | // additional query becomes heavily used we may decide to cache the result. |
| 1225 | // Walker instantiations will decide how to set the SkipSelf bool. |
| 1226 | MemoryAccess *getClobberingMemoryAccessBase(MemoryAccess *, unsigned &, bool); |
| 1227 | }; |
|   | /// CachingWalker类是一个模板类，用于执行别名分析并解决内存访问冲突。 |
|   | /// 尽管它不再自己执行缓存操作，但名称暂时保留。 |
| 1228 | /// A MemorySSAWalker that does AA walks to disambiguate accesses. It no |
| 1229 | /// longer does caching on its own, but the name has been retained for the |
| 1230 | /// moment. |
| 1231 | template <class AliasAnalysisType> |
| 1232 | class MemorySSA::CachingWalker final : public MemorySSAWalker { |
|   | /// 指向ClobberWalkerBase实例的指针，用于执行别名分析。 |
| 1233 | ClobberWalkerBase<AliasAnalysisType> *Walker; |
| 1234 | public: |
|   | /// 构造函数，初始化MemorySSAWalker和Walker。 |
|   | /// @param M MemorySSA实例的指针。 |
|   | /// @param W ClobberWalkerBase实例的指针。 |
| 1235 | CachingWalker(MemorySSA *M, ClobberWalkerBase<AliasAnalysisType> *W) |
| 1236 | : MemorySSAWalker(M), Walker(W) {} |
|   | /// 析构函数，使用默认实现。 |
| 1237 | ~CachingWalker() override = default; |
|   | /// 使用基类MemorySSAWalker的getClobberingMemoryAccess方法。 |
| 1238 | using MemorySSAWalker::getClobberingMemoryAccess; |
|   | /// 获取覆盖指定内存访问的内存访问，不指定内存位置。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param UWL 向上遍历的限制。 |
| 1239 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, unsigned &UWL) { |
| 1240 | return Walker->getClobberingMemoryAccessBase(MA, UWL, false); |
| 1242 | } |
|   | /// 获取覆盖指定内存位置的内存访问。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param Loc 内存位置。 |
|   | /// @param UWL 向上遍历的限制。 |
| 1243 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, |
| 1244 | const MemoryLocation &Loc, |
| 1245 | unsigned &UWL) { |
| 1246 | return Walker->getClobberingMemoryAccessBase(MA, Loc, UWL); |
| 1249 | } |
|   | /// 重写基类方法，获取覆盖指定内存访问的内存访问。 |
|   | /// 这个函数不指定内存位置，因此使用默认的向上遍历限制。 |
| 1250 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA) override { |
| 1251 | unsigned UpwardWalkLimit = MaxCheckLimit; |
| 1252 | return getClobberingMemoryAccess(MA, UpwardWalkLimit); |
| 1253 |  |
| 1257 | } |
|   | /// 重写基类方法，获取覆盖指定内存位置的内存访问。 |
|   | /// 这个函数接受一个内存位置参数。 |
| 1258 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, |
| 1259 | const MemoryLocation &Loc) override { |
| 1260 | unsigned UpwardWalkLimit = MaxCheckLimit; |
| 1261 | return getClobberingMemoryAccess(MA, Loc, UpwardWalkLimit); |
| 1265 | } |
|   | /// 使特定内存访问的信息无效。 |
|   | /// 这个函数用于在内存SSA分析中，当某个内存访问可能不再有效时调用。 |
| 1266 | void invalidateInfo(MemoryAccess *MA) override { |
| 1267 | if (auto *MUD = dyn_cast<MemoryUseOrDef>(MA)) |
| 1268 | MUD->resetOptimized(); |
| 1280 | } |
| 1281 | }; |
|   | /// SkipSelfWalker类是一个模板类，用于执行别名分析并解决内存访问冲突， |
|   | /// 在此过程中跳过原始查询的访问。 |
| 1282 | template <class AliasAnalysisType> |
| 1283 | class MemorySSA::SkipSelfWalker final : public MemorySSAWalker { |
|   | /// 指向ClobberWalkerBase实例的指针，用于执行别名分析。 |
| 1284 | ClobberWalkerBase<AliasAnalysisType> *Walker; |
| 1285 | public: |
|   | /// 构造函数，初始化MemorySSAWalker和Walker。 |
|   | /// @param M MemorySSA实例的指针。 |
|   | /// @param W ClobberWalkerBase实例的指针。 |
| 1286 | SkipSelfWalker(MemorySSA *M, ClobberWalkerBase<AliasAnalysisType> *W) |
| 1287 | : MemorySSAWalker(M), Walker(W) {} |
|   | /// 析构函数，使用默认实现。 |
| 1288 | ~SkipSelfWalker() override = default; |
|   | /// 使用基类MemorySSAWalker的getClobberingMemoryAccess方法。 |
| 1289 | using MemorySSAWalker::getClobberingMemoryAccess; |
|   | /// 获取覆盖指定内存访问的内存访问，跳过原始查询的访问。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param UWL 向上遍历的限制。 |
| 1290 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, unsigned &UWL) { |
| 1291 | return Walker->getClobberingMemoryAccessBase(MA, UWL, true); |
| 1301 | } |
|   | /// 获取覆盖指定内存位置的内存访问。 |
|   | /// @param MA 起始内存访问。 |
|   | /// @param Loc 内存位置。 |
|   | /// @param UWL 向上遍历的限制。 |
| 1302 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, |
| 1303 | const MemoryLocation &Loc, |
| 1304 | unsigned &UWL) { |
| 1305 | return Walker->getClobberingMemoryAccessBase(MA, Loc, UWL); |
| 1306 |  |
| 1311 | } |
|   | /// 重写基类方法，获取覆盖指定内存访问的内存访问。 |
|   | /// 这个函数不指定内存位置，因此使用默认的向上遍历限制。 |
| 1312 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA) override { |
| 1313 | unsigned UpwardWalkLimit = MaxCheckLimit; |
| 1314 | return getClobberingMemoryAccess(MA, UpwardWalkLimit); |
| 1315 | } |
|   | /// 重写基类方法，获取覆盖指定内存位置的内存访问。 |
|   | /// 这个函数接受一个内存位置参数。 |
| 1316 | MemoryAccess *getClobberingMemoryAccess(MemoryAccess *MA, |
| 1317 | const MemoryLocation &Loc) override { |
| 1318 | unsigned UpwardWalkLimit = MaxCheckLimit; |
| 1319 | return getClobberingMemoryAccess(MA, Loc, UpwardWalkLimit); |
| 1320 | } |
|   | /// 使特定内存访问的信息无效。 |
|   | /// 这个函数用于在内存SSA分析中，当某个内存访问可能不再有效时调用。 |
| 1321 | void invalidateInfo(MemoryAccess *MA) override { |
| 1322 | if (auto *MUD = dyn_cast<MemoryUseOrDef>(MA)) |
| 1323 | MUD->resetOptimized(); |
| 1324 | } |
| 1325 | }; |
| 1326 | } // end namespace llvm |
|   | /// 重命名后继Phi节点。 |
|   | /// @param BB 当前基本块。 |
|   | /// @param IncomingVal 传入的内存访问值。 |
|   | /// @param RenameAllUses 是否重命名所有使用。 |
| 1327 | void MemorySSA::renameSuccessorPhis(BasicBlock *BB, MemoryAccess *IncomingVal, |
| 1328 | bool RenameAllUses) { |
|   | // 将值传递给后继节点。 |
| 1329 | // Pass through values to our successors |
| 1330 | for (const BasicBlock *S : successors(BB)) { |
| 1331 | auto It = PerBlockAccesses.find(S); |
|   | // 重命名后继块中的Phi节点。 |
| 1332 | // Rename the phi nodes in our successor block |
| 1333 | if (It == PerBlockAccesses.end() || !isa<MemoryPhi>(It->second->front())) |
| 1334 | continue; |
| 1335 | AccessList *Accesses = It->second.get(); |
| 1336 | auto *Phi = cast<MemoryPhi>(&Accesses->front()); |
| 1337 | if (RenameAllUses) { |
| 1338 | bool ReplacementDone = false; |
| 1339 | for (unsigned I = 0, E = Phi->getNumIncomingValues(); I != E; ++I) |
| 1340 | if (Phi->getIncomingBlock(I) == BB) { |
| 1341 | Phi->setIncomingValue(I, IncomingVal); |
| 1342 | ReplacementDone = true; |
| 1349 | } |
| 1350 | (void) ReplacementDone; |
| 1351 | assert(ReplacementDone && "Incomplete phi during partial rename"); |
| 1352 | } else |
| 1353 | Phi->addIncoming(IncomingVal, BB); |
| 1357 | } |
| 1358 |  |
| 1362 | } |
|   | /// 将单个基本块重命名为MemorySSA形式。 |
|   | /// 使用标准的SSA重命名算法。 |
|   | /// @returns 新的传入值。 |
| 1363 | /// Rename a single basic block into MemorySSA form. |
| 1364 | /// Uses the standard SSA renaming algorithm. |
| 1365 | /// \returns The new incoming value. |
| 1366 | MemoryAccess *MemorySSA::renameBlock(BasicBlock *BB, MemoryAccess *IncomingVal, |
| 1367 | bool RenameAllUses) { |
| 1368 | auto It = PerBlockAccesses.find(BB); |
|   | // 如果列表为空，则跳过大部分处理。 |
| 1369 | // Skip most processing if the list is empty. |
| 1370 | if (It != PerBlockAccesses.end()) { |
| 1371 | AccessList *Accesses = It->second.get(); |
| 1372 | for (MemoryAccess &L : *Accesses) { |
| 1373 | if (MemoryUseOrDef *MUD = dyn_cast<MemoryUseOrDef>(&L)) { |
| 1374 | if (MUD->getDefiningAccess() == nullptr || RenameAllUses) |
| 1375 | MUD->setDefiningAccess(IncomingVal); |
| 1376 | if (isa<MemoryDef>(&L)) |
| 1377 | IncomingVal = &L; |
| 1378 | } else { |
| 1379 | IncomingVal = &L; |
| 1391 | } |
| 1392 | } |
| 1398 | } |
| 1399 | return IncomingVal; |
| 1403 | } |
|   | /// 实现标准的SSA重命名算法。 |
|   | /// 遍历支配树，重命名访问，并填充后继中的Phi节点。 |
| 1404 | /// This is the standard SSA renaming algorithm. |
| 1405 | /// |
| 1406 | /// We walk the dominator tree in preorder, renaming accesses, and then filling |
| 1407 | /// in phi nodes in our successors. |
| 1408 | void MemorySSA::renamePass(DomTreeNode *Root, MemoryAccess *IncomingVal, |
| 1409 | SmallPtrSetImpl<BasicBlock *> &Visited, |
| 1410 | bool SkipVisited, bool RenameAllUses) { |
| 1411 | assert(Root && "Trying to rename accesses in an unreachable block"); |
| 1412 | SmallVector<RenamePassData, 32> WorkStack; |
|   | // 如果已经重命名了这个块并且我们正在跳过，跳过所有操作。 |
| 1413 | // Skip everything if we already renamed this block and we are skipping. |
| 1414 | // Note: You can't sink this into the if, because we need it to occur |
| 1415 | // regardless of whether we skip blocks or not. |
| 1416 | bool AlreadyVisited = !Visited.insert(Root->getBlock()).second; |
| 1417 | if (SkipVisited && AlreadyVisited) |
| 1418 | return; |
| 1419 | IncomingVal = renameBlock(Root->getBlock(), IncomingVal, RenameAllUses); |
| 1420 | renameSuccessorPhis(Root->getBlock(), IncomingVal, RenameAllUses); |
| 1421 | WorkStack.push_back({Root, Root->begin(), IncomingVal}); |
| 1422 | while (!WorkStack.empty()) { |
| 1423 | DomTreeNode *Node = WorkStack.back().DTN; |
| 1424 | DomTreeNode::const_iterator ChildIt = WorkStack.back().ChildIt; |
| 1425 | IncomingVal = WorkStack.back().IncomingVal; |
| 1426 | if (ChildIt == Node->end()) { |
| 1427 | WorkStack.pop_back(); |
| 1428 | } else { |
| 1429 | DomTreeNode *Child = *ChildIt; |
| 1430 | ++WorkStack.back().ChildIt; |
| 1431 | BasicBlock *BB = Child->getBlock(); |
| 1432 | // Note: You can't sink this into the if, because we need it to occur |
| 1433 | // regardless of whether we skip blocks or not. |
| 1434 | AlreadyVisited = !Visited.insert(BB).second; |
| 1435 | if (SkipVisited && AlreadyVisited) { |
|   | // 如果在重命名多个块时已经访问过这个块，找出传入值，即最后一个定义。 |
| 1436 | // We already visited this during our renaming, which can happen when |
| 1437 | // being asked to rename multiple blocks. Figure out the incoming val, |
| 1438 | // which is the last def. |
| 1439 | // Incoming value can only change if there is a block def, and in that |
| 1440 | // case, it's the last block def in the list. |
| 1441 | if (auto *BlockDefs = getWritableBlockDefs(BB)) |
| 1442 | IncomingVal = &*BlockDefs->rbegin(); |
| 1443 | } else |
| 1444 | IncomingVal = renameBlock(BB, IncomingVal, RenameAllUses); |
| 1445 | renameSuccessorPhis(BB, IncomingVal, RenameAllUses); |
| 1446 | WorkStack.push_back({Child, Child->begin(), IncomingVal}); |
| 1450 | } |
| 1456 | } |
| 1458 | } |
|   | /// 处理不可达块中的内存访问，通过删除不可达块中的Phi节点， |
|   | /// 并将所有其他不可达的MemoryAccess标记为使用活动入口定义。 |
| 1459 | /// This handles unreachable block accesses by deleting phi nodes in |
| 1460 | /// unreachable blocks, and marking all other unreachable MemoryAccess's as |
| 1461 | /// being uses of the live on entry definition. |
| 1462 | void MemorySSA::markUnreachableAsLiveOnEntry(BasicBlock *BB) { |
| 1463 | assert(!DT->isReachableFromEntry(BB) && |
| 1464 | "Reachable block found while handling unreachable blocks"); |
|   | // 确保在我们可达的后继块中的Phi节点，即使我们的块是前向不可达的， |
|   | // 也要为我们的入边设置LiveOnEntryDef。我们完全可以从CFG中完全断开这些块， |
|   | // 但我们现在不这么做。 |
| 1465 | // Make sure phi nodes in our reachable successors end up with a |
| 1466 | // LiveOnEntryDef for our incoming edge, even though our block is forward |
| 1467 | // unreachable.  We could just disconnect these blocks from the CFG fully, |
| 1468 | // but we do not right now. |
| 1469 | for (const BasicBlock *S : successors(BB)) { |
| 1470 | if (!DT->isReachableFromEntry(S)) |
| 1471 | continue; |
| 1472 | auto It = PerBlockAccesses.find(S); |
|   | // 重命名我们后继块中的Phi节点 |
| 1473 | // Rename the phi nodes in our successor block |
| 1474 | if (It == PerBlockAccesses.end() || !isa<MemoryPhi>(It->second->front())) |
| 1475 | continue; |
| 1476 | AccessList *Accesses = It->second.get(); |
| 1477 | auto *Phi = cast<MemoryPhi>(&Accesses->front()); |
| 1478 | Phi->addIncoming(LiveOnEntryDef.get(), BB); |
| 1479 |  |
| 1485 |  |
| 1492 | } |
| 1493 | auto It = PerBlockAccesses.find(BB); |
| 1494 | if (It == PerBlockAccesses.end()) |
| 1495 | return; |
| 1496 | auto &Accesses = It->second; |
| 1497 | for (auto AI = Accesses->begin(), AE = Accesses->end(); AI != AE;) { |
| 1498 | auto Next = std::next(AI); |
|   | // 如果我们有一个Phi节点，只需删除它。我们将把所有用户替换为活动入口。 |
| 1499 | // If we have a phi, just remove it. We are going to replace all |
| 1500 | // users with live on entry. |
| 1501 | if (auto *UseOrDef = dyn_cast<MemoryUseOrDef>(AI)) |
| 1502 | UseOrDef->setDefiningAccess(LiveOnEntryDef.get()); |
| 1503 | else |
| 1504 | Accesses->erase(AI); |
| 1505 | AI = Next; |
| 1506 | } |
| 1507 | } |
|   | /// MemorySSA类的构造函数。 |
|   | /// 初始化MemorySSA对象并构建内存SSA形式。 |
| 1508 | MemorySSA::MemorySSA(Function &Func, AliasAnalysis *AA, DominatorTree *DT) |
| 1509 | : AA(nullptr), DT(DT), F(Func), LiveOnEntryDef(nullptr), Walker(nullptr), |
| 1510 | SkipWalker(nullptr), NextID(0) { |
| 1511 | // Build MemorySSA using a batch alias analysis. This reuses the internal |
| 1512 | // state that AA collects during an alias()/getModRefInfo() call. This is |
| 1513 | // safe because there are no CFG changes while building MemorySSA and can |
| 1514 | // significantly reduce the time spent by the compiler in AA, because we will |
| 1515 | // make queries about all the instructions in the Function. |
|   | /// 断言提供了别名分析对象。 |
| 1516 | assert(AA && "No alias analysis?"); |
|   | /// 使用别名分析对象创建批量分析结果，以便重用别名分析的内部状态。 |
| 1517 | BatchAAResults BatchAA(*AA); |
|   | /// 使用批量别名分析结果构建内存SSA。 |
| 1518 | buildMemorySSA(BatchAA); |
| 1519 | // Intentionally leave AA to nullptr while building so we don't accidently |
| 1520 | // use non-batch AliasAnalysis. |
|   | /// 为了避免在构建过程中不小心使用非批量别名分析，故意将AA设置为nullptr。 |
|   | /// 然后在这里设置回提供的别名分析对象。 |
| 1521 | this->AA = AA; |
|   | /// 创建walker对象，用于后续的别名分析和内存访问重命名。 |
| 1522 | // Also create the walker here. |
| 1523 | getWalker(); |
| 1524 | } |
|   | /// MemorySSA类的析构函数。 |
|   | /// 清理MemorySSA对象，释放所有资源。 |
| 1525 | MemorySSA::~MemorySSA() { |
|   | /// 遍历每个基本块的所有内存访问，释放它们的所有引用。 |
| 1526 | // Drop all our references |
| 1527 | for (const auto &Pair : PerBlockAccesses) |
| 1528 | for (MemoryAccess &MA : *Pair.second) |
|   | /// 释放单个内存访问对象的所有引用。 |
| 1529 | MA.dropAllReferences(); |
| 1532 |  |
| 1541 | } |
|   | /// 获取或创建指定基本块的内存访问列表。 |
|   | /// @param BB 基本块指针。 |
|   | /// @return 指向内存访问列表的指针。 |
| 1542 | MemorySSA::AccessList *MemorySSA::getOrCreateAccessList(const BasicBlock *BB) { |
| 1543 | auto Res = PerBlockAccesses.insert(std::make_pair(BB, nullptr)); |
|   | /// 如果基本块之前不在映射中，创建一个新的内存访问列表。 |
| 1544 | if (Res.second) |
| 1545 | Res.first->second = std::make_unique<AccessList>(); |
|   | /// 返回指向内存访问列表的指针。 |
| 1546 | return Res.first->second.get(); |
| 1547 | } |
|   | /// 获取或创建指定基本块的内存定义列表。 |
|   | /// @param BB 基本块指针。 |
|   | /// @return 指向内存定义列表的指针。 |
| 1548 | MemorySSA::DefsList *MemorySSA::getOrCreateDefsList(const BasicBlock *BB) { |
|   | /// 尝试将基本块插入到PerBlockDefs映射中。 |
| 1549 | auto Res = PerBlockDefs.insert(std::make_pair(BB, nullptr)); |
|   | /// 如果基本块之前不在映射中，创建一个新的内存定义列表。 |
| 1550 | if (Res.second) |
| 1551 | Res.first->second = std::make_unique<DefsList>(); |
|   | /// 返回指向内存定义列表的指针。 |
| 1552 | return Res.first->second.get(); |
| 1556 |  |
| 1562 | } |
| 1563 | namespace llvm { |
|   | /// 批量更新所有MemoryUse的defining access，指向实际覆盖它们的内存访问。 |
| 1564 | /// This class is a batch walker of all MemoryUse's in the program, and points |
| 1565 | /// their defining access at the thing that actually clobbers them.  Because it |
| 1566 | /// is a batch walker that touches everything, it does not operate like the |
| 1567 | /// other walkers.  This walker is basically performing a top-down SSA renaming |
| 1568 | /// pass, where the version stack is used as the cache.  This enables it to be |
| 1569 | /// significantly more time and memory efficient than using the regular walker, |
| 1570 | /// which is walking bottom-up. |
| 1571 | class MemorySSA::OptimizeUses { |
| 1572 | public: |
|   | /// 构造函数。 |
|   | /// @param MSSA 指向MemorySSA实例的指针。 |
|   | /// @param Walker 用于别名分析的CachingWalker实例。 |
|   | /// @param BAA 批量别名分析结果。 |
|   | /// @param DT 支配树实例。 |
| 1573 | OptimizeUses(MemorySSA *MSSA, CachingWalker<BatchAAResults> *Walker, |
| 1574 | BatchAAResults *BAA, DominatorTree *DT) |
| 1575 | : MSSA(MSSA), Walker(Walker), AA(BAA), DT(DT) {} |
|   | /// 优化所有内存使用。 |
| 1576 | void optimizeUses(); |
| 1577 | private: |
|   | /// 表示内存位置在栈中的位置信息。 |
| 1578 | /// This represents where a given memorylocation is in the stack. |
| 1579 | struct MemlocStackInfo { |
| 1580 | // This essentially is keeping track of versions of the stack. Whenever |
| 1581 | // the stack changes due to pushes or pops, these versions increase. |
| 1582 | unsigned long StackEpoch; |
| 1583 | unsigned long PopEpoch; |
| 1584 | // This is the lower bound of places on the stack to check. It is equal to |
| 1585 | // the place the last stack walk ended. |
| 1586 | // Note: Correctness depends on this being initialized to 0, which densemap |
| 1587 | // does |
| 1588 | unsigned long LowerBound; |
| 1589 | const BasicBlock *LowerBoundBlock; |
| 1590 | // This is where the last walk for this memory location ended. |
| 1591 | unsigned long LastKill; |
| 1592 | bool LastKillValid; |
| 1593 | Optional<AliasResult> AR; |
| 1594 | }; |
|   | /// 在给定的基本块内优化内存使用。 |
| 1595 | void optimizeUsesInBlock(const BasicBlock *, unsigned long &, unsigned long &, |
| 1596 | SmallVectorImpl<MemoryAccess *> &, |
| 1597 | DenseMap<MemoryLocOrCall, MemlocStackInfo> &); |
| 1598 | MemorySSA *MSSA; |
| 1599 | CachingWalker<BatchAAResults> *Walker; |
| 1600 | BatchAAResults *AA; |
| 1601 | DominatorTree *DT; |
| 1602 | }; |
| 1603 | } // end namespace llvm |
|   | /// 在给定的基本块内优化内存使用。 |
|   | /// 使用单一栈来跟踪每个内存位置需要检查的栈位置，以及上次结束的位置。 |
| 1604 | /// Optimize the uses in a given block This is basically the SSA renaming |
| 1605 | /// algorithm, with one caveat: We are able to use a single stack for all |
| 1606 | /// MemoryUses.  This is because the set of *possible* reaching MemoryDefs is |
| 1607 | /// the same for every MemoryUse.  The *actual* clobbering MemoryDef is just |
| 1608 | /// going to be some position in that stack of possible ones. |
| 1609 | /// |
| 1610 | /// We track the stack positions that each MemoryLocation needs |
| 1611 | /// to check, and last ended at.  This is because we only want to check the |
| 1612 | /// things that changed since last time.  The same MemoryLocation should |
| 1613 | /// get clobbered by the same store (getModRefInfo does not use invariantness or |
| 1614 | /// things like this, and if they start, we can modify MemoryLocOrCall to |
| 1615 | /// include relevant data) |
| 1616 | void MemorySSA::OptimizeUses::optimizeUsesInBlock( |
| 1617 | const BasicBlock *BB, unsigned long &StackEpoch, unsigned long &PopEpoch, |
| 1618 | SmallVectorImpl<MemoryAccess *> &VersionStack, |
| 1619 | DenseMap<MemoryLocOrCall, MemlocStackInfo> &LocStackInfo) { |
|   | /// 如果没有内存访问，无需操作。 |
| 1620 | /// If no accesses, nothing to do. |
| 1621 | MemorySSA::AccessList *Accesses = MSSA->getWritableBlockAccesses(BB); |
| 1622 | if (Accesses == nullptr) |
| 1623 | return; |
|   | // 从栈中弹出不支配当前块的所有内容，并增加PopEpoch。 |
| 1624 | // Pop everything that doesn't dominate the current block off the stack, |
| 1625 | // increment the PopEpoch to account for this. |
| 1626 | while (true) { |
| 1627 | assert( |
| 1628 | !VersionStack.empty() && |
| 1629 | "Version stack should have liveOnEntry sentinel dominating everything"); |
| 1630 | BasicBlock *BackBlock = VersionStack.back()->getBlock(); |
| 1631 | if (DT->dominates(BackBlock, BB)) |
| 1632 | break; |
| 1633 | while (VersionStack.back()->getBlock() == BackBlock) |
| 1634 | VersionStack.pop_back(); |
| 1635 | ++PopEpoch; |
| 1641 | } |
| 1642 | for (MemoryAccess &MA : *Accesses) { |
| 1643 | auto *MU = dyn_cast<MemoryUse>(&MA); |
| 1644 | if (!MU) { |
| 1645 | VersionStack.push_back(&MA); |
| 1646 | ++StackEpoch; |
| 1647 | continue; |
| 1651 | } |
|   | // 如果使用可以被简单地优化为liveOnEntry，那么设置defining access。 |
| 1652 | if (isUseTriviallyOptimizableToLiveOnEntry(*AA, MU->getMemoryInst())) { |
| 1653 | MU->setDefiningAccess(MSSA->getLiveOnEntryDef(), true, None); |
| 1654 | continue; |
| 1655 |  |
| 1659 |  |
| 1666 | } |
| 1667 | MemoryLocOrCall UseMLOC(MU); |
| 1668 | auto &LocInfo = LocStackInfo[UseMLOC]; |
|   | // 如果PopEpoch改变，意味着我们由于改变块而从栈顶移除了内容。 |
|   | // 我们可能需要重置下限或最后杀死信息。 |
| 1669 | // If the pop epoch changed, it means we've removed stuff from top of |
| 1670 | // stack due to changing blocks. We may have to reset the lower bound or |
| 1671 | // last kill info. |
| 1672 | if (LocInfo.PopEpoch != PopEpoch) { |
| 1673 | LocInfo.PopEpoch = PopEpoch; |
| 1674 | LocInfo.StackEpoch = StackEpoch; |
|   | // 如果下限在不再支配我们的内容中，我们需要重置它。 |
| 1675 | // If the lower bound was in something that no longer dominates us, we |
| 1676 | // have to reset it. |
| 1677 | // We can't simply track stack size, because the stack may have had |
| 1678 | // pushes/pops in the meantime. |
| 1679 | // XXX: This is non-optimal, but only is slower cases with heavily |
| 1680 | // branching dominator trees.  To get the optimal number of queries would |
| 1681 | // be to make lowerbound and lastkill a per-loc stack, and pop it until |
| 1682 | // the top of that stack dominates us.  This does not seem worth it ATM. |
| 1683 | // A much cheaper optimization would be to always explore the deepest |
| 1684 | // branch of the dominator tree first. This will guarantee this resets on |
| 1685 | // the smallest set of blocks. |
| 1686 | if (LocInfo.LowerBoundBlock && LocInfo.LowerBoundBlock != BB && |
| 1687 | !DT->dominates(LocInfo.LowerBoundBlock, BB)) { |
| 1688 | // Reset the lower bound of things to check. |
| 1689 | // TODO: Some day we should be able to reset to last kill, rather than |
| 1690 | // 0. |
| 1691 | LocInfo.LowerBound = 0; |
| 1692 | LocInfo.LowerBoundBlock = VersionStack[0]->getBlock(); |
| 1693 | LocInfo.LastKillValid = false; |
| 1701 | } |
| 1702 | } else if (LocInfo.StackEpoch != StackEpoch) { |
| 1703 | // If all that has changed is the StackEpoch, we only have to check the |
| 1704 | // new things on the stack, because we've checked everything before.  In |
| 1705 | // this case, the lower bound of things to check remains the same. |
| 1706 | LocInfo.PopEpoch = PopEpoch; |
| 1707 | LocInfo.StackEpoch = StackEpoch; |
| 1715 | } |
|   | // 如果LastKillValid为false，则初始化LastKill和AR。 |
| 1716 | if (!LocInfo.LastKillValid) { |
| 1717 | LocInfo.LastKill = VersionStack.size() - 1; |
| 1718 | LocInfo.LastKillValid = true; |
| 1719 | LocInfo.AR = MayAlias; |
| 1724 | } |
|   | // 确保LastKill和LowerBound在范围内。 |
| 1725 | // At this point, we should have corrected last kill and LowerBound to be |
| 1726 | // in bounds. |
| 1727 | assert(LocInfo.LowerBound < VersionStack.size() && |
| 1728 | "Lower bound out of range"); |
| 1729 | assert(LocInfo.LastKill < VersionStack.size() && |
| 1730 | "Last kill info out of range"); |
|   | // 新的上限是栈顶。 |
| 1731 | // In any case, the new upper bound is the top of the stack. |
| 1732 | unsigned long UpperBound = VersionStack.size() - 1; |
|   | // 如果需要检查的存储数量超过最大限制，则跳过优化。 |
| 1733 | if (UpperBound - LocInfo.LowerBound > MaxCheckLimit) { |
| 1734 | LLVM_DEBUG(dbgs() << "MemorySSA skipping optimization of " << *MU << " (" |
| 1735 | << *(MU->getMemoryInst()) << ")" |
| 1736 | << " because there are " |
| 1737 | << UpperBound - LocInfo.LowerBound |
| 1738 | << " stores to disambiguate\n"); |
| 1739 | // Because we did not walk, LastKill is no longer valid, as this may |
| 1740 | // have been a kill. |
| 1741 | LocInfo.LastKillValid = false; |
| 1742 | continue; |
| 1743 | } |
| 1744 | bool FoundClobberResult = false; |
| 1745 | unsigned UpwardWalkLimit = MaxCheckLimit; |
| 1746 | while (UpperBound > LocInfo.LowerBound) { |
| 1747 | if (isa<MemoryPhi>(VersionStack[UpperBound])) { |
|   | // 对于Phi，使用walker找到结果，然后去那里。 |
| 1748 | // For phis, use the walker, see where we ended up, go there |
| 1749 | MemoryAccess *Result = |
| 1750 | Walker->getClobberingMemoryAccess(MU, UpwardWalkLimit); |
|   | // 我们保证找到它或者有错误。 |
| 1751 | // We are guaranteed to find it or something is wrong |
| 1752 | while (VersionStack[UpperBound] != Result) { |
| 1753 | assert(UpperBound != 0); |
| 1754 | --UpperBound; |
| 1763 | } |
| 1764 | FoundClobberResult = true; |
| 1765 | break; |
| 1776 | } |
| 1777 | MemoryDef *MD = cast<MemoryDef>(VersionStack[UpperBound]); |
|   | // 如果指针的生命周期在这条指令结束，则是live on entry。 |
| 1778 | // If the lifetime of the pointer ends at this instruction, it's live on |
| 1779 | // entry. |
| 1780 | if (!UseMLOC.IsCall && lifetimeEndsAt(MD, UseMLOC.getLoc(), *AA)) { |
| 1781 | // Reset UpperBound to liveOnEntryDef's place in the stack |
| 1782 | UpperBound = 0; |
| 1783 | FoundClobberResult = true; |
| 1784 | LocInfo.AR = MustAlias; |
| 1785 | break; |
| 1790 | } |
| 1791 | ClobberAlias CA = instructionClobbersQuery(MD, MU, UseMLOC, *AA); |
| 1792 | if (CA.IsClobber) { |
| 1793 | FoundClobberResult = true; |
| 1794 | LocInfo.AR = CA.AR; |
| 1795 | break; |
| 1805 | } |
| 1806 | --UpperBound; |
| 1807 | } |
|   | // 注意：Phi总是有AliasResult AR设置为MayAlias。 |
| 1808 | // Note: Phis always have AliasResult AR set to MayAlias ATM. |
|   | // 在这个循环结束时，UpperBound要么是一个clobber， |
|   | // 要么下限PHI walking可能使其小于LowerBound。 |
| 1809 | // At the end of this loop, UpperBound is either a clobber, or lower bound |
| 1810 | // PHI walking may cause it to be < LowerBound, and in fact, < LastKill. |
| 1811 | if (FoundClobberResult || UpperBound < LocInfo.LastKill) { |
|   | // 我们现在被UpperBound处的内存访问杀死。 |
| 1812 | // We were last killed now by where we got to |
| 1813 | if (MSSA->isLiveOnEntryDef(VersionStack[UpperBound])) |
| 1814 | LocInfo.AR = None; |
| 1815 | MU->setDefiningAccess(VersionStack[UpperBound], true, LocInfo.AR); |
| 1816 | LocInfo.LastKill = UpperBound; |
| 1817 | } else { |
|   | // 否则，我们检查了所有新的内容，现在我们知道可以到达LastKill。 |
| 1818 | // Otherwise, we checked all the new ones, and now we know we can get to |
| 1819 | // LastKill. |
| 1820 | MU->setDefiningAccess(VersionStack[LocInfo.LastKill], true, LocInfo.AR); |
| 1825 |  |
| 1829 | } |
| 1830 | LocInfo.LowerBound = VersionStack.size() - 1; |
| 1831 | LocInfo.LowerBoundBlock = BB; |
| 1832 | } |
| 1847 | } |
|   | /// 优化内存使用，使它们指向实际覆盖它们的定义。 |
| 1848 | /// Optimize uses to point to their actual clobbering definitions. |
| 1849 | void MemorySSA::OptimizeUses::optimizeUses() { |
| 1850 | SmallVector<MemoryAccess *, 16> VersionStack; |
| 1851 | DenseMap<MemoryLocOrCall, MemlocStackInfo> LocStackInfo; |
|   | // 将活动入口定义（liveOnEntryDef）推入版本栈。 |
| 1852 | VersionStack.push_back(MSSA->getLiveOnEntryDef()); |
| 1853 | unsigned long StackEpoch = 1; |
| 1854 | unsigned long PopEpoch = 1; |
|   | // 执行非递归的自顶向下支配树遍历。 |
| 1855 | // We perform a non-recursive top-down dominator tree walk. |
| 1856 | for (const auto *DomNode : depth_first(DT->getRootNode())) |
| 1857 | optimizeUsesInBlock(DomNode->getBlock(), StackEpoch, PopEpoch, VersionStack, |
| 1858 | LocStackInfo); |
| 1861 | } |
|   | /// 在适当的位置放置MemoryPhi节点。 |
| 1862 | void MemorySSA::placePHINodes( |
| 1863 | const SmallPtrSetImpl<BasicBlock *> &DefiningBlocks) { |
|   | // 确定MemoryPhi应该放置的位置。 |
| 1864 | // Determine where our MemoryPhi's should go |
| 1865 | ForwardIDFCalculator IDFs(*DT); |
| 1866 | IDFs.setDefiningBlocks(DefiningBlocks); |
| 1867 | SmallVector<BasicBlock *, 32> IDFBlocks; |
| 1868 | IDFs.calculate(IDFBlocks); |
|   | // 现在放置MemoryPhi节点。 |
| 1869 | // Now place MemoryPhi nodes. |
| 1870 | for (auto &BB : IDFBlocks) |
| 1871 | createMemoryPhi(BB); |
| 1872 | } |
|   | /// 构建 MemorySSA 形式。 |
| 1873 | void MemorySSA::buildMemorySSA(BatchAAResults &BAA) { |
|   | // 创建一个表示“入口处活动”的访问，用于参数或全局变量的用户， |
|   | // 其中它们使用的内存在函数开始之前就已经定义了。 |
|   | // 我们实际上并不将其插入到 IR 中。 |
| 1874 | // We create an access to represent "live on entry", for things like |
| 1875 | // arguments or users of globals, where the memory they use is defined before |
| 1876 | // the beginning of the function. We do not actually insert it into the IR. |
| 1877 | // We do not define a live on exit for the immediate uses, and thus our |
| 1878 | // semantics do *not* imply that something with no immediate uses can simply |
| 1879 | // be removed. |
| 1880 | BasicBlock &StartingPoint = F.getEntryBlock(); |
| 1881 | LiveOnEntryDef.reset(new MemoryDef(F.getContext(), nullptr, nullptr, |
| 1882 | &StartingPoint, NextID++)); |
|   | // 维护每个基本块的内存访问列表，以空间换时间。 |
|   | // 我们也可以通过在指令流中查找每个可能的指令的内存访问。 |
| 1883 | // We maintain lists of memory accesses per-block, trading memory for time. We |
| 1884 | // could just look up the memory access for every possible instruction in the |
| 1885 | // stream. |
| 1886 | SmallPtrSet<BasicBlock *, 32> DefiningBlocks; |
|   | // 遍历每个块，确定定义发生的位置，并将所有访问链接在一起。 |
| 1887 | // Go through each block, figure out where defs occur, and chain together all |
| 1888 | // the accesses. |
| 1889 | for (BasicBlock &B : F) { |
| 1890 | bool InsertIntoDef = false; |
| 1891 | AccessList *Accesses = nullptr; |
| 1892 | DefsList *Defs = nullptr; |
| 1893 | for (Instruction &I : B) { |
| 1894 | MemoryUseOrDef *MUD = createNewAccess(&I, &BAA); |
| 1895 | if (!MUD) |
| 1896 | continue; |
| 1897 | if (!Accesses) |
| 1898 | Accesses = getOrCreateAccessList(&B); |
| 1899 | Accesses->push_back(MUD); |
| 1900 | if (isa<MemoryDef>(MUD)) { |
| 1901 | InsertIntoDef = true; |
| 1902 | if (!Defs) |
| 1903 | Defs = getOrCreateDefsList(&B); |
| 1904 | Defs->push_back(*MUD); |
| 1906 | } |
| 1908 | } |
| 1909 | if (InsertIntoDef) |
| 1910 | DefiningBlocks.insert(&B); |
| 1916 | } |
| 1917 | placePHINodes(DefiningBlocks); |
|   | // 执行常规 SSA 重命名。 |
| 1918 | // Now do regular SSA renaming on the MemoryDef/MemoryUse. Visited will get |
| 1919 | // filled in with all blocks. |
| 1920 | SmallPtrSet<BasicBlock *, 16> Visited; |
| 1921 | renamePass(DT->getRootNode(), LiveOnEntryDef.get(), Visited); |
|   | // 使用批量别名分析结果创建 ClobberWalkerBase 和 CachingWalker。 |
| 1922 | ClobberWalkerBase<BatchAAResults> WalkerBase(this, &BAA, DT); |
| 1923 | CachingWalker<BatchAAResults> WalkerLocal(this, &WalkerBase); |
|   | // 优化使用，使它们指向实际覆盖它们的定义。 |
| 1924 | OptimizeUses(this, &WalkerLocal, &BAA, DT).optimizeUses(); |
|   | // 将不可达块中的使用标记为入口处活动，以便它们有地方去。 |
| 1925 | // Mark the uses in unreachable blocks as live on entry, so that they go |
| 1926 | // somewhere. |
| 1927 | for (auto &BB : F) |
| 1928 | if (!Visited.count(&BB)) |
| 1929 | markUnreachableAsLiveOnEntry(&BB); |
| 1930 | } |
|   | /// 获取 MemorySSA 的 Walker 实例。 |
| 1931 | MemorySSAWalker *MemorySSA::getWalker() { return getWalkerImpl(); } |
|   | /// 获取 CachingWalker 实例。 |
| 1932 | MemorySSA::CachingWalker<AliasAnalysis> *MemorySSA::getWalkerImpl() { |
|   | // 如果 Walker 已经存在，直接返回。 |
| 1933 | if (Walker) |
| 1934 | return Walker.get(); |
|   | // 如果 WalkerBase 不存在，创建它。 |
| 1935 | if (!WalkerBase) |
| 1936 | WalkerBase = |
| 1937 | std::make_unique<ClobberWalkerBase<AliasAnalysis>>(this, AA, DT); |
|   | // 创建 CachingWalker 实例。 |
| 1938 | Walker = |
| 1939 | std::make_unique<CachingWalker<AliasAnalysis>>(this, WalkerBase.get()); |
| 1940 | return Walker.get(); |
| 1941 |  |
| 1948 |  |
| 1953 | } |
|   | /// 获取 SkipSelfWalker 实例。 |
| 1954 | MemorySSAWalker *MemorySSA::getSkipSelfWalker() { |
|   | /// 如果 SkipWalker 已经存在，直接返回。 |
| 1955 | if (SkipWalker) |
| 1956 | return SkipWalker.get(); |
|   | /// 如果 WalkerBase 不存在，创建它。 |
| 1957 | if (!WalkerBase) |
| 1958 | WalkerBase = |
| 1959 | std::make_unique<ClobberWalkerBase<AliasAnalysis>>(this, AA, DT); |
|   | /// 创建 SkipSelfWalker 实例。 |
| 1960 | SkipWalker = |
| 1961 | std::make_unique<SkipSelfWalker<AliasAnalysis>>(this, WalkerBase.get()); |
| 1962 | return SkipWalker.get(); |
| 1963 | } |
|   | // 这是一个辅助函数，由创建例程使用。 |
|   | // 它将NewAccess插入到给定基本块的访问和定义列表中，在给定的插入点。 |
| 1964 | // This is a helper function used by the creation routines. It places NewAccess |
| 1965 | // into the access and defs lists for a given basic block, at the given |
| 1966 | // insertion point. |
| 1967 | void MemorySSA::insertIntoListsForBlock(MemoryAccess *NewAccess, |
| 1968 | const BasicBlock *BB, |
| 1969 | InsertionPlace Point) { |
|   | // 获取或创建给定基本块BB的访问列表。 |
| 1970 | auto *Accesses = getOrCreateAccessList(BB); |
|   | // 如果插入点是开始位置（Beginning），则执行以下操作。 |
| 1971 | if (Point == Beginning) { |
|   | // 如果NewAccess是一个phi节点，它会被放在最前面，否则，它会放在任何phi节点之后。 |
| 1972 | // If it's a phi node, it goes first, otherwise, it goes after any phi |
| 1973 | // nodes. |
| 1974 | if (isa<MemoryPhi>(NewAccess)) { |
|   | // 将NewAccess插入到访问列表的前面。 |
| 1975 | Accesses->push_front(NewAccess); |
|   | // 获取或创建给定基本块BB的定义列表。 |
| 1976 | auto *Defs = getOrCreateDefsList(BB); |
|   | // 将NewAccess插入到定义列表的前面。 |
| 1977 | Defs->push_front(*NewAccess); |
| 1978 | } else { |
|   | // 找到访问列表中第一个非phi节点的位置。 |
| 1979 | auto AI = find_if_not( |
| 1980 | *Accesses, [](const MemoryAccess &MA) { return isa<MemoryPhi>(MA); }); |
|   | // 将NewAccess插入到该位置。 |
| 1981 | Accesses->insert(AI, NewAccess); |
|   | // 如果NewAccess不是一个使用（MemoryUse）节点，那么执行以下操作。 |
| 1982 | if (!isa<MemoryUse>(NewAccess)) { |
|   | // 获取或创建给定基本块BB的定义列表。 |
| 1983 | auto *Defs = getOrCreateDefsList(BB); |
|   | // 找到定义列表中第一个非phi节点的位置。 |
| 1984 | auto DI = find_if_not( |
| 1985 | *Defs, [](const MemoryAccess &MA) { return isa<MemoryPhi>(MA); }); |
|   | // 将NewAccess插入到该位置。 |
| 1986 | Defs->insert(DI, *NewAccess); |
| 1995 | } |
| 1997 | } |
| 1998 | } else { |
|   | // 如果插入点不是开始位置，将NewAccess添加到访问列表的末尾。 |
| 1999 | Accesses->push_back(NewAccess); |
|   | // 如果NewAccess不是一个使用（MemoryUse）节点，那么执行以下操作。 |
| 2000 | if (!isa<MemoryUse>(NewAccess)) { |
|   | // 获取或创建给定基本块BB的定义列表。 |
| 2001 | auto *Defs = getOrCreateDefsList(BB); |
|   | // 将NewAccess添加到定义列表的末尾。 |
| 2002 | Defs->push_back(*NewAccess); |
| 2015 | } |
| 2020 | } |
|   | // 从BlockNumberingValid中移除BB，因为插入操作可能会影响到块的编号。 |
| 2021 | BlockNumberingValid.erase(BB); |
| 2022 | } |
|   | // 在给定的基本块BB的访问列表中，将What内存访问对象插入到 |
|   | // InsertPt迭代器指定的位置之前。 |
| 2023 | void MemorySSA::insertIntoListsBefore(MemoryAccess *What, const BasicBlock *BB, |
| 2024 | AccessList::iterator InsertPt) { |
|   | // 获取可写的访问列表，该列表包含BB中所有的内存访问。 |
| 2025 | auto *Accesses = getWritableBlockAccesses(BB); |
|   | // 检查InsertPt是否指向访问列表的末尾。 |
| 2026 | bool WasEnd = InsertPt == Accesses->end(); |
|   | // 在InsertPt之前插入What对象到访问列表。 |
| 2027 | Accesses->insert(AccessList::iterator(InsertPt), What); |
|   | // 如果What不是一个MemoryUse对象，即它是一个定义或phi节点，那么执行以下操作。 |
| 2028 | if (!isa<MemoryUse>(What)) { |
|   | // 获取或创建当前基本块BB的定义列表。 |
| 2029 | auto *Defs = getOrCreateDefsList(BB); |
|   | // 如果InsertPt指向列表末尾，直接将What添加到定义列表的末尾。 |
|   | // 如果InsertPt指向一个已存在的定义，我们可以直接使用迭代器插入。 |
|   | // 如果InsertPt指向一个使用，我们需要找到下一个定义。 |
| 2030 | // If we got asked to insert at the end, we have an easy job, just shove it |
| 2031 | // at the end. If we got asked to insert before an existing def, we also get |
| 2032 | // an iterator. If we got asked to insert before a use, we have to hunt for |
| 2033 | // the next def. |
| 2034 | if (WasEnd) { |
| 2035 | Defs->push_back(*What); |
| 2036 | } else if (isa<MemoryDef>(InsertPt)) { |
|   | // 如果InsertPt指向一个定义，直接在该定义之前插入What。 |
| 2037 | Defs->insert(InsertPt->getDefsIterator(), *What); |
| 2038 | } else { |
|   | // 遍历访问列表，找到下一个定义节点。 |
| 2039 | while (InsertPt != Accesses->end() && !isa<MemoryDef>(InsertPt)) |
| 2040 | ++InsertPt; |
|   | // 如果找到了定义，或者已经到达列表末尾。 |
| 2041 | // Either we found a def, or we are inserting at the end |
| 2042 | if (InsertPt == Accesses->end()) |
| 2043 | Defs->push_back(*What); |
| 2044 | else |
| 2045 | Defs->insert(InsertPt->getDefsIterator(), *What); |
| 2050 | } |
| 2051 | } |
|   | // 插入操作可能会影响到块的编号，因此从BlockNumberingValid中移除BB。 |
| 2052 | BlockNumberingValid.erase(BB); |
| 2053 | } |
|   | // 准备将What内存访问对象移动到BB基本块。 |
| 2054 | void MemorySSA::prepareForMoveTo(MemoryAccess *What, BasicBlock *BB) { |
|   | // 保持What在查找表中的记录，但是从当前的列表中移除。 |
| 2055 | // Keep it in the lookup tables, remove from the lists |
| 2056 | removeFromLists(What, false); |
|   | // 注意移动操作应该隐式地使MemoryUse的优化状态失效（phi节点不能被优化）。 |
|   | // 但是，对于MemoryDef，它不会这样做。 |
| 2057 | // Note that moving should implicitly invalidate the optimized state of a |
| 2058 | // MemoryUse (and Phis can't be optimized). However, it doesn't do so for a |
| 2059 | // MemoryDef. |
| 2060 | if (auto *MD = dyn_cast<MemoryDef>(What)) |
| 2061 | MD->resetOptimized(); |
| 2062 | What->setBlock(BB); |
| 2069 | } |
|   | // 将What移动到IR中的Where之前的位置。最终结果是What将属于正确的列表， |
|   | // 并且设置了正确的Block，但是在其他方面不会是正确的。它不会有正确的定义访问， |
|   | // 如果它是一个定义，它下面的部分不会被正确更新。 |
| 2070 | // Move What before Where in the IR.  The end result is that What will belong to |
| 2071 | // the right lists and have the right Block set, but will not otherwise be |
| 2072 | // correct. It will not have the right defining access, and if it is a def, |
| 2073 | // things below it will not properly be updated. |
| 2074 | void MemorySSA::moveTo(MemoryUseOrDef *What, BasicBlock *BB, |
| 2075 | AccessList::iterator Where) { |
|   | // 准备将What移动到BB。 |
| 2076 | prepareForMoveTo(What, BB); |
|   | // 在BB基本块的访问列表中，将What插入到Where迭代器指定的位置之前。 |
| 2077 | insertIntoListsBefore(What, BB, Where); |
| 2085 | } |
|   | // 将What内存访问对象移动到BB基本块的Point指定位置。 |
| 2086 | void MemorySSA::moveTo(MemoryAccess *What, BasicBlock *BB, |
| 2087 | InsertionPlace Point) { |
|   | // 如果What是一个phi节点，检查Point是否为Beginning，因为phi节点只能移动到块的开始位置。 |
| 2088 | if (isa<MemoryPhi>(What)) { |
| 2089 | assert(Point == Beginning && |
| 2090 | "Can only move a Phi at the beginning of the block"); |
|   | // 更新查找表条目，从What原来的基本块中移除，然后插入到新的基本块BB中。 |
| 2091 | // Update lookup table entry |
| 2092 | ValueToMemoryAccess.erase(What->getBlock()); |
| 2093 | bool Inserted = ValueToMemoryAccess.insert({BB, What}).second; |
| 2094 | (void)Inserted; |
| 2095 | assert(Inserted && "Cannot move a Phi to a block that already has one"); |
| 2096 | } |
|   | // 准备将What移动到BB基本块，这包括从当前列表中移除What，并更新其所属的基本块。 |
| 2097 | prepareForMoveTo(What, BB); |
|   | // 将What插入到BB基本块的访问和定义列表中，根据Point指定的位置。 |
| 2098 | insertIntoListsForBlock(What, BB, Point); |
| 2099 | } |
|   | // 创建一个新的MemoryPhi节点，并将其插入到指定基本块BB的开始位置。 |
| 2100 | MemoryPhi *MemorySSA::createMemoryPhi(BasicBlock *BB) { |
|   | // 断言BB没有已经存在的MemoryPhi节点。 |
| 2101 | assert(!getMemoryAccess(BB) && "MemoryPhi already exists for this BB"); |
|   | // 使用LLVM上下文、基本块BB和下一个可用的ID创建一个新的MemoryPhi节点。 |
| 2102 | MemoryPhi *Phi = new MemoryPhi(BB->getContext(), BB, NextID++); |
|   | // MemoryPhi节点总是被放置在基本块的开始位置。 |
| 2103 | // Phi's always are placed at the front of the block. |
| 2104 | insertIntoListsForBlock(Phi, BB, Beginning); |
|   | // 在查找表中记录BB对应的MemoryPhi节点。 |
| 2105 | ValueToMemoryAccess[BB] = Phi; |
| 2106 | return Phi; |
| 2109 |  |
| 2112 |  |
| 2119 | } |
|   | // 创建一个新的定义访问（MemoryUseOrDef），它对应于指令I，并使用给定的定义和模板。 |
| 2120 | MemoryUseOrDef *MemorySSA::createDefinedAccess(Instruction *I, |
| 2121 | MemoryAccess *Definition, |
| 2122 | const MemoryUseOrDef *Template, |
| 2123 | bool CreationMustSucceed) { |
|   | // 断言I不能是PHI节点，因为PHI节点不能有定义访问。 |
| 2124 | assert(!isa<PHINode>(I) && "Cannot create a defined access for a PHI"); |
|   | // 使用指令I、别名分析器AA和模板创建一个新的访问节点。 |
| 2125 | MemoryUseOrDef *NewAccess = createNewAccess(I, AA, Template); |
|   | // 如果CreationMustSucceed为真，断言NewAccess不是空指针，即创建必须成功。 |
| 2126 | if (CreationMustSucceed) |
| 2127 | assert(NewAccess != nullptr && "Tried to create a memory access for a " |
| 2128 | "non-memory touching instruction"); |
|   | // 如果NewAccess创建成功，设置其定义访问。 |
| 2129 | if (NewAccess) |
| 2130 | NewAccess->setDefiningAccess(Definition); |
| 2131 | return NewAccess; |
| 2132 | } |
|   | // 判断指令是否有排序约束。 |
|   | // 特别指出，这只考虑存储和加载操作，因为其他操作仍然被getModRefInfo视为ModRef。 |
| 2133 | // Return true if the instruction has ordering constraints. |
| 2134 | // Note specifically that this only considers stores and loads |
| 2135 | // because others are still considered ModRef by getModRefInfo. |
| 2136 | static inline bool isOrdered(const Instruction *I) { |
| 2137 | if (auto *SI = dyn_cast<StoreInst>(I)) { |
| 2138 | if (!SI->isUnordered()) |
| 2139 | return true; |
| 2140 | } else if (auto *LI = dyn_cast<LoadInst>(I)) { |
| 2141 | if (!LI->isUnordered()) |
| 2142 | return true; |
| 2143 | } |
| 2144 | return false; |
| 2146 | } |
|   | /// 辅助函数，用于创建新的内存访问 |
| 2147 | /// Helper function to create new memory accesses |
| 2148 | template <typename AliasAnalysisType> |
| 2149 | MemoryUseOrDef *MemorySSA::createNewAccess(Instruction *I, |
| 2150 | AliasAnalysisType *AAP, |
| 2151 | const MemoryUseOrDef *Template) { |
|   | // 假设内在函数具有控制依赖性，我们通过声称它任意写入来建模。 |
|   | // 当我们有一个非标准的AA管道时，调试信息内在函数可能被视为清除。 |
|   | // 在这里忽略这些假的内存依赖。 |
| 2152 | // The assume intrinsic has a control dependency which we model by claiming |
| 2153 | // that it writes arbitrarily. Debuginfo intrinsics may be considered |
| 2154 | // clobbers when we have a nonstandard AA pipeline. Ignore these fake memory |
| 2155 | // dependencies here. |
| 2156 | // FIXME: Replace this special casing with a more accurate modelling of |
| 2157 | // assume's control dependency. |
| 2158 | if (IntrinsicInst *II = dyn_cast<IntrinsicInst>(I)) |
| 2159 | if (II->getIntrinsicID() == Intrinsic::assume) |
| 2160 | return nullptr; |
|   | // 使用非标准的AA管道可能会给我们留下I的意外的modref结果，所以添加一个检查， |
|   | // 不要模拟可能不从内存读取或写入的指令。这对于正确性是必要的。 |
| 2161 | // Using a nonstandard AA pipelines might leave us with unexpected modref |
| 2162 | // results for I, so add a check to not model instructions that may not read |
| 2163 | // from or write to memory. This is necessary for correctness. |
| 2164 | if (!I->mayReadFromMemory() && !I->mayWriteToMemory()) |
| 2165 | return nullptr; |
| 2166 | bool Def, Use; |
| 2167 | if (Template) { |
|   | // 如果提供了模板，根据模板确定Def和Use。 |
| 2168 | Def = dyn_cast_or_null<MemoryDef>(Template) != nullptr; |
| 2169 | Use = dyn_cast_or_null<MemoryUse>(Template) != nullptr; |
| 2170 | #if !defined(NDEBUG) |
|   | // 在非调试模式下，我们不执行这个检查。 |
| 2171 | ModRefInfo ModRef = AAP->getModRefInfo(I, None); |
| 2172 | bool DefCheck, UseCheck; |
| 2173 | DefCheck = isModSet(ModRef) || isOrdered(I); |
| 2174 | UseCheck = isRefSet(ModRef); |
| 2175 | assert(Def == DefCheck && (Def || Use == UseCheck) && "Invalid template"); |
| 2176 | #endif |
| 2177 | } else { |
|   | // 如果没有提供模板，使用别名分析来确定指令对内存的影响。 |
| 2178 | // Find out what affect this instruction has on memory. |
| 2179 | ModRefInfo ModRef = AAP->getModRefInfo(I, None); |
|   | // 使用isOrdered检查确保易失性操作最终成为定义（原子操作目前无论如何都是ModRef）。 |
|   | // 直到我们将排序链和内存链分开，这至少使人们能够看到易失性的一些相对排序。 |
|   | // 注意，getClobberingMemoryAccess仍然会给出一个绕过其他易失性加载的答案。 |
|   | // TODO: 将内存别名和排序分成两个不同的链，以便我们可以精确表示 |
|   | //“哪些内存将被这个读写/被清除”和“我可以移动这个操作超过哪些指令”。 |
| 2180 | // The isOrdered check is used to ensure that volatiles end up as defs |
| 2181 | // (atomics end up as ModRef right now anyway).  Until we separate the |
| 2182 | // ordering chain from the memory chain, this enables people to see at least |
| 2183 | // some relative ordering to volatiles.  Note that getClobberingMemoryAccess |
| 2184 | // will still give an answer that bypasses other volatile loads.  TODO: |
| 2185 | // Separate memory aliasing and ordering into two different chains so that |
| 2186 | // we can precisely represent both "what memory will this read/write/is |
| 2187 | // clobbered by" and "what instructions can I move this past". |
| 2188 | Def = isModSet(ModRef) || isOrdered(I); |
| 2189 | Use = isRefSet(ModRef); |
| 2192 |  |
| 2203 | } |
|   | // 有可能指令根本不修改内存。在构建过程中，我们忽略它们。 |
| 2204 | // It's possible for an instruction to not modify memory at all. During |
| 2205 | // construction, we ignore them. |
| 2206 | if (!Def && !Use) |
| 2207 | return nullptr; |
|   | // 根据Def和Use的值创建MemoryDef或MemoryUse对象。 |
| 2208 | MemoryUseOrDef *MUD; |
| 2209 | if (Def) |
| 2210 | MUD = new MemoryDef(I->getContext(), nullptr, I, I->getParent(), NextID++); |
| 2213 | else |
| 2214 | MUD = new MemoryUse(I->getContext(), nullptr, I, I->getParent()); |
|   | // 在查找表中记录指令对应的内存访问。 |
| 2215 | ValueToMemoryAccess[I] = MUD; |
| 2216 | return MUD; |
| 2219 | } |
|   | /// 如果Replacer支配Replacee，则返回true。 |
| 2220 | /// Returns true if \p Replacer dominates \p Replacee . |
| 2221 | bool MemorySSA::dominatesUse(const MemoryAccess *Replacer, |
| 2222 | const MemoryAccess *Replacee) const { |
|   | // 如果Replacee是MemoryUseOrDef类型，直接检查Replacer是否支配Replacee的基本块。 |
| 2223 | if (isa<MemoryUseOrDef>(Replacee)) |
| 2224 | return DT->dominates(Replacer->getBlock(), Replacee->getBlock()); |
| 2225 | const auto *MP = cast<MemoryPhi>(Replacee); |
|   | // 对于phi节点，使用发生在phi节点的前驱基本块中。 |
|   | // 由于我们可能在phi节点中多次出现，我们需要检查每个操作数以确保Replacer支配Replacee出现的每个操作数。 |
| 2226 | // For a phi node, the use occurs in the predecessor block of the phi node. |
| 2227 | // Since we may occur multiple times in the phi node, we have to check each |
| 2228 | // operand to ensure Replacer dominates each operand where Replacee occurs. |
| 2229 | for (const Use &Arg : MP->operands()) { |
| 2230 | if (Arg.get() != Replacee && |
| 2231 | !DT->dominates(Replacer->getBlock(), MP->getIncomingBlock(Arg))) |
| 2234 |  |
| 2239 |  |
| 2245 | return false; |
| 2246 | } |
| 2247 | return true; |
| 2249 |  |
| 2255 | } |
|   | /// 从MemorySSA的所有查找表中正确移除MA。 |
| 2256 | /// Properly remove \p MA from all of MemorySSA's lookup tables. |
| 2257 | void MemorySSA::removeFromLookups(MemoryAccess *MA) { |
|   | // 断言MA没有使用，即它没有被其他内存访问对象引用。 |
| 2258 | assert(MA->use_empty() && |
| 2259 | "Trying to remove memory access that still has uses"); |
|   | // 从块编号中移除MA。 |
| 2260 | BlockNumbering.erase(MA); |
|   | // 如果MA是MemoryUseOrDef类型，设置其定义访问为nullptr。 |
| 2261 | if (auto *MUD = dyn_cast<MemoryUseOrDef>(MA)) |
| 2262 | MUD->setDefiningAccess(nullptr); |
|   | // 如果需要，使walker的缓存失效。 |
| 2263 | // Invalidate our walker's cache if necessary |
| 2264 | if (!isa<MemoryUse>(MA)) |
| 2265 | getWalker()->invalidateInfo(MA); |
|   | // 获取与MA关联的指令或基本块。 |
| 2266 | Value *MemoryInst; |
| 2267 | if (const auto *MUD = dyn_cast<MemoryUseOrDef>(MA)) |
| 2268 | MemoryInst = MUD->getMemoryInst(); |
| 2269 | else |
| 2270 | MemoryInst = MA->getBlock(); |
|   | // 在ValueToMemoryAccess查找表中找到MA并移除。 |
| 2271 | auto VMA = ValueToMemoryAccess.find(MemoryInst); |
| 2272 | if (VMA->second == MA) |
| 2273 | ValueToMemoryAccess.erase(VMA); |
| 2277 |  |
| 2279 | } |
|   | /// 从MemorySSA的所有列表中正确移除MA。 |
| 2280 | /// |
|   | /// 由于侵入式列表和使用列表的工作方式，按正确的顺序进行移除很重要。 |
|   | /// ShouldDelete默认为true，表示不仅移除内存访问，还会删除它。 |
| 2281 | /// Properly remove \p MA from all of MemorySSA's lists. |
| 2282 | /// |
| 2283 | /// Because of the way the intrusive list and use lists work, it is important to |
| 2284 | /// do removal in the right order. |
| 2285 | /// ShouldDelete defaults to true, and will cause the memory access to also be |
| 2286 | /// deleted, not just removed. |
| 2287 | void MemorySSA::removeFromLists(MemoryAccess *MA, bool ShouldDelete) { |
| 2288 | BasicBlock *BB = MA->getBlock(); |
|   | // 访问列表拥有引用，因此我们首先从非拥有列表中移除它。 |
| 2289 | // The access list owns the reference, so we erase it from the non-owning list |
| 2290 | // first. |
| 2291 | if (!isa<MemoryUse>(MA)) { |
| 2292 | auto DefsIt = PerBlockDefs.find(BB); |
| 2293 | std::unique_ptr<DefsList> &Defs = DefsIt->second; |
| 2294 | Defs->remove(*MA); |
| 2295 | if (Defs->empty()) |
| 2296 | PerBlockDefs.erase(DefsIt); |
| 2300 | } |
|   | // 这里的erase调用将删除MA。如果我们不想删除它，我们调用remove。 |
| 2301 | // The erase call here will delete it. If we don't want it deleted, we call |
| 2302 | // remove instead. |
| 2303 | auto AccessIt = PerBlockAccesses.find(BB); |
| 2304 | std::unique_ptr<AccessList> &Accesses = AccessIt->second; |
| 2305 | if (ShouldDelete) |
| 2306 | Accesses->erase(MA); |
| 2307 | else |
| 2308 | Accesses->remove(MA); |
|   | // 如果访问列表为空，从PerBlockAccesses中移除并使BlockNumberingValid失效。 |
| 2309 | if (Accesses->empty()) { |
| 2310 | PerBlockAccesses.erase(AccessIt); |
| 2311 | BlockNumberingValid.erase(BB); |
| 2312 | } |
| 2313 | } |
|   | /// 打印MemorySSA信息到原始输出流OS。 |
| 2314 | void MemorySSA::print(raw_ostream &OS) const { |
| 2315 | MemorySSAAnnotatedWriter Writer(this); |
| 2316 | F.print(OS, &Writer); |
| 2317 | } |
|   | /// 如果定义了NDEBUG，则dump函数不会被包含在构建中。 |
|   | /// 如果定义了LLVM_ENABLE_DUMP，则启用dump方法。 |
|   | /// dump方法用于调试，它会打印MemorySSA信息到dbgs()输出流， |
|   | /// dbgs()是LLVM的一个调试输出流。 |
| 2318 | #if !defined(NDEBUG) || defined(LLVM_ENABLE_DUMP) |
| 2319 | LLVM_DUMP_METHOD void MemorySSA::dump() const { print(dbgs()); } |
| 2320 | #endif |
|   | /// 验证MemorySSA数据结构的正确性。 |
| 2321 | void MemorySSA::verifyMemorySSA() const { |
| 2322 | verifyOrderingDominationAndDefUses(F); |
| 2323 | verifyDominationNumbers(F); |
| 2324 | verifyPrevDefInPhis(F); |
|   | // 之前，验证过程还会检查MemorySSA缓存的clobberingAccess是否与之后从AA查询到的相同。 |
|   | // 但由于BasicAA的脆弱性，它在分析时有任意的限制， |
|   | // 在某些情况下，正确的转换会导致BasicAA在转换前后返回不同的别名答案。 |
|   | // 因此，这样的验证不再准确。Invalidating MemorySSA不是一个选项， |
|   | // 因为BasicAA的结果可能非常随机，在最坏的情况下， |
|   | // 我们可能需要在每次转换后从头重建MemorySSA，这违背了使用它的目的。 |
|   | // 有关此类情况的一个例子，参见D51960中添加的test4。 |
| 2325 | // Previously, the verification used to also verify that the clobberingAccess |
| 2326 | // cached by MemorySSA is the same as the clobberingAccess found at a later |
| 2327 | // query to AA. This does not hold true in general due to the current fragility |
| 2328 | // of BasicAA which has arbitrary caps on the things it analyzes before giving |
| 2329 | // up. As a result, transformations that are correct, will lead to BasicAA |
| 2330 | // returning different Alias answers before and after that transformation. |
| 2331 | // Invalidating MemorySSA is not an option, as the results in BasicAA can be so |
| 2332 | // random, in the worst case we'd need to rebuild MemorySSA from scratch after |
| 2333 | // every transformation, which defeats the purpose of using it. For such an |
| 2334 | // example, see test4 added in D51960. |
| 2335 | } |
|   | /// 验证MemorySSA中phi节点的前驱定义是否正确。 |
| 2336 | void MemorySSA::verifyPrevDefInPhis(Function &F) const { |
| 2337 | #if !defined(NDEBUG) && defined(EXPENSIVE_CHECKS) |
|   | // 只有当我们处于调试模式（NDEBUG未定义）并且定义了昂贵的检查（EXPENSIVE_CHECKS）时，才执行这个函数。 |
| 2338 | for (const BasicBlock &BB : F) { |
| 2339 | if (MemoryPhi *Phi = getMemoryAccess(&BB)) { |
| 2340 | for (unsigned I = 0, E = Phi->getNumIncomingValues(); I != E; ++I) { |
| 2341 | auto *Pred = Phi->getIncomingBlock(I); |
| 2342 | auto *IncAcc = Phi->getIncomingValue(I); |
|   | // 如果前驱基本块Pred有有效的支配树节点， |
|   | // 检查其最后一个定义是否与phi节点的输入值相同。 |
| 2343 | // If Pred has no unreachable predecessors, get last def looking at |
| 2344 | // IDoms. If, while walkings IDoms, any of these has an unreachable |
| 2345 | // predecessor, then the incoming def can be any access. |
| 2346 | if (auto *DTNode = DT->getNode(Pred)) { |
| 2347 | while (DTNode) { |
| 2348 | if (auto *DefList = getBlockDefs(DTNode->getBlock())) { |
| 2349 | auto *LastAcc = &*(--DefList->end()); |
| 2350 | assert(LastAcc == IncAcc && |
| 2351 | "Incorrect incoming access into phi."); |
| 2352 | break; |
| 2356 | } |
| 2357 | DTNode = DTNode->getIDom(); |
| 2358 | } |
| 2359 | } else { |
|   | // 如果前驱基本块没有有效的支配树节点（可能有不可达的前驱）， |
|   | // 则输入访问可以是任何访问。 |
|   | // 如果前驱基本块有不可达的前驱，但至少有一个定义， |
|   | // 那么输入访问可以是前驱中的最后一个定义， |
|   | // 或者可能已经被优化为LiveOnEntry（LoE）。 |
|   | // 在更新之后，LoE可能已经被另一个访问替换，所以IncAcc可以是任何访问。 |
|   | // 如果前驱基本块有不可达的前驱并且没有定义，输入访问应该是LoE； |
|   | // 然而，在更新之后，它可能是任何访问。 |
| 2360 | // If Pred has unreachable predecessors, but has at least a Def, the |
| 2361 | // incoming access can be the last Def in Pred, or it could have been |
| 2362 | // optimized to LoE. After an update, though, the LoE may have been |
| 2363 | // replaced by another access, so IncAcc may be any access. |
| 2364 | // If Pred has unreachable predecessors and no Defs, incoming access |
| 2365 | // should be LoE; However, after an update, it may be any access. |
| 2366 |  |
| 2368 |  |
| 2376 | } |
| 2377 | } |
| 2384 |  |
| 2386 |  |
| 2392 | } |
| 2393 |  |
| 2398 |  |
| 2405 | } |
| 2406 | #endif |
| 2413 | } |
|   | /// 验证所有我们认为有有效支配编号的块实际上确实有有效的支配编号。 |
| 2414 | /// Verify that all of the blocks we believe to have valid domination numbers |
| 2415 | /// actually have valid domination numbers. |
| 2416 | void MemorySSA::verifyDominationNumbers(const Function &F) const { |
| 2417 | #ifndef NDEBUG |
|   | // 如果BlockNumberingValid为空，则没有块需要验证。 |
| 2418 | if (BlockNumberingValid.empty()) |
| 2419 | return; |
|   | // 创建一个小型指针集合，包含所有被认为有有效支配编号的块。 |
| 2420 | SmallPtrSet<const BasicBlock *, 16> ValidBlocks = BlockNumberingValid; |
| 2421 | for (const BasicBlock &BB : F) { |
|   | // 如果当前块不在有效块集合中，则跳过。 |
| 2422 | if (!ValidBlocks.count(&BB)) |
| 2423 | continue; |
|   | // 从有效块集合中移除当前块。 |
| 2424 | ValidBlocks.erase(&BB); |
|   | // 获取当前块的所有内存访问。 |
| 2425 | const AccessList *Accesses = getBlockAccesses(&BB); |
| 2426 | // It's correct to say an empty block has valid numbering. |
| 2427 | if (!Accesses) |
| 2428 | continue; |
|   | // 块编号从1开始。 |
| 2429 | // Block numbering starts at 1. |
| 2430 | unsigned long LastNumber = 0; |
| 2431 | for (const MemoryAccess &MA : *Accesses) { |
|   | // 在BlockNumbering中查找当前内存访问的编号。 |
| 2432 | auto ThisNumberIter = BlockNumbering.find(&MA); |
|   | // 断言内存访问在有效块中有支配编号。 |
| 2433 | assert(ThisNumberIter != BlockNumbering.end() && |
| 2434 | "MemoryAccess has no domination number in a valid block!"); |
|   | // 获取当前内存访问的编号。 |
| 2435 | unsigned long ThisNumber = ThisNumberIter->second; |
|   | // 断言支配编号是严格递增的。 |
| 2436 | assert(ThisNumber > LastNumber && |
| 2437 | "Domination numbers should be strictly increasing!"); |
| 2438 | LastNumber = ThisNumber; |
| 2441 | } |
| 2442 |  |
| 2448 | } |
|   | // 断言所有有效的BasicBlocks都应该存在于F中，否则可能存在悬挂指针。 |
| 2449 | assert(ValidBlocks.empty() && |
| 2450 | "All valid BasicBlocks should exist in F -- dangling pointers?"); |
| 2451 | #endif |
| 2452 | } |
|   | /// 验证排序：MemoryAccesses的顺序和存在性与影响内存的指令的顺序和存在性相匹配。 |
|   | /// 验证支配：每个定义支配其所有使用。 |
|   | /// 验证定义-使用：对于每个使用，它出现在相应定义的使用列表中。 |
| 2453 | /// Verify ordering: the order and existence of MemoryAccesses matches the |
| 2454 | /// order and existence of memory affecting instructions. |
| 2455 | /// Verify domination: each definition dominates all of its uses. |
| 2456 | /// Verify def-uses: the immediate use information - walk all the memory |
| 2457 | /// accesses and verifying that, for each use, it appears in the appropriate |
| 2458 | /// def's use list |
| 2459 | void MemorySSA::verifyOrderingDominationAndDefUses(Function &F) const { |
| 2460 | #if !defined(NDEBUG) |
|   | // 遍历所有块，比较查找结果和访问列表的结果，以及块中的顺序与访问列表中的顺序。 |
| 2461 | // Walk all the blocks, comparing what the lookups think and what the access |
| 2462 | // lists think, as well as the order in the blocks vs the order in the access |
| 2463 | // lists. |
| 2464 | SmallVector<MemoryAccess *, 32> ActualAccesses; |
| 2465 | SmallVector<MemoryAccess *, 32> ActualDefs; |
| 2466 | for (BasicBlock &B : F) { |
| 2467 | const AccessList *AL = getBlockAccesses(&B); |
| 2468 | const auto *DL = getBlockDefs(&B); |
| 2469 | MemoryPhi *Phi = getMemoryAccess(&B); |
| 2470 | if (Phi) { |
|   | // 验证排序。 |
| 2471 | // Verify ordering. |
| 2472 | ActualAccesses.push_back(Phi); |
| 2473 | ActualDefs.push_back(Phi); |
| 2474 | // Verify domination |
| 2475 | for (const Use &U : Phi->uses()) |
| 2476 | assert(dominates(Phi, U) && "Memory PHI does not dominate it's uses"); |
| 2477 | #if defined(EXPENSIVE_CHECKS) |
|   | // 验证定义-使用。 |
| 2478 | // Verify def-uses. |
| 2479 | assert(Phi->getNumOperands() == static_cast<unsigned>(std::distance( |
| 2480 | pred_begin(&B), pred_end(&B))) && |
| 2481 | "Incomplete MemoryPhi Node"); |
| 2482 | for (unsigned I = 0, E = Phi->getNumIncomingValues(); I != E; ++I) { |
| 2483 | verifyUseInDefs(Phi->getIncomingValue(I), Phi); |
| 2484 | assert(find(predecessors(&B), Phi->getIncomingBlock(I)) != |
| 2485 | pred_end(&B) && |
| 2486 | "Incoming phi block not a block predecessor"); |
| 2487 | } |
| 2488 | #endif |
| 2489 | } |
| 2490 | for (Instruction &I : B) { |
| 2491 | MemoryUseOrDef *MA = getMemoryAccess(&I); |
|   | // 断言我们有内存影响的指令在块中，但它们不在访问列表或定义列表中。 |
| 2492 | assert((!MA || (AL && (isa<MemoryUse>(MA) || DL))) && |
| 2493 | "We have memory affecting instructions " |
| 2494 | "in this block but they are not in the " |
| 2495 | "access list or defs list"); |
| 2496 | if (MA) { |
|   | // 验证排序。 |
| 2497 | // Verify ordering. |
| 2498 | ActualAccesses.push_back(MA); |
| 2499 | if (MemoryAccess *MD = dyn_cast<MemoryDef>(MA)) { |
|   | // 验证排序。 |
| 2500 | // Verify ordering. |
| 2501 | ActualDefs.push_back(MA); |
|   | // 验证支配。 |
| 2502 | // Verify domination. |
| 2503 | for (const Use &U : MD->uses()) |
| 2504 | assert(dominates(MD, U) && |
| 2505 | "Memory Def does not dominate it's uses"); |
| 2506 | } |
| 2507 | #if defined(EXPENSIVE_CHECKS) |
|   | // 验证定义-使用。 |
| 2508 | // Verify def-uses. |
| 2509 | verifyUseInDefs(MA->getDefiningAccess(), MA); |
| 2510 | #endif |
| 2511 | } |
| 2512 | } |
|   | // 要么我们遇到了断言，真的没有访问，或者我们既有访问也有访问列表。对于定义也是如此。 |
| 2513 | // Either we hit the assert, really have no accesses, or we have both |
| 2514 | // accesses and an access list. Same with defs. |
| 2515 | if (!AL && !DL) |
| 2516 | continue; |
|   | // 验证排序。 |
| 2517 | // Verify ordering. |
| 2518 | assert(AL->size() == ActualAccesses.size() && |
| 2519 | "We don't have the same number of accesses in the block as on the " |
| 2520 | "access list"); |
| 2521 | assert((DL || ActualDefs.size() == 0) && |
| 2522 | "Either we should have a defs list, or we should have no defs"); |
| 2523 | assert((!DL || DL->size() == ActualDefs.size()) && |
| 2524 | "We don't have the same number of defs in the block as on the " |
| 2525 | "def list"); |
| 2526 | auto ALI = AL->begin(); |
| 2527 | auto AAI = ActualAccesses.begin(); |
| 2528 | while (ALI != AL->end() && AAI != ActualAccesses.end()) { |
| 2529 | assert(&*ALI == *AAI && "Not the same accesses in the same order"); |
| 2530 | ++ALI; |
| 2531 | ++AAI; |
| 2532 | } |
| 2533 | ActualAccesses.clear(); |
| 2534 | if (DL) { |
| 2535 | auto DLI = DL->begin(); |
| 2536 | auto ADI = ActualDefs.begin(); |
| 2537 | while (DLI != DL->end() && ADI != ActualDefs.end()) { |
| 2538 | assert(&*DLI == *ADI && "Not the same defs in the same order"); |
| 2539 | ++DLI; |
| 2540 | ++ADI; |
| 2541 | } |
| 2542 | } |
| 2543 | ActualDefs.clear(); |
| 2544 | } |
| 2545 | #endif |
| 2546 | } |
|   | /// 在MemorySSA中验证def-use列表，通过验证\p Use是否出现在\p Def的使用列表中。 |
| 2547 | /// Verify the def-use lists in MemorySSA, by verifying that \p Use |
| 2548 | /// appears in the use list of \p Def. |
| 2549 | void MemorySSA::verifyUseInDefs(MemoryAccess *Def, MemoryAccess *Use) const { |
| 2550 | #ifndef NDEBUG |
|   | // 如果是入口处的活动使用，可能会导致我们得到一个NULL的def |
| 2551 | // The live on entry use may cause us to get a NULL def here |
| 2552 | if (!Def) |
| 2553 | assert(isLiveOnEntryDef(Use) && |
| 2554 | "Null def but use not point to live on entry def"); |
| 2555 | else |
| 2556 | assert(is_contained(Def->users(), Use) && |
| 2557 | "Did not find use in def's use list"); |
| 2558 | #endif |
| 2559 | } |
|   | /// 对块进行局部编号，以便可以在常数时间内确定指令的顺序。 |
| 2560 | /// Perform a local numbering on blocks so that instruction ordering can be |
| 2561 | /// determined in constant time. |
|   | /// TODO: 我们目前只是按顺序编号。如果我们按N编号， |
|   | /// 我们可以允许至少N-1个insertBefore或insertAfter的序列 |
|   | /// （以及至少log2(N)个混合前后的序列）而不需要使编号失效。 |
| 2562 | /// TODO: We currently just number in order.  If we numbered by N, we could |
| 2563 | /// allow at least N-1 sequences of insertBefore or insertAfter (and at least |
| 2564 | /// log2(N) sequences of mixed before and after) without needing to invalidate |
| 2565 | /// the numbering. |
| 2566 | void MemorySSA::renumberBlock(const BasicBlock *B) const { |
|   | // 前增量确保数字真正从1开始。 |
| 2567 | // The pre-increment ensures the numbers really start at 1. |
| 2568 | unsigned long CurrentNumber = 0; |
| 2569 | const AccessList *AL = getBlockAccesses(B); |
| 2570 | assert(AL != nullptr && "Asking to renumber an empty block"); |
| 2571 | for (const auto &I : *AL) |
| 2572 | BlockNumbering[&I] = ++CurrentNumber; |
| 2573 | BlockNumberingValid.insert(B); |
| 2574 | } |
|   | /// 确定在同一个块中的两个内存访问，\p Dominator 是否支配 \p Dominatee。 |
|   | /// 如果 \p Dominator 支配 \p Dominatee，则返回 True。 |
| 2575 | /// Determine, for two memory accesses in the same block, |
| 2576 | /// whether \p Dominator dominates \p Dominatee. |
| 2577 | /// \returns True if \p Dominator dominates \p Dominatee. |
| 2578 | bool MemorySSA::locallyDominates(const MemoryAccess *Dominator, |
| 2579 | const MemoryAccess *Dominatee) const { |
| 2580 | const BasicBlock *DominatorBlock = Dominator->getBlock(); |
|   | // 确保两个内存访问在同一个块中 |
| 2581 | assert((DominatorBlock == Dominatee->getBlock()) && |
| 2582 | "Asking for local domination when accesses are in different blocks!"); |
|   | // 一个节点总是支配它自己 |
| 2583 | // A node dominates itself. |
| 2584 | if (Dominatee == Dominator) |
| 2585 | return true; |
|   | // 如果 Dominatee 是在函数入口处定义的，那么它不会被另一个内存访问支配 |
| 2586 | // When Dominatee is defined on function entry, it is not dominated by another |
| 2587 | // memory access. |
| 2588 | if (isLiveOnEntryDef(Dominatee)) |
| 2589 | return false; |
|   | // 如果 Dominator 是在函数入口处定义的，那么它支配其他内存访问 |
| 2590 | // When Dominator is defined on function entry, it dominates the other memory |
| 2591 | // access. |
| 2592 | if (isLiveOnEntryDef(Dominator)) |
| 2593 | return true; |
|   | // 如果 Dominator 所在的块没有有效的编号，则重新编号 |
| 2594 | if (!BlockNumberingValid.count(DominatorBlock)) |
| 2595 | renumberBlock(DominatorBlock); |
|   | // 获取 Dominator 和 Dominatee 的编号 |
| 2596 | unsigned long DominatorNum = BlockNumbering.lookup(Dominator); |
|   | // 所有的编号都从 1 开始 |
| 2597 | // All numbers start with 1 |
| 2598 | assert(DominatorNum != 0 && "Block was not numbered properly"); |
| 2599 | unsigned long DominateeNum = BlockNumbering.lookup(Dominatee); |
| 2600 | assert(DominateeNum != 0 && "Block was not numbered properly"); |
|   | // 如果 Dominator 的编号小于 Dominatee 的编号，则 Dominator 支配 Dominatee |
| 2601 | return DominatorNum < DominateeNum; |
| 2602 | } |
|   | /// 确定 \p Dominator 是否支配 \p Dominatee。 |
|   | /// 如果 \p Dominator 支配 \p Dominatee，则返回 True。 |
| 2603 | bool MemorySSA::dominates(const MemoryAccess *Dominator, |
| 2604 | const MemoryAccess *Dominatee) const { |
| 2605 | if (Dominator == Dominatee) |
| 2606 | return true; |
|   | // 如果 Dominatee 是在函数入口处定义的，那么它不会被任何内存访问支配 |
| 2607 | if (isLiveOnEntryDef(Dominatee)) |
| 2608 | return false; |
|   | // 如果 Dominator 和 Dominatee 不在同一个块中，则使用支配树（Dominator Tree）来确定 |
| 2609 | if (Dominator->getBlock() != Dominatee->getBlock()) |
| 2610 | return DT->dominates(Dominator->getBlock(), Dominatee->getBlock()); |
|   | // 如果在同一个块中，则调用 locallyDominates 函数来确定 |
| 2611 | return locallyDominates(Dominator, Dominatee); |
| 2612 | } |
|   | /// 确定 \p Dominator 是否支配 \p Dominatee。 |
|   | /// 如果 \p Dominator 支配 \p Dominatee，则返回 True。 |
| 2613 | bool MemorySSA::dominates(const MemoryAccess *Dominator, |
| 2614 | const Use &Dominatee) const { |
| 2615 | if (MemoryPhi *MP = dyn_cast<MemoryPhi>(Dominatee.getUser())) { |
| 2616 | BasicBlock *UseBB = MP->getIncomingBlock(Dominatee); |
|   | // Def 必须支配 phi 的进入块。 |
| 2617 | // The def must dominate the incoming block of the phi. |
| 2618 | if (UseBB != Dominator->getBlock()) |
| 2619 | return DT->dominates(Dominator->getBlock(), UseBB); |
|   | // 如果 UseBB 和 DefBB 是同一个块，那么在块内进行比较。 |
| 2620 | // If the UseBB and the DefBB are the same, compare locally. |
| 2621 | return locallyDominates(Dominator, cast<MemoryAccess>(Dominatee)); |
| 2622 | } |
|   | // 如果这不是 PHI 节点的使用，那么正常的 dominates 函数已经可以处理。 |
| 2623 | // If it's not a PHI node use, the normal dominates can already handle it. |
| 2624 | return dominates(Dominator, cast<MemoryAccess>(Dominatee.getUser())); |
| 2625 | } |
|   | // 定义一个静态常量字符串，用于表示“liveOnEntry”，即在函数入口处的活动定义。 |
| 2626 | const static char LiveOnEntryStr[] = "liveOnEntry"; |
|   | // 打印 MemoryAccess 对象，根据其值 ID 调用相应的打印函数。 |
| 2627 | void MemoryAccess::print(raw_ostream &OS) const { |
| 2628 | switch (getValueID()) { |
| 2629 | case MemoryPhiVal: return static_cast<const MemoryPhi *>(this)->print(OS); |
| 2630 | case MemoryDefVal: return static_cast<const MemoryDef *>(this)->print(OS); |
| 2631 | case MemoryUseVal: return static_cast<const MemoryUse *>(this)->print(OS); |
| 2632 | } |
| 2633 | llvm_unreachable("invalid value id"); |
| 2634 | } |
|   | // 打印 MemoryDef 对象，包括其 ID、定义它的访问（如果有）， |
|   | // 以及优化后的状态（如果有）。 |
| 2635 | void MemoryDef::print(raw_ostream &OS) const { |
| 2636 | MemoryAccess *UO = getDefiningAccess(); |
| 2637 | auto printID = [&OS](MemoryAccess *A) { |
| 2638 | if (A && A->getID()) |
| 2639 | OS << A->getID(); |
| 2640 | else |
| 2641 | OS << LiveOnEntryStr; |
| 2642 | }; |
| 2643 | OS << getID() << " = MemoryDef("; |
| 2644 | printID(UO); |
| 2645 | OS << ")"; |
| 2646 | if (isOptimized()) { |
| 2647 | OS << "->"; |
| 2648 | printID(getOptimized()); |
| 2649 | if (Optional<AliasResult> AR = getOptimizedAccessType()) |
| 2650 | OS << " " << *AR; |
| 2651 | } |
| 2652 | } |
|   | // 打印 MemoryPhi对象。 |
| 2653 | void MemoryPhi::print(raw_ostream &OS) const { |
| 2654 | bool First = true; |
| 2655 | OS << getID() << " = MemoryPhi("; |
|   | // 遍历 MemoryPhi 的所有操作数 |
| 2656 | for (const auto &Op : operands()) { |
| 2657 | BasicBlock *BB = getIncomingBlock(Op); |
| 2658 | MemoryAccess *MA = cast<MemoryAccess>(Op); |
|   | // 如果不是第一个操作数，输出逗号分隔符 |
| 2659 | if (!First) |
| 2660 | OS << ','; |
| 2661 | else |
| 2662 | First = false; |
| 2663 | OS << '{'; |
|   | // 如果基本块有名称，则输出名称；否则，以操作数的形式输出基本块 |
| 2664 | if (BB->hasName()) |
| 2665 | OS << BB->getName(); |
| 2666 | else |
| 2667 | BB->printAsOperand(OS, false); |
| 2668 | OS << ','; |
|   | // 输出操作数的 ID，如果没有 ID，则输出 "liveOnEntry" |
| 2669 | if (unsigned ID = MA->getID()) |
| 2670 | OS << ID; |
| 2671 | else |
| 2672 | OS << LiveOnEntryStr; |
| 2673 | OS << '}'; |
| 2674 | } |
| 2675 | OS << ')'; |
| 2676 | } |
|   | // MemoryPhi::print 函数用于打印 MemoryPhi 对象， |
|   | // 包括其 ID、操作数以及操作数对应的基本块。 |
| 2677 | void MemoryUse::print(raw_ostream &OS) const { |
| 2678 | MemoryAccess *UO = getDefiningAccess(); |
| 2679 | OS << "MemoryUse("; |
|   | // 输出定义 MemoryUse 的 MemoryAccess 的 ID，如果没有 ID，则输出 "liveOnEntry" |
| 2680 | if (UO && UO->getID()) |
| 2681 | OS << UO->getID(); |
| 2682 | else |
| 2683 | OS << LiveOnEntryStr; |
| 2684 | OS << ')'; |
|   | // 如果有优化后的访问类型，则输出优化后的结果 |
| 2685 | if (Optional<AliasResult> AR = getOptimizedAccessType()) |
| 2686 | OS << " " << *AR; |
| 2687 | } |
|   | // MemoryUse::print 函数用于打印 MemoryUse 对象，包括其定义访问和优化后的访问类型。 |
| 2688 | void MemoryAccess::dump() const { |
|   | // 在非调试模式下，不能完全移除虚拟函数 |
| 2689 | // Cannot completely remove virtual function even in release mode. |
| 2690 | #if !defined(NDEBUG) || defined(LLVM_ENABLE_DUMP) |
| 2691 | print(dbgs()); |
| 2692 | dbgs() << "\n"; |
| 2693 | #endif |
| 2694 | } |
|   | // 定义 MemorySSAPrinterLegacyPass 的唯一 ID。 |
| 2695 | char MemorySSAPrinterLegacyPass::ID = 0; |
|   | // MemorySSAPrinterLegacyPass 构造函数，注册传递。 |
| 2696 | MemorySSAPrinterLegacyPass::MemorySSAPrinterLegacyPass() : FunctionPass(ID) { |
| 2697 | initializeMemorySSAPrinterLegacyPassPass(*PassRegistry::getPassRegistry()); |
| 2698 | } |
|   | // 指定传递的分析使用情况，该传递保留了所有分析， |
|   | // 并且需要 MemorySSAWrapperPass 分析。 |
| 2699 | void MemorySSAPrinterLegacyPass::getAnalysisUsage(AnalysisUsage &AU) const { |
| 2700 | AU.setPreservesAll(); |
| 2701 | AU.addRequired<MemorySSAWrapperPass>(); |
| 2702 | } |
|   | // 运行传递，打印 MemorySSA 结构，如果启用了验证，则验证 MemorySSA 结构。 |
| 2703 | bool MemorySSAPrinterLegacyPass::runOnFunction(Function &F) { |
| 2704 | auto &MSSA = getAnalysis<MemorySSAWrapperPass>().getMSSA(); |
| 2705 | MSSA.print(dbgs()); |
| 2706 | if (VerifyMemorySSA) |
| 2707 | MSSA.verifyMemorySSA(); |
| 2708 | return false; |
| 2709 | } |
|   | // 定义 MemorySSAAnalysis 的分析键。 |
| 2710 | AnalysisKey MemorySSAAnalysis::Key; |
|   | // 运行 MemorySSAAnalysis 分析，创建 MemorySSA 结构。 |
| 2711 | MemorySSAAnalysis::Result MemorySSAAnalysis::run(Function &F, |
| 2712 | FunctionAnalysisManager &AM) { |
| 2713 | auto &DT = AM.getResult<DominatorTreeAnalysis>(F); |
| 2714 | auto &AA = AM.getResult<AAManager>(F); |
| 2715 | return MemorySSAAnalysis::Result(std::make_unique<MemorySSA>(F, &AA, &DT)); |
| 2716 | } |
|   | // 确定 MemorySSAAnalysis 结果何时失效。 |
| 2717 | bool MemorySSAAnalysis::Result::invalidate( |
| 2718 | Function &F, const PreservedAnalyses &PA, |
| 2719 | FunctionAnalysisManager::Invalidator &Inv) { |
| 2720 | auto PAC = PA.getChecker<MemorySSAAnalysis>(); |
| 2721 | return !(PAC.preserved() || PAC.preservedSet<AllAnalysesOn<Function>>()) || |
| 2722 | Inv.invalidate<AAManager>(F, PA) || |
| 2723 | Inv.invalidate<DominatorTreeAnalysis>(F, PA); |
| 2724 | } |
|   | // 运行 MemorySSAPrinterPass 传递，打印 MemorySSA 结构。 |
| 2725 | PreservedAnalyses MemorySSAPrinterPass::run(Function &F, |
| 2726 | FunctionAnalysisManager &AM) { |
| 2727 | OS << "MemorySSA for function: " << F.getName() << "\n"; |
| 2728 | AM.getResult<MemorySSAAnalysis>(F).getMSSA().print(OS); |
| 2729 | return PreservedAnalyses::all(); |
| 2730 | } |
|   | // 运行 MemorySSAVerifierPass 传递，验证 MemorySSA 结构。 |
| 2731 | PreservedAnalyses MemorySSAVerifierPass::run(Function &F, |
| 2732 | FunctionAnalysisManager &AM) { |
| 2733 | AM.getResult<MemorySSAAnalysis>(F).getMSSA().verifyMemorySSA(); |
| 2734 | return PreservedAnalyses::all(); |
| 2735 | } |
|   | // 定义 MemorySSAWrapperPass 的唯一 ID。 |
| 2736 | char MemorySSAWrapperPass::ID = 0; |
|   | // MemorySSAWrapperPass 构造函数，注册传递。 |
| 2737 | MemorySSAWrapperPass::MemorySSAWrapperPass() : FunctionPass(ID) { |
| 2738 | initializeMemorySSAWrapperPassPass(*PassRegistry::getPassRegistry()); |
| 2739 | } |
| 2740 | void MemorySSAWrapperPass::releaseMemory() { MSSA.reset(); } |
|   | // 指定传递的分析使用情况，该传递保留了所有分析， |
|   | // 并且需要 DominatorTreeWrapperPass 和 AAResultsWrapperPass 分析。 |
| 2741 | void MemorySSAWrapperPass::getAnalysisUsage(AnalysisUsage &AU) const { |
| 2742 | AU.setPreservesAll(); |
| 2743 | AU.addRequiredTransitive<DominatorTreeWrapperPass>(); |
| 2744 | AU.addRequiredTransitive<AAResultsWrapperPass>(); |
| 2745 | } |
|   | // MemorySSAWrapperPass 的 runOnFunction 方法，用于在函数级别上运行。 |
|   | // 这个方法获取 DominatorTree 和 AliasAnalysis 结果， |
|   | // 然后创建一个 MemorySSA 实例。 |
| 2746 | bool MemorySSAWrapperPass::runOnFunction(Function &F) { |
| 2747 | auto &DT = getAnalysis<DominatorTreeWrapperPass>().getDomTree(); |
| 2748 | auto &AA = getAnalysis<AAResultsWrapperPass>().getAAResults(); |
| 2749 | MSSA.reset(new MemorySSA(F, &AA, &DT)); |
| 2750 | return false; |
| 2751 | } |
|   | // verifyAnalysis 方法，如果启用了 MemorySSA 验证， |
|   | // 将调用 MemorySSA 实例的 verifyMemorySSA 方法。 |
| 2752 | void MemorySSAWrapperPass::verifyAnalysis() const { |
| 2753 | if (VerifyMemorySSA) |
| 2754 | MSSA->verifyMemorySSA(); |
| 2755 | } |
|   | // print 方法，用于打印 MemorySSA 实例。 |
| 2756 | void MemorySSAWrapperPass::print(raw_ostream &OS, const Module *M) const { |
| 2757 | MSSA->print(OS); |
| 2758 | } |
|   | // MemorySSAWalker 的构造函数，接收一个 MemorySSA 实例。 |
| 2759 | MemorySSAWalker::MemorySSAWalker(MemorySSA *M) : MSSA(M) {} |
|   | // ClobberWalkerBase 的模板方法，用于从给定的起始访问点开始，沿着 use-def 链向上查找实际 clobber 位置。 |
|   | // 这个方法考虑了 MemoryPhi、liveOnEntryDef 和内存栅栏指令。 |
|   | // 使用 UpwardsMemoryQuery 结构来存储查询信息，并使用 Walker 来执行实际的查找。 |
|   | // 最终返回找到的 clobbering memory access。 |
| 2760 | /// Walk the use-def chains starting at \p StartingAccess and find |
| 2761 | /// the MemoryAccess that actually clobbers Loc. |
| 2762 | /// |
| 2763 | /// \returns our clobbering memory access |
| 2764 | template <typename AliasAnalysisType> |
| 2765 | MemoryAccess * |
| 2766 | MemorySSA::ClobberWalkerBase<AliasAnalysisType>::getClobberingMemoryAccessBase( |
| 2767 | MemoryAccess *StartingAccess, const MemoryLocation &Loc, |
| 2768 | unsigned &UpwardWalkLimit) { |
| 2769 | if (isa<MemoryPhi>(StartingAccess)) |
| 2770 | return StartingAccess; |
| 2771 | auto *StartingUseOrDef = cast<MemoryUseOrDef>(StartingAccess); |
| 2772 | if (MSSA->isLiveOnEntryDef(StartingUseOrDef)) |
| 2773 | return StartingUseOrDef; |
| 2774 | Instruction *I = StartingUseOrDef->getMemoryInst(); |
|   | // 如果遇到内存栅栏指令，保守地认为它是 clobber，不继续向上查找。 |
| 2775 | // Conservatively, fences are always clobbers, so don't perform the walk if we |
| 2776 | // hit a fence. |
| 2777 | if (!isa<CallBase>(I) && I->isFenceLike()) |
| 2778 | return StartingUseOrDef; |
| 2779 | UpwardsMemoryQuery Q; |
| 2780 | Q.OriginalAccess = StartingUseOrDef; |
| 2781 | Q.StartingLoc = Loc; |
| 2782 | Q.Inst = I; |
| 2783 | Q.IsCall = false; |
|   | // 不向上走到 def 的 def，因为我们已经认为它是 clobbering access。 |
| 2784 | // Unlike the other function, do not walk to the def of a def, because we are |
| 2785 | // handed something we already believe is the clobbering access. |
| 2786 | // We never set SkipSelf to true in Q in this method. |
| 2787 | MemoryAccess *DefiningAccess = isa<MemoryUse>(StartingUseOrDef) |
| 2788 | ? StartingUseOrDef->getDefiningAccess() |
| 2789 | : StartingUseOrDef; |
| 2790 | MemoryAccess *Clobber = |
| 2791 | Walker.findClobber(DefiningAccess, Q, UpwardWalkLimit); |
| 2792 | LLVM_DEBUG(dbgs() << "Starting Memory SSA clobber for " << *I << " is "); |
| 2793 | LLVM_DEBUG(dbgs() << *StartingUseOrDef << "\n"); |
| 2794 | LLVM_DEBUG(dbgs() << "Final Memory SSA clobber for " << *I << " is "); |
| 2795 | LLVM_DEBUG(dbgs() << *Clobber << "\n"); |
| 2796 | return Clobber; |
| 2797 | } |
| 2798 | template <typename AliasAnalysisType> |
| 2799 | MemoryAccess * |
| 2800 | MemorySSA::ClobberWalkerBase<AliasAnalysisType>::getClobberingMemoryAccessBase( |
| 2801 | MemoryAccess *MA, unsigned &UpwardWalkLimit, bool SkipSelf) { |
| 2802 | auto *StartingAccess = dyn_cast<MemoryUseOrDef>(MA); |
|   | // 如果这是一个 MemoryPhi，我们不能做任何事情。 |
| 2803 | // If this is a MemoryPhi, we can't do anything. |
| 2804 | if (!StartingAccess) |
| 2805 | return MA; |
| 2806 | bool IsOptimized = false; |
|   | // 如果这已经是一个优化过的 use 或 def，返回优化后的结果。 |
| 2807 | // If this is an already optimized use or def, return the optimized result. |
| 2808 | // Note: Currently, we store the optimized def result in a separate field, |
| 2809 | // since we can't use the defining access. |
| 2810 | if (StartingAccess->isOptimized()) { |
| 2811 | if (!SkipSelf || !isa<MemoryDef>(StartingAccess)) |
| 2812 | return StartingAccess->getOptimized(); |
| 2813 | IsOptimized = true; |
| 2814 | } |
| 2815 | const Instruction *I = StartingAccess->getMemoryInst(); |
|   | // 我们不能对 fence 做任何合理的处理，因为它们保守地清除所有内存。 |
| 2816 | // We can't sanely do anything with a fence, since they conservatively clobber |
| 2817 | // all memory, and have no locations to get pointers from to try to |
| 2818 | // disambiguate. |
| 2819 | if (!isa<CallBase>(I) && I->isFenceLike()) |
| 2820 | return StartingAccess; |
| 2821 | UpwardsMemoryQuery Q(I, StartingAccess); |
| 2822 | if (isUseTriviallyOptimizableToLiveOnEntry(*Walker.getAA(), I)) { |
| 2823 | MemoryAccess *LiveOnEntry = MSSA->getLiveOnEntryDef(); |
| 2824 | StartingAccess->setOptimized(LiveOnEntry); |
| 2825 | StartingAccess->setOptimizedAccessType(None); |
| 2826 | return LiveOnEntry; |
| 2827 | } |
| 2828 | MemoryAccess *OptimizedAccess; |
| 2829 | if (!IsOptimized) { |
|   | // 从我们认为已经清除此位置的访问开始 |
| 2830 | // Start with the thing we already think clobbers this location |
| 2831 | MemoryAccess *DefiningAccess = StartingAccess->getDefiningAccess(); |
|   | // 在这一点上，DefiningAccess 可能是 live on entry def。 |
| 2832 | // At this point, DefiningAccess may be the live on entry def. |
| 2833 | // If it is, we will not get a better result. |
| 2834 | if (MSSA->isLiveOnEntryDef(DefiningAccess)) { |
| 2835 | StartingAccess->setOptimized(DefiningAccess); |
| 2836 | StartingAccess->setOptimizedAccessType(None); |
| 2837 | return DefiningAccess; |
| 2838 | } |
| 2839 | OptimizedAccess = Walker.findClobber(DefiningAccess, Q, UpwardWalkLimit); |
| 2840 | StartingAccess->setOptimized(OptimizedAccess); |
| 2841 | if (MSSA->isLiveOnEntryDef(OptimizedAccess)) |
| 2842 | StartingAccess->setOptimizedAccessType(None); |
| 2843 | else if (Q.AR == MustAlias) |
| 2844 | StartingAccess->setOptimizedAccessType(MustAlias); |
| 2845 | } else |
| 2846 | OptimizedAccess = StartingAccess->getOptimized(); |
| 2847 | LLVM_DEBUG(dbgs() << "Starting Memory SSA clobber for " << *I << " is "); |
| 2848 | LLVM_DEBUG(dbgs() << *StartingAccess << "\n"); |
| 2849 | LLVM_DEBUG(dbgs() << "Optimized Memory SSA clobber for " << *I << " is "); |
| 2850 | LLVM_DEBUG(dbgs() << *OptimizedAccess << "\n"); |
| 2851 | MemoryAccess *Result; |
| 2852 | if (SkipSelf && isa<MemoryPhi>(OptimizedAccess) && |
| 2853 | isa<MemoryDef>(StartingAccess) && UpwardWalkLimit) { |
| 2854 | assert(isa<MemoryDef>(Q.OriginalAccess)); |
| 2855 | Q.SkipSelfAccess = true; |
| 2856 | Result = Walker.findClobber(OptimizedAccess, Q, UpwardWalkLimit); |
| 2857 | } else |
| 2858 | Result = OptimizedAccess; |
| 2859 | LLVM_DEBUG(dbgs() << "Result Memory SSA clobber [SkipSelf = " << SkipSelf); |
| 2860 | LLVM_DEBUG(dbgs() << "] for " << *I << " is " << *Result << "\n"); |
| 2861 | return Result; |
| 2862 | } |
|   | // DoNothingMemorySSAWalker 的一个方法，它简单地返回给定 MemoryAccess |
|   | // 对象的定义访问。如果 MA 是一个 MemoryUseOrDef 对象， |
|   | // 它返回该对象的 defining access；否则，直接返回 MA。 |
| 2863 | MemoryAccess * |
| 2864 | DoNothingMemorySSAWalker::getClobberingMemoryAccess(MemoryAccess *MA) { |
| 2865 | if (auto *Use = dyn_cast<MemoryUseOrDef>(MA)) |
| 2866 | return Use->getDefiningAccess(); |
| 2867 | return MA; |
| 2868 | } |
|   | // 另一个重载的 getClobberingMemoryAccess 方法，它接受一个 MemoryLocation 参数（在这个实现中被忽略）。 |
|   | // 这个方法也简单地返回给定 MemoryAccess 对象的 defining access，或者在不是 MemoryUseOrDef 对象时返回自身。 |
| 2869 | MemoryAccess *DoNothingMemorySSAWalker::getClobberingMemoryAccess( |
| 2870 | MemoryAccess *StartingAccess, const MemoryLocation &) { |
| 2871 | if (auto *Use = dyn_cast<MemoryUseOrDef>(StartingAccess)) |
| 2872 | return Use->getDefiningAccess(); |
| 2873 | return StartingAccess; |
| 2874 | } |
|   | // MemoryPhi 类的 deleteMe 方法，它删除传入的 MemoryPhi 对象。 |
|   | // Self 参数是指向要删除对象的指针，它被转换为 MemoryPhi 类型然后被删除。 |
| 2875 | void MemoryPhi::deleteMe(DerivedUser *Self) { |
| 2876 | delete static_cast<MemoryPhi *>(Self); |
| 2877 | } |
|   | // MemoryDef 类的 deleteMe 方法，它删除传入的 MemoryDef 对象。 |
|   | // Self 参数是指向要删除对象的指针，它被转换为 MemoryDef 类型然后被删除。 |
| 2878 | void MemoryDef::deleteMe(DerivedUser *Self) { |
| 2879 | delete static_cast<MemoryDef *>(Self); |
| 2880 | } |
|   | // MemoryUse 类的 deleteMe 方法，它删除传入的 MemoryUse 对象。 |
|   | // Self 参数是指向要删除对象的指针，它被转换为 MemoryUse 类型然后被删除。 |
| 2881 | void MemoryUse::deleteMe(DerivedUser *Self) { |
| 2882 | delete static_cast<MemoryUse *>(Self); |
| 2883 | } |

LLVM中的loop-closed SSA

llvm\lib\Transforms\Utils\LCSSA.cpp

| 1 | //===-- LCSSA.cpp - Convert loops into loop-closed SSA form ---------------===// |
| --- | --- |
| 2 | // |
| 3 | // Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions. |
| 4 | // See https://llvm.org/LICENSE.txt for license information. |
| 5 | // SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception |
| 6 | // |
| 7 | //===----------------------------------------------------------------------===// |
| 8 | // |
|   | // 这个循环转换传递通过在循环末尾放置 phi 节点来提升循环中的活值， |
|   | // 这样做的主要好处是简化了后续的循环优化过程。 |
|   | // 这种转换是 LLVM 编译器中用于提高代码优化效率的一种技术。 |
| 9 | // This pass transforms loops by placing phi nodes at the end of the loops for |
| 10 | // all values that are live across the loop boundary.  For example, it turns |
| 11 | // the left into the right code: |
| 12 | // |
| 13 | // for (...)                for (...) |
| 14 | //   if (c)                   if (c) |
| 15 | //     X1 = ...                 X1 = ... |
| 16 | //   else                     else |
| 17 | //     X2 = ...                 X2 = ... |
| 18 | //   X3 = phi(X1, X2)         X3 = phi(X1, X2) |
| 19 | // ... = X3 + 4             X4 = phi(X3) |
| 20 | //                          ... = X4 + 4 |
| 21 | // |
| 22 | // This is still valid LLVM; the extra phi nodes are purely redundant, and will |
| 23 | // be trivially eliminated by InstCombine.  The major benefit of this |
| 24 | // transformation is that it makes many other loop optimizations, such as |
| 25 | // LoopUnswitching, simpler. |
| 26 | // |
| 27 | //===----------------------------------------------------------------------===// |
| 28 |  |
| 29 | #include "llvm/Transforms/Utils/LCSSA.h" |
| 30 | #include "llvm/ADT/STLExtras.h" |
| 31 | #include "llvm/ADT/Statistic.h" |
| 32 | #include "llvm/Analysis/AliasAnalysis.h" |
| 33 | #include "llvm/Analysis/BasicAliasAnalysis.h" |
| 34 | #include "llvm/Analysis/BranchProbabilityInfo.h" |
| 35 | #include "llvm/Analysis/GlobalsModRef.h" |
| 36 | #include "llvm/Analysis/LoopPass.h" |
| 37 | #include "llvm/Analysis/MemorySSA.h" |
| 38 | #include "llvm/Analysis/ScalarEvolution.h" |
| 39 | #include "llvm/Analysis/ScalarEvolutionAliasAnalysis.h" |
| 40 | #include "llvm/IR/Constants.h" |
| 41 | #include "llvm/IR/Dominators.h" |
| 42 | #include "llvm/IR/Function.h" |
| 43 | #include "llvm/IR/Instructions.h" |
| 44 | #include "llvm/IR/IntrinsicInst.h" |
| 45 | #include "llvm/IR/PredIteratorCache.h" |
| 46 | #include "llvm/InitializePasses.h" |
| 47 | #include "llvm/Pass.h" |
| 48 | #include "llvm/Support/CommandLine.h" |
| 49 | #include "llvm/Transforms/Utils.h" |
| 50 | #include "llvm/Transforms/Utils/Local.h" |
| 51 | #include "llvm/Transforms/Utils/LoopUtils.h" |
| 52 | #include "llvm/Transforms/Utils/SSAUpdater.h" |
| 53 | using namespace llvm; |
|   | // 定义调试类型为 "lcssa"。 |
| 54 |  |
| 55 | #define DEBUG_TYPE "lcssa" |
|   | // 定义一个统计变量，用于记录跨越循环边界的活变量数量。 |
| 56 |  |
| 57 | STATISTIC(NumLCSSA, "Number of live out of a loop variables"); |
| 58 |  |
| 59 | #ifdef EXPENSIVE_CHECKS |
| 60 | static bool VerifyLoopLCSSA = true; |
| 61 | #else |
| 62 | static bool VerifyLoopLCSSA = false; |
| 63 | #endif |
|   | // 定义一个命令行选项，用于在编译时控制是否验证 LCSSA 形式。 |
|   | // 这是一个耗时的操作，因此默认情况下在非昂贵检查模式下是关闭的。 |
| 64 | static cl::opt<bool, true> |
| 65 | VerifyLoopLCSSAFlag("verify-loop-lcssa", cl::location(VerifyLoopLCSSA), |
| 66 | cl::Hidden, |
| 67 | cl::desc("Verify loop lcssa form (time consuming)")); |
|   | /// 辅助函数，用于检查给定的基本块是否是循环的退出块。 |
| 68 |  |
| 69 | /// Return true if the specified block is in the list. |
| 70 | static bool isExitBlock(BasicBlock *BB, |
| 71 | const SmallVectorImpl<BasicBlock *> &ExitBlocks) { |
| 72 | return is_contained(ExitBlocks, BB); |
| 73 | } |
|   | /// 该函数接受一个指令工作列表、支配树、循环信息和标量演化分析作为参数， |
|   | /// 并返回一个布尔值表示是否进行了更改。 |
| 74 |  |
| 75 | /// For every instruction from the worklist, check to see if it has any uses |
| 76 | /// that are outside the current loop.  If so, insert LCSSA PHI nodes and |
| 77 | /// rewrite the uses. |
| 78 | bool llvm::formLCSSAForInstructions(SmallVectorImpl<Instruction *> &Worklist, |
| 79 | const DominatorTree &DT, const LoopInfo &LI, |
| 80 | ScalarEvolution *SE) { |
|   | // 存储需要重写的使用（Use）对象的指针。 |
| 81 | SmallVector<Use *, 16> UsesToRewrite; |
|   | // 存储需要移除的 PHI 节点。 |
| 82 | SmallSetVector<PHINode *, 16> PHIsToRemove; |
|   | // 用于缓存循环的前驱块。 |
| 83 | PredIteratorCache PredCache; |
|   | // 标记是否对代码进行了更改。 |
| 84 | bool Changed = false; |
|   | // 缓存每个循环的退出块，避免重复计算。 |
| 85 |  |
| 86 | // Cache the Loop ExitBlocks across this loop.  We expect to get a lot of |
| 87 | // instructions within the same loops, computing the exit blocks is |
| 88 | // expensive, and we're not mutating the loop structure. |
| 89 | SmallDenseMap<Loop*, SmallVector<BasicBlock *,1>> LoopExitBlocks; |
|   | // 遍历工作列表中的每个指令。 |
| 90 |  |
| 91 | while (!Worklist.empty()) { |
| 92 | UsesToRewrite.clear(); |
| 93 |  |
| 94 | Instruction *I = Worklist.pop_back_val(); |
| 95 | assert(!I->getType()->isTokenTy() && "Tokens shouldn't be in the worklist"); |
| 96 | BasicBlock *InstBB = I->getParent(); |
| 97 | Loop *L = LI.getLoopFor(InstBB); |
| 98 | assert(L && "Instruction belongs to a BB that's not part of a loop"); |
| 99 | if (!LoopExitBlocks.count(L)) |
| 100 | L->getExitBlocks(LoopExitBlocks[L]); |
| 101 | assert(LoopExitBlocks.count(L)); |
| 102 | const SmallVectorImpl<BasicBlock *> &ExitBlocks = LoopExitBlocks[L]; |
|   | // 如果没有退出块，则跳过当前指令。 |
| 103 |  |
| 104 | if (ExitBlocks.empty()) |
| 105 | continue; |
|   | // 检查指令的每个使用，如果使用在循环外部，则记录下来。 |
| 106 |  |
| 107 | for (Use &U : I->uses()) { |
| 108 | Instruction *User = cast<Instruction>(U.getUser()); |
| 109 | BasicBlock *UserBB = User->getParent(); |
|   | // 如果使用指令是 PHI 节点，则获取对应的入边块。 |
| 110 | if (auto *PN = dyn_cast<PHINode>(User)) |
| 111 | UserBB = PN->getIncomingBlock(U); |
|   | // 如果使用不在指令所在的块，且不在循环内，则需要重写。 |
| 112 |  |
| 113 | if (InstBB != UserBB && !L->contains(UserBB)) |
| 114 | UsesToRewrite.push_back(&U); |
| 115 | } |
|   | // 如果没有使用需要重写，则跳过当前指令。 |
| 116 |  |
| 117 | // If there are no uses outside the loop, exit with no change. |
| 118 | if (UsesToRewrite.empty()) |
| 119 | continue; |
|   | // 应用 LCSSA 转换的统计。 |
| 120 |  |
| 121 | ++NumLCSSA; // We are applying the transformation |
|   | // 特殊处理 invoke 指令，因为它们的结果在 unwind 边上不可用。 |
| 122 |  |
| 123 | // Invoke instructions are special in that their result value is not |
| 124 | // available along their unwind edge. The code below tests to see whether |
| 125 | // DomBB dominates the value, so adjust DomBB to the normal destination |
| 126 | // block, which is effectively where the value is first usable. |
| 127 | BasicBlock *DomBB = InstBB; |
| 128 | if (auto *Inv = dyn_cast<InvokeInst>(I)) |
| 129 | DomBB = Inv->getNormalDest(); |
|   | // 获取支配树节点。 |
| 130 |  |
| 131 | const DomTreeNode *DomNode = DT.getNode(DomBB); |
|   | // 存储新插入的 PHI 节点。 |
| 132 |  |
| 133 | SmallVector<PHINode *, 16> AddedPHIs; |
|   | // 存储需要后续处理的 PHI 节点。 |
| 134 | SmallVector<PHINode *, 8> PostProcessPHIs; |
|   | // 存储插入的 PHI 节点。 |
| 135 |  |
| 136 | SmallVector<PHINode *, 4> InsertedPHIs; |
|   | // 创建 SSA 更新器。 |
| 137 | SSAUpdater SSAUpdate(&InsertedPHIs); |
| 138 | SSAUpdate.Initialize(I->getType(), I->getName()); |
|   | // 强制重新计算指令的值，因为一些用户现在需要使用新的 PHI 节点。 |
| 139 |  |
| 140 | // Force re-computation of I, as some users now need to use the new PHI |
| 141 | // node. |
| 142 | if (SE) |
| 143 | SE->forgetValue(I); |
|   | // 在所有被值支配的退出块中插入 LCSSA PHI 节点，并添加到 PHI 映射中。 |
| 144 |  |
| 145 | // Insert the LCSSA phi's into all of the exit blocks dominated by the |
| 146 | // value, and add them to the Phi's map. |
| 147 | for (BasicBlock *ExitBB : ExitBlocks) { |
|   | // 如果当前块不被 DomNode 支配，则跳过。 |
| 148 | if (!DT.dominates(DomNode, DT.getNode(ExitBB))) |
| 149 | continue; |
|   | // 如果已经为这个块插入了 PHI 节点，则不再处理。 |
| 150 |  |
| 151 | // If we already inserted something for this BB, don't reprocess it. |
| 152 | if (SSAUpdate.HasValueForBlock(ExitBB)) |
| 153 | continue; |
|   | // 在退出块的开头创建新的 PHI 节点。 |
| 154 |  |
| 155 | PHINode *PN = PHINode::Create(I->getType(), PredCache.size(ExitBB), |
| 156 | I->getName() + ".lcssa", &ExitBB->front()); |
|   | // 从原始指令获取调试位置。 |
| 157 | // Get the debug location from the original instruction. |
| 158 | PN->setDebugLoc(I->getDebugLoc()); |
|   | // 为 PHI 节点添加来自循环内的输入。 |
| 159 | // Add inputs from inside the loop for this PHI. |
| 160 | for (BasicBlock *Pred : PredCache.get(ExitBB)) { |
| 161 | PN->addIncoming(I, Pred); |
|   | // 如果退出块的前驱不在循环内，则需要重写对应的入边使用。 |
| 162 |  |
| 163 | // If the exit block has a predecessor not within the loop, arrange for |
| 164 | // the incoming value use corresponding to that predecessor to be |
| 165 | // rewritten in terms of a different LCSSA PHI. |
| 166 | if (!L->contains(Pred)) |
| 167 | UsesToRewrite.push_back( |
| 168 | &PN->getOperandUse(PN->getOperandNumForIncomingValue( |
| 169 | PN->getNumIncomingValues() - 1))); |
| 170 | } |
|   | // 记录新插入的 PHI 节点。 |
| 171 |  |
| 172 | AddedPHIs.push_back(PN); |
|   | // 记录这个块中活跃的值。 |
| 173 |  |
| 174 | // Remember that this phi makes the value alive in this block. |
| 175 | SSAUpdate.AddAvailableValue(ExitBB, PN); |
|   | // 如果退出块是其他循环的头部，可能需要后续处理。 |
| 176 |  |
| 177 | // LoopSimplify might fail to simplify some loops (e.g. when indirect |
| 178 | // branches are involved). In such situations, it might happen that an |
| 179 | // exit for Loop L1 is the header of a disjoint Loop L2. Thus, when we |
| 180 | // create PHIs in such an exit block, we are also inserting PHIs into L2's |
| 181 | // header. This could break LCSSA form for L2 because these inserted PHIs |
| 182 | // can also have uses outside of L2. Remember all PHIs in such situation |
| 183 | // as to revisit than later on. FIXME: Remove this if indirectbr support |
| 184 | // into LoopSimplify gets improved. |
| 185 | if (auto *OtherLoop = LI.getLoopFor(ExitBB)) |
| 186 | if (!L->contains(OtherLoop)) |
| 187 | PostProcessPHIs.push_back(PN); |
| 188 | } |
|   | // 重写循环外的所有使用，使用新插入的 PHI 节点。 |
| 189 |  |
| 190 | // Rewrite all uses outside the loop in terms of the new PHIs we just |
| 191 | // inserted. |
| 192 | for (Use *UseToRewrite : UsesToRewrite) { |
|   | // 如果使用在退出块中，则直接使用新插入的 PHI 节点。 |
| 193 | // If this use is in an exit block, rewrite to use the newly inserted PHI. |
| 194 | // This is required for correctness because SSAUpdate doesn't handle uses |
| 195 | // in the same block.  It assumes the PHI we inserted is at the end of the |
| 196 | // block. |
| 197 | Instruction *User = cast<Instruction>(UseToRewrite->getUser()); |
| 198 | BasicBlock *UserBB = User->getParent(); |
| 199 | if (auto *PN = dyn_cast<PHINode>(User)) |
| 200 | UserBB = PN->getIncomingBlock(*UseToRewrite); |
| 201 |  |
| 202 | if (isa<PHINode>(UserBB->begin()) && isExitBlock(UserBB, ExitBlocks)) { |
| 203 | UseToRewrite->set(&UserBB->front()); |
| 204 | continue; |
| 205 | } |
|   | // 如果只添加了一个 PHI 节点，则直接重命名使用。 |
| 206 |  |
| 207 | // If we added a single PHI, it must dominate all uses and we can directly |
| 208 | // rename it. |
| 209 | if (AddedPHIs.size() == 1) { |
| 210 | UseToRewrite->set(AddedPHIs[0]); |
| 211 | continue; |
| 212 | } |
|   | // 否则，执行完整的 PHI 插入。 |
| 213 |  |
| 214 | // Otherwise, do full PHI insertion. |
| 215 | SSAUpdate.RewriteUse(*UseToRewrite); |
| 216 | } |
|   | // 更新位于循环外的现有调试值使用。 |
| 217 |  |
| 218 | SmallVector<DbgValueInst *, 4> DbgValues; |
| 219 | llvm::findDbgValues(DbgValues, I); |
| 220 |  |
| 221 | // Update pre-existing debug value uses that reside outside the loop. |
| 222 | auto &Ctx = I->getContext(); |
| 223 | for (auto DVI : DbgValues) { |
| 224 | BasicBlock *UserBB = DVI->getParent(); |
| 225 | if (InstBB == UserBB || L->contains(UserBB)) |
| 226 | continue; |
| 227 | // We currently only handle debug values residing in blocks that were |
| 228 | // traversed while rewriting the uses. If we inserted just a single PHI, |
| 229 | // we will handle all relevant debug values. |
| 230 | Value *V = AddedPHIs.size() == 1 ? AddedPHIs[0] |
| 231 | : SSAUpdate.FindValueForBlock(UserBB); |
| 232 | if (V) |
| 233 | DVI->setOperand(0, MetadataAsValue::get(Ctx, ValueAsMetadata::get(V))); |
| 234 | } |
|   | // 如果 SSAUpdater 在其他循环中插入了 phi-nodes，则需要后续处理以保持 LCSSA 形式。 |
| 235 |  |
| 236 | // SSAUpdater might have inserted phi-nodes inside other loops. We'll need |
| 237 | // to post-process them to keep LCSSA form. |
| 238 | for (PHINode *InsertedPN : InsertedPHIs) { |
| 239 | if (auto *OtherLoop = LI.getLoopFor(InsertedPN->getParent())) |
| 240 | if (!L->contains(OtherLoop)) |
| 241 | PostProcessPHIs.push_back(InsertedPN); |
| 242 | } |
|   | // 对插入到其他不相交循环中的 PHI 指令进行后续处理，并正确更新它们的退出。 |
| 243 |  |
| 244 | // Post process PHI instructions that were inserted into another disjoint |
| 245 | // loop and update their exits properly. |
| 246 | for (auto *PostProcessPN : PostProcessPHIs) |
| 247 | if (!PostProcessPN->use_empty()) |
| 248 | Worklist.push_back(PostProcessPN); |
|   | // 跟踪没有重写任何使用的 PHI 节点，以便移除它们。 |
|   | // 如果新的 PHI 节点被使用，则存储它，以便我们可以尝试传播 dbg.value 内联 intrinsics。 |
| 249 |  |
| 250 | // Keep track of PHI nodes that we want to remove because they did not have |
| 251 | // any uses rewritten. If the new PHI is used, store it so that we can |
| 252 | // try to propagate dbg.value intrinsics to it. |
| 253 | SmallVector<PHINode *, 2> NeedDbgValues; |
| 254 | for (PHINode *PN : AddedPHIs) |
| 255 | if (PN->use_empty()) |
| 256 | PHIsToRemove.insert(PN); |
| 257 | else |
| 258 | NeedDbgValues.push_back(PN); |
| 259 | insertDebugValuesForPHIs(InstBB, NeedDbgValues); |
| 260 | Changed = true; |
| 261 | } |
|   | // 移除没有重写任何使用的 PHI 节点。我们需要重新检查 use_empty()， |
| 262 | // Remove PHI nodes that did not have any uses rewritten. We need to redo the |
| 263 | // use_empty() check here, because even if the PHI node wasn't used when added |
| 264 | // to PHIsToRemove, later added PHI nodes can be using it.  This cleanup is |
| 265 | // not guaranteed to handle trees/cycles of PHI nodes that only are used by |
| 266 | // each other. Such situations has only been noticed when the input IR |
| 267 | // contains unreachable code, and leaving some extra redundant PHI nodes in |
| 268 | // such situations is considered a minor problem. |
| 269 | for (PHINode *PN : PHIsToRemove) |
| 270 | if (PN->use_empty()) |
| 271 | PN->eraseFromParent(); |
| 272 | return Changed; |
| 273 | } |
|   | // 计算循环 `L` 中至少支配一个退出块的基本块集合。 |
| 274 |  |
| 275 | // Compute the set of BasicBlocks in the loop `L` dominating at least one exit. |
| 276 | static void computeBlocksDominatingExits( |
| 277 | Loop &L, const DominatorTree &DT, SmallVector<BasicBlock *, 8> &ExitBlocks, |
| 278 | SmallSetVector<BasicBlock *, 8> &BlocksDominatingExits) { |
|   | // 工作列表，存储基本块。 |
| 279 | SmallVector<BasicBlock *, 8> BBWorklist; |
|   | // 从退出块开始，因为每个块至少支配它自己（不严格）。 |
| 280 |  |
| 281 | // We start from the exit blocks, as every block trivially dominates itself |
| 282 | // (not strictly). |
| 283 | for (BasicBlock *BB : ExitBlocks) |
| 284 | BBWorklist.push_back(BB); |
| 285 |  |
| 286 | while (!BBWorklist.empty()) { |
| 287 | BasicBlock *BB = BBWorklist.pop_back_val(); |
|   | // 如果是循环头，跳过。 |
| 288 |  |
| 289 | // Check if this is a loop header. If this is the case, we're done. |
| 290 | if (L.getHeader() == BB) |
| 291 | continue; |
|   | // 将支配树中 BB 的直接父节点添加到工作列表，除非已经访问过。 |
| 292 |  |
| 293 | // Otherwise, add its immediate predecessor in the dominator tree to the |
| 294 | // worklist, unless we visited it already. |
| 295 | BasicBlock *IDomBB = DT.getNode(BB)->getIDom()->getBlock(); |
|   | // 退出块的直接支配块可能不在循环内。如果退出块被循环外的块直接支配， |
|   | // 则意味着并非所有从该支配块到退出块的路径都经过循环。 |
| 296 |  |
| 297 | // Exit blocks can have an immediate dominator not beloinging to the |
| 298 | // loop. For an exit block to be immediately dominated by another block |
| 299 | // outside the loop, it implies not all paths from that dominator, to the |
| 300 | // exit block, go through the loop. |
| 301 | // Example: |
| 302 | // |
| 303 | // |---- A |
| 304 | // |     | |
| 305 | // |     B<-- |
| 306 | // |     |  | |
| 307 | // |---> C -- |
| 308 | //       | |
| 309 | //       D |
| 310 | // |
| 311 | // C is the exit block of the loop and it's immediately dominated by A, |
| 312 | // which doesn't belong to the loop. |
| 313 | if (!L.contains(IDomBB)) |
| 314 | continue; |
|   | // 如果成功插入，则将 IDomBB 添加到工作列表。 |
| 315 |  |
| 316 | if (BlocksDominatingExits.insert(IDomBB)) |
| 317 | BBWorklist.push_back(IDomBB); |
| 318 | } |
| 319 | } |
| 320 |  |
| 321 | bool llvm::formLCSSA(Loop &L, const DominatorTree &DT, const LoopInfo *LI, |
| 322 | ScalarEvolution *SE) { |
| 323 | bool Changed = false; |
| 324 |  |
| 325 | #ifdef EXPENSIVE_CHECKS |
|   | // 验证所有子循环已经在 LCSSA 形式。 |
| 326 | // Verify all sub-loops are in LCSSA form already. |
| 327 | for (Loop *SubLoop: L) |
| 328 | assert(SubLoop->isRecursivelyLCSSAForm(DT, *LI) && "Subloop not in LCSSA!"); |
| 329 | #endif |
|   | // 获取循环的退出块。 |
| 330 |  |
| 331 | SmallVector<BasicBlock *, 8> ExitBlocks; |
| 332 | L.getExitBlocks(ExitBlocks); |
| 333 | if (ExitBlocks.empty()) |
| 334 | return false; |
|   | // 存储支配退出块的基本块。 |
| 335 |  |
| 336 | SmallSetVector<BasicBlock *, 8> BlocksDominatingExits; |
|   | // 避免使用扫描，利用支配信息。 |
|   | // 如果一个块不支配任何循环退出块，那么循环内定义的值都不能在外部使用。 |
|   | // 我们提前计算满足条件的块集，沿着支配树向上走直到遇到循环头。 |
| 337 |  |
| 338 | // We want to avoid use-scanning leveraging dominance informations. |
| 339 | // If a block doesn't dominate any of the loop exits, the none of the values |
| 340 | // defined in the loop can be used outside. |
| 341 | // We compute the set of blocks fullfilling the conditions in advance |
| 342 | // walking the dominator tree upwards until we hit a loop header. |
| 343 | computeBlocksDominatingExits(L, DT, ExitBlocks, BlocksDominatingExits); |
|   | // 工作列表，存储需要转换为 LCSSA 的指令。 |
| 344 |  |
| 345 | SmallVector<Instruction *, 8> Worklist; |
|   | // 查看循环中的所有指令，检查是否有在循环外部的使用。如果有，将它们加入工作列表以重写这些使用。 |
| 346 |  |
| 347 | // Look at all the instructions in the loop, checking to see if they have uses |
| 348 | // outside the loop.  If so, put them into the worklist to rewrite those uses. |
| 349 | for (BasicBlock *BB : BlocksDominatingExits) { |
|   | // 跳过任何子循环的块，它们必须已经在 LCSSA 形式。 |
| 350 | // Skip blocks that are part of any sub-loops, they must be in LCSSA |
| 351 | // already. |
| 352 | if (LI->getLoopFor(BB) != &L) |
| 353 | continue; |
| 354 | for (Instruction &I : *BB) { |
|   | // 快速排除两种常见情况：没有使用的指令（如存储）和在同一块中的单个使用。 |
| 355 | // Reject two common cases fast: instructions with no uses (like stores) |
| 356 | // and instructions with one use that is in the same block as this. |
| 357 | if (I.use_empty() || |
| 358 | (I.hasOneUse() && I.user_back()->getParent() == BB && |
| 359 | !isa<PHINode>(I.user_back()))) |
| 360 | continue; |
|   | // 跳过 Token 类型的指令，因为它们不能用于 PHI 节点。 |
| 361 |  |
| 362 | // Tokens cannot be used in PHI nodes, so we skip over them. |
| 363 | // We can run into tokens which are live out of a loop with catchswitch |
| 364 | // instructions in Windows EH if the catchswitch has one catchpad which |
| 365 | // is inside the loop and another which is not. |
| 366 | if (I.getType()->isTokenTy()) |
| 367 | continue; |
| 368 |  |
| 369 | Worklist.push_back(&I); |
| 370 | } |
| 371 | } |
|   | // 对工作列表中的指令应用 LCSSA 转换。 |
| 372 | Changed = formLCSSAForInstructions(Worklist, DT, *LI, SE); |
|   | // 如果修改了代码，从 SCEV 中移除循环的任何缓存以避免悬挂条目。 |
| 373 |  |
| 374 | // If we modified the code, remove any caches about the loop from SCEV to |
| 375 | // avoid dangling entries. |
| 376 | // FIXME: This is a big hammer, can we clear the cache more selectively? |
| 377 | if (SE && Changed) |
| 378 | SE->forgetLoop(&L); |
|   | // 断言循环已经在 LCSSA 形式。 |
| 379 |  |
| 380 | assert(L.isLCSSAForm(DT)); |
| 381 |  |
| 382 | return Changed; |
| 383 | } |
|   | /// 深度优先处理循环嵌套。 |
| 384 |  |
| 385 | /// Process a loop nest depth first. |
| 386 | bool llvm::formLCSSARecursively(Loop &L, const DominatorTree &DT, |
| 387 | const LoopInfo *LI, ScalarEvolution *SE) { |
| 388 | bool Changed = false; |
|   | /// 递归地深度优先遍历内部循环。 |
| 389 |  |
| 390 | // Recurse depth-first through inner loops. |
| 391 | for (Loop *SubLoop : L.getSubLoops()) |
| 392 | Changed |= formLCSSARecursively(*SubLoop, DT, LI, SE); |
|   | /// 对当前循环应用LCSSA（Loop Closed Static Single Assignment）转换。 |
| 393 |  |
| 394 | Changed |= formLCSSA(L, DT, LI, SE); |
| 395 | return Changed; |
| 396 | } |
|   | /// 处理函数中的所有循环，从最内层循环开始向外处理。 |
| 397 |  |
| 398 | /// Process all loops in the function, inner-most out. |
| 399 | static bool formLCSSAOnAllLoops(const LoopInfo *LI, const DominatorTree &DT, |
| 400 | ScalarEvolution *SE) { |
| 401 | bool Changed = false; |
|   | // 遍历函数中的所有循环。 |
|   | // 这里使用LoopInfo对象来获取函数中所有的循环，并遍历它们。 |
| 402 | for (auto &L : *LI) |
| 403 | Changed |= formLCSSARecursively(*L, DT, LI, SE); |
|   | // 返回一个布尔值，指示是否对任何循环进行了修改。 |
| 404 | return Changed; |
| 405 | } |
|   | // 匿名命名空间，用于限制LCSSAWrapperPass的作用域。 |
| 406 |  |
| 407 | namespace { |
|   | // 定义一个结构体LCSSAWrapperPass，继承自FunctionPass。 |
| 408 | struct LCSSAWrapperPass : public FunctionPass { |
|   | // 静态成员变量ID，用于唯一标识这个Pass。 |
| 409 | static char ID; // Pass identification, replacement for typeid |
|   | // 构造函数，初始化FunctionPass，并注册这个Pass。 |
| 410 | LCSSAWrapperPass() : FunctionPass(ID) { |
| 411 | initializeLCSSAWrapperPassPass(*PassRegistry::getPassRegistry()); |
| 412 | } |
|   | // 缓存当前函数的分析信息。 |
| 413 |  |
| 414 | // Cached analysis information for the current function. |
| 415 | DominatorTree *DT; |
| 416 | LoopInfo *LI; |
| 417 | ScalarEvolution *SE; |
|   | // 重写runOnFunction方法，用于对函数F执行LCSSA转换。 |
| 418 |  |
| 419 | bool runOnFunction(Function &F) override; |
| 420 | void verifyAnalysis() const override { |
|   | // 重写verifyAnalysis方法，用于验证LCSSA转换的正确性。 |
|   | // 这个检查非常昂贵，可能会引起高达10倍的编译速度下降，因此默认情况下是禁用的。 |
| 421 | // This check is very expensive. On the loop intensive compiles it may cause |
| 422 | // up to 10x slowdown. Currently it's disabled by default. LPPassManager |
| 423 | // always does limited form of the LCSSA verification. Similar reasoning |
| 424 | // was used for the LoopInfo verifier. |
| 425 | if (VerifyLoopLCSSA) { |
| 426 | assert(all_of(*LI, |
| 427 | [&](Loop *L) { |
| 428 | return L->isRecursivelyLCSSAForm(*DT, *LI); |
| 429 | }) && |
| 430 | "LCSSA form is broken!"); |
| 431 | } |
| 432 | }; |
|   | /// LCSSA转换需要自然循环信息，并且要求循环预头部被插入到控制流图（CFG）中。 |
|   | /// 它维护了这些信息以及CFG，同时还需要支配树信息。 |
| 433 |  |
| 434 | /// This transformation requires natural loop information & requires that |
| 435 | /// loop preheaders be inserted into the CFG.  It maintains both of these, |
| 436 | /// as well as the CFG.  It also requires dominator information. |
|   | // 重写getAnalysisUsage方法，用于指定这个Pass需要和保持的分析信息。 |
| 437 | void getAnalysisUsage(AnalysisUsage &AU) const override { |
| 438 | AU.setPreservesCFG(); |
|   | // 需要支配树信息。 |
| 439 |  |
| 440 | AU.addRequired<DominatorTreeWrapperPass>(); |
|   | // 需要循环信息。 |
| 441 | AU.addRequired<LoopInfoWrapperPass>(); |
|   | // 保持LoopSimplifyPass的信息。 |
| 442 | AU.addPreservedID(LoopSimplifyID); |
|   | // 保持别名分析结果。 |
| 443 | AU.addPreserved<AAResultsWrapperPass>(); |
|   | // 保持基本别名分析。 |
| 444 | AU.addPreserved<BasicAAWrapperPass>(); |
|   | // 保持全局别名分析。 |
| 445 | AU.addPreserved<GlobalsAAWrapperPass>(); |
|   | // 保持标量演化信息。 |
| 446 | AU.addPreserved<ScalarEvolutionWrapperPass>(); |
|   | // 保持标量常量演化分析。 |
| 447 | AU.addPreserved<SCEVAAWrapperPass>(); |
|   | // 保持分支概率信息。 |
| 448 | AU.addPreserved<BranchProbabilityInfoWrapperPass>(); |
|   | // 保持内存SSA信息。 |
| 449 | AU.addPreserved<MemorySSAWrapperPass>(); |
|   | // 这是在LPPassManager中执行LCSSA验证所需的。 |
| 450 |  |
| 451 | // This is needed to perform LCSSA verification inside LPPassManager |
|   | // 需要LCSSA验证Pass。 |
| 452 | AU.addRequired<LCSSAVerificationPass>(); |
|   | // 保持LCSSA验证Pass的信息。 |
| 453 | AU.addPreserved<LCSSAVerificationPass>(); |
| 454 | } |
| 455 | }; |
| 456 | } |
|   | // 定义LCSSAWrapperPass的ID，用于唯一标识这个Pass。 |
| 457 |  |
| 458 | char LCSSAWrapperPass::ID = 0; |
|   | // 初始化LCSSAWrapperPass。 |
| 459 | INITIALIZE_PASS_BEGIN(LCSSAWrapperPass, "lcssa", "Loop-Closed SSA Form Pass", |
| 460 | false, false) |
|   | // LCSSAWrapperPass依赖于DominatorTreeWrapperPass。 |
| 461 | INITIALIZE_PASS_DEPENDENCY(DominatorTreeWrapperPass) |
|   | // LCSSAWrapperPass依赖于LoopInfoWrapperPass。 |
| 462 | INITIALIZE_PASS_DEPENDENCY(LoopInfoWrapperPass) |
|   | // LCSSAWrapperPass依赖于LCSSAVerificationPass。 |
| 463 | INITIALIZE_PASS_DEPENDENCY(LCSSAVerificationPass) |
|   | // 结束LCSSAWrapperPass的初始化。 |
| 464 | INITIALIZE_PASS_END(LCSSAWrapperPass, "lcssa", "Loop-Closed SSA Form Pass", |
| 465 | false, false) |
|   | // 创建LCSSAWrapperPass的工厂函数。 |
| 466 |  |
| 467 | Pass *llvm::createLCSSAPass() { return new LCSSAWrapperPass(); } |
|   | // 提供对LCSSAWrapperPass ID的引用。 |
| 468 | char &llvm::LCSSAID = LCSSAWrapperPass::ID; |
|   | // 将函数F转换为循环封闭的SSA形式。 |
| 469 |  |
| 470 | /// Transform \p F into loop-closed SSA form. |
| 471 | bool LCSSAWrapperPass::runOnFunction(Function &F) { |
| 472 | LI = &getAnalysis<LoopInfoWrapperPass>().getLoopInfo(); |
| 473 | DT = &getAnalysis<DominatorTreeWrapperPass>().getDomTree(); |
| 474 | auto *SEWP = getAnalysisIfAvailable<ScalarEvolutionWrapperPass>(); |
| 475 | SE = SEWP ? &SEWP->getSE() : nullptr; |
|   | // 对所有循环应用LCSSA转换，并返回是否发生了变化。 |
| 476 |  |
| 477 | return formLCSSAOnAllLoops(LI, *DT, SE); |
| 478 | } |
|   | // 使用新的Pass管理器运行LCSSA转换的函数。 |
| 479 |  |
| 480 | PreservedAnalyses LCSSAPass::run(Function &F, FunctionAnalysisManager &AM) { |
| 481 | auto &LI = AM.getResult<LoopAnalysis>(F); |
| 482 | auto &DT = AM.getResult<DominatorTreeAnalysis>(F); |
| 483 | auto *SE = AM.getCachedResult<ScalarEvolutionAnalysis>(F); |
| 484 | if (!formLCSSAOnAllLoops(&LI, DT, SE)) |
| 485 | return PreservedAnalyses::all(); |
| 486 |  |
| 487 | PreservedAnalyses PA; |
| 488 | PA.preserveSet<CFGAnalyses>(); |
| 489 | PA.preserve<BasicAA>(); |
| 490 | PA.preserve<GlobalsAA>(); |
| 491 | PA.preserve<SCEVAA>(); |
| 492 | PA.preserve<ScalarEvolutionAnalysis>(); |
| 493 | // BPI maps terminators to probabilities, since we don't modify the CFG, no |
| 494 | // updates are needed to preserve it. |
| 495 | PA.preserve<BranchProbabilityAnalysis>(); |
| 496 | PA.preserve<MemorySSAAnalysis>(); |
|   | // 返回保留的分析集合。 |
| 497 | return PA; |
| 498 | } |

