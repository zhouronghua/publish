# 第 6 章 循环优化

![图](images/image_0053.png)

本章是系列文章的第六章，介绍了循环的分析方法。循环优化的逻辑相对简单，但对性能提升的效果却非常明显。循环优化的分析还产生了一个图灵奖。

6.1 循环的重要性

90/10定律，90%的算力消耗在10%的代码上，这些代码绝大多数都是各种循环

循环的优化对获得更高的性能非常重要

基于循环的迭代空间转换的优化（本章不涉及）

维持循环的迭代空间不变进行的优化（本章重点）：

代码提升（code hoisting）

强度削减（strength reduction）

循环展开（loop unrolling）

等等

6.2 分解控制流图

对于下面的C代码，分析一下有几重循环？怎么从控制流图中定义循环？

![图](images/image_0054.gif)

1 #include <stdio.h>

2 int main(int argc, char **argv) {

3   int sum = 0;

4   int i = 1;

5   while (i < argc) {

6     char *c = argv[i++];

7     while (*c != '\0') {

8       c++;

9       sum++;

10     }

11   }

12   printf("sum = %d\n", sum);

13 }

![图](images/image_0055.gif)

控制流图的生成方法就不多说了，忘记的同学可以回过头去看看第二章（2.1.3 LLVM），生成的svg图如下：

![图](images/image_0056.png)

控制流图中的自然循环是具有下列属性的节点的集合S：

存在一个头结点h

S中的任意一个元素都存在路径到头结点h

S外不存在任何节点有边指向S中除h意外的其他节点

编译器中说的循环（loop）和拓扑意义上的环（cycle）是不同的。编译器领域中的环只能有一个入口，多个入口的环在编译器领域不叫做循环，因为绝大多数对循环的优化在多入口的环中都不适用。

多个入口的环在编码过程中也非常罕见，所以也不是编译器需要关心的场景。

6.2.1 控制流图的简化过程

如果对于边(n1, n2)，n1是n2的唯一前驱，或者n1和n2是强连通图的一部分，可以用下面的方法简化：

删除边(n1, n2)

新建节点n12

将所有n1的前驱改成n12的前驱

将所有n2的后继改成n12的后继

删除节点n1和n2

重复上述操作，直到控制流图保持不变。

例如下面的控制流图：

![图](images/image_0057.png)

简化流程是这样的：

![图](images/image_0058.png)

![图](images/image_0059.png)

为什么要简化控制流图：

入口单一，可以在优化过程中在头结点处增加 冗余代码

简化后的图数据流分析速度更快

常规的循环语法，例如for，while，repeat，continue和break都会产生可简化的控制流图

goto会产生不可简化的流图

6.3 自然循环

6.3.1 支配节点（Dominators）

节点d是节点n的支配节点，当且仅当所有从控制流图入口到n的所有路径都经过d。

D[s0] = {s0} D[n] = {n} ∪ (∩ p∈ pred[n]D[p]), for n ≠ s0
支配节点的计算：

![图](images/image_0060.png)

6.3.2 直接支配节点（Immediate Dominators）

每个阶段n都 只有唯一一个直接支配节点idom(n)，定义如下：

idom(n) 不是n

idom(n)是n的支配节点

idom(n)不是n的其他支配节点的支配节点

6.3.3 支配节点树（Dominator Tree）

把每个节点的直接支配节点画一条边到该节点，就形成了图的支配节点树：

![图](images/image_0061.png)

嵌套循环中优先优化内存循环。

循环的头节点h：在循环的节点集中，存在一个节点n，h是它的支配节点，并且存在边(n, h)。

如果两个循环的头结点存在支配关系，则被支配的头节点所在的循环称为内循环，支配的头节点所在的循环称为外循环。

6.4 安全的不变代码提升（SAFE INVARIANT CODE HOISTING）

6.4.1 循环不变性

如果某个计算在循环的每次迭代中都产生同样的值，则该计算时循环不变的。

循环不变表达式的通常优化方法是将该表达式提升到循环外。

满足下面任意一条要求的表达式是循环不变表达式：

表达式的参数是常量

表达式的参数定义在循环外

表达式的参数是循环不变表达式，并且在该表达式之前没有其他定义

将循环不变表达式提升到循环外的做法称为代码提升。

6.4.2 安全的不变代码提升

在程序点d，如果满足下面3个条件，可以对表达式t = a + b 安全的进行代码提升：

d是所有t生效区域内节点的支配节点

t在循环内只有一个定义

t在循环的头结点外没有使用

6.4.3 循环倒置（Loop Inversion）

将常规的while循环转换成repeat-util循环的做法称为循环倒置。倒置后的循环可以安全的进行不变代码提升。

repeat-utill循环在循环过程中每次迭代只需要进行一次跳转，所以性能也比常规的while循环要好。

6.5 因变量（INDUCTION VARIABLES）

6.5.1 基本概念

基本因变量（Basic induction variable）：如果一个变量i在循环内部仅定义一次，并且每次定义都是在原有值基础上增加或者减少循环不变量的值。

派生因变量（Derived induction variables）：如果一个变量k在循环内部仅定义一次，并且k是一个因变量与循环不变量的乘积或者和。

i系列的派生因变量（a derived induction variable in the family of i）：如果一个变量k定义中使用的因变量j仅定义一次，并且定义在循环内部，在j和k之间没有i的定义。

6.5.2 强度削减

将乘法运算换算成加法运算。例如下面的优化：

![图](images/image_0062.png)

强度削减的算法基本上就是将派生因变量转换成基本因变量。算法过程一般如下：

对所有j = i * c, 假定变量i每个迭代增加b，i 初始化为a，那j每个迭代就要增加 b*c。

在循环外新增一个变量j'为第一次迭代时的j的值， j' = a*c

在循环外新增一个变量k，用来保存每个迭代j需要增加的值b*c

这样循环内部就可以优化成

j = j'

j' += k

6.5.3 无用代码删除（Dead Code Elimination）

首先删除的是j'，因为k'已经完成了类似的功能：

![图](images/image_0063.png)

由于i除了定义就只有和循环不变量的比较，所以实际上i也是可以删除的：

![图](images/image_0064.png)

删除冗余拷贝：

![图](images/image_0065.png)

循环倒置：

![图](images/image_0066.png)

初始版本和最终优化版本的对比：

![图](images/image_0067.png)

6.5.4 循环展开

循环展开是通过减少循环次数并增加循环内部的计算来优化的一种方式。例如对下面的代码：

![图](images/image_0068.png)

以2为因子进行循环展开之后的结果是这样的：

![图](images/image_0069.png)

6.6 循环优化简史

Lowry, E. S. and Medlock, C. W. "Object Code Optimization". CACM 12(1), 13-22 (1969) 引入因变量优化和支配节点的概念。

Allen, F. E. "Control Flow Analysis". SIGPLAN Notices 23(7) 308-317. (1970)引入控制流图的化简，并因此获得图灵奖。

6.7 LLVM的循环优化实现

6.7.1 LLVM中的Dominators

支配关系分析，是很多程序分析和优化的基础，尤其在循环分析中特别关键，相对于其他模块，循环分析除了需要普通的支配关系，还需要知道某个循环的直接支配节点，直接支配节点可以生成支配节点树，支配节点数中的上下级关系构成了各种优化或者代码提升过程中的必要条件。

llvm\lib\IR\Dominators.cpp

| 32 | //  验证工作非常耗时，一般只在debug版本开启 |
| --- | --- |
| 33 | bool llvm::VerifyDomInfo = false; |
| 34 | static cl::opt<bool, true> |
| 35 | VerifyDomInfoX("verify-dom-info", cl::location(VerifyDomInfo), cl::Hidden, |
| 36 | cl::desc("Verify dominator info (time consuming)")); |
| 37 | //  相对于VerifyDomInfo打开情况下的全部验证， |
| 38 | //  EXPENSIVE_CHECKS仅验证一些基本的信息， |
| 39 | //  当然，也可以选择什么校验都不做，这就是默认做法 |
| 40 | #ifdef EXPENSIVE_CHECKS |
| 41 | static constexpr bool ExpensiveChecksEnabled = true; |
| 42 | #else |
| 43 | static constexpr bool ExpensiveChecksEnabled = false; |
| 44 | #endif |
| 45 | //  如果一个函数中仅有两个基本块， |
| 46 | //  并且两个基本块之间仅有一条边那很多分析可以简化。 |
| 47 | bool BasicBlockEdge::isSingleEdge() const { |
| 48 | const Instruction *TI = Start->getTerminator(); |
| 49 | unsigned NumEdgesToEnd = 0; |
| 50 | for (unsigned int i = 0, n = TI->getNumSuccessors(); i < n; ++i) { |
| 51 | if (TI->getSuccessor(i) == End) |
| 52 | ++NumEdgesToEnd; |
| 53 | if (NumEdgesToEnd >= 2) |
| 54 | return false; |
| 55 | } |
| 56 | assert(NumEdgesToEnd == 1); |
| 57 | return true; |
| 58 | } |
| 59 | //===----------------------------------------------------------------------===// |
| 60 | //  下面是支配树的实现。 |
| 61 | //  DominatorTree Implementation |
| 62 | //===----------------------------------------------------------------------===// |
| 63 | // |
| 64 | // 下面这些函数是在头文件里面实现的模板，这里特定化一下。 |
| 65 | // Provide public access to DominatorTree information.  Implementation details |
| 66 | // can be found in Dominators.h, GenericDomTree.h, and |
| 67 | // GenericDomTreeConstruction.h. |
| 68 | // |
| 69 | //===----------------------------------------------------------------------===// |
| 70 | // 支配树和后支配树的大多数逻辑都是一样的， |
| 71 | // 只不过后支配树有可能存在多个根节点，其他都是打印的差异。 |
| 72 | template class llvm::DomTreeNodeBase<BasicBlock>; |
| 73 | template class llvm::DominatorTreeBase<BasicBlock, false>; // DomTreeBase |
| 74 | template class llvm::DominatorTreeBase<BasicBlock, true>; // PostDomTreeBase |
| 75 | template class llvm::cfg::Update<BasicBlock *>; |
| 76 | // 基于基本块的支配树是对普通支配树的简化， |
| 77 | // 基本块支配树的每个节点都是基本块。 |
| 78 | // 如果说DomTree是对支配树本身的抽象， |
| 79 | // 那DomTreeBuilder就是对支配树构建过程的抽象，有点工厂模式的味道。 |
| 80 | template void llvm::DomTreeBuilder::Calculate<DomTreeBuilder::BBDomTree>( |
| 81 | DomTreeBuilder::BBDomTree &DT); |
| 82 | template void |
| 83 | llvm::DomTreeBuilder::CalculateWithUpdates<DomTreeBuilder::BBDomTree>( |
| 84 | DomTreeBuilder::BBDomTree &DT, BBUpdates U); |
| 85 | // 后支配树的做法和支配树类似。 |
| 86 | template void llvm::DomTreeBuilder::Calculate<DomTreeBuilder::BBPostDomTree>( |
| 87 | DomTreeBuilder::BBPostDomTree &DT); |
| 88 | // No CalculateWithUpdates<PostDomTree> instantiation, unless a usecase arises. |
| 89 | // 增。 |
| 90 | template void llvm::DomTreeBuilder::InsertEdge<DomTreeBuilder::BBDomTree>( |
| 91 | DomTreeBuilder::BBDomTree &DT, BasicBlock *From, BasicBlock *To); |
| 92 | template void llvm::DomTreeBuilder::InsertEdge<DomTreeBuilder::BBPostDomTree>( |
| 93 | DomTreeBuilder::BBPostDomTree &DT, BasicBlock *From, BasicBlock *To); |
| 94 | // 删。 |
| 95 | template void llvm::DomTreeBuilder::DeleteEdge<DomTreeBuilder::BBDomTree>( |
| 96 | DomTreeBuilder::BBDomTree &DT, BasicBlock *From, BasicBlock *To); |
| 97 | template void llvm::DomTreeBuilder::DeleteEdge<DomTreeBuilder::BBPostDomTree>( |
| 98 | DomTreeBuilder::BBPostDomTree &DT, BasicBlock *From, BasicBlock *To); |
| 99 | // 改。 |
| 100 | template void llvm::DomTreeBuilder::ApplyUpdates<DomTreeBuilder::BBDomTree>( |
| 101 | DomTreeBuilder::BBDomTree &DT, DomTreeBuilder::BBUpdates); |
| 102 | template void llvm::DomTreeBuilder::ApplyUpdates<DomTreeBuilder::BBPostDomTree>( |
| 103 | DomTreeBuilder::BBPostDomTree &DT, DomTreeBuilder::BBUpdates); |
| 104 | // 验。 |
| 105 | template bool llvm::DomTreeBuilder::Verify<DomTreeBuilder::BBDomTree>( |
| 106 | const DomTreeBuilder::BBDomTree &DT, |
| 107 | DomTreeBuilder::BBDomTree::VerificationLevel VL); |
| 108 | template bool llvm::DomTreeBuilder::Verify<DomTreeBuilder::BBPostDomTree>( |
| 109 | const DomTreeBuilder::BBPostDomTree &DT, |
| 110 | DomTreeBuilder::BBPostDomTree::VerificationLevel VL); |
| 111 | // invalidate本意是失效，所以如果所有分析都属于需要保留的内容， |
| 112 | // 那就失效失败，返回false。反之，就失效成功了。 |
| 113 | // 一般调用invalidate是为了触发支配树重新生成。 |
| 114 | bool DominatorTree::invalidate(Function &F, const PreservedAnalyses &PA, |
| 115 | FunctionAnalysisManager::Invalidator &) { |
| 116 | // Check whether the analysis, all analyses on functions, or the function's |
| 117 | // CFG have been preserved. |
| 118 | auto PAC = PA.getChecker<DominatorTreeAnalysis>(); |
| 119 | return !(PAC.preserved() || PAC.preservedSet<AllAnalysesOn<Function>>() || |
| 120 | PAC.preservedSet<CFGAnalyses>()); |
| 121 | } |
| 122 | // Def支配User的实际含义是，如果某个变量在Def中有定义， |
| 123 | // 在User处有使用，那么在User处的使用一定在Def的定义之后。 |
| 124 | // 也就是说，如果User中的使用在Def的定义之前， |
| 125 | // 那么就会出现未定义的行为。 |
| 126 | // 这是因为在程序执行时，必须先定义变量，然后才能使用它。 |
| 127 | // 是否支配 - 如果Def支配User中的使用，则返回true。 |
| 128 | // 如果Def和User位于相同的基本块中，则需要执行此特殊检查。 |
| 129 | // 请注意，Def不支配其自身的任何使用！ |
| 130 | // dominates - Return true if Def dominates a use in User. This performs |
| 131 | // the special checks necessary if Def and User are in the same basic block. |
| 132 | // Note that Def doesn't dominate a use in Def itself! |
| 133 | bool DominatorTree::dominates(const Instruction *Def, |
| 134 | const Instruction *User) const { |
| 135 | // 获取User所在的基本块 |
| 136 | const BasicBlock *UseBB = User->getParent(); |
| 137 | // 获取Def所在的基本块 |
| 138 | const BasicBlock *DefBB = Def->getParent(); |
| 139 | // 任何无法到达的use都被支配，即使Def == User。 |
| 140 | // Any unreachable use is dominated, even if Def == User. |
| 141 | if (!isReachableFromEntry(UseBB)) |
| 142 | return true; |
| 143 | // 无法到达的定义不支配任何东西。 |
| 144 | // Unreachable definitions don't dominate anything. |
| 145 | if (!isReachableFromEntry(DefBB)) |
| 146 | return false; |
| 147 | // 指令在其自身上不支配任何使用 |
| 148 | // An instruction doesn't dominate a use in itself. |
| 149 | if (Def == User) |
| 150 | return false; |
| 151 | // invoke指令定义的值只在其支配UseBB中的每个指令时才支配指令。 |
| 152 | // PHI仅在其支配UseBB中的每个可能使用时才被支配。 |
| 153 | // The value defined by an invoke dominates an instruction only if it |
| 154 | // dominates every instruction in UseBB. |
| 155 | // A PHI is dominated only if the instruction dominates every possible use in |
| 156 | // the UseBB. |
| 157 | if (isa<InvokeInst>(Def) || isa<CallBrInst>(Def) || isa<PHINode>(User)) |
| 158 | return dominates(Def, UseBB); |
| 159 | if (DefBB != UseBB) |
| 160 | return dominates(DefBB, UseBB); |
| 161 | // 如果Def不在UseBB中，则调用dominates(DefBB, UseBB)函数, |
| 162 | // 检查Def所在的基本块是否支配UseBB中的指令。 |
| 163 | // 否则，如果Def在UseBB中，则只需检查Def是否在User之前。 |
| 164 | return Def->comesBefore(User); |
| 165 | } |
| 166 | // 如果Def在任何位于UseBB中的指令中使用，则返回true。 |
| 167 | // 请注意，dominates(Def, Def->getParent())将返回false。 |
| 168 | // true if Def would dominate a use in any instruction in UseBB. |
| 169 | // note that dominates(Def, Def->getParent()) is false. |
| 170 | bool DominatorTree::dominates(const Instruction *Def, |
| 171 | const BasicBlock *UseBB) const { |
| 172 | // 获取Def所在的基本块 |
| 173 | const BasicBlock *DefBB = Def->getParent(); |
| 174 | // 任何无法到达的use都被支配，即使DefBB == UseBB。 |
| 175 | // Any unreachable use is dominated, even if DefBB == UseBB. |
| 176 | if (!isReachableFromEntry(UseBB)) |
| 177 | return true; |
| 178 | // 无法到达的定义不支配任何东西。 |
| 179 | // Unreachable definitions don't dominate anything. |
| 180 | if (!isReachableFromEntry(DefBB)) |
| 181 | return false; |
| 182 | // 如果DefBB等于UseBB，则返回false，因为这种情况下无法判断是否支配。 |
| 183 | if (DefBB == UseBB) |
| 184 | return false; |
| 185 | // 如果Def是一个invoke指令，则需要检查其正常目标是否可以到达UseBB。 |
| 186 | // 这是因为invoke结果只能在其正常目标中使用，不能在异常目标中使用。 |
| 187 | // Invoke results are only usable in the normal destination, not in the |
| 188 | // exceptional destination. |
| 189 | if (const auto *II = dyn_cast<InvokeInst>(Def)) { |
| 190 | // II是一个invoke指令，获取其正常目标基本块 |
| 191 | BasicBlock *NormalDest = II->getNormalDest(); |
| 192 | // 创建一个基本块边缘对象E，表示从DefBB到NormalDest的跳转。 |
| 193 | BasicBlockEdge E(DefBB, NormalDest); |
| 194 | // 调用dominates(E)函数检查从DefBB到NormalDest的跳转， |
| 195 | // 是否可以支配UseBB中的指令。 |
| 196 | return dominates(E, UseBB); |
| 197 | } |
| 198 | // 如果Def是一个CallBrInst类型的指令，则类似地只能在默认目标中使用其结果。 |
| 199 | // Callbr results are similarly only usable in the default destination. |
| 200 | if (const auto *CBI = dyn_cast<CallBrInst>(Def)) { |
| 201 | // 获取CallBrInst指令的默认目标基本块。 |
| 202 | BasicBlock *NormalDest = CBI->getDefaultDest(); |
| 203 | // 创建一个基本块边缘对象E，表示从DefBB到NormalDest的跳转。 |
| 204 | BasicBlockEdge E(DefBB, NormalDest); |
| 205 | // 调用dominates(E, UseBB)函数检查从DefBB到NormalDest的跳转是否可以支配UseBB中的指令。 |
| 206 | return dominates(E, UseBB); |
| 207 | } |
| 208 | // 如果不是CallBrInst类型的指令， |
| 209 | // 则直接调用dominates(DefBB, UseBB)函数检查DefBB是否可以支配UseBB中的指令。 |
| 210 | return dominates(DefBB, UseBB); |
| 211 | } |
| 212 | bool DominatorTree::dominates(const BasicBlockEdge &BBE, |
| 213 | const BasicBlock *UseBB) const { |
| 214 | // 如果该边的终点不支配使用的基本块，则该边也不支配。 |
| 215 | // If the BB the edge ends in doesn't dominate the use BB, then the |
| 216 | // edge also doesn't. |
| 217 | const BasicBlock *Start = BBE.getStart(); // 获取该边的起点 |
| 218 | const BasicBlock *End = BBE.getEnd(); // 获取该边的终点 |
| 219 | // 如果终点不支配使用的基本块，则返回false |
| 220 | if (!dominates(End, UseBB)) |
| 221 | return false; |
| 222 | // 简单情况：如果终点只有一个前驱节点， |
| 223 | // 终点支配使用的基本块意味着该边也支配。 |
| 224 | // Simple case: if the end BB has a single predecessor, the fact that it |
| 225 | // dominates the use block implies that the edge also does. |
| 226 | if (End->getSinglePredecessor()) |
| 227 | return true; |
| 228 | // 来自invoke的正常边是关键的。 |
| 229 | // 从概念上讲，我们想做的是将其拆分并检查新块是否支配使用的基本块。 |
| 230 | // 用X表示新块，图形将如下所示： |
| 231 | // The normal edge from the invoke is critical. Conceptually, what we would |
| 232 | // like to do is split it and check if the new block dominates the use. |
| 233 | // With X being the new block, the graph would look like: |
| 234 | // |
| 235 | //        DefBB |
| 236 | //          /\      .  . |
| 237 | //         /  \     .  . |
| 238 | //        /    \    .  . |
| 239 | //       /      \   |  | |
| 240 | //      A        X  B  C |
| 241 | //      |         \ | / |
| 242 | //      .          \|/ |
| 243 | //      .      NormalDest |
| 244 | //      . |
| 245 | // |
| 246 | // 根据支配的定义，如果X支配NormalDest的所有前驱节点（例如X、B、C）， |
| 247 | // 则NormalDest被X支配。 |
| 248 | // X显然支配自身，因此我们只需要检查它是否支配其他前驱节点。 |
| 249 | // 由于X唯一的出口是通过NormalDest， |
| 250 | // 因此X只能在其支配NormalDest的情况下支配节点。 |
| 251 | // Given the definition of dominance, NormalDest is dominated by X iff X |
| 252 | // dominates all of NormalDest's predecessors (X, B, C in the example). X |
| 253 | // trivially dominates itself, so we only have to find if it dominates the |
| 254 | // other predecessors. Since the only way out of X is via NormalDest, X can |
| 255 | // only properly dominate a node if NormalDest dominates that node too. |
| 256 | int IsDuplicateEdge = 0; // 记录重复边的数量 |
| 257 | for (const_pred_iterator PI = pred_begin(End), E = pred_end(End); |
| 258 | PI != E; ++PI) { |
| 259 | const BasicBlock *BB = *PI; // 获取当前前驱节点 |
| 260 | if (BB == Start) { |
| 261 | // 如果起点和终点之间有多个边，根据定义，它们不能支配任何东西。 |
| 262 | // 如果存在重复边，则返回false |
| 263 | // If there are multiple edges between Start and End, by definition they |
| 264 | // can't dominate anything. |
| 265 | if (IsDuplicateEdge++) |
| 266 | return false; |
| 267 | continue; |
| 268 | } |
| 269 | if (!dominates(End, BB)) // 如果终点不支配当前前驱节点，则返回false |
| 270 | return false; |
| 271 | } |
| 272 | return true; // 所有前驱节点都由该边所支配，因此返回true |
| 273 | } |
| 274 | bool DominatorTree::dominates(const BasicBlockEdge &BBE, const Use &U) const { |
| 275 | // 将Use的User转换为Instruction指针。 |
| 276 | Instruction *UserInst = cast<Instruction>(U.getUser()); |
| 277 | // 如果UserInst是指令，并且该指令是一个PHI节点， |
| 278 | // 且该PHI节点在BBE的结束处，并且该PHI节点的输入块是BBE的开始处， |
| 279 | // 则该BBE支配该PHI节点。 |
| 280 | // A PHI in the end of the edge is dominated by it. |
| 281 | PHINode *PN = dyn_cast<PHINode>(UserInst); |
| 282 | if (PN && PN->getParent() == BBE.getEnd() && |
| 283 | PN->getIncomingBlock(U) == BBE.getStart()) |
| 284 | return true; |
| 285 | // 否则，使用edge-dominates-block查询，它可以正确处理疯狂的关键边情况。 |
| 286 | // 获取Use的所在基本块。 |
| 287 | // Otherwise use the edge-dominates-block query, which |
| 288 | // handles the crazy critical edge cases properly. |
| 289 | const BasicBlock *UseBB; |
| 290 | if (PN) |
| 291 | UseBB = PN->getIncomingBlock(U); |
| 292 | else |
| 293 | UseBB = UserInst->getParent(); |
| 294 | // 调用dominates函数查询BBE是否支配UseBB |
| 295 | return dominates(BBE, UseBB); |
| 296 | } |
| 297 | // 这段代码定义了一个名为DominatorTree的类的成员函数dominates， |
| 298 | // 这个函数用于确定一个定义点（Def）是否支配一个使用点（U）。 |
| 299 | // 支配关系在程序中意味着，如果一个变量在某处被定义，并且在另一处使用， |
| 300 | // 那么定义点必须在使用点之前。 |
| 301 | bool DominatorTree::dominates(const Instruction *Def, const Use &U) const { |
| 302 | // 函数首先获取定义点所在的基本块（DefBB）。 |
| 303 | Instruction *UserInst = cast<Instruction>(U.getUser()); |
| 304 | const BasicBlock *DefBB = Def->getParent(); |
| 305 | // 然后确定使用点所在的基本块（UseBB）。 |
| 306 | // Determine the block in which the use happens. PHI nodes use |
| 307 | // their operands on edges; simulate this by thinking of the use |
| 308 | // happening at the end of the predecessor block. |
| 309 | const BasicBlock *UseBB; |
| 310 | if (PHINode *PN = dyn_cast<PHINode>(UserInst)) |
| 311 | UseBB = PN->getIncomingBlock(U); |
| 312 | else |
| 313 | UseBB = UserInst->getParent(); |
| 314 | // 如果使用点所在的基元块无法从程序入口可达，那么该使用点被支配， |
| 315 | // 即使定义点等于用户点。 |
| 316 | // Any unreachable use is dominated, even if Def == User. |
| 317 | if (!isReachableFromEntry(UseBB)) |
| 318 | return true; |
| 319 | // 如果定义点无法从程序入口可达，那么该定义不支配任何内容。 |
| 320 | // Unreachable definitions don't dominate anything. |
| 321 | if (!isReachableFromEntry(DefBB)) |
| 322 | return false; |
| 323 | // 如果定义点是一个InvokeInst（即 invoke 指令）， |
| 324 | // 该指令定义了其正常成功目标（NormalDest）的返回值， |
| 325 | // 那么这个支配关系需要特别处理。 |
| 326 | // 这种情况下，该指令不支配其所在块内的任何内容，除非该块中有一个 PHI 节点。 |
| 327 | // 所以，我们不需要再遍历该块。 |
| 328 | // Invoke instructions define their return values on the edges to their normal |
| 329 | // successors, so we have to handle them specially. |
| 330 | // Among other things, this means they don't dominate anything in |
| 331 | // their own block, except possibly a phi, so we don't need to |
| 332 | // walk the block in any case. |
| 333 | if (const InvokeInst *II = dyn_cast<InvokeInst>(Def)) { |
| 334 | BasicBlock *NormalDest = II->getNormalDest(); |
| 335 | BasicBlockEdge E(DefBB, NormalDest); |
| 336 | return dominates(E, U); |
| 337 | } |
| 338 | // 如果定义点是一个CallBrInst（即 callbr 指令）， |
| 339 | // 其结果只能在其默认目标中使用，因此需要进行类似的特别处理。 |
| 340 | // Callbr results are similarly only usable in the default destination. |
| 341 | if (const auto *CBI = dyn_cast<CallBrInst>(Def)) { |
| 342 | BasicBlock *NormalDest = CBI->getDefaultDest(); |
| 343 | BasicBlockEdge E(DefBB, NormalDest); |
| 344 | return dominates(E, U); |
| 345 | } |
| 346 | // 如果定义点和使用点在不同的基元块中， |
| 347 | // 那么会进行一个简单的 CFG 支配树查询。 |
| 348 | // If the def and use are in different blocks, do a simple CFG dominator |
| 349 | // tree query. |
| 350 | if (DefBB != UseBB) |
| 351 | return dominates(DefBB, UseBB); |
| 352 | // 如果定义点和使用点在同一个基元块中，如果定义点是一个 invoke 指令， |
| 353 | // 那么它不支配该块内的任何内容。 |
| 354 | // 如果定义点是一个 PHI 节点，那么它支配该块内的所有内容。 |
| 355 | // Ok, def and use are in the same block. If the def is an invoke, it |
| 356 | // doesn't dominate anything in the block. If it's a PHI, it dominates |
| 357 | // everything in the block. |
| 358 | // 最后，如果定义点是一个 PHI 节点，那么返回 true。 |
| 359 | if (isa<PHINode>(UserInst)) |
| 360 | return true; |
| 361 | // 否则，返回定义点是否在用户点之前的布尔值。 |
| 362 | return Def->comesBefore(UserInst); |
| 363 | } |
| 364 | // 本函数接收一个Use对象的引用作为参数，返回一个布尔值， |
| 365 | // 表示这个Use是否可以从入口（entry）到达。 |
| 366 | bool DominatorTree::isReachableFromEntry(const Use &U) const { |
| 367 | // 获取U的用户，并尝试将其动态转换为Instruction对象。 |
| 368 | // U的用户应该是Instruction对象，如果不是， |
| 369 | // 说明U的用户可能是常量表达式（ConstantExpr）。 |
| 370 | Instruction *I = dyn_cast<Instruction>(U.getUser()); |
| 371 | // 如果U的用户不是Instruction对象，即U的用户是常量表达式， |
| 372 | // 虽然它们实际上并不可从入口块到达，但它们也不需要像不可到达的代码那样被处理。 |
| 373 | // 因此返回true。 |
| 374 | // ConstantExprs aren't really reachable from the entry block, but they |
| 375 | // don't need to be treated like unreachable code either. |
| 376 | if (!I) return true; |
| 377 | // 如果U的用户是PHINode对象，尝试将其动态转换为PHINode对象。 |
| 378 | //  PHINode是特殊的Instruction对象，它表示基本块之间的分支。 |
| 379 | // PHI nodes use their operands on their incoming edges. |
| 380 | if (PHINode *PN = dyn_cast<PHINode>(I)) |
| 381 | // 对于PHINode，我们需要判断它的输入块是否可以从入口到达， |
| 382 | // 因此递归调用isReachableFromEntry函数。 |
| 383 | return isReachableFromEntry(PN->getIncomingBlock(U)); |
| 384 | // 如果U的用户不是PHINode对象，那么它是普通的Instruction对象。 |
| 385 | // 对于普通的Instruction对象，我们需要判断它所在的块是否可以从入口到达， |
| 386 | // 因此递归调用isReachableFromEntry函数。 |
| 387 | // Everything else uses their operands in their own block. |
| 388 | return isReachableFromEntry(I->getParent()); |
| 389 | } |
| 390 | // 如果边BBE1与边BBE2匹配，或者BBE1支配BBE2的起点， |
| 391 | // 则边BBE1支配边BBE2。 |
| 392 | // Edge BBE1 dominates edge BBE2 if they match or BBE1 dominates start of BBE2. |
| 393 | bool DominatorTree::dominates(const BasicBlockEdge &BBE1, |
| 394 | const BasicBlockEdge &BBE2) const { |
| 395 | if (BBE1.getStart() == BBE2.getStart() && BBE1.getEnd() == BBE2.getEnd()) |
|  | return true; |
|  | return dominates(BBE1, BBE2.getStart()); |
|  | } |
|  | //===----------------------------------------------------------------------===// |
|  | //  DominatorTreeAnalysis and related pass implementations |
|  | //  支配树分析和相关pass的实现。 |
|  | //===----------------------------------------------------------------------===// |
|  | // |
|  | // This implements the DominatorTreeAnalysis which is used with the new pass |
|  | // manager. It also implements some methods from utility passes. |
|  | // 这段代码实现了DominatorTreeAnalysis，它是与新的传递管理器一起使用的。 |
|  | // 它还实现了一些来自实用传递的方法。 |
|  | // |
|  | //===----------------------------------------------------------------------===// |
|  | // run函数是DominatorTreeAnalysis的一个成员， |
|  | // 它接受一个Function对象和一个FunctionAnalysisManager对象作为参数， |
|  | // 重新计算函数的支配者树，并返回计算结果。 |
|  | DominatorTree DominatorTreeAnalysis::run(Function &F, |
|  | FunctionAnalysisManager &) { |
|  | DominatorTree DT;  // 创建一个DominatorTree对象DT |
|  | DT.recalculate(F);  // 重新计算F的支配者树 |
|  | return DT;  // 返回计算结果 |
|  | } |
|  | // Key是一个静态成员变量，它的类型是AnalysisKey， |
|  | // 用于在FunctionAnalysisManager中注册DominatorTreeAnalysis。 |
|  | AnalysisKey DominatorTreeAnalysis::Key; |
|  | // DominatorTreePrinterPass的构造函数，接受一个raw_ostream对象作为参数， |
|  | // 并将其存储在成员变量OS中。 |
|  | DominatorTreePrinterPass::DominatorTreePrinterPass(raw_ostream &OS) : OS(OS) {} |
|  | // run函数是DominatorTreePrinterPass的一个成员， |
|  | // 它接受一个Function对象和一个FunctionAnalysisManager对象作为参数， |
|  | // 打印函数的支配者树，并返回PreservedAnalyses::all()，表示该传递保留了所有的分析结果。 |
|  | PreservedAnalyses DominatorTreePrinterPass::run(Function &F, |
|  | FunctionAnalysisManager &AM) { |
|  | OS << "DominatorTree for function: " << F.getName() << "\n";  // 打印函数的名称 |
|  | AM.getResult<DominatorTreeAnalysis>(F).print(OS);  // 打印函数的支配者树 |
|  | return PreservedAnalyses::all();  // 返回PreservedAnalyses::all() |
|  | } |
|  | // run函数是DominatorTreeVerifierPass的一个成员， |
|  | // 它接受一个Function对象和一个FunctionAnalysisManager对象作为参数， |
|  | // 验证函数的支配者树，并返回PreservedAnalyses::all()，表示该传递保留了所有的分析结果。 |
|  | PreservedAnalyses DominatorTreeVerifierPass::run(Function &F, |
|  | FunctionAnalysisManager &AM) { |
|  | auto &DT = AM.getResult<DominatorTreeAnalysis>(F);  // 获取函数的支配者树 |
|  | assert(DT.verify());  // 验证支配者树 |
|  | (void)DT;  // 防止编译器警告，在release版本中此处并未使用DT |
|  | return PreservedAnalyses::all();  // 返回PreservedAnalyses::all() |
|  | } |
|  | //===----------------------------------------------------------------------===// |
|  | //  DominatorTreeWrapperPass Implementation |
|  | //===----------------------------------------------------------------------===// |
|  | // |
|  | // The implementation details of the wrapper pass that holds a DominatorTree |
|  | // suitable for use with the legacy pass manager. |
|  | // 这段代码是Wrapper Pass的实现细节， |
|  | // 它持有一个适合与旧传递管理器一起使用的DominatorTree。 |
|  | // |
|  | //===----------------------------------------------------------------------===// |
|  | // ID是一个静态成员变量，它的类型是char， |
|  | // 用于在PassRegistry中注册DominatorTreeWrapperPass。 |
|  | char DominatorTreeWrapperPass::ID = 0; |
|  | // DominatorTreeWrapperPass的构造函数， |
|  | // 它调用initializeDominatorTreeWrapperPassPass函数来初始化传递。 |
|  | DominatorTreeWrapperPass::DominatorTreeWrapperPass() : FunctionPass(ID) { |
|  | initializeDominatorTreeWrapperPassPass(*PassRegistry::getPassRegistry());  // 初始化传递 |
|  | } |
|  | // 初始化一个Pass，Pass的名称是"domtree"， |
|  | // 描述是"Dominator Tree Construction"，并且它在函数级别的优化和验证阶段运行。 |
|  | INITIALIZE_PASS(DominatorTreeWrapperPass, "domtree", |
|  | "Dominator Tree Construction", true, true) |
|  | // runOnFunction方法是这个Pass的主要部分，它在每个函数上运行。 |
|  | // 这个方法会重新计算给定函数的支配者树，并返回false，表示这个Pass并没有修改函数。 |
|  | bool DominatorTreeWrapperPass::runOnFunction(Function &F) { |
|  | DT.recalculate(F);  // 重新计算支配者树 |
|  | return false;  // 返回false，表示没有修改 |
|  | } |
|  | // verifyAnalysis方法用于验证分析结果的有效性。 |
|  | // 如果开启了完全验证（VerifyDomInfo），它会执行完全的支配者树验证。 |
|  | // 如果没有开启完全验证但是开启了昂贵检查（ExpensiveChecksEnabled）， |
|  | // 它会执行基本的支配者树验证。 |
|  | void DominatorTreeWrapperPass::verifyAnalysis() const { |
|  | if (VerifyDomInfo) |
|  | assert(DT.verify(DominatorTree::VerificationLevel::Full));  // 进行完全的支配者树验证 |
|  | else if (ExpensiveChecksEnabled) |
|  | assert(DT.verify(DominatorTree::VerificationLevel::Basic));  // 进行基本的支配者树验证 |
|  | } |
|  | // print方法用于打印支配者树的信息。 |
|  | void DominatorTreeWrapperPass::print(raw_ostream &OS, const Module *) const { |
|  | DT.print(OS);  // 打印支配者树的信息 |
|  | } |

LLVM中的loop-unroll

llvm\lib\Transforms\Utils\LoopUnroll.cpp

| 79 | // 用例打印pass相关的名字 |
| --- | --- |
| 80 | #define DEBUG_TYPE "loop-unroll" |
| 81 | // LLVM中并没有其他LoopUnroll，mlir里面倒是另外有一个， |
| 82 | // 但和这个也是相互独立的，所以，没明白作者为何加这句注释。 |
| 83 | // TODO: Should these be here or in LoopUnroll? |
| 84 | // 下面的计数器名称还是比较显眼的，做了多少个loop的展开， |
| 85 | // 总的loop个数和总的无条件展开的loop个数。 |
| 86 | STATISTIC(NumCompletelyUnrolled, "Number of loops completely unrolled"); |
| 87 | STATISTIC(NumUnrolled, "Number of loops unrolled (completely or otherwise)"); |
| 88 | STATISTIC(NumUnrolledNotLatch, "Number of loops unrolled without a conditional " |
| 89 | "latch (completely or otherwise)"); |
| 90 | // 定义一个静态的cl::opt<bool>类型的变量UnrollRuntimeEpilog， |
| 91 | // 它的名字是"unroll-runtime-epilog"。 |
| 92 | // cl::init(false)表示这个选项的默认值是false。 |
| 93 | // cl::Hidden表示这个选项在命令行中是隐藏的，用户不能直接设置。 |
| 94 | // cl::desc()提供了这个选项的描述，说明这个选项的用途。 |
| 95 | // 默认使用epilog。 |
| 96 | static cl::opt<bool> |
| 97 | UnrollRuntimeEpilog("unroll-runtime-epilog", cl::init(false), cl::Hidden, |
| 98 | cl::desc("Allow runtime unrolled loops to be unrolled " |
| 99 | "with epilog instead of prolog.")); |
| 100 | // 定义一个静态的cl::opt<bool>类型的变量UnrollVerifyDomtree， |
| 101 | // 它的名字是"unroll-verify-domtree"。 |
| 102 | // cl::Hidden表示这个选项在命令行中是隐藏的，用户不能直接设置。 |
| 103 | // cl::desc()提供了这个选项的描述，说明这个选项的用途。 |
| 104 | // #ifdef EXPENSIVE_CHECKS和#else指令用于条件编译， |
| 105 | // 如果定义了EXPENSIVE_CHECKS，则cl::init(true)，否则cl::init(false)。 |
| 106 | static cl::opt<bool> |
| 107 | UnrollVerifyDomtree("unroll-verify-domtree", cl::Hidden, |
| 108 | cl::desc("Verify domtree after unrolling"), |
| 109 | #ifdef EXPENSIVE_CHECKS |
| 110 | cl::init(true) |
| 111 | #else |
| 112 | cl::init(false) |
| 113 | #endif |
| 114 | ); |
| 115 | /// 这段代码是在检查在展开循环后是否需要插入phi节点以保持LCSSA形式。 |
| 116 | /// Check if unrolling created a situation where we need to insert phi nodes to |
| 117 | /// preserve LCSSA form. |
| 118 | /// \param Blocks is a vector of basic blocks representing unrolled loop. |
| 119 | /// \param L is the outer loop. |
| 120 | /// 可能存在一些BB在L内部，一些BB在L外部。 |
| 121 | /// 如果某个变量的定义在L内部，使用在L外部，那就要插入一个φ节点， |
| 122 | /// 要不然就打破了LCSSA范式。 |
| 123 | /// LCSSA是loop closed SSA的简称，直译是闭环SSA， |
| 124 | /// 是对循环中的变量传递到循环外时，插入了额外的φ节点之后的代码。 |
| 125 | /// LCSSA和普通SSA等价，但对其他分析和处理更加友好， |
| 126 | /// 更详细的实现参见llvm\lib\Transforms\Utils\LCSSA.cpp。 |
| 127 | /// It's possible that some of the blocks are in L, and some are not. In this |
| 128 | /// case, if there is a use is outside L, and definition is inside L, we need to |
| 129 | /// insert a phi-node, otherwise LCSSA will be broken. |
| 130 | /// 这个函数是一个辅助函数， |
| 131 | /// 用于检查在展开循环后是否需要插入phi节点以保持LCSSA形式。 |
| 132 | /// 参数Blocks是一个基本块向量，代表展开后的循环。 |
| 133 | /// 参数L是外部循环。可能的情况是，一些块在L中，一些不在。 |
| 134 | /// 在这种情况下，如果有一个在L之外的使用和一个在L之内的定义， |
| 135 | /// 我们需要插入一个phi节点，否则LCSSA将被破坏。 |
| 136 | /// 如果发生这种情况，该函数将返回true，指示需要修复LCSSA。 |
| 137 | /// The function is just a helper function for llvm::UnrollLoop that returns |
| 138 | /// true if this situation occurs, indicating that LCSSA needs to be fixed. |
| 139 | static bool needToInsertPhisForLCSSA(Loop *L, std::vector<BasicBlock *> Blocks, |
| 140 | LoopInfo *LI) { |
| 141 | // 遍历Blocks向量中的所有基本块。 |
| 142 | for (BasicBlock *BB : Blocks) { |
| 143 | // 如果基本块BB属于循环L，则跳过这个基本块，进行下一个基本块的遍历。 |
| 144 | if (LI->getLoopFor(BB) == L) |
| 145 | continue; |
| 146 | // 遍历基本块BB中的所有指令。 |
| 147 | for (Instruction &I : *BB) { |
| 148 | // 遍历指令I的所有操作数。 |
| 149 | for (Use &U : I.operands()) { |
| 150 | // 如果操作数U是一个指令，则获取这个指令。 |
| 151 | if (auto Def = dyn_cast<Instruction>(U)) { |
| 152 | // 获取定义这个指令的基本块所在的循环。 |
| 153 | Loop *DefLoop = LI->getLoopFor(Def->getParent()); |
| 154 | // 如果定义指令的基本块不在任何循环中，则跳过这个操作数， |
| 155 | // 进行下一个操作数的遍历。 |
| 156 | if (!DefLoop) |
| 157 | continue; |
| 158 | // 如果定义指令的基本块所在的循环包含了循环L， |
| 159 | // 则返回true，表示需要插入PHI指令。 |
| 160 | if (DefLoop->contains(L)) |
| 161 | return true; |
| 162 | } |
| 163 | } |
| 164 | } |
| 165 | } |
| 166 | // 如果遍历了所有的基本块和指令，都没有发现需要插入PHI指令的情况，则返回false。 |
| 167 | return false; |
| 168 | } |
| 169 | /// 这个函数是用来将克隆的基本块添加到循环信息中， |
| 170 | /// 并在必要时为克隆的基本块创建新的循环，同时添加从原始循环到新循环的映射。 |
| 171 | /// 如果未创建新循环，则返回nullptr，否则返回原始基本块所在的原始循环的指针。 |
| 172 | /// Adds ClonedBB to LoopInfo, creates a new loop for ClonedBB if necessary |
| 173 | /// and adds a mapping from the original loop to the new loop to NewLoops. |
| 174 | /// Returns nullptr if no new loop was created and a pointer to the |
| 175 | /// original loop OriginalBB was part of otherwise. |
| 176 | const Loop* llvm::addClonedBlockToLoopInfo(BasicBlock *OriginalBB, |
| 177 | BasicBlock *ClonedBB, LoopInfo *LI, |
| 178 | NewLoopsMap &NewLoops) { |
| 179 | // 确定New所在的循环。 |
| 180 | // Figure out which loop New is in. |
| 181 | const Loop *OldLoop = LI->getLoopFor(OriginalBB); |
| 182 | assert(OldLoop && "Should (at least) be in the loop being unrolled!"); |
| 183 | // 如果NewLoop还没有被创建，就创建它。 |
| 184 | Loop *&NewLoop = NewLoops[OldLoop]; |
| 185 | if (!NewLoop) { |
| 186 | // 发现了一个新的子循环。 |
| 187 | // Found a new sub-loop. |
| 188 | assert(OriginalBB == OldLoop->getHeader() && |
| 189 | "Header should be first in RPO"); |
| 190 | // 分配一个新的循环。 |
| 191 | NewLoop = LI->AllocateLoop(); |
| 192 | // 获取新循环的父循环。 |
| 193 | Loop *NewLoopParent = NewLoops.lookup(OldLoop->getParentLoop()); |
| 194 | // 如果父循环存在，则将新循环添加为父循环的子循环。 |
| 195 | if (NewLoopParent) |
| 196 | NewLoopParent->addChildLoop(NewLoop); |
| 197 | // 否则，将新循环添加为顶级循环。 |
| 198 | else |
| 199 | LI->addTopLevelLoop(NewLoop); |
| 200 | // 将克隆的基本块添加到新循环中。 |
| 201 | NewLoop->addBasicBlockToLoop(ClonedBB, *LI); |
| 202 | return OldLoop; |
| 203 | } else { |
| 204 | // 将克隆的基本块添加到现有的新循环中。 |
| 205 | NewLoop->addBasicBlockToLoop(ClonedBB, *LI); |
| 206 | return nullptr; |
| 207 | } |
| 208 | } |
| 209 | /// 该函数选择哪种类型的展开（epilog 或 prolog）更有利。 |
| 210 | /// 当存在从常量开始的 PHI 时，epilog 展开更有利。 |
| 211 | /// 在这种情况下，epilog 将保留从常量开始的 PHI， |
| 212 | /// 但 prolog 会将其转换为非常量。 |
| 213 | /// The function chooses which type of unroll (epilog or prolog) is more |
| 214 | /// profitabale. |
| 215 | /// Epilog unroll is more profitable when there is PHI that starts from |
| 216 | /// constant.  In this case epilog will leave PHI start from constant, |
| 217 | /// but prolog will convert it to non-constant. |
| 218 | /// |
| 219 | /// loop: |
| 220 | ///   PN = PHI [I, Latch], [CI, PreHeader] |
| 221 | ///   I = foo(PN) |
| 222 | ///   ... |
| 223 | /// |
| 224 | /// Epilog算法展开循环的例子。 |
| 225 | /// Epilog unroll case. |
| 226 | /// loop: |
| 227 | ///   PN = PHI [I2, Latch], [CI, PreHeader] |
| 228 | ///   I1 = foo(PN) |
| 229 | ///   I2 = foo(I1) |
| 230 | ///   ... |
| 231 | /// Prolog算法展开循环的例子。 |
| 232 | /// Prolog unroll case. |
| 233 | ///   NewPN = PHI [PrologI, Prolog], [CI, PreHeader] |
| 234 | /// loop: |
| 235 | ///   PN = PHI [I2, Latch], [NewPN, PreHeader] |
| 236 | ///   I1 = foo(PN) |
| 237 | ///   I2 = foo(I1) |
| 238 | ///   ... |
| 239 | /// |
| 240 | /// 如果存在从常量开始的PHI的节点，epilog展开将保留这个常量开始的PHI节点， |
| 241 | /// 而prolog展开则会将其转换为非常量，此时应该选择epilog。 |
| 242 | static bool isEpilogProfitable(Loop *L) { |
| 243 | BasicBlock *PreHeader = L->getLoopPreheader(); |
| 244 | BasicBlock *Header = L->getHeader(); |
| 245 | assert(PreHeader && Header); |
| 246 | for (const PHINode &PN : Header->phis()) { |
| 247 | if (isa<ConstantInt>(PN.getIncomingValueForBlock(PreHeader))) |
| 248 | return true; |
| 249 | } |
| 250 | return false; |
| 251 | } |
| 252 | /// 这个函数的目标是在循环展开后执行一些清理和简化操作。 |
| 253 | /// 它对新的循环中的IV（IV是induction variables的简称， |
| 254 | /// 有的地方翻译成推导变量，诱导变量，我们这里译为因变量， |
| 255 | /// 意思是随着循环的递进，会随着循环线性变化的变量）进行简化， |
| 256 | /// 并对指令进行快速的简化/dce（死代码消除）传递。 |
| 257 | /// Perform some cleanup and simplifications on loops after unrolling. It is |
| 258 | /// useful to simplify the IV's in the new loop, as well as do a quick |
| 259 | /// simplify/dce pass of the instructions. |
| 260 | void llvm::simplifyLoopAfterUnroll(Loop *L, bool SimplifyIVs, LoopInfo *LI, |
| 261 | ScalarEvolution *SE, DominatorTree *DT, |
| 262 | AssumptionCache *AC, |
| 263 | const TargetTransformInfo *TTI) { |
| 264 | // 简化任何在部分展开的循环中的因变量。 |
| 265 | // Simplify any new induction variables in the partially unrolled loop. |
| 266 | if (SE && SimplifyIVs) { |
| 267 | // 定义一个存储已删除指令的弱引用向量 |
| 268 | SmallVector<WeakTrackingVH, 16> DeadInsts; |
| 269 | // 简化循环中的新归纳变量 |
| 270 | simplifyLoopIVs(L, SE, DT, LI, TTI, DeadInsts); |
| 271 | // Aggressively clean up dead instructions that simplifyLoopIVs already |
| 272 | // identified. Any remaining should be cleaned up below. |
| 273 | // 激进地清理 simplifyLoopIVs 已经标识的已死亡指令。 |
| 274 | // 任何剩余的指令都应在下面清理。 |
| 275 | while (!DeadInsts.empty()) { |
| 276 | // 从已删除指令的向量中弹出一个元素。 |
| 277 | Value *V = DeadInsts.pop_back_val(); |
| 278 | // 如果这个元素是一个指令（或可能是一个指令） |
| 279 | if (Instruction *Inst = dyn_cast_or_null<Instruction>(V)) |
| 280 | // 删除这个指令及其依赖的指令。 |
| 281 | RecursivelyDeleteTriviallyDeadInstructions(Inst); |
| 282 | } |
| 283 | } |
| 284 | // 在这一点上，代码具有良好形式。我们现在对插入的代码进行一次快速的扫描， |
| 285 | // 同时进行常量传播和无用代码的消除操作。 |
| 286 | // At this point, the code is well formed.  We now do a quick sweep over the |
| 287 | // inserted code, doing constant propagation and dead code elimination as we |
| 288 | // go. |
| 289 | const DataLayout &DL = L->getHeader()->getModule()->getDataLayout(); |
| 290 | for (BasicBlock *BB : L->getBlocks()) { |
| 291 | for (BasicBlock::iterator I = BB->begin(), E = BB->end(); I != E;) { |
| 292 | Instruction *Inst = &*I++; |
| 293 | // 如果某条指令可以被简化，并且替换值不会破坏 LCSSA |
| 294 | // （Least Common Subexpression Analysis）形式， |
| 295 | // 那么用简化值替换该指令的所有使用。 |
| 296 | if (Value *V = SimplifyInstruction(Inst, {DL, nullptr, DT, AC})) |
| 297 | if (LI->replacementPreservesLCSSAForm(Inst, V)) |
| 298 | Inst->replaceAllUsesWith(V); |
| 299 | // 如果某条指令无条件地等价于一个常量，那么删除该指令。 |
| 300 | if (isInstructionTriviallyDead(Inst)) |
| 301 | BB->getInstList().erase(Inst); |
| 302 | } |
| 303 | } |
| 304 | // TODO: 在剥离（peeling）或解构（unrolling）之后， |
| 305 | // 之前循环中的条件很可能会折叠为常量， |
| 306 | // 在这里急切地传播这些常量将需要运行更少的清理转译。 |
| 307 | // 或者，一个 LoopEarlyCSE（循环早期公共子表达式消除）可能是合适的。 |
| 308 | // TODO: after peeling or unrolling, previously loop variant conditions are |
| 309 | // likely to fold to constants, eagerly propagating those here will require |
| 310 | // fewer cleanup passes to be run.  Alternatively, a LoopEarlyCSE might be |
| 311 | // appropriate. |
| 312 | } |
| 313 | /// Unroll the given loop by Count. The loop must be in LCSSA form.  Unrolling |
| 314 | /// can only fail when the loop's latch block is not terminated by a conditional |
| 315 | /// branch instruction. However, if the trip count (and multiple) are not known, |
| 316 | /// loop unrolling will mostly produce more code that is no faster. |
| 317 | /// 这个方法接受一个带循环次数的循环（必须处于LCSSA形式）， |
| 318 | /// 然后进行循环展开。展开可能会失败， |
| 319 | /// 如果循环的Latch块不是由条件分支指令终止的。但是， |
| 320 | /// 如果不知道循环次数（和倍数），那么循环展开通常会产生更多的代码，并不会更快。 |
| 321 | /// |
| 322 | /// TripCount is the upper bound of the iteration on which control exits |
| 323 | /// LatchBlock. Control may exit the loop prior to TripCount iterations either |
| 324 | /// via an early branch in other loop block or via LatchBlock terminator. This |
| 325 | /// is relaxed from the general definition of trip count which is the number of |
| 326 | /// times the loop header executes. Note that UnrollLoop assumes that the loop |
| 327 | /// counter test is in LatchBlock in order to remove unnecesssary instances of |
| 328 | /// the test.  If control can exit the loop from the LatchBlock's terminator |
| 329 | /// prior to TripCount iterations, flag PreserveCondBr needs to be set. |
| 330 | /// 这里定义了一个变量TripCount，表示当控制流退出Latch块的上界迭代次数。 |
| 331 | /// 在TripCount迭代之前，控制流可以通过其他循环块的早期分支或通过Latch块的终止器退出循环。 |
| 332 | /// 这与行程次数的通用定义（即循环头执行的次数）有所不同。 |
| 333 | /// 注意，UnrollLoop假定循环计数器测试是在LatchBlock中进行的， |
| 334 | /// 以删除不必要的测试实例。 |
| 335 | /// 如果在TripCount迭代之前控制可以从LatchBlock的终止器中退出循环， |
| 336 | /// 则需要设置PreserveCondBr标志。 |
| 337 | /// 简单说一下llvm代码里面的循环定义。一个循环外面可以有一个或者多个进入段 |
| 338 | /// （entering block，如果只有一个进入段，则可以称为循环的前驱）。 |
| 339 | /// 进入段的后继是循环的头，循环的头有边一个或者多个退出块（exiting block）。 |
| 340 | /// 退出块判断是进入锁存块（latch block，也就是通常说的循环体）， |
| 341 | /// 还是退出循环（通过边指向出口块，exit block）。 |
| 342 | /// 锁存块执行完之后会回到循环头。 |
| 343 | /// |
| 344 | /// PreserveCondBr indicates whether the conditional branch of the LatchBlock |
| 345 | /// needs to be preserved. It is needed when we use trip count upper bound to |
| 346 | /// fully unroll the loop. If PreserveOnlyFirst is also set then only the first |
| 347 | /// conditional branch needs to be preserved. |
| 348 | /// PreserveCondBr表示是否需要保留LatchBlock的条件分支。 |
| 349 | /// 当我们使用行程计数的上界来完全展开循环时，这是必需的。 |
| 350 | /// 如果还设置了PreserveOnlyFirst，那么只需要保留第一个条件分支。 |
| 351 | /// |
| 352 | /// Similarly, TripMultiple divides the number of times that the LatchBlock may |
| 353 | /// execute without exiting the loop. |
| 354 | /// 同样，TripMultiple表示LatchBlock在不退出循环的情况下可以执行的倍数。 |
| 355 | /// 例如，如果我们知道循环的次数是2的倍数，则TripMultiple可以是2， |
| 356 | /// 循环展开可以通过展开一次的方式将循环的总次数减半。 |
| 357 | /// 类似的，如果TripMultiple是3，则可以展开2次的方式将循环次数减少2/3。 |
| 358 | /// 如果循环的次数既不是常量，也无法确定是某个整数的倍数， |
| 359 | /// 这里的循环展开就不一定优化了执行时间。 |
| 360 | /// 例如，如果运行过程中计算出来循环次数是1，但编译阶段不知道是1， |
| 361 | /// 这里无论怎么做优化都是负优化。编译优化一般只做可靠的优化， |
| 362 | /// 对可能产生优化，也可能产生负优化的代码，尽量保持原来的代码不动。 |
| 363 | /// |
| 364 | /// If AllowRuntime is true then UnrollLoop will consider unrolling loops that |
| 365 | /// have a runtime (i.e. not compile time constant) trip count.  Unrolling these |
| 366 | /// loops require a unroll "prologue" that runs "RuntimeTripCount % Count" |
| 367 | /// iterations before branching into the unrolled loop.  UnrollLoop will not |
| 368 | /// runtime-unroll the loop if computing RuntimeTripCount will be expensive and |
| 369 | /// AllowExpensiveTripCount is false. |
| 370 | /// 如果AllowRuntime为真，那么UnrollLoop将考虑展开具有运行时 |
| 371 | /// （即不是编译时常量）行程计数的循环。 |
| 372 | /// 展开这些循环需要一个“序言”，在分支进入展开的循环之前， |
| 373 | /// 先求RuntimeTripCount 除 Count的余数，进行余数次迭代。 |
| 374 | /// 如果计算RuntimeTripCount的开销很大且AllowExpensiveTripCount为假， |
| 375 | /// UnrollLoop将不会运行时展开循环。 |
| 376 | /// |
| 377 | /// If we want to perform PGO-based loop peeling, PeelCount is set to the |
| 378 | /// number of iterations we want to peel off. |
| 379 | ///如果我们想执行基于PGO的循环剥离，PeelCount被设置为要剥离的迭代次数。 |
| 380 | /// |
| 381 | /// The LoopInfo Analysis that is passed will be kept consistent. |
| 382 | ///传递的LoopInfo Analysis将保持一致。 |
| 383 | /// |
| 384 | /// This utility preserves LoopInfo. It will also preserve ScalarEvolution and |
| 385 | /// DominatorTree if they are non-null. |
| 386 | ///此实用程序保留LoopInfo。如果非空，还将保留ScalarEvolution和DominatorTree。 |
| 387 | /// |
| 388 | /// If RemainderLoop is non-null, it will receive the remainder loop (if |
| 389 | /// required and not fully unrolled). |
| 390 | ///如果RemainderLoop不为空，它将接收到剩余的循环（如果需要并且未完全展开）。 |
| 391 | LoopUnrollResult llvm::UnrollLoop(Loop *L, UnrollLoopOptions ULO, LoopInfo *LI, |
| 392 | ScalarEvolution *SE, DominatorTree *DT, |
| 393 | AssumptionCache *AC, |
| 394 | const TargetTransformInfo *TTI, |
| 395 | OptimizationRemarkEmitter *ORE, |
| 396 | bool PreserveLCSSA, Loop **RemainderLoop) { |
| 397 | // 获取循环的预头部（Preheader）BasicBlock。 |
| 398 | // 预头部是循环之前的一个BasicBlock，它只包含无条件跳转到循环头部的指令。 |
| 399 | // 如果没有预头部，说明循环的预处理（例如预头部插入）失败。 |
| 400 | // 必须先找到循环的头，否则无法判断是否是循环。 |
| 401 | BasicBlock *Preheader = L->getLoopPreheader(); |
| 402 | if (!Preheader) { |
| 403 | LLVM_DEBUG(dbgs() << "  Can't unroll; loop preheader-insertion failed.\n"); |
| 404 | return LoopUnrollResult::Unmodified; |
| 405 | } |
| 406 | // 获取循环的锁存块（Latch Block）。 |
| 407 | // 锁存块是循环中最后一个可能再次跳转到循环头部的BasicBlock。 |
| 408 | // 如果没有锁存块，说明循环的出口块插入失败。 |
| 409 | BasicBlock *LatchBlock = L->getLoopLatch(); |
| 410 | if (!LatchBlock) { |
| 411 | LLVM_DEBUG(dbgs() << "  Can't unroll; loop exit-block-insertion failed.\n"); |
| 412 | return LoopUnrollResult::Unmodified; |
| 413 | } |
| 414 | // 检查循环体是否安全克隆。如果循环体中包含indirectbr指令（间接跳转），则不能克隆。 |
| 415 | // Loops with indirectbr cannot be cloned. |
| 416 | if (!L->isSafeToClone()) { |
| 417 | LLVM_DEBUG(dbgs() << "  Can't unroll; Loop body cannot be cloned.\n"); |
| 418 | return LoopUnrollResult::Unmodified; |
| 419 | } |
| 420 | // 当前的循环展开过程可以展开具有以下特点的循环： |
| 421 | // (1) 只有一个锁存块；并且 |
| 422 | // (2a) 锁存块是无条件的；或者 |
| 423 | // (2b) 锁存块是有条件的，并且是退出块 |
| 424 | // FIXME: 实现可以扩展到处理更复杂的情况，例如具有多个锁存块的循环。 |
| 425 | // The current loop unroll pass can unroll loops that have |
| 426 | // (1) single latch; and |
| 427 | // (2a) latch is unconditional; or |
| 428 | // (2b) latch is conditional and is an exiting block |
| 429 | // FIXME: The implementation can be extended to work with more complicated |
| 430 | // cases, e.g. loops with multiple latches. |
| 431 | BasicBlock *Header = L->getHeader(); |
| 432 | BranchInst *LatchBI = dyn_cast<BranchInst>(LatchBlock->getTerminator()); |
| 433 | // 一个条件分支，它退出循环，在某些情况下可以在展开的循环中优化为无条件分支。 |
| 434 | // A conditional branch which exits the loop, which can be optimized to an |
| 435 | // unconditional branch in the unrolled loop in some cases. |
| 436 | BranchInst *ExitingBI = nullptr; // 退出循环的分支指令，初始化为nullptr。 |
| 437 | bool LatchIsExiting = L->isLoopExiting(LatchBlock); // 检查锁存块是否是退出循环的块。 |
| 438 | if (LatchIsExiting) |
| 439 | ExitingBI = LatchBI; // 如果锁存块是退出块，则设置ExitingBI为LatchBI。 |
| 440 | else if (BasicBlock *ExitingBlock = L->getExitingBlock()) |
| 441 | ExitingBI = dyn_cast<BranchInst>(ExitingBlock->getTerminator()); |
| 442 | // 如果锁存块没有BranchInst，或者锁存块是有条件的但不是退出块，则不能展开循环。 |
| 443 | if (!LatchBI || (LatchBI->isConditional() && !LatchIsExiting)) { |
| 444 | LLVM_DEBUG( |
| 445 | dbgs() << "Can't unroll; a conditional latch must exit the loop"); |
| 446 | return LoopUnrollResult::Unmodified; // 返回未修改的结果 |
| 447 | } |
| 448 | // 调试输出，显示退出块的信息。 |
| 449 | LLVM_DEBUG({ |
| 450 | if (ExitingBI) |
| 451 | dbgs() << "  Exiting Block = " << ExitingBI->getParent()->getName() |
| 452 | << "\n"; |
| 453 | else |
| 454 | dbgs() << "  No single exiting block\n"; |
| 455 | }); |
| 456 | // 如果循环头部的地址被取过，则不进行展开。 |
| 457 | // 循环旋转传递在许多情况下可以避免这个问题。 |
| 458 | if (Header->hasAddressTaken()) { |
| 459 | // The loop-rotate pass can be helpful to avoid this in many cases. |
| 460 | LLVM_DEBUG( |
| 461 | dbgs() << "  Won't unroll loop: address of header block is taken.\n"); |
| 462 | return LoopUnrollResult::Unmodified; |
| 463 | } |
| 464 | // 如果提供了循环的迭代次数（TripCount），则进行调试输出。 |
| 465 | if (ULO.TripCount != 0) |
| 466 | LLVM_DEBUG(dbgs() << "  Trip Count = " << ULO.TripCount << "\n"); |
| 467 | // 如果提供了循环的迭代倍数（TripMultiple），则进行调试输出。 |
| 468 | if (ULO.TripMultiple != 1) |
| 469 | LLVM_DEBUG(dbgs() << "  Trip Multiple = " << ULO.TripMultiple << "\n"); |
| 470 | // 有效地“删除”那些超出迭代次数（TripCount）且永远不会执行的展开迭代。 |
| 471 | // Effectively "DCE" unrolled iterations that are beyond the tripcount |
| 472 | // and will never be executed. |
| 473 | if (ULO.TripCount != 0 && ULO.Count > ULO.TripCount) |
| 474 | ULO.Count = ULO.TripCount; // 如果指定的展开次数大于TripCount，则将其限制为TripCount。 |
| 475 | // 如果没有需要展开的迭代（TripCount为0，Count小于2，且PeelCount为0）， |
| 476 | // 则不进入展开代码。 |
| 477 | // Don't enter the unroll code if there is nothing to do. |
| 478 | if (ULO.TripCount == 0 && ULO.Count < 2 && ULO.PeelCount == 0) { |
| 479 | LLVM_DEBUG(dbgs() << "Won't unroll; almost nothing to do\n"); |
| 480 | return LoopUnrollResult::Unmodified; |
| 481 | } |
| 482 | // 断言确保ULO.Count（展开次数）和ULO.TripMultiple（迭代倍数）都是正数。 |
| 483 | assert(ULO.Count > 0); |
| 484 | assert(ULO.TripMultiple > 0); |
| 485 | // 断言确保如果提供了TripCount（迭代次数），那么它应该是TripMultiple（迭代倍数）的整数倍。 |
| 486 | assert(ULO.TripCount == 0 || ULO.TripCount % ULO.TripMultiple == 0); |
| 487 | // 判断我们是否要完全 |
| 488 | // Are we eliminating the loop control altogether? |
| 489 | bool CompletelyUnroll = ULO.Count == ULO.TripCount; |
| 490 | // 定义一个存储退出块的小型向量 |
| 491 | SmallVector<BasicBlock *, 4> ExitBlocks; |
| 492 | // 获取循环L的所有退出块，并将它们添加到ExitBlocks向量中 |
| 493 | L->getExitBlocks(ExitBlocks); |
| 494 | // 获取循环L的所有基本块的向量 |
| 495 | std::vector<BasicBlock*> OriginalLoopBlocks = L->getBlocks(); |
| 496 | // 遍历循环L的所有退出块，并检查是否存在phi节点。 |
| 497 | // 我们保守地假设这些phi节点 |
| 498 | // 是为了保持LCSSA形式而插入的，这意味着完全展开可能会破坏这种形式。 |
| 499 | //我们需要在转换后原地修复它，或者完全重建LCSSA。 |
| 500 | // TODO: 目前，我们只是为外部循环重新计算LCSSA，但应该有可能原地修复它。 |
| 501 | // Go through all exits of L and see if there are any phi-nodes there. We just |
| 502 | // conservatively assume that they're inserted to preserve LCSSA form, which |
| 503 | // means that complete unrolling might break this form. We need to either fix |
| 504 | // it in-place after the transformation, or entirely rebuild LCSSA. TODO: For |
| 505 | // now we just recompute LCSSA for the outer loop, but it should be possible |
| 506 | // to fix it in-place. |
| 507 | bool NeedToFixLCSSA = PreserveLCSSA && CompletelyUnroll && |
| 508 | any_of(ExitBlocks, [](const BasicBlock *BB) { |
| 509 | return isa<PHINode>(BB->begin()); |
| 510 | }); |
| 511 | // 如果编译器无法计算出循环的迭代次数，并且指定了unroll-runtime标志， |
| 512 | // 我们则假设运行时的迭代次数。 |
| 513 | // We assume a run-time trip count if the compiler cannot |
| 514 | // figure out the loop trip count and the unroll-runtime |
| 515 | // flag is specified. |
| 516 | bool RuntimeTripCount = |
| 517 | (ULO.TripCount == 0 && ULO.Count > 0 && ULO.AllowRuntime); |
| 518 | // 断言检查：我们期望在相同的循环中不同时进行运行时迭代次数的展开和剥离 |
| 519 | assert((!RuntimeTripCount || !ULO.PeelCount) && |
| 520 | "Did not expect runtime trip-count unrolling " |
| 521 | "and peeling for the same loop"); |
| 522 | // 初始化一个标志，表示循环是否已被剥离 |
| 523 | bool Peeled = false; |
| 524 | // 如果ULO.PeelCount非零，表示需要剥离循环 |
| 525 | if (ULO.PeelCount) { |
| 526 | // 尝试剥离循环，剥离次数为ULO.PeelCount，并更新Peeled标志 |
| 527 | Peeled = peelLoop(L, ULO.PeelCount, LI, SE, DT, AC, PreserveLCSSA); |
| 528 | // 如果剥离成功，可能会导致循环的preheader和迭代次数发生变化 |
| 529 | // 如果我们稍后要展开循环，我们希望这些值是最新的 |
| 530 | // Successful peeling may result in a change in the loop preheader/trip |
| 531 | // counts. If we later unroll the loop, we want these to be updated. |
| 532 | if (Peeled) { |
| 533 | // 根据我们的性价比检查，唯一有意义的退出应该是latch块 |
| 534 | // 其他退出都导向deopt，所以我们不关心它们 |
| 535 | // According to our guards and profitability checks the only |
| 536 | // meaningful exit should be latch block. Other exits go to deopt, |
| 537 | // so we do not worry about them. |
| 538 | BasicBlock *ExitingBlock = L->getLoopLatch(); |
| 539 | // 断言检查：确保存在退出块，且它是循环的latch块 |
| 540 | assert(ExitingBlock && "Loop without exiting block?"); |
| 541 | // 断言检查：确保latch块确实是循环的退出块 |
| 542 | assert(L->isLoopExiting(ExitingBlock) && "Latch is not exiting?"); |
| 543 | // 更新循环的preheader |
| 544 | Preheader = L->getLoopPreheader(); |
| 545 | // 重新计算并更新循环的迭代次数和迭代倍数 |
| 546 | ULO.TripCount = SE->getSmallConstantTripCount(L, ExitingBlock); |
| 547 | ULO.TripMultiple = SE->getSmallConstantTripMultiple(L, ExitingBlock); |
| 548 | } |
| 549 | } |
| 550 | // 如果循环中包含收敛指令，那么展开计数必须是循环迭代倍数的约数。 |
| 551 | // Loops containing convergent instructions must have a count that divides |
| 552 | // their TripMultiple. |
| 553 | LLVM_DEBUG( |
| 554 | { |
| 555 | // 初始化一个标志，表示循环中是否存在收敛指令 |
| 556 | bool HasConvergent = false; |
| 557 | // 遍历循环中的所有基本块 |
| 558 | for (auto &BB : L->blocks()) |
| 559 | // 遍历基本块中的所有指令 |
| 560 | for (auto &I : *BB) |
| 561 | // 如果指令是一个调用基类（包括函数调用、内联汇编等） |
| 562 | if (auto *CB = dyn_cast<CallBase>(&I)) |
| 563 | // 检查调用是否标记为收敛，并更新HasConvergent标志 |
| 564 | HasConvergent |= CB->isConvergent(); |
| 565 | // 断言检查：如果循环中存在收敛操作，确保展开计数能整除迭代倍数 |
| 566 | assert((!HasConvergent || ULO.TripMultiple % ULO.Count == 0) && |
| 567 | "Unroll count must divide trip multiple if loop contains a " |
| 568 | "convergent operation."); |
| 569 | }); |
| 570 | // 根据命令行选项UnrollRuntimeEpilog的出现次数来决定是否考虑尾部的盈利性。 |
| 571 | // 如果没有明确指定，则调用isEpilogProfitable函数来判断循环尾部的盈利性。 |
| 572 | bool EpilogProfitability = |
| 573 | UnrollRuntimeEpilog.getNumOccurrences() ? UnrollRuntimeEpilog |
| 574 | : isEpilogProfitable(L); |
| 575 | // 如果运行时迭代次数已知，且迭代倍数不能被展开计数整除， |
| 576 | // 尝试展开循环的余数部分。如果无法展开，根据ULO.Force的值决定是否强制继续展开， |
| 577 | // 或者返回未修改状态。 |
| 578 | if (RuntimeTripCount && ULO.TripMultiple % ULO.Count != 0 && |
| 579 | !UnrollRuntimeLoopRemainder(L, ULO.Count, ULO.AllowExpensiveTripCount, |
| 580 | EpilogProfitability, ULO.UnrollRemainder, |
| 581 | ULO.ForgetAllSCEV, LI, SE, DT, AC, TTI, |
| 582 | PreserveLCSSA, RemainderLoop)) { |
| 583 | if (ULO.Force) |
| 584 | // 如果强制展开，则忽略运行时迭代次数 |
| 585 | RuntimeTripCount = false; |
| 586 | else { |
| 587 | // 如果不强制展开，则输出调试信息并返回未修改状态 |
| 588 | LLVM_DEBUG(dbgs() << "Won't unroll; remainder loop could not be " |
| 589 | "generated when assuming runtime trip count\n"); |
| 590 | return LoopUnrollResult::Unmodified; |
| 591 | } |
| 592 | } |
| 593 | // 如果已知循环的迭代次数，我们可以计算出余数 |
| 594 | // If we know the trip count, we know the multiple... |
| 595 | unsigned BreakoutTrip = 0; |
| 596 | if (ULO.TripCount != 0) { |
| 597 | // 如果ULO.TripCount非零，计算展开后的余数迭代次数 |
| 598 | BreakoutTrip = ULO.TripCount % ULO.Count; |
| 599 | // 并将迭代倍数设置为0，因为我们已经知道确切的迭代次数 |
| 600 | ULO.TripMultiple = 0; |
| 601 | } else { |
| 602 | // 如果不知道确切的迭代次数，则计算展开计数和迭代倍数的最大公约数， |
| 603 | // 并将其设置为新的迭代倍数和余数迭代次数。 |
| 604 | // Figure out what multiple to use. |
| 605 | BreakoutTrip = ULO.TripMultiple = |
| 606 | (unsigned)GreatestCommonDivisor64(ULO.Count, ULO.TripMultiple); |
| 607 | } |
| 608 | // 使用ore命名空间，这通常是LLVM优化注释和报告的命名空间。 |
| 609 | using namespace ore; |
| 610 | // 报告完全展开决策。 |
| 611 | // Report the unrolling decision. |
| 612 | if (CompletelyUnroll) { |
| 613 | // 如果是完全展开，则在调试输出中打印相关信息。 |
| 614 | LLVM_DEBUG(dbgs() << "COMPLETELY UNROLLING loop %" << Header->getName() |
| 615 | << " with trip count " << ULO.TripCount << "!\n"); |
| 616 | // 如果存在优化注释报告器（ORE），则发出一个优化注释。 |
| 617 | if (ORE) |
| 618 | ORE->emit([&]() { |
| 619 | // 创建一个优化注释，包含循环被完全展开的信息和展开次数。 |
| 620 | return OptimizationRemark(DEBUG_TYPE, "FullyUnrolled", L->getStartLoc(), |
| 621 | L->getHeader()) |
| 622 | << "completely unrolled loop with " |
| 623 | << NV("UnrollCount", ULO.TripCount) << " iterations"; |
| 624 | }); |
| 625 | // 报告剥离决策。 |
| 626 | } else if (ULO.PeelCount) { |
| 627 | // 如果是剥离，则在调试输出中打印相关信息。 |
| 628 | LLVM_DEBUG(dbgs() << "PEELING loop %" << Header->getName() |
| 629 | << " with iteration count " << ULO.PeelCount << "!\n"); |
| 630 | // 如果存在优化注释报告器（ORE），则发出一个优化注释。 |
| 631 | if (ORE) |
| 632 | ORE->emit([&]() { |
| 633 | // 创建一个优化注释，包含循环被剥离的信息和剥离次数。 |
| 634 | return OptimizationRemark(DEBUG_TYPE, "Peeled", L->getStartLoc(), |
| 635 | L->getHeader()) |
| 636 | << " peeled loop by " << NV("PeelCount", ULO.PeelCount) |
| 637 | << " iterations"; |
| 638 | }); |
| 639 | // 报告部分展开决策。 |
| 640 | } else { |
| 641 | // 定义一个lambda函数来构建部分展开的优化注释。 |
| 642 | auto DiagBuilder = [&]() { |
| 643 | // 创建一个优化注释，包含循环被部分展开的信息和展开因子。 |
| 644 | OptimizationRemark Diag(DEBUG_TYPE, "PartialUnrolled", L->getStartLoc(), |
| 645 | L->getHeader()); |
| 646 | return Diag << "unrolled loop by a factor of " |
| 647 | << NV("UnrollCount", ULO.Count); |
| 648 | }; |
| 649 | // 在调试输出中打印部分展开循环的信息 |
| 650 | LLVM_DEBUG(dbgs() << "UNROLLING loop %" << Header->getName() << " by " |
| 651 | << ULO.Count); |
| 652 | // 处理展开时的不同情况。 |
| 653 | if (ULO.TripMultiple == 0 || BreakoutTrip != ULO.TripMultiple) { |
| 654 | // 如果TripMultiple为0或者BreakoutTrip不等于TripMultiple，则打印并发出带有breakout的注释。 |
| 655 | LLVM_DEBUG(dbgs() << " with a breakout at trip " << BreakoutTrip); |
| 656 | if (ORE) |
| 657 | ORE->emit([&]() { |
| 658 | // 使用之前定义的DiagBuilder来构建并发出优化注释，包含breakout的信息。 |
| 659 | return DiagBuilder() << " with a breakout at trip " |
| 660 | << NV("BreakoutTrip", BreakoutTrip); |
| 661 | }); |
| 662 | } else if (ULO.TripMultiple != 1) { |
| 663 | // 如果每个分支有多个迭代（TripMultiple不等于1），则打印并发出相应的注释。 |
| 664 | LLVM_DEBUG(dbgs() << " with " << ULO.TripMultiple << " trips per branch"); |
| 665 | if (ORE) |
| 666 | ORE->emit([&]() { |
| 667 | // 使用DiagBuilder构建并发出优化注释，包含每个分支的迭代次数信息。 |
| 668 | return DiagBuilder() |
| 669 | << " with " << NV("TripMultiple", ULO.TripMultiple) |
| 670 | << " trips per branch"; |
| 671 | }); |
| 672 | } else if (RuntimeTripCount) { |
| 673 | // 如果存在运行时迭代次数（RuntimeTripCount），则打印并发出相应的注释。 |
| 674 | LLVM_DEBUG(dbgs() << " with run-time trip count"); |
| 675 | if (ORE) |
| 676 | ORE->emit( |
| 677 | [&]() { return DiagBuilder() << " with run-time trip count"; }); |
| 678 | } |
| 679 | // 在调试输出中添加换行符，结束当前消息。 |
| 680 | LLVM_DEBUG(dbgs() << "!\n"); |
| 681 | } |
| 682 | // We are going to make changes to this loop. SCEV may be keeping cached info |
| 683 | // about it, in particular about backedge taken count. The changes we make |
| 684 | // are guaranteed to invalidate this information for our loop. It is tempting |
| 685 | // to only invalidate the loop being unrolled, but it is incorrect as long as |
| 686 | // all exiting branches from all inner loops have impact on the outer loops, |
| 687 | // and if something changes inside them then any of outer loops may also |
| 688 | // change. When we forget outermost loop, we also forget all contained loops |
| 689 | // and this is what we need here. |
| 690 | // 如果存在SE，并且ULO要求忘记所有SCEV（循环不变代码退出值）信息， |
| 691 | // 则调用forgetAllLoops()方法来忘记所有循环的信息。否则，只忘记最顶层的循环L。 |
| 692 | if (SE) { |
| 693 | if (ULO.ForgetAllSCEV) |
| 694 | SE->forgetAllLoops(); |
| 695 | else |
| 696 | SE->forgetTopmostLoop(L); |
| 697 | } |
| 698 | // 如果LatchIsExiting为假，表示循环的出口不是Latch块， |
| 699 | // 那么增加未展开的循环计数器NumUnrolledNotLatch。 |
| 700 | if (!LatchIsExiting) |
| 701 | ++NumUnrolledNotLatch; |
| 702 | // 定义一个可选布尔值ContinueOnTrue，初始为无值。 |
| 703 | Optional<bool> ContinueOnTrue = None; |
| 704 | // 如果存在ExitingBI（可能代表退出基本块），则定义LoopExit为ExitingBI的第一个后继块。 |
| 705 | // 同时，根据ContinueOnTrue的值，确定是否继续循环。 |
| 706 | BasicBlock *LoopExit = nullptr; |
| 707 | if (ExitingBI) { |
| 708 | ContinueOnTrue = L->contains(ExitingBI->getSuccessor(0)); |
| 709 | LoopExit = ExitingBI->getSuccessor(*ContinueOnTrue); |
| 710 | } |
| 711 | // For the first iteration of the loop, we should use the precloned values for |
| 712 | // PHI nodes.  Insert associations now. |
| 713 | ValueToValueMapTy LastValueMap; |
| 714 | std::vector<PHINode*> OrigPHINode; |
| 715 | for (BasicBlock::iterator I = Header->begin(); isa<PHINode>(I); ++I) { |
| 716 | OrigPHINode.push_back(cast<PHINode>(I)); |
| 717 | } |
| 718 | // 初始化一些向量，用于存储循环的头部、退出块、退出后继块和Latch块。 |
| 719 | std::vector<BasicBlock *> Headers; |
| 720 | std::vector<BasicBlock *> ExitingBlocks; |
| 721 | std::vector<BasicBlock *> ExitingSucc; |
| 722 | std::vector<BasicBlock *> Latches; |
| 723 | Headers.push_back(Header); |
| 724 | Latches.push_back(LatchBlock); |
| 725 | if (ExitingBI) { |
| 726 | ExitingBlocks.push_back(ExitingBI->getParent()); |
| 727 | ExitingSucc.push_back(ExitingBI->getSuccessor(!(*ContinueOnTrue))); |
| 728 | } |
| 729 | // The current on-the-fly SSA update requires blocks to be processed in |
| 730 | // reverse postorder so that LastValueMap contains the correct value at each |
| 731 | // exit. |
| 732 | // 使用LoopBlocksDFS对象DFS来对循环L进行深度优先搜索，并执行搜索。 |
| 733 | LoopBlocksDFS DFS(L); |
| 734 | DFS.perform(LI); |
| 735 | // Stash the DFS iterators before adding blocks to the loop. |
| 736 | // 在添加新块到循环之前，保存DFS的迭代器。 |
| 737 | LoopBlocksDFS::RPOIterator BlockBegin = DFS.beginRPO(); |
| 738 | LoopBlocksDFS::RPOIterator BlockEnd = DFS.endRPO(); |
| 739 | // 获取循环L中的所有基本块。 |
| 740 | std::vector<BasicBlock*> UnrolledLoopBlocks = L->getBlocks(); |
| 741 | // Loop Unrolling might create new loops. While we do preserve LoopInfo, we |
| 742 | // might break loop-simplified form for these loops (as they, e.g., would |
| 743 | // share the same exit blocks). We'll keep track of loops for which we can |
| 744 | // break this so that later we can re-simplify them. |
| 745 | // 循环展开可能会创建新的循环。我们虽然保留了LoopInfo，但可能会破坏这些新循环的简化形式。 |
| 746 | // 我们将跟踪这些循环，以便稍后重新简化它们。 |
| 747 | SmallSetVector<Loop *, 4> LoopsToSimplify; |
| 748 | for (Loop *SubLoop : *L) |
| 749 | LoopsToSimplify.insert(SubLoop); |
| 750 | // 如果当前循环所在的父级基本块是用于性能分析的调试信息，则对每个指令进行处理。 |
| 751 | if (Header->getParent()->isDebugInfoForProfiling()) |
| 752 | for (BasicBlock *BB : L->getBlocks()) |
| 753 | for (Instruction &I : *BB) |
| 754 | if (!isa<DbgInfoIntrinsic>(&I)) |
| 755 | if (const DILocation *DIL = I.getDebugLoc()) { |
| 756 | auto NewDIL = DIL->cloneByMultiplyingDuplicationFactor(ULO.Count); |
| 757 | if (NewDIL) |
| 758 | I.setDebugLoc(NewDIL.getValue()); |
| 759 | else |
| 760 | LLVM_DEBUG(dbgs() |
| 761 | << "Failed to create new discriminator: " |
| 762 | << DIL->getFilename() << " Line: " << DIL->getLine()); |
| 763 | } |
| 764 | // 循环展开的主体部分，迭代ULO.Count次，每次迭代都会创建新的块副本。 |
| 765 | for (unsigned It = 1; It != ULO.Count; ++It) { |
| 766 | SmallVector<BasicBlock *, 8> NewBlocks; |
| 767 | SmallDenseMap<const Loop *, Loop *, 4> NewLoops; |
| 768 | NewLoops[L] = L; |
| 769 | // 遍历DFS迭代器，克隆每个基本块，并使用ValueToValueMapTy来映射原始值到新值。 |
| 770 | for (LoopBlocksDFS::RPOIterator BB = BlockBegin; BB != BlockEnd; ++BB) { |
| 771 | ValueToValueMapTy VMap; |
| 772 | BasicBlock *New = CloneBasicBlock(*BB, VMap, "." + Twine(It)); |
| 773 | Header->getParent()->getBasicBlockList().push_back(New); |
| 774 | // 断言确保Header块不是子循环的头部，并且它属于当前循环L。 |
| 775 | assert((*BB != Header || LI->getLoopFor(*BB) == L) && |
| 776 | "Header should not be in a sub-loop"); |
| 777 | // Tell LI about New. |
| 778 | // 将克隆的块添加到LoopInfo中，并更新NewLoops映射。 |
| 779 | const Loop *OldLoop = addClonedBlockToLoopInfo(*BB, New, LI, NewLoops); |
| 780 | if (OldLoop) |
| 781 | LoopsToSimplify.insert(NewLoops[OldLoop]); |
| 782 | // 如果当前块是循环的头部，更新PHI节点以使用来自上一个块的值。 |
| 783 | if (*BB == Header) |
| 784 | // Loop over all of the PHI nodes in the block, changing them to use |
| 785 | // the incoming values from the previous block. |
| 786 | for (PHINode *OrigPHI : OrigPHINode) { |
| 787 | PHINode *NewPHI = cast<PHINode>(VMap[OrigPHI]); |
| 788 | Value *InVal = NewPHI->getIncomingValueForBlock(LatchBlock); |
| 789 | if (Instruction *InValI = dyn_cast<Instruction>(InVal)) |
| 790 | if (It > 1 && L->contains(InValI)) |
| 791 | InVal = LastValueMap[InValI]; |
| 792 | VMap[OrigPHI] = InVal; |
| 793 | New->getInstList().erase(NewPHI); |
| 794 | } |
| 795 | // Update our running map of newest clones |
| 796 | // 更新LastValueMap，使其包含最新的克隆块。 |
| 797 | LastValueMap[*BB] = New; |
| 798 | for (ValueToValueMapTy::iterator VI = VMap.begin(), VE = VMap.end(); |
| 799 | VI != VE; ++VI) |
| 800 | LastValueMap[VI->first] = VI->second; |
| 801 | // Add phi entries for newly created values to all exit blocks. |
| 802 | // 为所有退出块添加新的PHI入口。 |
| 803 | for (BasicBlock *Succ : successors(*BB)) { |
| 804 | if (L->contains(Succ)) |
| 805 | continue; |
| 806 | for (PHINode &PHI : Succ->phis()) { |
| 807 | Value *Incoming = PHI.getIncomingValueForBlock(*BB); |
| 808 | ValueToValueMapTy::iterator It = LastValueMap.find(Incoming); |
| 809 | if (It != LastValueMap.end()) |
| 810 | Incoming = It->second; |
| 811 | PHI.addIncoming(Incoming, New); |
| 812 | } |
| 813 | } |
| 814 | // Keep track of new headers and latches as we create them, so that |
| 815 | // we can insert the proper branches later. |
| 816 | // 更新新头部和Latch块的列表。 |
| 817 | if (*BB == Header) |
| 818 | Headers.push_back(New); |
| 819 | if (*BB == LatchBlock) |
| 820 | Latches.push_back(New); |
| 821 | // Keep track of the exiting block and its successor block contained in |
| 822 | // the loop for the current iteration. |
| 823 | // 更新当前迭代中包含在循环内的退出块及其后继块的列表。 |
| 824 | if (ExitingBI) { |
| 825 | if (*BB == ExitingBlocks[0]) |
| 826 | ExitingBlocks.push_back(New); |
| 827 | if (*BB == ExitingSucc[0]) |
| 828 | ExitingSucc.push_back(New); |
| 829 | } |
| 830 | // 将新创建的块添加到NewBlocks列表中， |
| 831 | // 并将其添加到循环的所有块列表UnrolledLoopBlocks中。 |
| 832 | NewBlocks.push_back(New); |
| 833 | UnrolledLoopBlocks.push_back(New); |
| 834 | // Update DomTree: since we just copy the loop body, and each copy has a |
| 835 | // dedicated entry block (copy of the header block), this header's copy |
| 836 | // dominates all copied blocks. That means, dominance relations in the |
| 837 | // copied body are the same as in the original body. |
| 838 | // 更新支配树（DomTree）。由于我们刚刚复制了循环体，每个复制都有一个专门的入口块（头部块的副本）， |
| 839 | // 这个头部的副本支配所有复制的块。这意味着，复制体中的支配关系与原始体中的相同。 |
| 840 | if (DT) { |
| 841 | if (*BB == Header) |
| 842 | DT->addNewBlock(New, Latches[It - 1]); |
| 843 | else { |
| 844 | auto BBDomNode = DT->getNode(*BB); |
| 845 | auto BBIDom = BBDomNode->getIDom(); |
| 846 | BasicBlock *OriginalBBIDom = BBIDom->getBlock(); |
| 847 | DT->addNewBlock( |
| 848 | New, cast<BasicBlock>(LastValueMap[cast<Value>(OriginalBBIDom)])); |
| 849 | } |
| 850 | } |
| 851 | } |
| 852 | // Remap all instructions in the most recent iteration |
| 853 | // 重映射最新迭代中的所有指令。 |
| 854 | remapInstructionsInBlocks(NewBlocks, LastValueMap); |
| 855 | for (BasicBlock *NewBlock : NewBlocks) { |
| 856 | for (Instruction &I : *NewBlock) { |
| 857 | if (auto *II = dyn_cast<IntrinsicInst>(&I)) |
| 858 | if (II->getIntrinsicID() == Intrinsic::assume) |
| 859 | AC->registerAssumption(II); |
| 860 | } |
| 861 | } |
| 862 | } |
| 863 | // Loop over the PHI nodes in the original block, setting incoming values. |
| 864 | // 遍历原始块中的PHI节点，设置传入值。 |
| 865 | for (PHINode *PN : OrigPHINode) { |
| 866 | if (CompletelyUnroll) { |
| 867 | // 如果完全展开循环，则用PHI节点的预头部块的传入值替换所有使用，并从头部块中删除PHI节点。 |
| 868 | PN->replaceAllUsesWith(PN->getIncomingValueForBlock(Preheader)); |
| 869 | Header->getInstList().erase(PN); |
| 870 | } else if (ULO.Count > 1) { |
| 871 | // 如果不是完全展开，并且ULO.Count大于1，则移除PHI节点的来自Latch块的传入值。 |
| 872 | Value *InVal = PN->removeIncomingValue(LatchBlock, false); |
| 873 | // If this value was defined in the loop, take the value defined by the |
| 874 | // last iteration of the loop. |
| 875 | // 如果这个值在循环中定义，取最后一次迭代的值。 |
| 876 | if (Instruction *InValI = dyn_cast<Instruction>(InVal)) { |
| 877 | if (L->contains(InValI)) |
| 878 | InVal = LastValueMap[InVal]; |
| 879 | } |
| 880 | // 断言最后一个Latch块是LastValueMap中Latch块的映射，并且添加新的传入值到PHI节点。 |
| 881 | assert(Latches.back() == LastValueMap[LatchBlock] && "bad last latch"); |
| 882 | PN->addIncoming(InVal, Latches.back()); |
| 883 | } |
| 884 | } |
| 885 | // 定义一个lambda函数setDest，用于设置源块Src到目标块Dest的分支，以及是否需要条件分支。 |
| 886 | auto setDest = [](BasicBlock *Src, BasicBlock *Dest, BasicBlock *BlockInLoop, |
| 887 | bool NeedConditional, Optional<bool> ContinueOnTrue, |
| 888 | bool IsDestLoopExit) { |
| 889 | // 转换Src块的终结者为分支指令。 |
| 890 | auto *Term = cast<BranchInst>(Src->getTerminator()); |
| 891 | if (NeedConditional) { |
| 892 | // Update the conditional branch's successor for the following |
| 893 | // iteration. |
| 894 | // 如果需要条件分支，更新条件分支的后继块为下一次迭代。 |
| 895 | assert(ContinueOnTrue.hasValue() && |
| 896 | "Expecting valid ContinueOnTrue when NeedConditional is true"); |
| 897 | Term->setSuccessor(!(*ContinueOnTrue), Dest); |
| 898 | } else { |
| 899 | // Remove phi operands at this loop exit |
| 900 | // 如果不需要条件分支，移除phi操作数在这个循环出口。 |
| 901 | if (!IsDestLoopExit) { |
| 902 | BasicBlock *BB = Src; |
| 903 | for (BasicBlock *Succ : successors(BB)) { |
| 904 | // Preserve the incoming value from BB if we are jumping to the block |
| 905 | // in the current loop. |
| 906 | // 保留从BB来的传入值，如果我们跳转到当前循环迭代中的块。 |
| 907 | if (Succ == BlockInLoop) |
| 908 | continue; |
| 909 | for (PHINode &Phi : Succ->phis()) |
| 910 | Phi.removeIncomingValue(BB, false); |
| 911 | } |
| 912 | } |
| 913 | // Replace the conditional branch with an unconditional one. |
| 914 | // 用无条件分支替换条件分支。 |
| 915 | BranchInst::Create(Dest, Term); |
| 916 | Term->eraseFromParent(); |
| 917 | } |
| 918 | }; |
| 919 | // Connect latches of the unrolled iterations to the headers of the next |
| 920 | // iteration. If the latch is also the exiting block, the conditional branch |
| 921 | // may have to be preserved. |
| 922 | // 连接展开迭代的Latch块到下一个迭代的头部。如果Latch块也是退出块，条件分支可能需要保留。 |
| 923 | for (unsigned i = 0, e = Latches.size(); i != e; ++i) { |
| 924 | // The branch destination. |
| 925 | // 确定分支目的地。 |
| 926 | unsigned j = (i + 1) % e; |
| 927 | BasicBlock *Dest = Headers[j]; |
| 928 | bool NeedConditional = LatchIsExiting; |
| 929 | if (LatchIsExiting) { |
| 930 | // 如果Latch是退出块，根据运行时迭代次数和ULO设置确定是否需要条件分支。 |
| 931 | if (RuntimeTripCount && j != 0) |
| 932 | NeedConditional = false; |
| 933 | // For a complete unroll, make the last iteration end with a branch |
| 934 | // to the exit block. |
| 935 | // 如果完全展开循环，对于最后一次迭代，分支到退出块。 |
| 936 | if (CompletelyUnroll) { |
| 937 | if (j == 0) |
| 938 | Dest = LoopExit; |
| 939 | // If using trip count upper bound to completely unroll, we need to |
| 940 | // keep the conditional branch except the last one because the loop |
| 941 | // may exit after any iteration. |
| 942 | // 如果使用trip count upper bound来完全展开，我们需要保留条件分支，除了最后一个迭代。 |
| 943 | assert(NeedConditional && |
| 944 | "NeedCondition cannot be modified by both complete " |
| 945 | "unrolling and runtime unrolling"); |
| 946 | NeedConditional = |
| 947 | (ULO.PreserveCondBr && j && !(ULO.PreserveOnlyFirst && i != 0)); |
| 948 | // 如果我们知道循环的迭代次数或其倍数，我们可以安全地使用无条件分支。 |
| 949 | } else if (j != BreakoutTrip && |
| 950 | (ULO.TripMultiple == 0 || j % ULO.TripMultiple != 0)) { |
| 951 | // If we know the trip count or a multiple of it, we can safely use an |
| 952 | // unconditional branch for some iterations. |
| 953 | NeedConditional = false; |
| 954 | } |
| 955 | } |
| 956 | // 使用setDest函数设置从Latch[i]到Dest的分支，以及是否需要条件分支，ContinueOnTrue的值， |
| 957 | // 以及是否是目的地循环退出块。 |
| 958 | setDest(Latches[i], Dest, Headers[i], NeedConditional, ContinueOnTrue, |
| 959 | Dest == LoopExit); |
|  | } |
|  | // 如果Latch不是退出块，我们可能能够简化展开的退出块中的条件分支。 |
|  | if (!LatchIsExiting) { |
|  | // If the latch is not exiting, we may be able to simplify the conditional |
|  | // branches in the unrolled exiting blocks. |
|  | for (unsigned i = 0, e = ExitingBlocks.size(); i != e; ++i) { |
|  | // The branch destination. |
|  | // 确定分支目的地。 |
|  | unsigned j = (i + 1) % e; |
|  | bool NeedConditional = true; |
|  | // 如果有运行时迭代次数，或者不是第一次迭代，则不需要条件分支。 |
|  | if (RuntimeTripCount && j != 0) |
|  | NeedConditional = false; |
|  | if (CompletelyUnroll) |
|  | // We cannot drop the conditional branch for the last condition, as we |
|  | // may have to execute the loop body depending on the condition. |
|  | // 如果完全展开循环，我们不能为最后一个条件去掉条件分支，因为可能需要根据条件执行循环体。 |
|  | NeedConditional = j == 0 || ULO.PreserveCondBr; |
|  | else if (j != BreakoutTrip && |
|  | (ULO.TripMultiple == 0 || j % ULO.TripMultiple != 0)) |
|  | // If we know the trip count or a multiple of it, we can safely use an |
|  | // unconditional branch for some iterations. |
|  | // 如果我们知道循环的迭代次数或其倍数，我们可以安全地使用无条件分支。 |
|  | NeedConditional = false; |
|  | // Conditional branches from non-latch exiting block have successors |
|  | // either in the same loop iteration or outside the loop. The branches are |
|  | // already correct. |
|  | // 如果需要条件分支，则跳过。 |
|  | if (NeedConditional) |
|  | continue; |
|  | // 使用setDest函数设置从ExitingBlocks[i]到ExitingSucc[i]的分支，以及是否需要条件分支。 |
|  | setDest(ExitingBlocks[i], ExitingSucc[i], ExitingSucc[i], NeedConditional, |
|  | None, false); |
|  | } |
|  | // When completely unrolling, the last latch becomes unreachable. |
|  | // 如果完全展开循环，最后一次迭代的Latch变得无法到达。 |
|  | if (CompletelyUnroll) { |
|  | BranchInst *Term = cast<BranchInst>(Latches.back()->getTerminator()); |
|  | new UnreachableInst(Term->getContext(), Term); |
|  | Term->eraseFromParent(); |
|  | } |
|  | } |
|  | // Update dominators of blocks we might reach through exits. |
|  | // Immediate dominator of such block might change, because we add more |
|  | // routes which can lead to the exit: we can now reach it from the copied |
|  | // iterations too. |
|  | // 更新我们可能通过退出点到达的块的支配者。 |
|  | // 这样的块的直接支配者可能发生变化，因为我们添加了更多可以到达退出点的路径： |
|  | // 我们现在可以从复制的迭代中到达它。 |
|  | if (DT && ULO.Count > 1) { |
|  | for (auto *BB : OriginalLoopBlocks) { |
|  | auto *BBDomNode = DT->getNode(BB); |
|  | SmallVector<BasicBlock *, 16> ChildrenToUpdate; |
|  | for (auto *ChildDomNode : BBDomNode->children()) { |
|  | auto *ChildBB = ChildDomNode->getBlock(); |
|  | if (!L->contains(ChildBB)) |
|  | ChildrenToUpdate.push_back(ChildBB); |
|  | } |
|  | BasicBlock *NewIDom; |
|  | if (ExitingBI && BB == ExitingBlocks[0]) { |
|  | // The latch is special because we emit unconditional branches in |
|  | // some cases where the original loop contained a conditional branch. |
|  | // Since the latch is always at the bottom of the loop, if the latch |
|  | // dominated an exit before unrolling, the new dominator of that exit |
|  | // must also be a latch.  Specifically, the dominator is the first |
|  | // latch which ends in a conditional branch, or the last latch if |
|  | // there is no such latch. |
|  | // For loops exiting from non latch exiting block, we limit the |
|  | // branch simplification to single exiting block loops. |
|  | // 如果Latch是特殊的，因为在某些情况下，原始循环包含条件分支，我们发出了无条件分支。 |
|  | // 由于Latch总是在循环的底部，如果Latch在展开前支配了一个退出点， |
|  | // 那么该退出点的新支配者 |
|  | // 也必须是Latch。具体来说，新的支配者是第一个以条件分支结束的Latch， |
|  | // 或者如果没有这样的Latch，则是最后一个Latch。 |
|  | // 对于从非Latch退出块退出的循环，我们将分支简化限制在单个退出块循环中。 |
|  | NewIDom = ExitingBlocks.back(); |
|  | for (unsigned i = 0, e = ExitingBlocks.size(); i != e; ++i) { |
|  | Instruction *Term = ExitingBlocks[i]->getTerminator(); |
|  | if (isa<BranchInst>(Term) && cast<BranchInst>(Term)->isConditional()) { |
|  | NewIDom = |
|  | DT->findNearestCommonDominator(ExitingBlocks[i], Latches[i]); |
|  | break; |
|  | } |
|  | } |
|  | } else { |
|  | // The new idom of the block will be the nearest common dominator |
|  | // of all copies of the previous idom. This is equivalent to the |
|  | // nearest common dominator of the previous idom and the first latch, |
|  | // which dominates all copies of the previous idom. |
|  | // 新的支配者将是所有副本的前一个支配者的最近公共支配者。 |
|  | // 这等价于前一个支配者和第一个Latch的最近公共支配者，这支配了所有副本的前一个支配者。 |
|  | NewIDom = DT->findNearestCommonDominator(BB, LatchBlock); |
|  | } |
|  | for (auto *ChildBB : ChildrenToUpdate) |
|  | DT->changeImmediateDominator(ChildBB, NewIDom); |
|  | } |
|  | } |
|  | // 断言支配树的验证。 |
|  | assert(!DT || !UnrollVerifyDomtree || |
|  | DT->verify(DominatorTree::VerificationLevel::Fast)); |
|  | // 创建DomTreeUpdater对象DTU，使用延迟更新策略。 |
|  | DomTreeUpdater DTU(DT, DomTreeUpdater::UpdateStrategy::Lazy); |
|  | // Merge adjacent basic blocks, if possible. |
|  | // 如果可能，合并相邻的基本块。 |
|  | for (BasicBlock *Latch : Latches) { |
|  | BranchInst *Term = dyn_cast<BranchInst>(Latch->getTerminator()); |
|  | // 断言终止者为分支指令，除非完全展开且Latch不是退出块，且Latch是最后一个。 |
|  | assert((Term || |
|  | (CompletelyUnroll && !LatchIsExiting && Latch == Latches.back())) && |
|  | "Need a branch as terminator, except when fully unrolling with " |
|  | "unconditional latch"); |
|  | if (Term && Term->isUnconditional()) { |
|  | BasicBlock *Dest = Term->getSuccessor(0); |
|  | BasicBlock *Fold = Dest->getUniquePredecessor(); |
|  | if (MergeBlockIntoPredecessor(Dest, &DTU, LI)) { |
|  | // Dest has been folded into Fold. Update our worklists accordingly. |
|  | // 如果Dest已经被合并到其前驱Fold中，更新我们的工作列表。 |
|  | std::replace(Latches.begin(), Latches.end(), Dest, Fold); |
|  | UnrolledLoopBlocks.erase(std::remove(UnrolledLoopBlocks.begin(), |
|  | UnrolledLoopBlocks.end(), Dest), |
|  | UnrolledLoopBlocks.end()); |
|  | } |
|  | } |
|  | } |
|  | // Apply updates to the DomTree. |
|  | // 应用更新到支配树。 |
|  | DT = &DTU.getDomTree(); |
|  | // At this point, the code is well formed.  We now simplify the unrolled loop, |
|  | // doing constant propagation and dead code elimination as we go. |
|  | // 此时，代码已经形成良好。我们现在简化展开后的循环，同时进行常量传播和死代码消除。 |
|  | simplifyLoopAfterUnroll(L, !CompletelyUnroll && (ULO.Count > 1 || Peeled), LI, |
|  | SE, DT, AC, TTI); |
|  | // 增加完全展开的循环计数器。 |
|  | NumCompletelyUnrolled += CompletelyUnroll; |
|  | // 增加展开循环的计数器。 |
|  | ++NumUnrolled; |
|  | // 获取循环L的外层循环OuterL。 |
|  | Loop *OuterL = L->getParentLoop(); |
|  | // Update LoopInfo if the loop is completely removed. |
|  | // 如果循环完全展开，则更新LoopInfo。 |
|  | if (CompletelyUnroll) |
|  | LI->erase(L); |
|  | // After complete unrolling most of the blocks should be contained in OuterL. |
|  | // However, some of them might happen to be out of OuterL (e.g. if they |
|  | // precede a loop exit). In this case we might need to insert PHI nodes in |
|  | // order to preserve LCSSA form. |
|  | // We don't need to check this if we already know that we need to fix LCSSA |
|  | // form. |
|  | // TODO: For now we just recompute LCSSA for the outer loop in this case, but |
|  | // it should be possible to fix it in-place. |
|  | // 展开后，大多数块应该包含在OuterL中。然而，有些块可能不在OuterL中（例如，如果它们在循环退出前）。 |
|  | // 在这种情况下，我们可能需要插入PHI节点以保持LC SSA形式。 |
|  | // 如果我们已经知道需要修复LC SSA形式，我们就不需要检查这个。 |
|  | // TODO: 目前我们只是重新计算外层循环的LC SSA，但实际上我们应该能够就地修复它。 |
|  | if (PreserveLCSSA && OuterL && CompletelyUnroll && !NeedToFixLCSSA) |
|  | NeedToFixLCSSA |= ::needToInsertPhisForLCSSA(OuterL, UnrolledLoopBlocks, LI); |
|  | // If we have a pass and a DominatorTree we should re-simplify impacted loops |
|  | // to ensure subsequent analyses can rely on this form. We want to simplify |
|  | // at least one layer outside of the loop that was unrolled so that any |
|  | // changes to the parent loop exposed by the unrolling are considered. |
|  | // 如果我们有传递和支配树，我们应该重新简化受影响的循环，以确保后续分析可以依赖这种形式。 |
|  | // 我们想要至少简化展开循环的外层循环，这样通过展开循环暴露的任何对父循环的更改都会被考虑。 |
|  | if (DT) { |
|  | if (OuterL) { |
|  | // OuterL includes all loops for which we can break loop-simplify, so |
|  | // it's sufficient to simplify only it (it'll recursively simplify inner |
|  | // loops too). |
|  | // 如果OuterL包含所有我们可以打破循环简化的循环，那么我们只需要简化它（它会递归地简化内部循环）。 |
|  | if (NeedToFixLCSSA) { |
|  | // LCSSA must be performed on the outermost affected loop. The unrolled |
|  | // loop's last loop latch is guaranteed to be in the outermost loop |
|  | // after LoopInfo's been updated by LoopInfo::erase. |
|  | // LCSSA必须在最外层受影响的循环上执行。展开循环的最后一个循环Latch保证在LoopInfo |
|  | // 更新后在最外层循环中。 |
|  | Loop *LatchLoop = LI->getLoopFor(Latches.back()); |
|  | Loop *FixLCSSALoop = OuterL; |
|  | if (!FixLCSSALoop->contains(LatchLoop)) |
|  | while (FixLCSSALoop->getParentLoop() != LatchLoop) |
|  | FixLCSSALoop = FixLCSSALoop->getParentLoop(); |
|  | // 递归地形成LC SSA。 |
|  | formLCSSARecursively(*FixLCSSALoop, *DT, LI, SE); |
|  | } else if (PreserveLCSSA) { |
|  | // 断言OuterL在DT中的循环应该是LC SSA形式。 |
|  | assert(OuterL->isLCSSAForm(*DT) && |
|  | "Loops should be in LCSSA form after loop-unroll."); |
|  | } |
|  | // TODO: That potentially might be compile-time expensive. We should try |
|  | // to fix the loop-simplified form incrementally. |
|  | // TODO: 这可能会很耗时。我们应该尝试增量地修复循环简化形式。 |
|  | simplifyLoop(OuterL, DT, LI, SE, AC, nullptr, PreserveLCSSA); |
|  | } else { |
|  | // Simplify loops for which we might've broken loop-simplify form. |
|  | // 简化我们可能已经破坏了循环简化形式的循环。 |
|  | for (Loop *SubLoop : LoopsToSimplify) |
|  | simplifyLoop(SubLoop, DT, LI, SE, AC, nullptr, PreserveLCSSA); |
|  | } |
|  | } |
|  | // 返回展开结果，如果完全展开则返回FullyUnrolled，否则返回PartiallyUnrolled。 |
|  | return CompletelyUnroll ? LoopUnrollResult::FullyUnrolled |
|  | : LoopUnrollResult::PartiallyUnrolled; |
|  | } |
|  | /// GetUnrollMetadata用于从LLVM循环（llvm.loop）的元数据节点中 |
|  | /// 获取特定的循环展开提示元数据节点。这些元数据节点可能包含循环展开的相关信息， |
|  | /// 例如展开的次数。函数的目的是查找并返回与指定名称 |
|  | /// （例如"llvm.loop.unroll.count"）相关联的元数据节点。 |
|  | /// 如果找不到这样的节点，则返回nullptr。 |
|  | /// Given an llvm.loop loop id metadata node, returns the loop hint metadata |
|  | /// node with the given name (for example, "llvm.loop.unroll.count"). If no |
|  | /// such metadata node exists, then nullptr is returned. |
|  | /// 给定一个LLVM循环（llvm.loop）的循环ID元数据节点， |
|  | /// 返回具有给定名称（例如，“llvm.loop.unroll.count”）的循环提示元数据节点。 |
|  | /// 如果不存在这样的元数据节点，则返回nullptr。 |
|  | MDNode *llvm::GetUnrollMetadata(MDNode *LoopID, StringRef Name) { |
|  | // First operand should refer to the loop id itself. |
|  | // 循环ID的第一个操作数应该引用循环ID本身。 |
|  | assert(LoopID->getNumOperands() > 0 && "requires at least one operand"); |
|  | // 断言循环ID的第一个操作数是循环ID本身，这是一个验证步骤，确保我们有一个有效的循环ID元数据节点。 |
|  | assert(LoopID->getOperand(0) == LoopID && "invalid loop id"); |
|  | // 遍历循环ID元数据节点的所有操作数，从第二个操作数开始，因为第一个操作数是循环ID本身。 |
|  | for (unsigned i = 1, e = LoopID->getNumOperands(); i < e; ++i) { |
|  | // 尝试将当前操作数转换为MDNode类型。 |
|  | MDNode *MD = dyn_cast<MDNode>(LoopID->getOperand(i)); |
|  | if (!MD) |
|  | continue; // 如果转换失败，跳过当前操作数。 |
|  | // 尝试将MDNode的第一个操作数转换为MDString类型。 |
|  | MDString *S = dyn_cast<MDString>(MD->getOperand(0)); |
|  | if (!S) |
|  | continue; // 如果转换失败，跳过当前MDNode。 |
|  | // 检查当前MDString的字符串内容是否与我们要查找的名称Name匹配。 |
|  | if (Name.equals(S->getString())) |
|  | return MD; // 如果找到匹配的元数据节点，返回它。 |
|  | } |
|  | // 如果遍历完所有操作数都没有找到匹配的元数据节点，返回nullptr。 |
|  | return nullptr; |
|  | } |

![图](images/image_0070.png)

