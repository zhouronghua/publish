# 10 多分支分析

本章是系列文章的第十章，主要介绍 GPU 的编译原理，以及多核运行过程中内存分岔与控制流分岔的分析与处理。

## 10.1 什么是GPU

### 10.1.1 GPU的发展历史

软件控制的VGA帧缓冲区

频繁使用的图形栅格化程序

尝试用硬件来加速这些处理

流水线化的图形处理过程，例如变形，映射，切片，显示等等

工程师开始发现一些过程本身虽然不一样，但实现该功能的硬件是相似的，例如图着色

从单独的图着色API到泛化的API

GPU指令集的诞生→泛化的整数通用处理函数 → 带分支支持的处理函数

一个独立的栅格化处理芯片 + 通用处理芯片

然后栅格化处理芯片又集成到了GPU里面，变成通用处理芯片的一部分

……

### 10.1.2 计算机组织

传统的SIMD（Single Instruction Multiple Data）和SPMD（Single Program Multiple Data）到GPU的MSIMD（Multiple Single-Instruction Multiple-Data）

### 10.1.3 编程环境

主流编程环境主要有两种，开源的OpenCL和闭源的C for CUDA，后者是NVIDIA发布的，前者是其他公司组成的联盟发布的。这里主要说CUDA。

异构编程语言：一个能指定不同异构处理器上执行的编程语言。

传统的C编程语言做矩阵操作的例子：

![图](images/image_0144.gif)

1 void saxpy_serial(int n, float alpha, float *x, float *y) {

2   for (int i = 0; i < n; i++)

3     y[i] = alpha * x[i] + y[i];

4 }

5 // Invoke the serial function:

6 saxpy_serial(n, 2.0, x, y);

![图](images/image_0145.gif)

转换成CUDA的例子：

![图](images/image_0146.gif)

1 __global__ void saxpy_parallel(int n, float alpha, float *x, float *y) {

2   int i = blockIdx.x * blockDim.x + threadIdx.x;

3   if (i < n)

4     y[i] = alpha * x[i] + y[i];

5 }

6 // Invoke the parallel kernel:

7 int nblocks = (n + 255) / 256;

8 saxpy_parallel<<<nblocks, 256>>>(n, 2.0, x, y);

![图](images/image_0147.gif)

NV的GPU的组织结构：Grids → Blocks → Warps → Threads

![图](images/image_0148.png)

Cuda programs = CPU programs + kernels

Kernels调用语法：

kernel<<<dGrd, dBck>>>(A,B,w,C);
指定grids和block

CPU programs → host programs

kernels → PTX (Parallel Thread Execution) → SASS (Streaming ASSembly)

## 10.2 分岔（Divergence）

英文里面Divergence有分岔，分支，歧义等多种意思，这里表示程序执行到某个点之后，可能有多个分支的情况。

### 10.2.1 SIMD的优缺点

优点：更低的功耗，指令解码占用空间更少。

对于没有分支的线性程序，SIMD的性能非常好。但程序几乎不可避免会存在多个分支。

常见的分支主要有两类：

因为内存访问地址不一致导致的内存分岔

因为控制流分支导致的分岔

### 10.2.2 控制流分岔的例子

对下面的cuda 代码：

![图](images/image_0149.gif)

1 __global__ void ex(float *v) {

2   if (v[tid] < 0.0) {

3     v[tid] /= 2;

4   } else {

5     v[tid] = 0.0;

6   }

7 }

![图](images/image_0150.gif)

对应的控制流图是这样的：

![图](images/image_0151.png)

因为上面程序只有一处分岔（还记得上一章ILP中说的超级块么？上面的DAG转换成树之后只有2个叶子节点），如果有两个ALU，我们就可以在无视分岔的情况下把程序执行流水线画出来：

![图](images/image_0152.png)

### 10.2.3 什么样的输入性能最好？

对下面的cuda的例子，怎么样调整输入来达到最好的性能？

![图](images/image_0153.gif)

1 __global__ void dec2zero(int *v, int N) {

2   int xIndex = blockIdx.x * blockDim.x + threadIdx.x;

3   if (xIndex < N) {

4     while (v[xIndex] > 0) {

5       v[xIndex]--;

6     }

7   }

8 }

![图](images/image_0154.gif)

下面有五种初始化的方法：

![图](images/image_0155.gif)

1 void vecIncInit(int *data, int size) {

2   for (int i = 0; i < size; ++i) {

3     data[i] = size - i - 1;

4   }

5 }

6 void vecConsInit(int *data, int size) {

7   int cons = size / 2;

8   for (int i = 0; i < size; ++i) {

9     data[i] = cons;

10   }

11 }

12 void vecAltInit(int *data, int size) {

13   for (int i = 0; i < size; ++i) {

14     if (i % 2) {

15       data[i] = size;

16     }

17   }

18 }

19 void vecRandomInit(int *data, int size) {

20   for (int i = 0; i < size; ++i) {

21     data[i] = random() % size;

22   }

23 }

24 void vecHalfInit(int *data, int size) {

25   for (int i = 0; i < size / 2; ++i) {

26     data[i] = 0;

27   }

28   for (int i = size / 2; i < size; ++i) {

29     data[i] = size;

30   }

31 }

![图](images/image_0156.gif)

测试下来的结果，在总的执行近似的情况下，没有分岔和有一个分岔的性能是2倍的差异，正好印证了之前一个分岔需要2个ALU才能确保并行处理的观点。另外一个分岔的性能和另外触发了一个随机数生成器调用的性能接近：

|  | vecIncInit | vecConsInit | vecAltInit | vecRandomInit | vecHalfInit |
| --- | --- | --- | --- | --- | --- |
| 总时间 | 20480000 | 20480000 | 20476800 | 20294984 | 20480000 |
| 实际时间 | 16250 | 16153 | 32193 | 30210 | 16157 |

## 10.3 分岔的动态检测

### 10.3.1 分岔profiling

统计分岔执行时间和执行次数的方法

在并行世界，求程序的profile的过程远比单核世界复杂，因为需要一个算法找到那时正在运行的线程将这个profile的结果保存下来。

下面是常见的找记录者的算法：

![图](images/image_0157.gif)

1 int writer = 0;

2 bool gotWriter = false;

3 while (!gotWriter) {

4     bool iAmWriter = false;

5     if (laneid == writer) {

6       iAmWriter = true;

7     }

8   if ( ∃ t ∈ w | iAmWriter == true) {

9   gotWriter = true;

10   }

11   else {

12     writer++;

13   }

14 }

![图](images/image_0158.gif)

### 10.3.2 经典的双调排序Bitonic Sort

输入是乱序3/2/4/1，经过5次排序和4次交换之后，变成顺序的1/2/3/4

![图](images/image_0159.png)

双调排序的cuda代码如下：

![图](images/image_0160.gif)

1 __global__ static void bitonicSort(int *values) {

2   extern __shared__ int shared[];

3   const unsigned int tid = threadIdx.x;

4   shared[tid] = values[tid];

5   __syncthreads();

6   for (unsigned int k = 2; k <= NUM; k *= 2) {

7     for (unsigned int j = k / 2; j > 0; j /= 2) {

8       unsigned int ixj = tid ^ j;

9       if (ixj > tid) {

10         if ((tid & k) == 0) {

11           if (shared[tid] > shared[ixj]) {

12             swap(shared[tid], shared[ixj]);

13           }

14         } else {

15           if (shared[tid] < shared[ixj]) {

16             swap(shared[tid], shared[ixj]);

17           }

18         }

19       }

20       __syncthreads();

21     }

22   }

23   values[tid] = shared[tid];

24 }

![图](images/image_0161.gif)

我们先不看外面的for循环，针对核心的8到20行生成控制流图：

![图](images/image_0162.png)

如果对执行过程做一下trace，大概结果是这样（上面代码里面有4个if，所以转换成DAG之后就有4个分岔，对应执行时的4个线程）：

![图](images/image_0163.png)

第一轮优化，3个分岔变成2个：

![图](images/image_0164.gif)

1 unsigned int a, b;

2 if ((tid & k) == 0) {

3   b = tid;

4   a = ixj;

5 } else {

6   b = ixj;

7   a = tid;

8 }

9 if (sh[b] > sh[a]) {

10   swap(sh[b], sh[a]);

11 }

![图](images/image_0165.gif)

优化之后的控制流图变成这样（性能提升6.7%）：

![图](images/image_0166.png)

第二轮优化，2个分岔变成1个：

![图](images/image_0167.gif)

1 int p = (tid & k) == 0;

2 unsigned b = p ? tid : ixj;

3 unsigned a = p ? ixj : tid;

4 if (sh[b] > sh[a]) {

5   swap(sh[b], sh[a]);

6 }

![图](images/image_0168.gif)

实际上?表达式也是完成分岔的功能，但由于大多数指令集都有专门的问号表达式的指令，所以巧妙使用问号表达式将第一重分岔消掉，改进之后的CFG是这样的（性能提升9.2%）：

![图](images/image_0169.png)

### 10.3.3 总结

性能优化过程主要是消灭分岔，那前面提到的profile数据对这个性能优化有帮助么？

理论上不论profile数据是什么样的，能消灭的分岔肯定优先消灭掉。profile数据对分岔消除的提示是尽可能优先消除执行时间比较长，执行次数比较多的分岔。

抛开分岔问题本身，profile的数据会提示优化执行时间和执行次数比较多的BB。

## 10.4 分岔的静态检测

### 10.4.1 分岔变量和统一变量

分岔变量（Divergent Variables）：如果一个变量对不同线程会出现不同的值，则称该变量为分岔变量。

统一变量（Uniform Variables）：如果一个变量在不同线程呈现完全相同的值，则称该变量为统一变量。

成为分岔变量的几种场景：

tid是分岔变量

原子操作产生的变量是分岔变量

如果v对分岔变量有数据依赖，则v也是分岔变量

如果v对分岔变量有控制依赖，则v也是分岔变量

分岔变量在数据流图和控制流图上具有传播性。

### 10.4.2 找到依赖

在一个非SSA的程序里面，找到某个变量是分岔变量还是非分岔变量是有歧义的，因为一个变量被多次赋值，可能有些赋值生成统一变量，有些赋值生成分岔变量。

但在SSA格式程序中，变量的分岔属性值就要容易确定的多。

例如下面的例子中r2在未SSA化之前，可能是分岔变量，也可能是统一变量。右边SSA化之后，r2a和r2是分岔变量，r2b是统一变量。

![图](images/image_0170.png)

### 10.4.3 数据依赖图DDG

在ILP里面，我们曾经说过IDG，指令依赖图，这里说的数据依赖图和IDG其实也是类似的，关注的都是数据依赖，不过IDG关注的是指令执行过程的依赖，DDG关注的是数据本身的依赖。

对下面的CFG，会生成什么样的DDG？

![图](images/image_0171.png)

对应的DDG如下：

![图](images/image_0172.png)

这个数据依赖对ILP可能已经足够了，但对分岔分析还不够，有些分岔变量漏掉了！

例如j的值依赖B1里面的分支，这个分支的条件是个分岔变量，这也会导致j变成分岔变量。所以除了数据依赖外，还需要考虑控制依赖。

### 10.4.4 控制依赖图

影响区：一个分支断言的影响区是该断言影响的基本块的集合。

后支配：相对于支配属性而言，后支配属性是一个节点B2走到程序结束的每条路径都要经过B1，则称为B1后支配B2。

直接后支配：如果节点B1后支配节点B2，并且不存在一个节点B3，B1后支配B3，并且B3后支配B2，则称为B1是B2的直接后支配。

一个分支断言的影响区是该分支所在BB到分支的直接后支配BB。

为了方便表示控制依赖导致的后支配，我们将φ函数升级扩展成为带断言的φ函数。例如下图中的x本来只对x0和x1有数据依赖，现在它也对p2有数据依赖：

![图](images/image_0173.png)

升级φ函数之后的数据依赖图：

![图](images/image_0174.png)

## 10.5 分岔优化

### 10.5.1 同步栅栏删除

CUDA的ptx指令集默认分支命令都是会产生分岔的，除非特定加上.uni后缀：

![图](images/image_0175.png)

所以在明确肯定不会产生分岔变量的分支命令，可以加上.uni后缀：

![图](images/image_0176.png)

上面的截图来自PTX ISA :: CUDA Toolkit Documentation (nvidia.com)

### 10.5.2 寄存器分配

相对于传统单核的寄存器分配，溢出处理都是直接放到内存中，GPU场景下的寄存器溢出可以选择溢出老本地内存和全局内存，部分在多个核中共享的变量，还可以考虑放到共享内存中。

### 10.5.3 数据重定位

准排序算法

将数据切片，每个线程处理一个切片，并在每个切片排序完之后，再拷贝回来：

![图](images/image_0177.gif)

1 __global__ static void maxSort1(int *values, int N) {

2   // 1) COPY-INTO: Copy data from the values vector

3   // into  shared memory:

4   __shared__ int shared[THREAD_WORK_SIZE * NUM_THREADS];

5   for (unsigned k = 0; k < THREAD_WORK_SIZE; k++) {

6     unsigned loc = k * blockDim.x + threadIdx.x;

7     if (loc < N) {

8       shared[loc] = values[loc + blockIdx.x * blockDim.x];

9     }

10   }

11   __syncthreads();

12   // 2) SORT: each thread sorts its chunk of data

13   // with a  small sorting net.

14   int index1 = threadIdx.x * THREAD_WORK_SIZE;

15   int index2 = threadIdx.x * THREAD_WORK_SIZE + 1;

16   int index3 = threadIdx.x * THREAD_WORK_SIZE + 2;

17   int index4 = threadIdx.x * THREAD_WORK_SIZE + 3;

18   if (index4 < N) {

19     swapIfNecessary(shared, index1, index3);

20     swapIfNecessary(shared, index2, index4);

21     swapIfNecessary(shared, index1, index2);

22     swapIfNecessary(shared, index3, index4);

23     swapIfNecessary(shared, index2, index3);

24   }

25   __syncthreads();

26   // 3) SCATTER: the threads distribute their data

27   // along  the array.

28   __shared__ int scattered[THREAD_WORK_SIZE * 300];

29   unsigned int nextLoc = threadIdx.x;

30   for (unsigned i = 0; i < THREAD_WORK_SIZE; i++) {

31     scattered[nextLoc] = shared[threadIdx.x * THREAD_WORK_SIZE + i];

32     nextLoc += blockDim.x;

33   }

34   __syncthreads();

35   // 4) COPY-BACK: Copy the data back from the shared

36   // memory into the values vector:

37   for (unsigned k = 0; k < THREAD_WORK_SIZE; k++) {

38     unsigned loc = k * blockDim.x + threadIdx.x;

39     if (loc < N) {

40       values[loc + blockIdx.x * blockDim.x] = scattered[loc];

41     }

42   }

43 }

![图](images/image_0178.gif)

## 10.6 分岔研究历史

GPU的历史都比较新，所以关于GPU的分岔分析资料也比较新：

Ryoo, S. Rodrigues, C. Baghsorkhi, S. Stone, S. Kirk, D. and Hwu, Wen-Mei. "Optimization principles and application performance evaluation of a multithreaded GPU using CUDA", PPoPP, p 73-82 (2008) CUDA介绍。

Coutinho, B. Diogo, S. Pereira, F and Meira, W. "Divergence Analysis and Optimizations", PACT, p 320-329 (2011) 分岔分析与优化。

Sampaio, D. Martins, R. Collange, S. and Pereira, F. "Divergence Analysis", TOPLAS, 2013. 分岔分析。

## 10.7 LLVM的分岔优化实现

### 10.9.1 LLVM中的分岔分析

llvm\lib\Analysis\DivergenceAnalysis.cpp

llvm\lib\Analysis\DivergenceAnalysis.cpp

| 91 | #define DEBUG_TYPE "divergence-analysis" |
| --- | --- |
| 92 |  |
| 93 | // class DivergenceAnalysis |
|   | // DivergenceAnalysis类用于分析函数中的发散值。 |
| 94 | DivergenceAnalysis::DivergenceAnalysis( |
| 95 | const Function &F, const Loop *RegionLoop, const DominatorTree &DT, |
| 96 | const LoopInfo &LI, SyncDependenceAnalysis &SDA, bool IsLCSSAForm) |
| 97 | : F(F), RegionLoop(RegionLoop), DT(DT), LI(LI), SDA(SDA), |
| 98 | IsLCSSAForm(IsLCSSAForm) {} |
| 99 |  |
|   | // markDivergent - 标记一个值为发散值。 |
| 100 | void DivergenceAnalysis::markDivergent(const Value &DivVal) { |
|   | // 确保传入的值是Instruction或Argument类型 |
| 101 | assert(isa<Instruction>(DivVal) || isa<Argument>(DivVal)); |
|   | // 确保该值不是始终均匀的 |
| 102 | assert(!isAlwaysUniform(DivVal) && "cannot be a divergent"); |
|   | // 将该值插入到发散值集合中 |
| 103 | DivergentValues.insert(&DivVal); |
| 104 | } |
| 105 |  |
|   | // addUniformOverride - 添加一个值作为均匀值的覆盖。 |
| 106 | void DivergenceAnalysis::addUniformOverride(const Value &UniVal) { |
| 107 | UniformOverrides.insert(&UniVal); |
| 108 | } |
| 109 |  |
|   | // updateTerminator - 更新终结指令的发散状态。 |
| 110 | bool DivergenceAnalysis::updateTerminator(const Instruction &Term) const { |
|   | // 如果终结指令只有一个后继块，则不会导致发散 |
| 111 | if (Term.getNumSuccessors() <= 1) |
| 112 | return false; |
|   | // 处理条件分支指令 |
| 113 | if (auto *BranchTerm = dyn_cast<BranchInst>(&Term)) { |
| 114 | assert(BranchTerm->isConditional()); |
| 115 | return isDivergent(*BranchTerm->getCondition()); |
| 116 | } |
|   | // 处理Switch指令 |
| 117 | if (auto *SwitchTerm = dyn_cast<SwitchInst>(&Term)) { |
| 118 | return isDivergent(*SwitchTerm->getCondition()); |
| 119 | } |
|   | // 处理Invoke指令（忽略通过landingpad的异常执行） |
| 120 | if (isa<InvokeInst>(Term)) { |
| 121 | return false; // ignore abnormal executions through landingpad |
| 122 | } |
|   | // 其他终结指令不支持 |
| 123 |  |
| 124 | llvm_unreachable("unexpected terminator"); |
| 125 | } |
|   | // updateNormalInstruction - 更新普通指令的发散状态。 |
| 126 |  |
| 127 | bool DivergenceAnalysis::updateNormalInstruction(const Instruction &I) const { |
|   | // TODO: 处理具有副作用的函数调用等 |
|   | // 如果任何操作数是发散的，则该指令也是发散的 |
| 128 | // TODO function calls with side effects, etc |
| 129 | for (const auto &Op : I.operands()) { |
| 130 | if (isDivergent(*Op)) |
| 131 | return true; |
| 132 | } |
| 133 | return false; |
| 134 | } |
|   | // isTemporalDivergent - 检查一个值是否由于时间上的发散而发散。 |
| 135 |  |
| 136 | bool DivergenceAnalysis::isTemporalDivergent(const BasicBlock &ObservingBlock, |
| 137 | const Value &Val) const { |
| 138 | const auto *Inst = dyn_cast<const Instruction>(&Val); |
| 139 | if (!Inst) |
| 140 | return false; |
|   | // 检查任何携带Val的发散循环是否在控制流到达ObservingBlock之前终止 |
| 141 | // check whether any divergent loop carrying Val terminates before control |
| 142 | // proceeds to ObservingBlock |
| 143 | for (const auto *Loop = LI.getLoopFor(Inst->getParent()); |
| 144 | Loop != RegionLoop && !Loop->contains(&ObservingBlock); |
| 145 | Loop = Loop->getParentLoop()) { |
| 146 | if (DivergentLoops.find(Loop) != DivergentLoops.end()) |
| 147 | return true; |
| 148 | } |
| 149 |  |
| 150 | return false; |
| 151 | } |
|   | // updatePHINode - 更新PHI节点的发散状态。 |
| 152 |  |
| 153 | bool DivergenceAnalysis::updatePHINode(const PHINode &Phi) const { |
|   | // 如果Phi节点的父块是发散路径的汇合点，则Phi节点发散 |
| 154 | // joining divergent disjoint path in Phi parent block |
| 155 | if (!Phi.hasConstantOrUndefValue() && isJoinDivergent(*Phi.getParent())) { |
| 156 | return true; |
| 157 | } |
|   | // 一个输入值本身可能是发散的。 |
|   | // 或者，一个输入值在定义它的循环内可能是均匀的， |
|   | // 但从循环外部看可能是发散的。这种情况发生在发散循环退出时， |
|   | // 不同迭代中该均匀值的定义被丢弃。 |
| 158 |  |
| 159 | // |
|   | // 例如： |
|   | // for (int i = 0; i < n; ++i) { // 'i' 在循环内是均匀的 |
|   | //   if (i % thread_id == 0) break;    // 发散循环退出 |
| 167 | // } |
|   | // int divI = i;                 // divI 是发散的 |
| 168 | // An incoming value could be divergent by itself. |
| 169 | // Otherwise, an incoming value could be uniform within the loop |
| 170 | // that carries its definition but it may appear divergent |
| 171 | // from outside the loop. This happens when divergent loop exits |
| 172 | // drop definitions of that uniform value in different iterations. |
| 173 | // |
| 174 | // for (int i = 0; i < n; ++i) { // 'i' is uniform inside the loop |
| 175 | //   if (i % thread_id == 0) break;    // divergent loop exit |
| 176 | // } |
| 177 | // int divI = i;                 // divI is divergent |
| 178 | for (size_t i = 0; i < Phi.getNumIncomingValues(); ++i) { |
| 179 | const auto *InVal = Phi.getIncomingValue(i); |
| 180 | if (isDivergent(*Phi.getIncomingValue(i)) || |
| 181 | isTemporalDivergent(*Phi.getParent(), *InVal)) { |
| 182 |  |
| 186 |  |
| 193 | return true; |
| 194 | } |
| 196 | } |
| 197 | return false; |
| 203 |  |
| 206 |  |
| 212 | } |
|   | // 判断指令是否在当前分析的区域中。 |
| 213 | bool DivergenceAnalysis::inRegion(const Instruction &I) const { |
| 214 | return I.getParent() && inRegion(*I.getParent()); |
| 215 | } |
|   | // 判断基本块是否在当前分析的区域中。 |
| 216 | bool DivergenceAnalysis::inRegion(const BasicBlock &BB) const { |
| 217 | return (!RegionLoop && BB.getParent() == &F) || RegionLoop->contains(&BB); |
| 218 |  |
| 222 |  |
| 225 |  |
| 231 | } |
|   | // 检查指令是否使用了循环外的值。 |
| 232 | static bool usesLiveOut(const Instruction &I, const Loop *DivLoop) { |
| 233 | for (auto &Op : I.operands()) { |
| 234 | auto *OpInst = dyn_cast<Instruction>(&Op); |
| 235 | if (!OpInst) |
| 238 | continue; |
| 239 | if (DivLoop->contains(OpInst->getParent())) |
| 240 | return true; |
| 243 |  |
| 247 | } |
| 248 | return false; |
| 249 | } |
|   | // 将循环头部的循环携带值的所有用户标记为发散。 |
| 250 | // marks all users of loop-carried values of the loop headed by LoopHeader as |
| 251 | // divergent |
| 252 | void DivergenceAnalysis::taintLoopLiveOuts(const BasicBlock &LoopHeader) { |
| 253 | auto *DivLoop = LI.getLoopFor(&LoopHeader); |
| 254 | assert(DivLoop && "loopHeader is not actually part of a loop"); |
| 255 | SmallVector<BasicBlock *, 8> TaintStack; |
| 256 | DivLoop->getExitBlocks(TaintStack); |
|   | // 否则，循环携带值的潜在用户可能在DivLoop的支配区域内 |
|   | //（包括其边缘的phi节点） |
| 257 | // Otherwise potential users of loop-carried values could be anywhere in the |
| 258 | // dominance region of DivLoop (including its fringes for phi nodes) |
| 259 | DenseSet<const BasicBlock *> Visited; |
| 260 | for (auto *Block : TaintStack) { |
| 261 | Visited.insert(Block); |
| 267 | } |
| 268 | Visited.insert(&LoopHeader); |
| 269 | while (!TaintStack.empty()) { |
| 270 | auto *UserBlock = TaintStack.back(); |
| 271 | TaintStack.pop_back(); |
|   | // 不要将发散性传播到区域之外 |
| 272 | // don't spread divergence beyond the region |
| 273 | if (!inRegion(*UserBlock)) |
| 274 | continue; |
| 275 | assert(!DivLoop->contains(UserBlock) && |
| 276 | "irreducible control flow detected"); |
|   | // 支配区域边缘的phi节点 |
| 277 | // phi nodes at the fringes of the dominance region |
| 278 | if (!DT.dominates(&LoopHeader, UserBlock)) { |
|   | // UserBlock的所有PHI节点变为发散 |
| 279 | // all PHI nodes of UserBlock become divergent |
| 280 | for (auto &Phi : UserBlock->phis()) { |
| 281 | Worklist.push_back(&Phi); |
| 283 | } |
| 284 | continue; |
| 285 |  |
| 289 |  |
| 293 | } |
|   | // 标记DivLoop携带值的外部用户 |
| 294 | // taint outside users of values carried by DivLoop |
| 295 | for (auto &I : *UserBlock) { |
| 296 | if (isAlwaysUniform(I)) |
| 297 | continue; |
| 298 | if (isDivergent(I)) |
| 299 | continue; |
| 300 | if (!usesLiveOut(I, DivLoop)) |
| 301 | continue; |
| 302 | markDivergent(I); |
| 303 | if (I.isTerminator()) { |
| 304 | propagateBranchDivergence(I); |
| 305 | } else { |
| 306 | pushUsers(I); |
| 307 | } |
| 308 | } |
|   | // 访问支配区域内的所有块 |
| 309 | // visit all blocks in the dominance region |
| 310 | for (auto *SuccBlock : successors(UserBlock)) { |
| 311 | if (!Visited.insert(SuccBlock).second) { |
| 312 | continue; |
| 313 |  |
| 315 |  |
| 318 |  |
| 323 | } |
| 324 | TaintStack.push_back(SuccBlock); |
| 330 | } |
| 332 | } |
| 333 | } |
|   | // 将基本块中的所有PHI节点推入工作列表。 |
| 334 | void DivergenceAnalysis::pushPHINodes(const BasicBlock &Block) { |
|   | // 遍历基本块中的所有PHI节点。 |
| 335 | for (const auto &Phi : Block.phis()) { |
|   | // 如果PHI节点已经是发散的，则跳过。 |
| 336 | if (isDivergent(Phi)) |
| 337 | continue; |
|   | // 将PHI节点加入工作列表。 |
| 338 | Worklist.push_back(&Phi); |
| 339 | } |
| 340 | } |
|   | // 将值的所有用户推入工作列表。 |
| 341 | void DivergenceAnalysis::pushUsers(const Value &V) { |
|   | // 遍历值的所有用户。 |
| 342 | for (const auto *User : V.users()) { |
|   | // 将用户转换为指令，如果不是指令则跳过。 |
| 343 | const auto *UserInst = dyn_cast<const Instruction>(User); |
| 344 | if (!UserInst) |
| 345 | continue; |
|   | // 如果用户已经是发散的，则跳过。 |
| 346 | if (isDivergent(*UserInst)) |
| 347 | continue; |
|   | // 只在循环区域内计算发散性。 |
| 348 | // only compute divergent inside loop |
| 349 | if (!inRegion(*UserInst)) |
| 350 | continue; |
|   | // 将用户加入工作列表。 |
| 351 | Worklist.push_back(UserInst); |
| 352 |  |
| 355 |  |
| 361 | } |
| 362 |  |
| 368 | } |
|   | // 传播分支发散性到Join块。 |
| 369 | bool DivergenceAnalysis::propagateJoinDivergence(const BasicBlock &JoinBlock, |
| 370 | const Loop *BranchLoop) { |
| 371 | LLVM_DEBUG(dbgs() << "\tpropJoinDiv " << JoinBlock.getName() << "\n"); |
|   | // 如果Join块不在当前分析的区域内，则忽略发散性。 |
| 372 | // ignore divergence outside the region |
| 373 | if (!inRegion(JoinBlock)) { |
| 374 | return false; |
| 376 | } |
|   | // 将Join块中所有非发散的PHI节点推入工作列表。 |
| 377 | // push non-divergent phi nodes in JoinBlock to the worklist |
| 378 | pushPHINodes(JoinBlock); |
|   | // 标记Join块为发散性合并点。 |
| 379 | // disjoint-paths divergent at JoinBlock |
| 380 | markBlockJoinDivergent(JoinBlock); |
|   | // 如果Join块是分支循环的退出块，则返回true。 |
| 381 | // JoinBlock is a divergent loop exit |
| 382 | return BranchLoop && !BranchLoop->contains(&JoinBlock); |
| 386 |  |
| 390 |  |
| 397 | } |
|   | // 传播分支指令的发散性。 |
| 398 | void DivergenceAnalysis::propagateBranchDivergence(const Instruction &Term) { |
| 399 | LLVM_DEBUG(dbgs() << "propBranchDiv " << Term.getParent()->getName() << "\n"); |
|   | // 标记分支指令为发散。 |
| 400 | markDivergent(Term); |
|   | // 如果分支指令所在的块不可达，则不传播发散性。 |
| 401 | // Don't propagate divergence from unreachable blocks. |
| 402 | if (!DT.isReachableFromEntry(Term.getParent())) |
| 403 | return; |
|   | // 获取分支指令所在的循环。 |
| 404 | const auto *BranchLoop = LI.getLoopFor(Term.getParent()); |
|   | // 是否存在从BranchLoop（如果有）的发散循环退出。 |
| 405 | // whether there is a divergent loop exit from BranchLoop (if any) |
| 406 | bool IsBranchLoopDivergent = false; |
|   | // 遍历从Term出发在循环内通过不相交路径可达的所有块， |
|   | // 同时也会遍历因Term而变得发散的循环退出块。 |
| 407 | // iterate over all blocks reachable by disjoint from Term within the loop |
| 408 | // also iterates over loop exits that become divergent due to Term. |
| 409 | for (const auto *JoinBlock : SDA.join_blocks(Term)) { |
| 410 | IsBranchLoopDivergent |= propagateJoinDivergence(*JoinBlock, BranchLoop); |
| 413 | } |
|   | // 如果分支循环因Term中的发散分支而成为发散循环。 |
| 414 | // Branch loop is a divergent loop due to the divergent branch in Term |
| 415 | if (IsBranchLoopDivergent) { |
| 416 | assert(BranchLoop); |
|   | // 如果循环已经标记为发散，则直接返回。 |
| 417 | if (!DivergentLoops.insert(BranchLoop).second) { |
| 418 | return; |
| 419 | } |
|   | // 传播循环的发散性。 |
| 420 | propagateLoopDivergence(*BranchLoop); |
| 423 | } |
| 424 |  |
| 429 | } |
|   | // 传播循环的发散性。 |
| 430 | void DivergenceAnalysis::propagateLoopDivergence(const Loop &ExitingLoop) { |
| 431 | LLVM_DEBUG(dbgs() << "propLoopDiv " << ExitingLoop.getName() << "\n"); |
|   | // 不要将发散性传播到区域之外。 |
| 432 | // don't propagate beyond region |
| 433 | if (!inRegion(*ExitingLoop.getHeader())) |
| 434 | return; |
| 435 | const auto *BranchLoop = ExitingLoop.getParentLoop(); |
|   | // 循环携带值的使用可能发生在定义的支配区域内任何地方。 |
|   | // 所有循环携带的定义都被循环头支配（可约控制流）。 |
|   | // 因此，所有用户都必须在循环头的支配区域内， |
|   | // 除了PHI节点，它们也可以位于支配区域的边缘（传入的定义值）。 |
| 436 | // Uses of loop-carried values could occur anywhere |
| 437 | // within the dominance region of the definition. All loop-carried |
| 438 | // definitions are dominated by the loop header (reducible control). |
| 439 | // Thus all users have to be in the dominance region of the loop header, |
| 440 | // except PHI nodes that can also live at the fringes of the dom region |
| 441 | // (incoming defining value). |
| 442 | if (!IsLCSSAForm) |
| 443 | taintLoopLiveOuts(*ExitingLoop.getHeader()); |
|   | // 是否存在从BranchLoop（如果有）的发散循环退出。 |
| 444 | // whether there is a divergent loop exit from BranchLoop (if any) |
| 445 | bool IsBranchLoopDivergent = false; |
|   | // 遍历从ExitingLoop的退出块出发通过不相交路径可达的所有块， |
|   | // 同时也会遍历因ExitingLoop的退出而变得发散的循环退出块（BranchLoop）。 |
| 446 | // iterate over all blocks reachable by disjoint paths from exits of |
| 447 | // ExitingLoop also iterates over loop exits (of BranchLoop) that in turn |
| 448 | // become divergent. |
| 449 | for (const auto *JoinBlock : SDA.join_blocks(ExitingLoop)) { |
| 450 | IsBranchLoopDivergent |= propagateJoinDivergence(*JoinBlock, BranchLoop); |
| 451 | } |
|   | // 如果BranchLoop因ExitingLoop的发散退出而成为发散循环。 |
| 452 | // Branch loop is a divergent due to divergent loop exit in ExitingLoop |
| 453 | if (IsBranchLoopDivergent) { |
| 454 | assert(BranchLoop); |
| 455 | if (!DivergentLoops.insert(BranchLoop).second) { |
| 456 | return; |
| 458 | } |
| 459 | propagateLoopDivergence(*BranchLoop); |
| 460 |  |
| 462 | } |
| 463 |  |
| 466 | } |
|   | // 计算发散性。 |
| 467 | void DivergenceAnalysis::compute() { |
|   | // 将所有发散值的用户推入工作列表。 |
| 468 | for (auto *DivVal : DivergentValues) { |
| 469 | pushUsers(*DivVal); |
| 470 | } |
|   | // 传播发散性。 |
| 471 | // propagate divergence |
| 472 | while (!Worklist.empty()) { |
| 473 | const Instruction &I = *Worklist.back(); |
| 474 | Worklist.pop_back(); |
|   | // 维护覆盖指令的均匀性。 |
| 475 | // maintain uniformity of overrides |
| 476 | if (isAlwaysUniform(I)) |
| 477 | continue; |
| 478 | bool WasDivergent = isDivergent(I); |
| 479 | if (WasDivergent) |
| 480 | continue; |
|   | // 传播由终止符引起的发散性。 |
| 481 | // propagate divergence caused by terminator |
| 482 | if (I.isTerminator()) { |
| 483 | if (updateTerminator(I)) { |
|   | // 将控制发散性传播到受影响的指令。 |
| 484 | // propagate control divergence to affected instructions |
| 485 | propagateBranchDivergence(I); |
| 486 | continue; |
| 487 | } |
| 488 | } |
|   | // 更新由于发散操作数而引起的I的发散性。 |
| 489 | // update divergence of I due to divergent operands |
| 490 | bool DivergentUpd = false; |
| 491 | const auto *Phi = dyn_cast<const PHINode>(&I); |
| 492 | if (Phi) { |
| 493 | DivergentUpd = updatePHINode(*Phi); |
| 494 | } else { |
| 495 | DivergentUpd = updateNormalInstruction(I); |
| 496 | } |
|   | // 将值的发散性传播到用户。 |
| 497 | // propagate value divergence to users |
| 498 | if (DivergentUpd) { |
| 499 | markDivergent(I); |
| 500 | pushUsers(I); |
| 501 | } |
| 502 | } |
| 503 | } |
|   | // 判断值是否始终均匀。 |
| 504 | bool DivergenceAnalysis::isAlwaysUniform(const Value &V) const { |
| 505 | return UniformOverrides.find(&V) != UniformOverrides.end(); |
| 506 | } |
|   | // 判断值是否发散。 |
| 507 | bool DivergenceAnalysis::isDivergent(const Value &V) const { |
| 508 | return DivergentValues.find(&V) != DivergentValues.end(); |
| 509 | } |
|   | // 判断使用是否发散。 |
| 510 | bool DivergenceAnalysis::isDivergentUse(const Use &U) const { |
| 511 | Value &V = *U.get(); |
| 512 | Instruction &I = *cast<Instruction>(U.getUser()); |
| 513 | return isDivergent(V) || isTemporalDivergent(*I.getParent(), V); |
| 514 | } |
|   | // 打印发散分析的结果。 |
| 515 | void DivergenceAnalysis::print(raw_ostream &OS, const Module *) const { |
| 516 | if (DivergentValues.empty()) |
| 517 | return; |
|   | // 遍历函数中的所有指令，确保顺序是确定性的。 |
| 518 | // iterate instructions using instructions() to ensure a deterministic order. |
| 519 | for (auto &I : instructions(F)) { |
| 520 | if (isDivergent(I)) |
| 521 | OS << "DIVERGENT:" << I << '\n'; |
| 522 | } |
| 523 | } |
|   | // GPUDivergenceAnalysis类的构造函数。 |
|   | // 初始化GPUDivergenceAnalysis对象，设置相关成员变量，并标记发散值。 |
| 524 | // class GPUDivergenceAnalysis |
| 525 | GPUDivergenceAnalysis::GPUDivergenceAnalysis(Function &F, |
| 526 | const DominatorTree &DT, |
| 527 | const PostDominatorTree &PDT, |
| 528 | const LoopInfo &LI, |
| 529 | const TargetTransformInfo &TTI) |
| 530 | : SDA(DT, PDT, LI), DA(F, nullptr, DT, LI, SDA, false) { |
|   | // 遍历函数中的所有指令。 |
| 531 | for (auto &I : instructions(F)) { |
|   | // 如果指令是发散源，则标记为发散。 |
| 532 | if (TTI.isSourceOfDivergence(&I)) { |
| 533 | DA.markDivergent(I); |
|   | // 如果指令始终均匀，则添加到均匀覆盖集合中。 |
| 534 | } else if (TTI.isAlwaysUniform(&I)) { |
| 535 | DA.addUniformOverride(I); |
| 536 | } |
| 537 | } |
|   | // 遍历函数的所有参数。 |
| 538 | for (auto &Arg : F.args()) { |
|   | // 如果参数是发散源，则标记为发散。 |
| 539 | if (TTI.isSourceOfDivergence(&Arg)) { |
| 540 | DA.markDivergent(Arg); |
| 541 | } |
| 542 | } |
|   | // 计算发散性。 |
| 543 | DA.compute(); |
| 544 | } |
|   | // 判断值是否发散。 |
| 545 | bool GPUDivergenceAnalysis::isDivergent(const Value &val) const { |
| 546 | return DA.isDivergent(val); |
| 547 | } |
|   | // 判断使用是否发散。 |
| 548 | bool GPUDivergenceAnalysis::isDivergentUse(const Use &use) const { |
| 549 | return DA.isDivergentUse(use); |
| 550 | } |
|   | // 打印发散分析的结果。 |
| 551 | void GPUDivergenceAnalysis::print(raw_ostream &OS, const Module *mod) const { |
|   | // 打印内核的发散性信息。 |
| 552 | OS << "Divergence of kernel " << DA.getFunction().getName() << " {\n"; |
|   | // 调用DivergenceAnalysis的打印方法。 |
| 553 | DA.print(OS, mod); |
| 554 | OS << "}\n"; |
| 555 | } |

### 10.9.2 LLVM中的AMDGPU中的分岔分析对CodeGen的影响

llvm\lib\Target\AMDGPU\AMDGPUCodeGenPrepare.cpp

llvm\lib\Target\AMDGPU\AMDGPUCodeGenPrepare.cpp

| 51 | #define DEBUG_TYPE "amdgpu-codegenprepare" |
| --- | --- |
| 52 |  |
| 53 | using namespace llvm; |
| 54 |  |
| 55 | namespace { |
|   | // 命令行选项：在AMDGPUCodeGenPrepare中扩展子字节常量地址空间加载。 |
| 56 |  |
| 57 | static cl::opt<bool> WidenLoads( |
| 58 | "amdgpu-codegenprepare-widen-constant-loads", |
| 59 | cl::desc("Widen sub-dword constant address space loads in AMDGPUCodeGenPrepare"), |
| 60 | cl::ReallyHidden, |
| 61 | cl::init(false)); |
|   | // 命令行选项：在AMDGPUCodeGenPrepare中引入mul24内联函数。 |
| 62 |  |
| 63 | static cl::opt<bool> UseMul24Intrin( |
| 64 | "amdgpu-codegenprepare-mul24", |
| 65 | cl::desc("Introduce mul24 intrinsics in AMDGPUCodeGenPrepare"), |
| 66 | cl::ReallyHidden, |
| 67 | cl::init(true)); |
|   | // 命令行选项：通过使用通用IR扩展来合法化64位除法。 |
| 68 |  |
| 69 | // Legalize 64-bit division by using the generic IR expansion. |
| 70 | static cl::opt<bool> ExpandDiv64InIR( |
| 71 | "amdgpu-codegenprepare-expand-div64", |
| 72 | cl::desc("Expand 64-bit division in AMDGPUCodeGenPrepare"), |
| 73 | cl::ReallyHidden, |
| 74 | cl::init(false)); |
|   | // 命令行选项：保留所有除法操作，用于测试合法化器。 |
|   | // 此选项优先于ExpandDiv64InIR。 |
| 75 |  |
| 76 | // Leave all division operations as they are. This supersedes ExpandDiv64InIR |
| 77 | // and is used for testing the legalizer. |
| 78 | static cl::opt<bool> DisableIDivExpand( |
| 79 | "amdgpu-codegenprepare-disable-idiv-expansion", |
| 80 | cl::desc("Prevent expanding integer division in AMDGPUCodeGenPrepare"), |
| 81 | cl::ReallyHidden, |
| 82 | cl::init(false)); |
| 83 |  |
| 84 | class AMDGPUCodeGenPrepare : public FunctionPass, |
| 85 | public InstVisitor<AMDGPUCodeGenPrepare, bool> { |
| 86 | const GCNSubtarget *ST = nullptr; |
| 87 | AssumptionCache *AC = nullptr; |
| 88 | DominatorTree *DT = nullptr; |
| 89 | LegacyDivergenceAnalysis *DA = nullptr; |
| 90 | Module *Mod = nullptr; |
| 91 | const DataLayout *DL = nullptr; |
| 92 | bool HasUnsafeFPMath = false; |
| 93 | bool HasFP32Denormals = false; |
|   | // 将二元操作I的exact/nsw/nuw标志（如果有的话）复制到二元操作V。 |
|   | // 返回二元操作V。 |
| 94 |  |
| 95 | /// Copies exact/nsw/nuw flags (if any) from binary operation \p I to |
| 96 | /// binary operation \p V. |
| 97 | /// |
| 98 | /// \returns Binary operation \p V. |
| 99 | /// \returns \p T's base element bit width. |
| 100 | unsigned getBaseElementBitWidth(const Type *T) const; |
|   | // 返回给定类型T的等效32位整数类型。例如，如果T是i7，则返回i32； |
|   | // 如果T是<3 x i12>，则返回<3 x i32>。 |
| 101 |  |
| 102 | /// \returns Equivalent 32 bit integer type for given type \p T. For example, |
| 103 | /// if \p T is i7, then i32 is returned; if \p T is <3 x i12>, then <3 x i32> |
| 104 | /// is returned. |
| 105 | Type *getI32Ty(IRBuilder<> &B, const Type *T) const; |
|   | // 如果二元操作I是有符号的，则返回true，否则返回false。 |
| 106 |  |
| 107 | /// \returns True if binary operation \p I is a signed binary operation, false |
| 108 | /// otherwise. |
| 109 | bool isSigned(const BinaryOperator &I) const; |
|   | // 如果'select'操作I的条件来自有符号的'icmp'操作，则返回true，否则返回false。 |
| 110 |  |
| 111 | /// \returns True if the condition of 'select' operation \p I comes from a |
| 112 | /// signed 'icmp' operation, false otherwise. |
| 113 | bool isSigned(const SelectInst &I) const; |
|   | // 如果类型T需要被提升到32位整数类型，则返回true，否则返回false。 |
| 114 |  |
| 115 | /// \returns True if type \p T needs to be promoted to 32 bit integer type, |
| 116 | /// false otherwise. |
| 117 | bool needsPromotionToI32(const Type *T) const; |
|   | // 将统一的二元操作I提升到等效的32位二元操作。 |
|   | // I的基本元素位宽必须大于1且小于或等于16。提升是通过将操作数符号扩展或零扩展到32位， |
|   | // 替换I为等效的32位二元操作，并将32位二元操作的结果截断回I的原始类型。 |
|   | // 除法操作不会被提升。 |
|   | // 返回true，如果I被提升到等效的32位二元操作，否则返回false。 |
| 118 |  |
| 119 | /// Promotes uniform binary operation \p I to equivalent 32 bit binary |
| 120 | /// operation. |
| 121 | /// |
| 122 | /// \details \p I's base element bit width must be greater than 1 and less |
| 123 | /// than or equal 16. Promotion is done by sign or zero extending operands to |
| 124 | /// 32 bits, replacing \p I with equivalent 32 bit binary operation, and |
| 125 | /// truncating the result of 32 bit binary operation back to \p I's original |
| 126 | /// type. Division operation is not promoted. |
| 127 | /// |
| 128 | /// \returns True if \p I is promoted to equivalent 32 bit binary operation, |
| 129 | /// false otherwise. |
| 130 | bool promoteUniformOpToI32(BinaryOperator &I) const; |
|   | // 将统一的'icmp'操作I提升到32位'icmp'操作。 |
|   | // I的基本元素位宽必须大于1且小于或等于16。提升是通过将操作数符号扩展或零扩展到32位， |
|   | // 并替换I为32位'icmp'操作。 |
|   | // 返回true。 |
| 131 |  |
| 132 | /// Promotes uniform 'icmp' operation \p I to 32 bit 'icmp' operation. |
| 133 | /// |
| 134 | /// \details \p I's base element bit width must be greater than 1 and less |
| 135 | /// than or equal 16. Promotion is done by sign or zero extending operands to |
| 136 | /// 32 bits, and replacing \p I with 32 bit 'icmp' operation. |
| 137 | /// |
| 138 | /// \returns True. |
| 139 | bool promoteUniformOpToI32(ICmpInst &I) const; |
|   | // 将统一的'select'操作I提升到32位'select'操作。 |
|   | // I的基本元素位宽必须大于1且小于或等于16。提升是通过将操作数符号扩展或零扩展到32位， |
|   | // 替换I为32位'select'操作，并将32位'select'操作的结果截断回I的原始类型。 |
|   | // 返回true。 |
| 140 |  |
| 141 | /// Promotes uniform 'select' operation \p I to 32 bit 'select' |
| 142 | /// operation. |
| 143 | /// |
| 144 | /// \details \p I's base element bit width must be greater than 1 and less |
| 145 | /// than or equal 16. Promotion is done by sign or zero extending operands to |
| 146 | /// 32 bits, replacing \p I with 32 bit 'select' operation, and truncating the |
| 147 | /// result of 32 bit 'select' operation back to \p I's original type. |
| 148 | /// |
| 149 | /// \returns True. |
| 150 | bool promoteUniformOpToI32(SelectInst &I) const; |
|   | // 将统一的'bitreverse'内联函数I提升到32位'bitreverse'内联函数。 |
|   | // I的基本元素位宽必须大于1且小于或等于16。提升是通过将操作数零扩展到32位， |
|   | // 替换I为32位'bitreverse'内联函数，将32位'bitreverse'内联函数的结果向右移位（零填充）， |
|   | // 移位量为32减去I的基本元素位宽，并将移位操作的结果截断回I的原始类型。 |
|   | // 返回true。 |
| 151 |  |
| 152 | /// Promotes uniform 'bitreverse' intrinsic \p I to 32 bit 'bitreverse' |
| 153 | /// intrinsic. |
| 154 | /// |
| 155 | /// \details \p I's base element bit width must be greater than 1 and less |
| 156 | /// than or equal 16. Promotion is done by zero extending the operand to 32 |
| 157 | /// bits, replacing \p I with 32 bit 'bitreverse' intrinsic, shifting the |
| 158 | /// result of 32 bit 'bitreverse' intrinsic to the right with zero fill (the |
| 159 | /// shift amount is 32 minus \p I's base element bit width), and truncating |
| 160 | /// the result of the shift operation back to \p I's original type. |
| 161 | /// |
| 162 | /// \returns True. |
| 163 | bool promoteUniformBitreverseToI32(IntrinsicInst &I) const; |
| 164 |  |
| 165 |  |
| 166 | unsigned numBitsUnsigned(Value *Op, unsigned ScalarSize) const; |
| 167 | unsigned numBitsSigned(Value *Op, unsigned ScalarSize) const; |
| 168 | bool isI24(Value *V, unsigned ScalarSize) const; |
| 169 | bool isU24(Value *V, unsigned ScalarSize) const; |
|   | // 用llvm.amdgcn.mul.u24或llvm.amdgcn.mul.s24替换乘法指令。 |
|   | // SelectionDAG有一个问题，即断言位是已知的and。 |
| 170 |  |
| 171 | /// Replace mul instructions with llvm.amdgcn.mul.u24 or llvm.amdgcn.mul.s24. |
| 172 | /// SelectionDAG has an issue where an and asserting the bits are known |
| 173 | bool replaceMulWithMul24(BinaryOperator &I) const; |
|   | // 执行与DAGCombiner中同名函数相同的功能。由于我们在某些地方扩展了除法， |
|   | // 因此需要在隐藏之前执行此操作。 |
| 174 |  |
| 175 | /// Perform same function as equivalently named function in DAGCombiner. Since |
| 176 | /// we expand some divisions here, we need to perform this before obscuring. |
| 177 | bool foldBinOpIntoSelect(BinaryOperator &I) const; |
| 178 |  |
| 179 | bool divHasSpecialOptimization(BinaryOperator &I, |
| 180 | Value *Num, Value *Den) const; |
| 181 | int getDivNumBits(BinaryOperator &I, |
| 182 | Value *Num, Value *Den, |
| 183 | unsigned AtLeast, bool Signed) const; |
|   | // 扩展24位除法或取余。 |
| 184 |  |
| 185 | /// Expands 24 bit div or rem. |
| 186 | Value* expandDivRem24(IRBuilder<> &Builder, BinaryOperator &I, |
| 187 | Value *Num, Value *Den, |
| 188 | bool IsDiv, bool IsSigned) const; |
| 189 |  |
| 190 | Value *expandDivRem24Impl(IRBuilder<> &Builder, BinaryOperator &I, |
| 191 | Value *Num, Value *Den, unsigned NumBits, |
| 192 | bool IsDiv, bool IsSigned) const; |
|   | // 扩展32位除法或取余。 |
| 193 |  |
| 194 | /// Expands 32 bit div or rem. |
| 195 | Value* expandDivRem32(IRBuilder<> &Builder, BinaryOperator &I, |
| 196 | Value *Num, Value *Den) const; |
|   | // 将64位除法或取余缩小为32位。 |
| 197 |  |
| 198 | Value *shrinkDivRem64(IRBuilder<> &Builder, BinaryOperator &I, |
| 199 | Value *Num, Value *Den) const; |
| 200 | void expandDivRem64(BinaryOperator &I) const; |
|   | /// 扩展标量加载。 |
| 201 |  |
| 202 | /// |
|   | /// \details 扩展标量加载，对于从常量内存加载的统一、小类型加载，扩展到完整的32位， |
|   | /// 然后截断输入，以便使用标量加载而不是向量加载。 |
| 203 | /// |
| 204 | /// \returns True。 |
| 205 | /// Widen a scalar load. |
| 208 | /// |
| 209 | /// \details \p Widen scalar load for uniform, small type loads from constant |
| 210 | //  memory / to a full 32-bits and then truncate the input to allow a scalar |
| 211 | //  load instead of a vector load. |
| 212 | // |
| 213 | /// \returns True. |
| 214 | bool canWidenScalarExtLoad(LoadInst &I) const; |
| 215 | public: |
| 216 | static char ID; |
| 217 | AMDGPUCodeGenPrepare() : FunctionPass(ID) {} |
| 218 | bool visitFDiv(BinaryOperator &I); |
| 219 | bool visitInstruction(Instruction &I) { return false; } |
| 220 | bool visitBinaryOperator(BinaryOperator &I); |
| 221 | bool visitLoadInst(LoadInst &I); |
| 222 | bool visitICmpInst(ICmpInst &I); |
| 223 | bool visitSelectInst(SelectInst &I); |
| 224 |  |
| 225 | bool visitIntrinsicInst(IntrinsicInst &I); |
| 226 | bool visitBitreverseIntrinsicInst(IntrinsicInst &I); |
| 227 |  |
| 228 | bool doInitialization(Module &M) override; |
| 229 | bool runOnFunction(Function &F) override; |
| 230 |  |
| 231 | StringRef getPassName() const override { return "AMDGPU IR optimizations"; } |
| 232 |  |
| 233 | void getAnalysisUsage(AnalysisUsage &AU) const override { |
| 234 | AU.addRequired<AssumptionCacheTracker>(); |
| 235 | AU.addRequired<LegacyDivergenceAnalysis>(); |
|   | // FIXME: 除法扩展需要保留支配树。 |
| 236 |  |
| 237 | // FIXME: Division expansion needs to preserve the dominator tree. |
| 238 | if (!ExpandDiv64InIR) |
| 239 | AU.setPreservesAll(); |
| 240 | } |
| 241 | }; |
| 242 |  |
| 243 | } // end anonymous namespace |
|   | // getBaseElementBitWidth - 获取类型T的基本元素的位宽。 |
| 244 |  |
| 245 | unsigned AMDGPUCodeGenPrepare::getBaseElementBitWidth(const Type *T) const { |
|   | // 确保类型T需要提升到i32 |
| 246 | assert(needsPromotionToI32(T) && "T does not need promotion to i32"); |
|   | // 如果T是整数类型，直接返回其位宽 |
| 247 |  |
| 248 | if (T->isIntegerTy()) |
| 249 | return T->getIntegerBitWidth(); |
|   | // 如果T是向量类型，返回其元素类型的位宽 |
| 250 | return cast<VectorType>(T)->getElementType()->getIntegerBitWidth(); |
| 251 | } |
|   | // getI32Ty - 获取与类型T对应的i32类型（标量或向量）。 |
| 252 |  |
| 253 | Type *AMDGPUCodeGenPrepare::getI32Ty(IRBuilder<> &B, const Type *T) const { |
|   | // 确保类型T需要提升到i32 |
| 254 | assert(needsPromotionToI32(T) && "T does not need promotion to i32"); |
|   | // 如果T是整数类型，返回i32类型 |
| 255 |  |
| 256 | if (T->isIntegerTy()) |
| 257 | return B.getInt32Ty(); |
|   | // 如果T是向量类型，返回具有相同元素数量的i32向量类型 |
| 258 | return FixedVectorType::get(B.getInt32Ty(), cast<FixedVectorType>(T)); |
| 259 | } |
|   | // isSigned - 判断二元操作符I是否是有符号操作。 |
| 260 |  |
| 261 | bool AMDGPUCodeGenPrepare::isSigned(const BinaryOperator &I) const { |
| 262 | return I.getOpcode() == Instruction::AShr || |
| 263 | I.getOpcode() == Instruction::SDiv || I.getOpcode() == Instruction::SRem; |
| 264 | } |
|   | // isSigned - 判断选择指令I是否是有符号操作。 |
| 265 |  |
| 266 | bool AMDGPUCodeGenPrepare::isSigned(const SelectInst &I) const { |
|   | // 如果选择指令的条件是ICmpInst，则返回其是否是有符号比较 |
| 267 | return isa<ICmpInst>(I.getOperand(0)) ? |
| 268 | cast<ICmpInst>(I.getOperand(0))->isSigned() : false; |
| 269 | } |
|   | // needsPromotionToI32 - 判断类型T是否需要提升到i32。 |
| 270 |  |
| 271 | bool AMDGPUCodeGenPrepare::needsPromotionToI32(const Type *T) const { |
| 272 | const IntegerType *IntTy = dyn_cast<IntegerType>(T); |
|   | // 如果T是整数类型且位宽在2到16之间，则需要提升到i32 |
| 273 | if (IntTy && IntTy->getBitWidth() > 1 && IntTy->getBitWidth() <= 16) |
| 274 | return true; |
|   | // 如果T是向量类型 |
| 275 |  |
| 276 | if (const VectorType *VT = dyn_cast<VectorType>(T)) { |
|   | // TODO: 打包操作的支持有限，可能需要提升某些类型 |
|   | // 如果目标支持VOP3P指令，则不需要提升 |
| 277 | // TODO: The set of packed operations is more limited, so may want to |
| 278 | // promote some anyway. |
| 279 | if (ST->hasVOP3PInsts()) |
| 280 | return false; |
|   | // 递归检查向量元素类型是否需要提升 |
| 281 |  |
| 282 | return needsPromotionToI32(VT->getElementType()); |
| 283 | } |
| 284 |  |
| 285 | return false; |
| 286 | } |
|   | // promotedOpIsNSW - 判断提升到i32的操作是否应设置nsw（无符号溢出）标志。 |
| 287 |  |
| 288 | // Return true if the op promoted to i32 should have nsw set. |
| 289 | static bool promotedOpIsNSW(const Instruction &I) { |
| 290 | switch (I.getOpcode()) { |
| 291 | case Instruction::Shl:  // 左移 |
| 292 | case Instruction::Add:  // 加法 |
| 293 | case Instruction::Sub:  // 减法 |
| 294 | return true; |
| 295 | case Instruction::Mul:  // 乘法 |
| 296 | return I.hasNoUnsignedWrap();  // 如果乘法操作没有无符号溢出，则返回true |
| 297 | default: |
| 298 | return false; |
| 299 | } |
| 300 | } |
|   | // promotedOpIsNUW - 判断提升到i32的操作是否应设置nuw（无符号溢出）标志。 |
| 301 |  |
| 302 | // Return true if the op promoted to i32 should have nuw set. |
| 303 | static bool promotedOpIsNUW(const Instruction &I) { |
| 304 | switch (I.getOpcode()) { |
| 305 | case Instruction::Shl:  // 左移 |
| 306 | case Instruction::Add:  // 加法 |
| 307 | case Instruction::Mul:  // 乘法 |
| 308 | return true; |
| 309 | case Instruction::Sub:  // 减法 |
| 310 | return I.hasNoUnsignedWrap();  // 如果减法操作没有无符号溢出，则返回true |
| 311 | default: |
| 312 | return false; |
| 313 | } |
| 314 | } |
|   | // canWidenScalarExtLoad - 判断是否可以扩展标量加载操作。 |
| 315 |  |
| 316 | bool AMDGPUCodeGenPrepare::canWidenScalarExtLoad(LoadInst &I) const { |
| 317 | Type *Ty = I.getType(); |
| 318 | const DataLayout &DL = Mod->getDataLayout(); |
| 319 | int TySize = DL.getTypeSizeInBits(Ty); |
| 320 | Align Alignment = DL.getValueOrABITypeAlignment(I.getAlign(), Ty); |
|   | // 如果加载操作是简单的、类型大小小于32位、对齐大于等于4字节且是均匀的，则可以扩展 |
| 321 |  |
| 322 | return I.isSimple() && TySize < 32 && Alignment >= 4 && DA->isUniform(&I); |
| 323 | } |
|   | // promoteUniformOpToI32 - 将均匀的二元操作提升到i32。 |
| 324 |  |
| 325 | bool AMDGPUCodeGenPrepare::promoteUniformOpToI32(BinaryOperator &I) const { |
| 326 | assert(needsPromotionToI32(I.getType()) && |
| 327 | "I does not need promotion to i32"); |
|   | // 不支持除法或取余操作的提升 |
| 328 |  |
| 329 | if (I.getOpcode() == Instruction::SDiv || |
| 330 | I.getOpcode() == Instruction::UDiv || |
| 331 | I.getOpcode() == Instruction::SRem || |
| 332 | I.getOpcode() == Instruction::URem) |
| 333 | return false; |
| 334 |  |
| 335 | IRBuilder<> Builder(&I); |
| 336 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 337 |  |
| 338 | Type *I32Ty = getI32Ty(Builder, I.getType()); |
| 339 | Value *ExtOp0 = nullptr; |
| 340 | Value *ExtOp1 = nullptr; |
| 341 | Value *ExtRes = nullptr; |
| 342 | Value *TruncRes = nullptr; |
|   | // 根据操作符的符号性进行扩展 |
| 343 |  |
| 344 | if (isSigned(I)) { |
| 345 | ExtOp0 = Builder.CreateSExt(I.getOperand(0), I32Ty); |
| 346 | ExtOp1 = Builder.CreateSExt(I.getOperand(1), I32Ty); |
| 347 | } else { |
| 348 | ExtOp0 = Builder.CreateZExt(I.getOperand(0), I32Ty); |
| 349 | ExtOp1 = Builder.CreateZExt(I.getOperand(1), I32Ty); |
| 350 | } |
|   | // 创建提升后的二元操作 |
| 351 |  |
| 352 | ExtRes = Builder.CreateBinOp(I.getOpcode(), ExtOp0, ExtOp1); |
| 353 | if (Instruction *Inst = dyn_cast<Instruction>(ExtRes)) { |
|   | // 设置提升操作的nsw和nuw标志 |
| 354 | if (promotedOpIsNSW(cast<Instruction>(I))) |
| 355 | Inst->setHasNoSignedWrap(); |
| 356 |  |
| 357 | if (promotedOpIsNUW(cast<Instruction>(I))) |
| 358 | Inst->setHasNoUnsignedWrap(); |
|   | // 如果原操作是精确的，则设置提升操作的精确标志 |
| 359 |  |
| 360 | if (const auto *ExactOp = dyn_cast<PossiblyExactOperator>(&I)) |
| 361 | Inst->setIsExact(ExactOp->isExact()); |
| 362 | } |
|   | // 将结果截断回原类型 |
| 363 |  |
| 364 | TruncRes = Builder.CreateTrunc(ExtRes, I.getType()); |
|   | // 替换原操作的所有使用并删除原操作 |
| 365 |  |
| 366 | I.replaceAllUsesWith(TruncRes); |
| 367 | I.eraseFromParent(); |
| 368 |  |
| 369 | return true; |
| 370 | } |
|   | // promoteUniformOpToI32 - 将均匀的整数比较操作提升到i32。 |
| 371 |  |
| 372 | bool AMDGPUCodeGenPrepare::promoteUniformOpToI32(ICmpInst &I) const { |
| 373 | assert(needsPromotionToI32(I.getOperand(0)->getType()) && |
| 374 | "I does not need promotion to i32"); |
| 375 |  |
| 376 | IRBuilder<> Builder(&I); |
| 377 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 378 |  |
| 379 | Type *I32Ty = getI32Ty(Builder, I.getOperand(0)->getType()); |
| 380 | Value *ExtOp0 = nullptr; |
| 381 | Value *ExtOp1 = nullptr; |
| 382 | Value *NewICmp  = nullptr; |
|   | // 根据比较的符号性进行扩展 |
| 383 |  |
| 384 | if (I.isSigned()) { |
| 385 | ExtOp0 = Builder.CreateSExt(I.getOperand(0), I32Ty); |
| 386 | ExtOp1 = Builder.CreateSExt(I.getOperand(1), I32Ty); |
| 387 | } else { |
| 388 | ExtOp0 = Builder.CreateZExt(I.getOperand(0), I32Ty); |
| 389 | ExtOp1 = Builder.CreateZExt(I.getOperand(1), I32Ty); |
| 390 | } |
|   | // 创建提升后的比较操作 |
| 391 | NewICmp = Builder.CreateICmp(I.getPredicate(), ExtOp0, ExtOp1); |
|   | // 替换原操作的所有使用并删除原操作 |
| 392 |  |
| 393 | I.replaceAllUsesWith(NewICmp); |
| 394 | I.eraseFromParent(); |
| 395 |  |
| 396 | return true; |
| 397 | } |
|   | // promoteUniformOpToI32 - 将均匀的操作提升到i32。 |
| 398 |  |
| 399 | bool AMDGPUCodeGenPrepare::promoteUniformOpToI32(SelectInst &I) const { |
| 400 | assert(needsPromotionToI32(I.getType()) && |
| 401 | "I does not need promotion to i32"); |
| 402 |  |
| 403 | IRBuilder<> Builder(&I); |
| 404 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 405 |  |
| 406 | Type *I32Ty = getI32Ty(Builder, I.getType()); |
| 407 | Value *ExtOp1 = nullptr; |
| 408 | Value *ExtOp2 = nullptr; |
| 409 | Value *ExtRes = nullptr; |
| 410 | Value *TruncRes = nullptr; |
|   | // 根据操作的符号性进行扩展 |
| 411 |  |
| 412 | if (isSigned(I)) { |
| 413 | ExtOp1 = Builder.CreateSExt(I.getOperand(1), I32Ty); |
| 414 | ExtOp2 = Builder.CreateSExt(I.getOperand(2), I32Ty); |
| 415 | } else { |
| 416 | ExtOp1 = Builder.CreateZExt(I.getOperand(1), I32Ty); |
| 417 | ExtOp2 = Builder.CreateZExt(I.getOperand(2), I32Ty); |
| 418 | } |
|   | // 创建提升后的操作 |
| 419 | ExtRes = Builder.CreateSelect(I.getOperand(0), ExtOp1, ExtOp2); |
|   | // 将结果截断回原类型 |
| 420 | TruncRes = Builder.CreateTrunc(ExtRes, I.getType()); |
|   | // 替换原操作的所有使用并删除原操作 |
| 421 |  |
| 422 | I.replaceAllUsesWith(TruncRes); |
| 423 | I.eraseFromParent(); |
| 424 |  |
| 425 | return true; |
| 426 | } |
|   | // promoteUniformBitreverseToI32 - 将均匀的bitreverse操作提升到i32。 |
| 427 |  |
| 428 | bool AMDGPUCodeGenPrepare::promoteUniformBitreverseToI32( |
| 429 | IntrinsicInst &I) const { |
| 430 | assert(I.getIntrinsicID() == Intrinsic::bitreverse && |
| 431 | "I must be bitreverse intrinsic"); |
| 432 | assert(needsPromotionToI32(I.getType()) && |
| 433 | "I does not need promotion to i32"); |
| 434 |  |
| 435 | IRBuilder<> Builder(&I); |
| 436 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 437 |  |
| 438 | Type *I32Ty = getI32Ty(Builder, I.getType()); |
|   | // 获取i32类型的bitreverse内联函数 |
| 439 | Function *I32 = |
| 440 | Intrinsic::getDeclaration(Mod, Intrinsic::bitreverse, { I32Ty }); |
|   | // 扩展操作数到i32 |
| 441 | Value *ExtOp = Builder.CreateZExt(I.getOperand(0), I32Ty); |
|   | // 调用bitreverse内联函数 |
| 442 | Value *ExtRes = Builder.CreateCall(I32, { ExtOp }); |
|   | // 右移结果以匹配原类型的位宽 |
| 443 | Value *LShrOp = |
| 444 | Builder.CreateLShr(ExtRes, 32 - getBaseElementBitWidth(I.getType())); |
|   | // 将结果截断回原类型 |
| 445 | Value *TruncRes = |
| 446 | Builder.CreateTrunc(LShrOp, I.getType()); |
|   | // 替换原操作的所有使用并删除原操作 |
| 447 |  |
| 448 | I.replaceAllUsesWith(TruncRes); |
| 449 | I.eraseFromParent(); |
| 450 |  |
| 451 | return true; |
| 452 | } |
|   | // numBitsUnsigned - 计算无符号值的有效位数。 |
| 453 |  |
| 454 | unsigned AMDGPUCodeGenPrepare::numBitsUnsigned(Value *Op, |
| 455 | unsigned ScalarSize) const { |
| 456 | KnownBits Known = computeKnownBits(Op, *DL, 0, AC); |
| 457 | return ScalarSize - Known.countMinLeadingZeros(); |
| 458 | } |
|   | // numBitsSigned - 计算有符号值的有效位数。 |
| 459 |  |
| 460 | unsigned AMDGPUCodeGenPrepare::numBitsSigned(Value *Op, |
| 461 | unsigned ScalarSize) const { |
|   | // 对于有符号24位值，第23位必须是符号位。 |
| 462 | // In order for this to be a signed 24-bit value, bit 23, must |
| 463 | // be a sign bit. |
| 464 | return ScalarSize - ComputeNumSignBits(Op, *DL, 0, AC); |
| 465 | } |
|   | // isI24 - 判断值V是否可以表示为有符号24位整数。 |
| 466 |  |
| 467 | bool AMDGPUCodeGenPrepare::isI24(Value *V, unsigned ScalarSize) const { |
| 468 | return ScalarSize >= 24 && // 小于24位的类型应视为无符号24位值。 |
| 469 | // Types less than 24-bit should be treated as unsigned 24-bit values. |
| 470 | numBitsSigned(V, ScalarSize) < 24; |
| 471 | } |
|   | // isU24 - 判断值V是否可以表示为无符号24位整数。 |
| 472 |  |
| 473 | bool AMDGPUCodeGenPrepare::isU24(Value *V, unsigned ScalarSize) const { |
| 474 | return numBitsUnsigned(V, ScalarSize) <= 24; |
| 475 | } |
|   | // extractValues - 从向量值中提取所有元素。 |
| 476 |  |
| 477 | static void extractValues(IRBuilder<> &Builder, |
| 478 | SmallVectorImpl<Value *> &Values, Value *V) { |
| 479 | auto *VT = dyn_cast<FixedVectorType>(V->getType()); |
| 480 | if (!VT) { |
| 481 | Values.push_back(V); |
| 482 | return; |
| 483 | } |
|   | // 遍历向量中的每个元素并提取 |
| 484 |  |
| 485 | for (int I = 0, E = VT->getNumElements(); I != E; ++I) |
| 486 | Values.push_back(Builder.CreateExtractElement(V, I)); |
| 487 | } |
|   | // insertValues - 将一组值插入到向量中。 |
| 488 |  |
| 489 | static Value *insertValues(IRBuilder<> &Builder, |
| 490 | Type *Ty, |
| 491 | SmallVectorImpl<Value *> &Values) { |
| 492 | if (Values.size() == 1) |
| 493 | return Values[0]; |
|   | // 创建一个未定义的向量值 |
| 494 |  |
| 495 | Value *NewVal = UndefValue::get(Ty); |
|   | // 将每个值插入到向量的对应位置 |
| 496 | for (int I = 0, E = Values.size(); I != E; ++I) |
| 497 | NewVal = Builder.CreateInsertElement(NewVal, Values[I], I); |
| 498 |  |
| 499 | return NewVal; |
| 500 | } |
|   | // replaceMulWithMul24 - 将乘法操作替换为mul24内联函数。 |
| 501 |  |
| 502 | bool AMDGPUCodeGenPrepare::replaceMulWithMul24(BinaryOperator &I) const { |
| 503 | if (I.getOpcode() != Instruction::Mul) |
| 504 | return false; |
| 505 |  |
| 506 | Type *Ty = I.getType(); |
| 507 | unsigned Size = Ty->getScalarSizeInBits(); |
|   | // 如果类型大小小于等于16位且目标支持16位指令，则不替换 |
| 508 | if (Size <= 16 && ST->has16BitInsts()) |
| 509 | return false; |
|   | // 如果操作是均匀的，则优先使用标量乘法 |
| 510 |  |
| 511 | // Prefer scalar if this could be s_mul_i32 |
| 512 | if (DA->isUniform(&I)) |
| 513 | return false; |
| 514 |  |
| 515 | Value *LHS = I.getOperand(0); |
| 516 | Value *RHS = I.getOperand(1); |
| 517 | IRBuilder<> Builder(&I); |
| 518 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 519 |  |
| 520 | Intrinsic::ID IntrID = Intrinsic::not_intrinsic; |
|   | // 如果操作数是无符号24位整数且目标支持mul_u24，则使用mul_u24 |
| 521 |  |
| 522 | // TODO: Should this try to match mulhi24? |
| 523 | if (ST->hasMulU24() && isU24(LHS, Size) && isU24(RHS, Size)) { |
| 524 | IntrID = Intrinsic::amdgcn_mul_u24; |
| 525 | } else if (ST->hasMulI24() && isI24(LHS, Size) && isI24(RHS, Size)) { |
|   | // 如果操作数是有符号24位整数且目标支持mul_i24，则使用mul_i24 |
| 526 | IntrID = Intrinsic::amdgcn_mul_i24; |
| 527 | } else |
| 528 | return false; |
| 529 |  |
| 530 | SmallVector<Value *, 4> LHSVals; |
| 531 | SmallVector<Value *, 4> RHSVals; |
| 532 | SmallVector<Value *, 4> ResultVals; |
| 533 | extractValues(Builder, LHSVals, LHS); |
| 534 | extractValues(Builder, RHSVals, RHS); |
| 535 |  |
| 536 |  |
| 537 | IntegerType *I32Ty = Builder.getInt32Ty(); |
| 538 | FunctionCallee Intrin = Intrinsic::getDeclaration(Mod, IntrID); |
| 539 | for (int I = 0, E = LHSVals.size(); I != E; ++I) { |
| 540 | Value *LHS, *RHS; |
| 541 | if (IntrID == Intrinsic::amdgcn_mul_u24) { |
|   | // 对于无符号mul24，将操作数扩展或截断为i32 |
| 542 | LHS = Builder.CreateZExtOrTrunc(LHSVals[I], I32Ty); |
| 543 | RHS = Builder.CreateZExtOrTrunc(RHSVals[I], I32Ty); |
| 544 | } else { |
|   | // 对于有符号mul24，将操作数扩展或截断为i32 |
| 545 | LHS = Builder.CreateSExtOrTrunc(LHSVals[I], I32Ty); |
| 546 | RHS = Builder.CreateSExtOrTrunc(RHSVals[I], I32Ty); |
| 547 | } |
|   | // 调用mul24内联函数 |
| 548 |  |
| 549 | Value *Result = Builder.CreateCall(Intrin, {LHS, RHS}); |
| 550 |  |
| 551 | if (IntrID == Intrinsic::amdgcn_mul_u24) { |
|   | // 对于无符号mul24，将结果扩展或截断回原类型 |
| 552 | ResultVals.push_back(Builder.CreateZExtOrTrunc(Result, |
| 553 | LHSVals[I]->getType())); |
| 554 | } else { |
|   | // 对于有符号mul24，将结果扩展或截断回原类型 |
| 555 | ResultVals.push_back(Builder.CreateSExtOrTrunc(Result, |
| 556 | LHSVals[I]->getType())); |
| 557 | } |
| 558 | } |
|   | // 将结果值插入到向量中 |
| 559 |  |
| 560 | Value *NewVal = insertValues(Builder, Ty, ResultVals); |
| 561 | NewVal->takeName(&I); |
| 562 | I.replaceAllUsesWith(NewVal); |
| 563 | I.eraseFromParent(); |
| 564 |  |
| 565 | return true; |
| 566 | } |
|   | // findSelectThroughCast - 通过类型转换查找选择指令。 |
| 567 |  |
| 568 | // Find a select instruction, which may have been casted. This is mostly to deal |
| 569 | // with cases where i16 selects were promoted here to i32. |
| 570 | static SelectInst *findSelectThroughCast(Value *V, CastInst *&Cast) { |
| 571 | Cast = nullptr; |
| 572 | if (SelectInst *Sel = dyn_cast<SelectInst>(V)) |
| 573 | return Sel; |
| 574 |  |
| 575 | if ((Cast = dyn_cast<CastInst>(V))) { |
| 576 | if (SelectInst *Sel = dyn_cast<SelectInst>(Cast->getOperand(0))) |
| 577 | return Sel; |
| 578 | } |
| 579 |  |
| 580 | return nullptr; |
| 581 | } |
|   | // foldBinOpIntoSelect - 将二元操作折叠到选择指令中。 |
| 582 |  |
| 583 | bool AMDGPUCodeGenPrepare::foldBinOpIntoSelect(BinaryOperator &BO) const { |
|   | // 除非旧的选择指令将被移除，否则不执行此操作。 |
| 584 | // Don't do this unless the old select is going away. We want to eliminate the |
| 585 | // binary operator, not replace a binop with a select. |
| 586 | int SelOpNo = 0; |
| 587 |  |
| 588 | CastInst *CastOp; |
|   | // TODO: 应该尝试处理一些多用户的情况。 |
|   | // 对于除法操作，复制选择指令可能是有利的。 |
| 589 |  |
| 590 | // TODO: Should probably try to handle some cases with multiple |
| 591 | // users. Duplicating the select may be profitable for division. |
| 592 | SelectInst *Sel = findSelectThroughCast(BO.getOperand(0), CastOp); |
| 593 | if (!Sel || !Sel->hasOneUse()) { |
| 594 | SelOpNo = 1; |
| 595 | Sel = findSelectThroughCast(BO.getOperand(1), CastOp); |
| 596 | } |
| 597 |  |
| 598 | if (!Sel || !Sel->hasOneUse()) |
| 599 | return false; |
| 600 |  |
| 601 | Constant *CT = dyn_cast<Constant>(Sel->getTrueValue()); |
| 602 | Constant *CF = dyn_cast<Constant>(Sel->getFalseValue()); |
| 603 | Constant *CBO = dyn_cast<Constant>(BO.getOperand(SelOpNo ^ 1)); |
| 604 | if (!CBO || !CT || !CF) |
| 605 | return false; |
| 606 |  |
| 607 | if (CastOp) { |
| 608 | if (!CastOp->hasOneUse()) |
| 609 | return false; |
|   | // 折叠类型转换操作 |
| 610 | CT = ConstantFoldCastOperand(CastOp->getOpcode(), CT, BO.getType(), *DL); |
| 611 | CF = ConstantFoldCastOperand(CastOp->getOpcode(), CF, BO.getType(), *DL); |
| 612 | } |
|   | // TODO: 处理DAG组合器中的特殊0/-1情况，尽管我们主要需要处理除法。 |
| 613 |  |
| 614 | // TODO: Handle special 0/-1 cases DAG combine does, although we only really |
| 615 | // need to handle divisions here. |
| 616 | Constant *FoldedT = SelOpNo ? |
| 617 | ConstantFoldBinaryOpOperands(BO.getOpcode(), CBO, CT, *DL) : |
| 618 | ConstantFoldBinaryOpOperands(BO.getOpcode(), CT, CBO, *DL); |
| 619 | if (isa<ConstantExpr>(FoldedT)) |
| 620 | return false; |
| 621 |  |
| 622 | Constant *FoldedF = SelOpNo ? |
| 623 | ConstantFoldBinaryOpOperands(BO.getOpcode(), CBO, CF, *DL) : |
| 624 | ConstantFoldBinaryOpOperands(BO.getOpcode(), CF, CBO, *DL); |
| 625 | if (isa<ConstantExpr>(FoldedF)) |
| 626 | return false; |
| 627 |  |
| 628 | IRBuilder<> Builder(&BO); |
| 629 | Builder.SetCurrentDebugLocation(BO.getDebugLoc()); |
| 630 | if (const FPMathOperator *FPOp = dyn_cast<const FPMathOperator>(&BO)) |
| 631 | Builder.setFastMathFlags(FPOp->getFastMathFlags()); |
|   | // 创建新的选择指令 |
| 632 |  |
| 633 | Value *NewSelect = Builder.CreateSelect(Sel->getCondition(), |
| 634 | FoldedT, FoldedF); |
| 635 | NewSelect->takeName(&BO); |
| 636 | BO.replaceAllUsesWith(NewSelect); |
| 637 | BO.eraseFromParent(); |
| 638 | if (CastOp) |
| 639 | CastOp->eraseFromParent(); |
| 640 | Sel->eraseFromParent(); |
| 641 | return true; |
| 642 | } |
|   | // optimizeWithRcp - 使用rcp优化fdiv操作： |
| 643 |  |
| 644 | // |
|   | // 1/x -> rcp(x) 当rcp足够精确时，或者在不安全的浮点数学或afn下允许不精确的rcp。 |
| 645 | // |
|   | // a/b -> a*rcp(b) 当在不安全的浮点数学或afn下允许不精确的rcp时。 |
| 646 | // Optimize fdiv with rcp: |
| 647 | // |
| 648 | // 1/x -> rcp(x) when rcp is sufficiently accurate or inaccurate rcp is |
| 649 | //               allowed with unsafe-fp-math or afn. |
| 653 |  |
| 656 |  |
| 663 |  |
| 664 | // |
| 665 | // a/b -> a*rcp(b) when inaccurate rcp is allowed with unsafe-fp-math or afn. |
| 666 | static Value *optimizeWithRcp(Value *Num, Value *Den, bool AllowInaccurateRcp, |
| 667 | bool RcpIsAccurate, IRBuilder<> &Builder, |
| 668 | Module *Mod) { |
| 669 | if (!AllowInaccurateRcp && !RcpIsAccurate) |
| 670 | return nullptr; |
| 671 | Type *Ty = Den->getType(); |
| 672 | if (const ConstantFP *CLHS = dyn_cast<ConstantFP>(Num)) { |
| 673 | if (AllowInaccurateRcp || RcpIsAccurate) { |
| 674 | if (CLHS->isExactlyValue(1.0)) { |
| 677 |  |
| 680 | Function *Decl = Intrinsic::getDeclaration( |
| 681 | Mod, Intrinsic::amdgcn_rcp, Ty); |
|   | // v_rcp_f32和v_rsq_f32不支持非规格化数，根据CI文档，其最坏情况误差为1 ulp。 |
|   | // OpenCL要求1.0 / x的误差小于等于2.5 ulp，因此只要我们不使用非规格化数， |
|   | // 使用rcp总是可以的。 |
| 682 |  |
| 683 | // |
|   | // v_rcp_f16和v_rsq_f16支持非规格化数。 |
|   | // 注意：v_sqrt和v_rcp稍后会合并为v_rsq，因此我们在这里不插入rsq内联函数。 |
| 684 | // 1.0 / x -> rcp(x) |
| 685 | // v_rcp_f32 and v_rsq_f32 do not support denormals, and according to |
| 686 | // the CI documentation has a worst case error of 1 ulp. |
| 687 | // OpenCL requires <= 2.5 ulp for 1.0 / x, so it should always be OK to |
| 688 | // use it as long as we aren't trying to use denormals. |
| 689 |  |
| 693 |  |
| 694 | // |
| 695 | // v_rcp_f16 and v_rsq_f16 DO support denormals. |
| 696 | // NOTE: v_sqrt and v_rcp will be combined to v_rsq later. So we don't |
| 697 | //       insert rsq intrinsic here. |
| 698 | // 1.0 / x -> rcp(x) |
| 699 | return Builder.CreateCall(Decl, { Den }); |
| 700 | } |
|   | // 与1.0相同，但从常量中扩展符号。 |
| 701 | // Same as for 1.0, but expand the sign out of the constant. |
| 702 | if (CLHS->isExactlyValue(-1.0)) { |
| 703 | Function *Decl = Intrinsic::getDeclaration( |
| 704 | Mod, Intrinsic::amdgcn_rcp, Ty); |
| 705 | // -1.0 / x -> rcp (fneg x) |
| 706 | Value *FNeg = Builder.CreateFNeg(Den); |
| 707 | return Builder.CreateCall(Decl, { FNeg }); |
| 708 | } |
| 709 | } |
| 710 | } |
| 711 | if (AllowInaccurateRcp) { |
| 712 | Function *Decl = Intrinsic::getDeclaration( |
| 713 | Mod, Intrinsic::amdgcn_rcp, Ty); |
|   | // 转换为乘以倒数。 |
| 714 | // Turn into multiply by the reciprocal. |
| 715 | // x / y -> x * (1.0 / y) |
| 716 | Value *Recip = Builder.CreateCall(Decl, { Den }); |
| 717 | return Builder.CreateFMul(Num, Recip); |
| 720 |  |
| 725 | } |
| 726 |  |
| 729 | return nullptr; |
| 730 |  |
| 732 | } |
|   | // optimizeWithFDivFast - 使用fdiv.fast优化fdiv操作： |
| 734 |  |
| 735 | // |
|   | // a/b -> fdiv.fast(a, b) 当!fpmath >= 2.5ulp且非规格化数被刷新时。 |
| 736 | // |
|   | // 1/x -> fdiv.fast(1,x) 当!fpmath >= 2.5ulp时。 |
| 737 | // |
|   | // 注意：应首先尝试optimizeWithRcp，因为rcp是首选。 |
| 738 | // optimize with fdiv.fast: |
| 739 | // |
| 740 | // a/b -> fdiv.fast(a, b) when !fpmath >= 2.5ulp with denormals flushed. |
| 741 | // |
| 742 | // 1/x -> fdiv.fast(1,x)  when !fpmath >= 2.5ulp. |
| 743 | // |
| 744 | // NOTE: optimizeWithRcp should be tried first because rcp is the preference. |
| 745 | static Value *optimizeWithFDivFast(Value *Num, Value *Den, float ReqdAccuracy, |
| 746 | bool HasDenormals, IRBuilder<> &Builder, |
| 747 | Module *Mod) { |
|   | // fdiv.fast可以实现2.5 ULP的精度。 |
| 748 | // fdiv.fast can achieve 2.5 ULP accuracy. |
| 749 | if (ReqdAccuracy < 2.5f) |
| 750 | return nullptr; |
|   | // 只有f32支持fdiv.fast。 |
| 751 | // Only have fdiv.fast for f32. |
| 752 | Type *Ty = Den->getType(); |
| 753 | if (!Ty->isFloatTy()) |
| 754 | return nullptr; |
| 755 | bool NumIsOne = false; |
| 756 | if (const ConstantFP *CNum = dyn_cast<ConstantFP>(Num)) { |
| 757 | if (CNum->isExactlyValue(+1.0) || CNum->isExactlyValue(-1.0)) |
| 758 | NumIsOne = true; |
| 759 | } |
|   | // fdiv不支持非规格化数。但1.0/x总是可以使用它。 |
| 760 | // fdiv does not support denormals. But 1.0/x is always fine to use it. |
| 761 | if (HasDenormals && !NumIsOne) |
| 762 | return nullptr; |
| 763 | Function *Decl = Intrinsic::getDeclaration(Mod, Intrinsic::amdgcn_fdiv_fast); |
| 764 | return Builder.CreateCall(Decl, { Num, Den }); |
| 765 | } |
|   | // visitFDiv - 根据fpmath、快速数学标志以及非规格化数优化fdiv操作，使用rcp或fdiv.fast。 |
| 766 | // |
|   | // 使用rcp： |
|   | //   1/x -> rcp(x) 当rcp足够精确时，或者在不安全的浮点数学或afn下允许不精确的rcp。 |
| 767 | // |
|   | //   a/b -> a*rcp(b) 当在不安全的浮点数学或afn下允许不精确的rcp时。 |
| 770 |  |
| 774 |  |
| 777 |  |
| 781 |  |
| 782 | // |
|   | // 使用fdiv.fast： |
|   | //   a/b -> fdiv.fast(a, b) 当!fpmath >= 2.5ulp且非规格化数被刷新时。 |
| 783 | // |
|   | //   1/x -> fdiv.fast(1,x) 当!fpmath >= 2.5ulp时。 |
| 787 | // |
|   | // 注意：在两者都合法的情况下，rcp是首选。 |
| 788 | // Optimizations is performed based on fpmath, fast math flags as well as |
| 789 | // denormals to optimize fdiv with either rcp or fdiv.fast. |
| 790 | // |
| 791 | // With rcp: |
| 792 | //   1/x -> rcp(x) when rcp is sufficiently accurate or inaccurate rcp is |
| 793 | //                 allowed with unsafe-fp-math or afn. |
| 795 |  |
| 798 | // |
| 799 | //   a/b -> a*rcp(b) when inaccurate rcp is allowed with unsafe-fp-math or afn. |
| 802 | // |
| 803 | // With fdiv.fast: |
| 804 | //   a/b -> fdiv.fast(a, b) when !fpmath >= 2.5ulp with denormals flushed. |
| 805 | // |
| 806 | //   1/x -> fdiv.fast(1,x)  when !fpmath >= 2.5ulp. |
| 807 | // |
| 808 | // NOTE: rcp is the preference in cases that both are legal. |
| 809 | bool AMDGPUCodeGenPrepare::visitFDiv(BinaryOperator &FDiv) { |
| 810 | Type *Ty = FDiv.getType()->getScalarType(); |
|   | // 如果目标不支持f16，则没有fdiv16的内联函数。 |
| 811 | // No intrinsic for fdiv16 if target does not support f16. |
| 812 | if (Ty->isHalfTy() && !ST->has16BitInsts()) |
| 813 | return false; |
| 814 | const FPMathOperator *FPOp = cast<const FPMathOperator>(&FDiv); |
| 815 | const float ReqdAccuracy =  FPOp->getFPAccuracy(); |
|   | // 在不安全的浮点数学或afn下允许不精确的rcp。 |
| 816 | // Inaccurate rcp is allowed with unsafe-fp-math or afn. |
| 817 | FastMathFlags FMF = FPOp->getFastMathFlags(); |
| 818 | const bool AllowInaccurateRcp = HasUnsafeFPMath || FMF.approxFunc(); |
|   | // rcp_f16在!fpmath >= 1.0ulp时是精确的。 |
|   | // rcp_f32在!fpmath >= 1.0ulp且非规格化数被刷新时是精确的。 |
|   | // rcp_f64从不精确。 |
| 819 | // rcp_f16 is accurate for !fpmath >= 1.0ulp. |
| 820 | // rcp_f32 is accurate for !fpmath >= 1.0ulp and denormals are flushed. |
| 821 | // rcp_f64 is never accurate. |
| 822 | const bool RcpIsAccurate = (Ty->isHalfTy() && ReqdAccuracy >= 1.0f) || |
| 823 | (Ty->isFloatTy() && !HasFP32Denormals && ReqdAccuracy >= 1.0f); |
| 824 | IRBuilder<> Builder(FDiv.getParent(), std::next(FDiv.getIterator())); |
| 825 | Builder.setFastMathFlags(FMF); |
| 826 | Builder.SetCurrentDebugLocation(FDiv.getDebugLoc()); |
| 827 | Value *Num = FDiv.getOperand(0); |
| 828 | Value *Den = FDiv.getOperand(1); |
| 829 | Value *NewFDiv = nullptr; |
| 830 | if (auto *VT = dyn_cast<FixedVectorType>(FDiv.getType())) { |
| 831 | NewFDiv = UndefValue::get(VT); |
|   | // FIXME: 对于向量部分为常量的情况，无法正确处理。 |
|   | // 这在标量化器首先运行时有效。 |
| 832 | // FIXME: Doesn't do the right thing for cases where the vector is partially |
| 833 | // constant. This works when the scalarizer pass is run first. |
| 834 | for (unsigned I = 0, E = VT->getNumElements(); I != E; ++I) { |
| 835 | Value *NumEltI = Builder.CreateExtractElement(Num, I); |
| 836 | Value *DenEltI = Builder.CreateExtractElement(Den, I); |
|   | // 首先尝试rcp。 |
| 837 | // Try rcp first. |
| 838 | Value *NewElt = optimizeWithRcp(NumEltI, DenEltI, AllowInaccurateRcp, |
| 839 | RcpIsAccurate, Builder, Mod); |
| 840 | if (!NewElt) // 尝试fdiv.fast。 |
| 841 | NewElt = optimizeWithFDivFast(NumEltI, DenEltI, ReqdAccuracy, |
| 842 | HasFP32Denormals, Builder, Mod); |
| 843 | if (!NewElt) // 保持原样。 |
| 844 | NewElt = Builder.CreateFDiv(NumEltI, DenEltI); |
| 845 | NewFDiv = Builder.CreateInsertElement(NewFDiv, NewElt, I); |
| 850 |  |
| 854 |  |
| 860 | } |
| 861 | } else { // 标量FDiv。 |
|   | // 首先尝试rcp。 |
| 862 | // Try rcp first. |
| 863 | NewFDiv = optimizeWithRcp(Num, Den, AllowInaccurateRcp, RcpIsAccurate, |
| 864 | Builder, Mod); |
| 865 | if (!NewFDiv) { // 尝试fdiv.fast。 |
| 866 | NewFDiv = optimizeWithFDivFast(Num, Den, ReqdAccuracy, HasFP32Denormals, |
| 867 | Builder, Mod); |
| 872 | } |
| 873 | } |
| 874 | if (NewFDiv) { |
| 875 | FDiv.replaceAllUsesWith(NewFDiv); |
| 876 | NewFDiv->takeName(&FDiv); |
| 877 | FDiv.eraseFromParent(); |
| 878 | } |
| 879 | return !!NewFDiv; |
| 880 | } |
|   | // hasUnsafeFPMath - 检查函数是否启用了不安全的浮点数学优化。 |
| 881 | static bool hasUnsafeFPMath(const Function &F) { |
| 882 | Attribute Attr = F.getFnAttribute("unsafe-fp-math"); |
| 883 | return Attr.getValueAsString() == "true"; |
| 886 |  |
| 890 |  |
| 893 |  |
| 896 | } |
|   | // getMul64 - 执行64位乘法并返回结果的低32位和高32位。 |
| 897 | static std::pair<Value*, Value*> getMul64(IRBuilder<> &Builder, |
| 898 | Value *LHS, Value *RHS) { |
| 899 | Type *I32Ty = Builder.getInt32Ty(); |
| 900 | Type *I64Ty = Builder.getInt64Ty(); |
|   | // 将操作数扩展为64位 |
| 901 | Value *LHS_EXT64 = Builder.CreateZExt(LHS, I64Ty); |
| 902 | Value *RHS_EXT64 = Builder.CreateZExt(RHS, I64Ty); |
|   | // 执行64位乘法 |
| 903 | Value *MUL64 = Builder.CreateMul(LHS_EXT64, RHS_EXT64); |
|   | // 提取低32位 |
| 904 | Value *Lo = Builder.CreateTrunc(MUL64, I32Ty); |
|   | // 提取高32位 |
| 905 | Value *Hi = Builder.CreateLShr(MUL64, Builder.getInt64(32)); |
| 906 | Hi = Builder.CreateTrunc(Hi, I32Ty); |
| 907 | return std::make_pair(Lo, Hi); |
| 911 |  |
| 914 | } |
|   | // getMulHu - 获取64位乘法的高32位结果。 |
| 915 | static Value* getMulHu(IRBuilder<> &Builder, Value *LHS, Value *RHS) { |
| 916 | return getMul64(Builder, LHS, RHS).second; |
| 920 |  |
| 923 |  |
| 929 | } |
|   | // getDivNumBits - 计算除法操作所需的实际位数。\p AtLeast 是一个优化提示， |
|   | // 用于在第一次ComputeNumSignBits调用不足时绕过第二次调用。失败时返回-1。 |
| 930 | /// Figure out how many bits are really needed for this ddivision. \p AtLeast is |
| 931 | /// an optimization hint to bypass the second ComputeNumSignBits call if we the |
| 932 | /// first one is insufficient. Returns -1 on failure. |
| 933 | int AMDGPUCodeGenPrepare::getDivNumBits(BinaryOperator &I, |
| 934 | Value *Num, Value *Den, |
| 935 | unsigned AtLeast, bool IsSigned) const { |
| 936 | const DataLayout &DL = Mod->getDataLayout(); |
|   | // 计算分子的符号位数 |
| 937 | unsigned LHSSignBits = ComputeNumSignBits(Num, DL, 0, AC, &I); |
| 938 | if (LHSSignBits < AtLeast) |
| 939 | return -1; |
|   | // 计算分母的符号位数 |
| 940 | unsigned RHSSignBits = ComputeNumSignBits(Den, DL, 0, AC, &I); |
| 941 | if (RHSSignBits < AtLeast) |
| 942 | return -1; |
|   | // 取分子和分母符号位数的最小值 |
| 943 | unsigned SignBits = std::min(LHSSignBits, RHSSignBits); |
|   | // 计算除法操作所需的位数 |
| 944 | unsigned DivBits = Num->getType()->getScalarSizeInBits() - SignBits; |
| 946 |  |
| 949 |  |
| 956 |  |
| 959 | if (IsSigned) |
| 960 | ++DivBits; |
| 961 | return DivBits; |
| 964 | } |
|   | // expandDivRem24 - 扩展24位除法或取余操作。 |
| 965 | // The fractional part of a float is enough to accurately represent up to |
| 966 | // a 24-bit signed integer. |
| 967 | Value *AMDGPUCodeGenPrepare::expandDivRem24(IRBuilder<> &Builder, |
| 970 |  |
| 973 |  |
| 976 |  |
| 979 | BinaryOperator &I, |
| 980 | Value *Num, Value *Den, |
| 981 | bool IsDiv, bool IsSigned) const { |
|   | // 获取除法操作所需的位数 |
| 982 | int DivBits = getDivNumBits(I, Num, Den, 9, IsSigned); |
| 983 | if (DivBits == -1) |
| 984 | return nullptr; |
|   | // 调用实现函数进行扩展 |
| 985 | return expandDivRem24Impl(Builder, I, Num, Den, DivBits, IsDiv, IsSigned); |
| 987 |  |
| 993 |  |
| 995 | } |
|   | // expandDivRem24Impl - 实现24位除法或取余操作的扩展。 |
| 996 | Value *AMDGPUCodeGenPrepare::expandDivRem24Impl(IRBuilder<> &Builder, |
| 997 | BinaryOperator &I, |
| 998 | Value *Num, Value *Den, |
| 999 | unsigned DivBits, |
| 1000 | bool IsDiv, bool IsSigned) const { |
| 1001 | Type *I32Ty = Builder.getInt32Ty(); |
|   | // 将分子和分母截断为32位 |
| 1002 | Num = Builder.CreateTrunc(Num, I32Ty); |
| 1003 | Den = Builder.CreateTrunc(Den, I32Ty); |
| 1004 | Type *F32Ty = Builder.getFloatTy(); |
| 1005 | ConstantInt *One = Builder.getInt32(1); |
| 1006 | Value *JQ = One; |
| 1007 | if (IsSigned) { |
| 1008 | // char|short jq = ia ^ ib; |
| 1009 | JQ = Builder.CreateXor(Num, Den); |
| 1010 | // jq = jq >> (bitsize - 2) |
| 1011 | JQ = Builder.CreateAShr(JQ, Builder.getInt32(30)); |
| 1012 | // jq = jq | 0x1 |
| 1013 | JQ = Builder.CreateOr(JQ, One); |
| 1018 | } |
| 1019 | // int ia = (int)LHS; |
| 1020 | Value *IA = Num; |
| 1021 | // int ib, (int)RHS; |
| 1022 | Value *IB = Den; |
| 1023 | // float fa = (float)ia; |
| 1024 | Value *FA = IsSigned ? Builder.CreateSIToFP(IA, F32Ty) |
| 1025 | : Builder.CreateUIToFP(IA, F32Ty); |
| 1026 | // float fb = (float)ib; |
| 1027 | Value *FB = IsSigned ? Builder.CreateSIToFP(IB,F32Ty) |
| 1028 | : Builder.CreateUIToFP(IB,F32Ty); |
|   | // 获取rcp内联函数 |
| 1029 | Function *RcpDecl = Intrinsic::getDeclaration(Mod, Intrinsic::amdgcn_rcp, |
| 1030 | Builder.getFloatTy()); |
| 1031 | Value *RCP = Builder.CreateCall(RcpDecl, { FB }); |
| 1032 | Value *FQM = Builder.CreateFMul(FA, RCP); |
| 1033 | // fq = trunc(fqm); |
| 1034 | CallInst *FQ = Builder.CreateUnaryIntrinsic(Intrinsic::trunc, FQM); |
| 1035 | FQ->copyFastMathFlags(Builder.getFastMathFlags()); |
| 1036 | // float fqneg = -fq; |
| 1037 | Value *FQNeg = Builder.CreateFNeg(FQ); |
| 1038 | // float fr = mad(fqneg, fb, fa); |
| 1039 | auto FMAD = !ST->hasMadMacF32Insts() |
| 1040 | ? Intrinsic::fma |
| 1041 | : (Intrinsic::ID)Intrinsic::amdgcn_fmad_ftz; |
| 1042 | Value *FR = Builder.CreateIntrinsic(FMAD, |
| 1043 | {FQNeg->getType()}, {FQNeg, FB, FA}, FQ); |
| 1044 | // int iq = (int)fq; |
| 1045 | Value *IQ = IsSigned ? Builder.CreateFPToSI(FQ, I32Ty) |
| 1046 | : Builder.CreateFPToUI(FQ, I32Ty); |
| 1047 | // fr = fabs(fr); |
| 1048 | FR = Builder.CreateUnaryIntrinsic(Intrinsic::fabs, FR, FQ); |
| 1049 | // fb = fabs(fb); |
| 1050 | FB = Builder.CreateUnaryIntrinsic(Intrinsic::fabs, FB, FQ); |
| 1051 | // int cv = fr >= fb; |
| 1052 | Value *CV = Builder.CreateFCmpOGE(FR, FB); |
| 1053 | // jq = (cv ? jq : 0); |
| 1054 | JQ = Builder.CreateSelect(CV, JQ, Builder.getInt32(0)); |
| 1055 | // dst = iq + jq; |
| 1056 | Value *Div = Builder.CreateAdd(IQ, JQ); |
| 1057 | Value *Res = Div; |
| 1058 | if (!IsDiv) { |
| 1059 | // Rem needs compensation, it's easier to recompute it |
| 1060 | Value *Rem = Builder.CreateMul(Div, Den); |
| 1061 | Res = Builder.CreateSub(Num, Rem); |
| 1065 |  |
| 1068 |  |
| 1071 | } |
| 1072 | if (DivBits != 0 && DivBits < 32) { |
| 1073 | // Extend in register from the number of bits this divide really is. |
| 1074 | if (IsSigned) { |
| 1075 | int InRegBits = 32 - DivBits; |
| 1076 | Res = Builder.CreateShl(Res, InRegBits); |
| 1077 | Res = Builder.CreateAShr(Res, InRegBits); |
| 1078 | } else { |
| 1079 | ConstantInt *TruncMask |
| 1080 | = Builder.getInt32((UINT64_C(1) << DivBits) - 1); |
| 1081 | Res = Builder.CreateAnd(Res, TruncMask); |
| 1095 | } |
| 1099 | } |
| 1100 | return Res; |
| 1102 | } |
|   | // divHasSpecialOptimization - 尝试识别DAG将发出特殊优化的特殊情况， |
|   | // 这些优化比我们在此处执行的通用扩展更好。 |
| 1103 | // Try to recognize special cases the DAG will emit special, better expansions |
| 1104 | // than the general expansion we do here. |
| 1105 | // TODO: It would be better to just directly handle those optimizations here. |
| 1106 | bool AMDGPUCodeGenPrepare::divHasSpecialOptimization( |
| 1107 | BinaryOperator &I, Value *Num, Value *Den) const { |
| 1108 | if (Constant *C = dyn_cast<Constant>(Den)) { |
|   | // 如果分母是常量，并且类型大小小于等于32位，则DAG会生成更好的扩展。 |
| 1109 | // Arbitrary constants get a better expansion as long as a wider mulhi is |
| 1110 | // legal. |
| 1111 | if (C->getType()->getScalarSizeInBits() <= 32) |
| 1112 | return true; |
|   | // TODO: Sdiv检查是否为非精确值。 |
|   | // 如果没有更宽的mulhi，则只有2的幂次方会有更好的扩展。 |
| 1113 | // TODO: Sdiv check for not exact for some reason. |
| 1114 | // If there's no wider mulhi, there's only a better expansion for powers of |
| 1115 | // two. |
| 1116 | // TODO: Should really know for each vector element. |
| 1117 | if (isKnownToBeAPowerOfTwo(C, *DL, true, 0, AC, &I, DT)) |
| 1118 | return true; |
| 1119 | return false; |
| 1120 | } |
| 1121 | if (BinaryOperator *BinOpDen = dyn_cast<BinaryOperator>(Den)) { |
|   | // 如果分母是左移操作，并且操作数是2的幂次方， |
|   | // 则DAG会生成更好的扩展。 |
| 1122 | // fold (udiv x, (shl c, y)) -> x >>u (log2(c)+y) iff c is power of 2 |
| 1123 | if (BinOpDen->getOpcode() == Instruction::Shl && |
| 1124 | isa<Constant>(BinOpDen->getOperand(0)) && |
| 1125 | isKnownToBeAPowerOfTwo(BinOpDen->getOperand(0), *DL, true, |
| 1126 | 0, AC, &I, DT)) { |
| 1127 | return true; |
| 1134 |  |
| 1138 | } |
| 1139 |  |
| 1141 |  |
| 1143 | } |
| 1144 | return false; |
| 1145 | } |
|   | // getSign32 - 获取32位值的符号位。 |
| 1146 | static Value *getSign32(Value *V, IRBuilder<> &Builder, const DataLayout *DL) { |
|   | // 检查符号是否可以静态确定。 |
| 1147 | // Check whether the sign can be determined statically. |
| 1148 | KnownBits Known = computeKnownBits(V, *DL); |
| 1149 | if (Known.isNegative()) |
| 1150 | return Constant::getAllOnesValue(V->getType()); |
| 1151 | if (Known.isNonNegative()) |
| 1152 | return Constant::getNullValue(V->getType()); |
| 1153 | return Builder.CreateAShr(V, Builder.getInt32(31)); |
| 1155 |  |
| 1159 |  |
| 1164 | } |
|   | // expandDivRem32 - 扩展32位除法或取余操作。 |
| 1165 | Value *AMDGPUCodeGenPrepare::expandDivRem32(IRBuilder<> &Builder, |
| 1166 | BinaryOperator &I, Value *X, |
| 1167 | Value *Y) const { |
| 1172 |  |
| 1175 |  |
| 1177 | Instruction::BinaryOps Opc = I.getOpcode(); |
| 1178 | assert(Opc == Instruction::URem || Opc == Instruction::UDiv || |
| 1179 | Opc == Instruction::SRem || Opc == Instruction::SDiv); |
| 1180 | FastMathFlags FMF; |
| 1181 | FMF.setFast(); |
| 1182 | Builder.setFastMathFlags(FMF); |
| 1183 | if (divHasSpecialOptimization(I, X, Y)) |
| 1184 | return nullptr;  // 保留它以进行后续优化。 |
| 1185 | bool IsDiv = Opc == Instruction::UDiv || Opc == Instruction::SDiv; |
| 1186 | bool IsSigned = Opc == Instruction::SRem || Opc == Instruction::SDiv; |
| 1187 | Type *Ty = X->getType(); |
| 1188 | Type *I32Ty = Builder.getInt32Ty(); |
| 1189 | Type *F32Ty = Builder.getFloatTy(); |
| 1190 | if (Ty->getScalarSizeInBits() < 32) { |
| 1191 | if (IsSigned) { |
| 1192 | X = Builder.CreateSExt(X, I32Ty); |
| 1193 | Y = Builder.CreateSExt(Y, I32Ty); |
| 1194 | } else { |
| 1195 | X = Builder.CreateZExt(X, I32Ty); |
| 1196 | Y = Builder.CreateZExt(Y, I32Ty); |
| 1197 | } |
| 1198 | } |
| 1199 | if (Value *Res = expandDivRem24(Builder, I, X, Y, IsDiv, IsSigned)) { |
| 1200 | return IsSigned ? Builder.CreateSExtOrTrunc(Res, Ty) : |
| 1201 | Builder.CreateZExtOrTrunc(Res, Ty); |
| 1202 | } |
| 1203 | ConstantInt *Zero = Builder.getInt32(0); |
| 1204 | ConstantInt *One = Builder.getInt32(1); |
| 1205 | Value *Sign = nullptr; |
| 1206 | if (IsSigned) { |
| 1207 | Value *SignX = getSign32(X, Builder, DL); |
| 1208 | Value *SignY = getSign32(Y, Builder, DL); |
|   | // 余数的符号与LHS相同 |
| 1209 | // Remainder sign is the same as LHS |
| 1210 | Sign = IsDiv ? Builder.CreateXor(SignX, SignY) : SignX; |
| 1211 | X = Builder.CreateAdd(X, SignX); |
| 1212 | Y = Builder.CreateAdd(Y, SignY); |
| 1213 | X = Builder.CreateXor(X, SignX); |
| 1214 | Y = Builder.CreateXor(Y, SignY); |
| 1215 | } |
|   | // 以下算法基于“Software Integer Division”中的思想，Tom Rodeheffer, August 2008。 |
| 1216 | // |
| 1217 | // unsigned udiv(unsigned x, unsigned y) { |
|   | //   // 初始估计inv(y)。常数小于2^32，以确保这是inv(y)的下界，即使某些计算向上舍入。 |
| 1218 | //   unsigned z = (unsigned)((4294967296.0 - 512.0) * v_rcp_f32((float)y)); |
| 1219 |  |
| 1222 |  |
| 1226 |  |
| 1233 | // |
|   | //   // 一轮UNR（无符号整数牛顿迭代）以改进z。 |
|   | //   // 经验上，这保证给出inv(y)的“two-y”下界。 |
| 1234 | //   z += umulh(z, -y * z); |
| 1237 | // |
|   | //   // 商/余数估计。 |
| 1238 | //   unsigned q = umulh(x, z); |
| 1239 | //   unsigned r = x - q * y; |
| 1240 | // |
|   | //   // 两轮商/余数细化。 |
| 1241 | //   if (r >= y) { |
| 1242 | //     ++q; |
| 1243 | //     r -= y; |
| 1244 | //   } |
| 1245 | //   if (r >= y) { |
| 1246 | //     ++q; |
| 1247 | //     r -= y; |
| 1248 | //   } |
| 1249 | // |
| 1250 | //   return q; |
| 1251 | // } |
|   | // 初始估计inv(y)。 |
| 1252 | // The algorithm here is based on ideas from "Software Integer Division", Tom |
| 1253 | // Rodeheffer, August 2008. |
| 1256 |  |
| 1263 |  |
| 1265 | // |
| 1266 | // unsigned udiv(unsigned x, unsigned y) { |
| 1267 | //   // Initial estimate of inv(y). The constant is less than 2^32 to ensure |
| 1268 | //   // that this is a lower bound on inv(y), even if some of the calculations |
| 1269 | //   // round up. |
| 1270 | //   unsigned z = (unsigned)((4294967296.0 - 512.0) * v_rcp_f32((float)y)); |
| 1271 | // |
| 1272 | //   // One round of UNR (Unsigned integer Newton-Raphson) to improve z. |
| 1273 | //   // Empirically this is guaranteed to give a "two-y" lower bound on |
| 1274 | //   // inv(y). |
| 1275 | //   z += umulh(z, -y * z); |
| 1276 | // |
| 1277 | //   // Quotient/remainder estimate. |
| 1278 | //   unsigned q = umulh(x, z); |
| 1279 | //   unsigned r = x - q * y; |
| 1284 |  |
| 1290 |  |
| 1291 | // |
| 1292 | //   // Two rounds of quotient/remainder refinement. |
| 1293 | //   if (r >= y) { |
| 1294 | //     ++q; |
| 1295 | //     r -= y; |
| 1296 | //   } |
| 1297 | //   if (r >= y) { |
| 1298 | //     ++q; |
| 1299 | //     r -= y; |
| 1300 | //   } |
| 1302 | // |
| 1303 | //   return q; |
| 1304 | // } |
| 1305 | // Initial estimate of inv(y). |
| 1306 | Value *FloatY = Builder.CreateUIToFP(Y, F32Ty); |
| 1307 | Function *Rcp = Intrinsic::getDeclaration(Mod, Intrinsic::amdgcn_rcp, F32Ty); |
| 1308 | Value *RcpY = Builder.CreateCall(Rcp, {FloatY}); |
| 1309 | Constant *Scale = ConstantFP::get(F32Ty, BitsToFloat(0x4F7FFFFE)); |
| 1310 | Value *ScaledY = Builder.CreateFMul(RcpY, Scale); |
| 1311 | Value *Z = Builder.CreateFPToUI(ScaledY, I32Ty); |
|   | // 一轮UNR。 |
| 1312 | // One round of UNR. |
| 1313 | Value *NegY = Builder.CreateSub(Zero, Y); |
| 1314 | Value *NegYZ = Builder.CreateMul(NegY, Z); |
| 1315 | Z = Builder.CreateAdd(Z, getMulHu(Builder, Z, NegYZ)); |
|   | // 商/余数估计。 |
| 1316 | // Quotient/remainder estimate. |
| 1317 | Value *Q = getMulHu(Builder, X, Z); |
| 1318 | Value *R = Builder.CreateSub(X, Builder.CreateMul(Q, Y)); |
|   | // 第一轮商/余数细化。 |
| 1319 | // First quotient/remainder refinement. |
| 1320 | Value *Cond = Builder.CreateICmpUGE(R, Y); |
| 1321 | if (IsDiv) |
| 1322 | Q = Builder.CreateSelect(Cond, Builder.CreateAdd(Q, One), Q); |
| 1323 | R = Builder.CreateSelect(Cond, Builder.CreateSub(R, Y), R); |
|   | // 第二轮商/余数细化。 |
| 1324 | // Second quotient/remainder refinement. |
| 1325 | Cond = Builder.CreateICmpUGE(R, Y); |
| 1326 | Value *Res; |
| 1327 | if (IsDiv) |
| 1328 | Res = Builder.CreateSelect(Cond, Builder.CreateAdd(Q, One), Q); |
| 1329 | else |
| 1330 | Res = Builder.CreateSelect(Cond, Builder.CreateSub(R, Y), R); |
| 1331 | if (IsSigned) { |
| 1332 | Res = Builder.CreateXor(Res, Sign); |
| 1333 | Res = Builder.CreateSub(Res, Sign); |
| 1335 |  |
| 1339 |  |
| 1341 | } |
| 1342 | Res = Builder.CreateTrunc(Res, Ty); |
| 1343 | return Res; |
| 1349 | } |
|   | // shrinkDivRem64 - 将64位除法或取余操作缩小为32位或24位。 |
| 1350 | Value *AMDGPUCodeGenPrepare::shrinkDivRem64(IRBuilder<> &Builder, |
| 1351 | BinaryOperator &I, |
| 1352 | Value *Num, Value *Den) const { |
| 1353 | if (!ExpandDiv64InIR && divHasSpecialOptimization(I, Num, Den)) |
| 1354 | return nullptr;  // 保留它以进行后续优化。 |
| 1355 | Instruction::BinaryOps Opc = I.getOpcode(); |
| 1356 | bool IsDiv = Opc == Instruction::SDiv || Opc == Instruction::UDiv; |
| 1357 | bool IsSigned = Opc == Instruction::SDiv || Opc == Instruction::SRem; |
| 1358 | int NumDivBits = getDivNumBits(I, Num, Den, 32, IsSigned); |
| 1359 | if (NumDivBits == -1) |
| 1360 | return nullptr; |
| 1361 | Value *Narrowed = nullptr; |
| 1362 | if (NumDivBits <= 24) { |
| 1363 | Narrowed = expandDivRem24Impl(Builder, I, Num, Den, NumDivBits, |
| 1364 | IsDiv, IsSigned); |
| 1365 | } else if (NumDivBits <= 32) { |
| 1366 | Narrowed = expandDivRem32(Builder, I, Num, Den); |
| 1367 | } |
| 1368 | if (Narrowed) { |
| 1369 | return IsSigned ? Builder.CreateSExt(Narrowed, Num->getType()) : |
| 1370 | Builder.CreateZExt(Narrowed, Num->getType()); |
| 1371 | } |
| 1372 | return nullptr; |
| 1373 | } |
|   | // expandDivRem64 - 扩展64位除法或取余操作。 |
| 1374 | void AMDGPUCodeGenPrepare::expandDivRem64(BinaryOperator &I) const { |
| 1375 | Instruction::BinaryOps Opc = I.getOpcode(); |
|   | // 执行通用扩展。 |
| 1376 | // Do the general expansion. |
| 1377 | if (Opc == Instruction::UDiv || Opc == Instruction::SDiv) { |
| 1378 | expandDivisionUpTo64Bits(&I); |
| 1379 | return; |
| 1380 | } |
| 1381 | if (Opc == Instruction::URem || Opc == Instruction::SRem) { |
| 1382 | expandRemainderUpTo64Bits(&I); |
| 1383 | return; |
| 1384 | } |
| 1385 | llvm_unreachable("not a division"); |
| 1386 | } |
|   | // visitBinaryOperator - 处理二元操作符。 |
| 1387 | bool AMDGPUCodeGenPrepare::visitBinaryOperator(BinaryOperator &I) { |
| 1388 | if (foldBinOpIntoSelect(I)) |
| 1389 | return true; |
| 1390 | if (ST->has16BitInsts() && needsPromotionToI32(I.getType()) && |
| 1391 | DA->isUniform(&I) && promoteUniformOpToI32(I)) |
| 1392 | return true; |
| 1393 | if (UseMul24Intrin && replaceMulWithMul24(I)) |
| 1394 | return true; |
| 1395 | bool Changed = false; |
| 1396 | Instruction::BinaryOps Opc = I.getOpcode(); |
| 1397 | Type *Ty = I.getType(); |
| 1398 | Value *NewDiv = nullptr; |
| 1399 | unsigned ScalarSize = Ty->getScalarSizeInBits(); |
| 1400 | SmallVector<BinaryOperator *, 8> Div64ToExpand; |
| 1401 | if ((Opc == Instruction::URem || Opc == Instruction::UDiv || |
| 1402 | Opc == Instruction::SRem || Opc == Instruction::SDiv) && |
| 1403 | ScalarSize <= 64 && |
| 1404 | !DisableIDivExpand) { |
| 1405 | Value *Num = I.getOperand(0); |
| 1406 | Value *Den = I.getOperand(1); |
| 1407 | IRBuilder<> Builder(&I); |
| 1408 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 1409 | if (auto *VT = dyn_cast<FixedVectorType>(Ty)) { |
| 1410 | NewDiv = UndefValue::get(VT); |
| 1411 | for (unsigned N = 0, E = VT->getNumElements(); N != E; ++N) { |
| 1412 | Value *NumEltN = Builder.CreateExtractElement(Num, N); |
| 1413 | Value *DenEltN = Builder.CreateExtractElement(Den, N); |
| 1414 | Value *NewElt; |
| 1415 | if (ScalarSize <= 32) { |
| 1416 | NewElt = expandDivRem32(Builder, I, NumEltN, DenEltN); |
| 1417 | if (!NewElt) |
| 1418 | NewElt = Builder.CreateBinOp(Opc, NumEltN, DenEltN); |
| 1419 | } else { |
|   | // 在生成通用扩展之前，检查是否可以将64位除法缩小为32/24位。 |
| 1420 | // See if this 64-bit division can be shrunk to 32/24-bits before |
| 1421 | // producing the general expansion. |
| 1422 | NewElt = shrinkDivRem64(Builder, I, NumEltN, DenEltN); |
| 1423 | if (!NewElt) { |
|   | // 通用的64位扩展引入了控制流，并且不返回新值。 |
|   | // 只需插入标量副本并推迟扩展。 |
| 1424 | // The general 64-bit expansion introduces control flow and doesn't |
| 1425 | // return the new value. Just insert a scalar copy and defer |
| 1426 | // expanding it. |
| 1427 | NewElt = Builder.CreateBinOp(Opc, NumEltN, DenEltN); |
| 1428 | Div64ToExpand.push_back(cast<BinaryOperator>(NewElt)); |
| 1429 | } |
| 1430 | } |
| 1431 | NewDiv = Builder.CreateInsertElement(NewDiv, NewElt, N); |
| 1432 | } |
| 1433 | } else { |
| 1434 | if (ScalarSize <= 32) |
| 1435 | NewDiv = expandDivRem32(Builder, I, Num, Den); |
| 1436 | else { |
| 1437 | NewDiv = shrinkDivRem64(Builder, I, Num, Den); |
| 1438 | if (!NewDiv) |
| 1439 | Div64ToExpand.push_back(&I); |
| 1440 | } |
| 1441 | } |
| 1442 | if (NewDiv) { |
| 1443 | I.replaceAllUsesWith(NewDiv); |
| 1444 | I.eraseFromParent(); |
| 1445 | Changed = true; |
| 1446 | } |
| 1447 | } |
| 1448 | if (ExpandDiv64InIR) { |
|   | // TODO: 在特殊处理的常量情况下，我们得到的代码质量较差。 |
| 1449 | // TODO: We get much worse code in specially handled constant cases. |
| 1450 | for (BinaryOperator *Div : Div64ToExpand) { |
| 1451 | expandDivRem64(*Div); |
| 1452 | Changed = true; |
| 1453 | } |
| 1454 | } |
| 1455 | return Changed; |
| 1456 | } |
|   | // visitLoadInst - 处理加载指令。 |
| 1457 | bool AMDGPUCodeGenPrepare::visitLoadInst(LoadInst &I) { |
| 1458 | if (!WidenLoads) |
| 1459 | return false; |
| 1460 | if ((I.getPointerAddressSpace() == AMDGPUAS::CONSTANT_ADDRESS || |
| 1461 | I.getPointerAddressSpace() == AMDGPUAS::CONSTANT_ADDRESS_32BIT) && |
| 1462 | canWidenScalarExtLoad(I)) { |
| 1463 | IRBuilder<> Builder(&I); |
| 1464 | Builder.SetCurrentDebugLocation(I.getDebugLoc()); |
| 1465 | Type *I32Ty = Builder.getInt32Ty(); |
| 1466 | Type *PT = PointerType::get(I32Ty, I.getPointerAddressSpace()); |
| 1467 | Value *BitCast= Builder.CreateBitCast(I.getPointerOperand(), PT); |
| 1468 | LoadInst *WidenLoad = Builder.CreateLoad(I32Ty, BitCast); |
| 1469 | WidenLoad->copyMetadata(I); |
|   | // 如果有范围元数据，我们需要转换类型，并且不对高位做出假设。 |
| 1470 | // If we have range metadata, we need to convert the type, and not make |
| 1471 | // assumptions about the high bits. |
| 1472 | if (auto *Range = WidenLoad->getMetadata(LLVMContext::MD_range)) { |
| 1473 | ConstantInt *Lower = |
| 1474 | mdconst::extract<ConstantInt>(Range->getOperand(0)); |
| 1475 | if (Lower->getValue().isNullValue()) { |
| 1476 | WidenLoad->setMetadata(LLVMContext::MD_range, nullptr); |
| 1477 | } else { |
| 1478 | Metadata *LowAndHigh[] = { |
| 1479 | ConstantAsMetadata::get(ConstantInt::get(I32Ty, Lower->getValue().zext(32))), |
|   | // 不对高位做出假设。 |
| 1480 | // Don't make assumptions about the high bits. |
| 1481 | ConstantAsMetadata::get(ConstantInt::get(I32Ty, 0)) |
| 1482 | }; |
| 1483 | WidenLoad->setMetadata(LLVMContext::MD_range, |
| 1484 | MDNode::get(Mod->getContext(), LowAndHigh)); |
| 1485 | } |
| 1486 | } |
| 1487 | int TySize = Mod->getDataLayout().getTypeSizeInBits(I.getType()); |
| 1488 | Type *IntNTy = Builder.getIntNTy(TySize); |
| 1489 | Value *ValTrunc = Builder.CreateTrunc(WidenLoad, IntNTy); |
| 1490 | Value *ValOrig = Builder.CreateBitCast(ValTrunc, I.getType()); |
| 1491 | I.replaceAllUsesWith(ValOrig); |
| 1492 | I.eraseFromParent(); |
| 1493 | return true; |
| 1494 | } |
| 1495 | return false; |
| 1496 | } |
|   | // visitICmpInst - 处理整数比较指令。 |
| 1497 | bool AMDGPUCodeGenPrepare::visitICmpInst(ICmpInst &I) { |
| 1498 | bool Changed = false; |
| 1499 | if (ST->has16BitInsts() && needsPromotionToI32(I.getOperand(0)->getType()) && |
| 1500 | DA->isUniform(&I)) |
| 1501 | Changed |= promoteUniformOpToI32(I); |
| 1502 | return Changed; |
| 1503 | } |
|   | // visitSelectInst - 处理选择指令。 |
| 1504 | bool AMDGPUCodeGenPrepare::visitSelectInst(SelectInst &I) { |
| 1505 | bool Changed = false; |
| 1506 | if (ST->has16BitInsts() && needsPromotionToI32(I.getType()) && |
| 1507 | DA->isUniform(&I)) |
| 1508 | Changed |= promoteUniformOpToI32(I); |
| 1509 | return Changed; |
| 1510 | } |
|   | // visitIntrinsicInst - 处理内联函数指令。 |
| 1511 | bool AMDGPUCodeGenPrepare::visitIntrinsicInst(IntrinsicInst &I) { |
| 1512 | switch (I.getIntrinsicID()) { |
| 1513 | case Intrinsic::bitreverse: |
| 1514 | return visitBitreverseIntrinsicInst(I); |
| 1515 | default: |
| 1516 | return false; |
| 1517 | } |
| 1518 | } |
|   | // visitBitreverseIntrinsicInst - 处理bitreverse内联函数指令。 |
| 1519 | bool AMDGPUCodeGenPrepare::visitBitreverseIntrinsicInst(IntrinsicInst &I) { |
| 1520 | bool Changed = false; |
| 1521 | if (ST->has16BitInsts() && needsPromotionToI32(I.getType()) && |
| 1522 | DA->isUniform(&I)) |
| 1523 | Changed |= promoteUniformBitreverseToI32(I); |
| 1524 | return Changed; |
| 1525 | } |
|   | // doInitialization - 初始化模块。 |
| 1526 | bool AMDGPUCodeGenPrepare::doInitialization(Module &M) { |
| 1527 | Mod = &M; |
| 1528 | DL = &Mod->getDataLayout(); |
| 1529 | return false; |
| 1530 | } |
| 1531 | bool AMDGPUCodeGenPrepare::runOnFunction(Function &F) { |
|   | // 如果该函数被标记为跳过优化，则直接返回 false |
| 1532 | if (skipFunction(F)) |
| 1533 | return false; |
|   | // 获取目标平台的配置信息（TargetPassConfig） |
| 1534 | auto *TPC = getAnalysisIfAvailable<TargetPassConfig>(); |
| 1535 | if (!TPC) |
| 1536 | return false; // 如果无法获取目标配置，则返回 false |
|   | // 获取 AMDGPU 目标机器（TargetMachine）的引用 |
| 1537 | const AMDGPUTargetMachine &TM = TPC->getTM<AMDGPUTargetMachine>(); |
|   | // 获取当前函数的子目标信息（Subtarget），这里特指 GCN 架构的子目标 |
| 1538 | ST = &TM.getSubtarget<GCNSubtarget>(F); |
|   | // 获取假设缓存（AssumptionCache），用于优化过程中利用假设信息 |
| 1539 | AC = &getAnalysis<AssumptionCacheTracker>().getAssumptionCache(F); |
|   | // 获取分歧分析（DivergenceAnalysis），用于分析控制流的分歧情况 |
| 1540 | DA = &getAnalysis<LegacyDivergenceAnalysis>(); |
|   | // 获取支配树（DominatorTree）的包装器，用于支配关系分析 |
| 1541 | auto *DTWP = getAnalysisIfAvailable<DominatorTreeWrapperPass>(); |
| 1542 | DT = DTWP ? &DTWP->getDomTree() : nullptr; // 如果存在支配树，则获取其实例 |
|   | // 检查当前函数是否启用了不安全的浮点数学优化 |
| 1543 | HasUnsafeFPMath = hasUnsafeFPMath(F); |
|   | // 初始化 SIModeRegisterDefaults，用于处理 AMDGPU 的浮点模式设置 |
| 1544 | AMDGPU::SIModeRegisterDefaults Mode(F); |
|   | // 检查是否所有 FP32（单精度浮点数）的异常值（denormals）都处于某种模式 |
| 1545 | HasFP32Denormals = Mode.allFP32Denormals(); |
|   | // 初始化一个布尔变量，用于跟踪是否发生了任何更改 |
| 1546 | bool MadeChange = false; |
|   | // 声明一个迭代器，用于遍历函数中的基本块（Basic Block） |
| 1547 | Function::iterator NextBB; |
|   | // 遍历函数中的每一个基本块 |
| 1548 | for (Function::iterator FI = F.begin(), FE = F.end(); FI != FE; FI = NextBB) { |
|   | // 获取当前基本块的指针 |
| 1549 | BasicBlock *BB = &*FI; |
| 1550 | NextBB = std::next(FI); |
|   | // 声明一个迭代器，用于遍历当前基本块中的指令 |
| 1551 | BasicBlock::iterator Next; |
|   | // 遍历当前基本块中的每一条指令 |
| 1552 | for (BasicBlock::iterator I = BB->begin(), E = BB->end(); I != E; I = Next) { |
|   | // 预先计算下一条指令的迭代器，以便在循环中安全地删除或修改当前指令 |
| 1553 | Next = std::next(I); |
|   | // 调用 visit 函数处理当前指令，并将结果与 MadeChange 进行或运算 |
|   | // 如果 visit 函数返回 true，表示发生了更改，MadeChange 将变为 true |
| 1554 | MadeChange |= visit(*I); |
|   | // 检查是否发生了控制流的改变（例如，跳转或分支） |
| 1555 | if (Next != E) { // 控制流可能已经改变 |
|   | // 获取下一条指令所在的基本块 |
| 1556 | BasicBlock *NextInstBB = Next->getParent(); |
|   | // 如果下一条指令不在当前基本块中，说明控制流已经跳转到另一个基本块 |
| 1557 | if (NextInstBB != BB) { |
|   | // 更新当前基本块为下一条指令所在的基本块 |
| 1558 | BB = NextInstBB; |
|   | // 更新当前基本块的结束迭代器 |
| 1559 | E = BB->end(); |
|   | // 更新函数的结束迭代器，以确保循环的正确性 |
| 1560 | FE = F.end(); |
| 1561 | } |
| 1562 | } |
| 1563 | } |
| 1564 | } |
|   | // 返回是否发生了任何更改 |
| 1565 | return MadeChange; |
| 1566 | } |
|   | // 初始化 AMDGPUCodeGenPrepare Pass，并声明其依赖的其他 Pass |
| 1567 | INITIALIZE_PASS_BEGIN(AMDGPUCodeGenPrepare, DEBUG_TYPE, |
| 1568 | "AMDGPU IR optimizations", false, false) |
| 1569 | INITIALIZE_PASS_DEPENDENCY(AssumptionCacheTracker) // 依赖假设缓存 Pass |
| 1570 | INITIALIZE_PASS_DEPENDENCY(LegacyDivergenceAnalysis) // 依赖分歧分析 Pass |
| 1571 | INITIALIZE_PASS_END(AMDGPUCodeGenPrepare, DEBUG_TYPE, "AMDGPU IR optimizations", |
| 1572 | false, false) |
|   | // 定义 AMDGPUCodeGenPrepare Pass 的唯一标识符 |
| 1573 | char AMDGPUCodeGenPrepare::ID = 0; |
|   | // 创建一个 AMDGPUCodeGenPrepare Pass 的实例 |
| 1574 | FunctionPass *llvm::createAMDGPUCodeGenPreparePass() { |
| 1575 | return new AMDGPUCodeGenPrepare(); |
| 1576 | } |

